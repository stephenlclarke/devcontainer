# Unsupported capability implementation catalogue

<!-- markdownlint-disable MD013 -->

## Purpose and scope

This catalogue defines what must change before each capability that is currently rejected, semantically incomplete, or explicitly outside the stock-Apple compatibility claim can be called supported.

The north-star scope is 100% behavioural parity with Docker-based Development Containers. It is not an unbounded promise to implement every Docker Engine or Swarm feature. A Docker field that the pinned official Dev Containers CLI or a supported Compose workflow can emit is in scope. A Swarm-only field remains conditional until the supported toolchain emits it, but its current rejection and implementation boundary are still recorded here.

The source of truth for the current rejection paths is:

- [`DockerRouterSupport.swift`](Sources/DevContainerDockerAPI/DockerRouterSupport.swift), which validates Docker create, network, endpoint, volume, and mount requests before side effects;
- [`DockerRouter.swift`](Sources/DevContainerDockerAPI/DockerRouter.swift), which rejects unsupported drivers and privileged exec;
- [`AppleContainerRuntime.swift`](Sources/DevContainerAppleRuntime/AppleContainerRuntime.swift), which enforces the selected Apple distribution's real create and network capabilities;
- [`AppleProcessSession.swift`](Sources/DevContainerAppleRuntime/AppleProcessSession.swift), which exposes the remaining CLI fallback limitations;
- [`ExecutableComposeProvider.swift`](Sources/DevContainerComposeProvider/ExecutableComposeProvider.swift), which advertises provider capabilities.

This document covers confirmed unsupported capability paths. Partial or unverified Development Containers properties that need evidence rather than a new runtime primitive remain in [`CONFORMANCE.md`](CONFORMANCE.md) and the machine-readable [`spec-coverage.json`](Tests/Parity/spec-coverage.json).

## Support rules

A capability is complete only when all of the following are true:

1. The Docker request has a strict typed representation. Unknown fields still fail with `400`, and known but unavailable fields fail with `501` before side effects.
2. The runtime implements the Docker-observable semantics. A superficially similar Apple flag is not enough.
3. Create, inspect, list, events, restart, recovery, and cleanup project the same state as Docker where the capability affects them.
4. Docker, stock Apple, and `container-compose` fixtures retain raw request, state, event, error, cleanup, timing, and runtime-fingerprint evidence.
5. Stock support uses an unmodified tagged `apple/container` and `apple/containerization` release. Fork-only behaviour is reported as an enhanced provider capability, not attributed to stock Apple.
6. Capability discovery and `/info` remain truthful. A field is not advertised merely because the bridge can decode it.

## Existing tagged Apple primitives

Several current `501` responses do not require a new virtualisation primitive. The pinned [`apple/container` 1.1.0 `ContainerConfiguration`](https://github.com/apple/container/blob/1.1.0/Sources/ContainerResource/Container/ContainerConfiguration.swift) already carries:

- integer CPU and VM memory allocation;
- `/dev/shm` size and stop signal;
- DNS nameservers, domain, search domains, and options;
- sysctls and a read-only root filesystem;
- capability additions and drops;
- create-time networks and native IPv4/IPv6 subnet configuration.

Its [`ProcessConfiguration`](https://github.com/apple/container/blob/1.1.0/Sources/ContainerResource/Container/ProcessConfiguration.swift) also carries supplemental groups and resource limits. The pinned [`NetworkConfiguration`](https://github.com/apple/container/blob/1.1.0/Sources/ContainerResource/Network/NetworkConfiguration.swift) carries IPv4 and IPv6 subnets, plugin selection, labels, and plugin options, while attachment configuration carries a MAC address and MTU.

Those surfaces make a bridge implementation possible for the exact overlapping subset. They do not provide Docker's fractional CPU controls, cgroup limits, device requests, namespace sharing, restart policy, dynamic network attachment, or full security policy.

`apple/containerization` 0.35.0 has lower-level OCI structures for `noNewPrivileges`, seccomp, namespaces, masked paths, and read-only paths, but stock `apple/container` 1.1.0 does not expose a supported container-management contract for all of them. Those capabilities require an upstream `apple/container` surface and a tagged release before they enter the stock lane.

## Docker API coverage

| ID | Current surface | What implementation requires | Acceptance evidence |
| --- | --- | --- | --- |
| UC-API-001 | NC-001 and all strict request schemas | Generate endpoint and API-version-specific schemas from the pinned Moby API for every request the supported clients can emit. Keep typed DTOs for semantic translation, but derive an allowlist for every nested object and verify that each accepted member maps to a catalogue item or an implemented capability. Capture the pinned CLI and Compose request corpus and fail CI when a new member is not classified. | Every advertised endpoint from API 1.44 through 1.53 has unknown-key, wrong-type, omission, null, default, and version-boundary tests. An injected field fails closed, and every real client request is classified without weakening strict decoding. |

## Container identity and lifecycle

| ID | Rejected or incomplete surface | What implementation requires | Acceptance evidence |
| --- | --- | --- | --- |
| UC-CON-001 | `Hostname` and `Domainname` | Add distinct hostname and NIS domain-name members to `ContainerSpec`, persistence, inspect projection, and the Apple adapter. Do not substitute network aliases or `AttachmentConfiguration.hostname` for the container's UTS hostname. Upstream Apple must expose create-time UTS configuration, or a supported guest-init operation must set it before the user process starts. | Explicit and default hostname/domain name, `/etc/hostname`, `hostname(1)`, `uname`, inspect, DNS aliases, restart, recreate, and invalid-value results match Docker. |
| UC-CON-002 | `StopSignal` and `StopTimeout` | Use the tagged `ContainerConfiguration.stopSignal` surface for a create-time signal override, retain it in Docker metadata, and apply it when no stop request override is supplied. Persist Docker's default stop timeout separately because it is an engine policy, not a container runtime field. Resolve signal names and numbers exactly and preserve the timeout across service restart. | Image default, create override, request timeout override, zero timeout, invalid signal, graceful exit, forced kill, inspect, event order, and service-restart recovery match Docker. |
| UC-CON-003 | `HostConfig.RestartPolicy` | Add a typed restart policy and restart count to state. A service-owned supervisor must distinguish user stop/remove from process failure, implement `no`, `on-failure[:max]`, `always`, and `unless-stopped`, preserve Docker backoff and event transitions, and reconcile policy after the compatibility service restarts. It must not race removal or create duplicate restart loops. | Exit-zero and non-zero workloads, retry limits, exponential backoff, manual stop/start, daemon restart, host reboot, health failure, remove, and event sequences match Docker with no leaked process or duplicate restart. |
| UC-CON-004 | Docker labels containing `=` in a key or value | Make the typed `ContainerClient` path the required production path for label dictionaries, or upstream a lossless XPC/CLI transport. The CLI parser's `key=value` representation must not be used when it cannot round-trip the dictionary. Preserve the original dictionary in native state and Docker inspect rather than escaping it into a different value. | Empty, Unicode, whitespace, and embedded-`=` values round-trip through create, list filters, inspect, restart, reconciliation, and Compose discovery. |

## CPU, memory, process and OOM controls

| ID | Rejected fields | What implementation requires | Acceptance evidence |
| --- | --- | --- | --- |
| UC-RES-001 | `Memory`, `MemorySwap`, and `MemoryReservation` | Map the exact hard-memory subset to Apple VM allocation, then add guest cgroup v2 `memory.max`, `memory.high`, and `memory.swap.max` controls for Docker semantics that differ from VM sizing. Validate Docker's sentinel values and relationships before VM creation. The VM allocation must leave enough overhead for the guest while the container-visible cgroup reports the requested limit. | Inside-guest cgroup files, allocation failure, memory pressure, swap use, OOM events, inspect, stats, and `/info` flags match Docker for zero, finite, unlimited, reservation, and invalid combinations. |
| UC-RES-002 | `NanoCpus`, `CpuShares`, `CpuPeriod`, and `CpuQuota` | Use integer Apple vCPU allocation only as the outer capacity. Add cgroup v2 `cpu.max` for per-container quota, with exact NanoCPUs and CFS conversion and rounding rules. `CpuShares` needs more than a guest `cpu.weight`: stock Apple launches one VM per container, so weights in separate guests cannot provide Docker's relative scheduling between containers under host contention. Exact shares therefore require a tagged host-level VM scheduling-weight primitive or a shared Linux scheduling domain that preserves container isolation. | CPU-bound workloads, throttling statistics, cross-container relative weighting under host contention, inspect values, invalid and conflicting forms, guest-agent accounting, and `/info` flags match Docker within a declared measurement tolerance. |
| UC-RES-003 | `CpusetCpus` and `CpusetMems` | Docker's sets identify engine-host logical CPUs and NUMA nodes. Guest vCPU numbering in a private Apple VM is not an equivalent host affinity contract. Support therefore needs a tagged VM vCPU pinning/topology API, or a shared Linux scheduling domain with stable host-equivalent identifiers, before applying matching `cpuset.cpus` and `cpuset.mems`. If the runtime exposes one memory node, only requests semantically equivalent to that node can be accepted. | Valid, sparse, empty, unavailable, and conflicting sets produce matching host scheduling affinity, cgroup state, process affinity, errors, inspect data, and CPU-set capability reporting. |
| UC-RES-004 | `PidsLimit` | Create a container cgroup before the user process, set `pids.max`, place every init and exec process in the same governed hierarchy, and preserve the value across exec, restart, and service recovery. Guest-agent processes must not be charged as container workload unless Docker charges an equivalent process. | Fork-bomb boundary, exec accounting, unlimited and zero forms, PID 1, inspect, stats, OOM-independent failure, and cleanup match Docker. |
| UC-RES-005 | `ShmSize` | Add shared-memory size to `ContainerSpec` and translate it to tagged `ContainerConfiguration.shmSize`. Preserve Docker defaults and distinguish an explicit value from omission. Project the configured size into inspect. | Default, custom, minimum, invalid, full, restart, and Compose `shm_size` cases match Docker using `df`, allocation behaviour, inspect, and cleanup. |
| UC-RES-006 | `OomKillDisable` and `OomScoreAdj` | Add guest cgroup and process controls for Docker's OOM policy. `OomScoreAdj` must be applied to the container init before children are created. `OomKillDisable` requires a proved cgroup policy and must not be claimed if the guest kernel cannot reproduce Docker's behaviour. Record the setting and OOM outcome for inspect and events. | Valid bounds, invalid values, memory pressure, group and single-process OOM, restart policy interaction, inspect, events, and service recovery match Docker. |
| UC-RES-007 | `Ulimits` | Convert Docker limit names and soft/hard values into tagged Apple `ProcessConfiguration.Rlimit` values, using the pinned parser's supported mapping. Apply the same limits to init and exec where Docker does, reject unsupported limit types before create, and retain inspect values. | Every mapped limit, `unlimited`, soft/hard validation, inheritance into child and exec processes, failure errors, and inspect output match Docker. |
| UC-RES-008 | `GroupAdd` | Resolve numeric and named supplementary groups with Docker-equivalent image/user semantics, populate `ProcessConfiguration.supplementalGroups`, and define the behaviour for missing groups and duplicate GIDs. Apply the result to init and exec without changing the primary user. | Numeric, named, duplicate, missing, root, non-root, Feature-contributed, and exec group lists match Docker inside the guest and in error behaviour. |

## Devices and security

| ID | Rejected fields or operation | What implementation requires | Acceptance evidence |
| --- | --- | --- | --- |
| UC-DEV-001 | `HostConfig.DeviceRequests` and `hostRequirements.gpu` | Define a typed GPU/device request including count, IDs, driver, capabilities, and options. Apple Virtualization, the guest kernel, and the container runtime need a supported GPU transport and guest driver before the bridge can select devices. Optional GPU requirements must fall back exactly as the official CLI specifies; required requests must fail before resource creation. | Required, optional, absent, count, selected ID, capability, invalid request, GPU Feature, and real compute workload fixtures match Docker. Metadata or a visible device node alone is insufficient. |
| UC-DEV-002 | `HostConfig.Devices` and `DeviceCgroupRules` | Add typed source, destination, and `rwm` permissions. Implement safe host-device discovery, VM hot-plug or an explicit proxy, guest node creation, and cgroup device policy. Block, character, and synthetic devices need separate threat models. Never expose arbitrary macOS device paths through a string pass-through. | Read, write, and mknod permissions, absent device, hot-plug failure, block and character device workloads, inspect, cancellation, and removal match Docker without host escape. |
| UC-SEC-001 | Create `Privileged` and exec `Privileged` | Define the observable privileged contract rather than mapping it to `capAdd ALL`. It includes device access, capabilities, seccomp, AppArmor/SELinux treatment, masked/read-only paths, namespace and mount policy, sysctls, and nested-container requirements. Add an upstream tagged Apple primitive that intentionally relaxes those controls, with a separate exec policy. | Docker-in-Docker, device, mount, capability, namespace, sysctl, protected-path, denial, inspect, and cleanup tests all match. Stock mode remains rejected until the complete contract passes. |
| UC-SEC-002 | `SecurityOpt`, including `no-new-privileges`, seccomp, labels, and `systempaths=unconfined` | Parse every supported Docker form into a typed policy. Keep `seccomp=unconfined` as a no-op only while a tagged Apple release is proved not to install a conflicting default profile. Upstream `apple/container` must expose `containerization`'s OCI `noNewPrivileges`, seccomp, AppArmor/SELinux, masked-path, and read-only-path surfaces through a stable create API. Reject host-inapplicable or unavailable policies explicitly. | Positive and negative in-guest probes, profile parsing, privilege escalation attempts, protected paths, inspect, invalid profiles, error envelopes, and side-effect-free rejection match Docker. |
| UC-SEC-003 | `Sysctls`, `ReadonlyRootfs`, `MaskedPaths`, and `ReadonlyPaths` | Translate safe namespaced sysctls and read-only root to the tagged Apple configuration. Add explicit policy and allowlisting for host-affecting sysctls. Expose OCI masked/read-only path lists through tagged `apple/container`; apply them before the user process starts and retain them for inspect. | Namespaced and forbidden sysctls, read-only root with writable mounts, masked paths, read-only paths, init/exec, restart, inspect, and failure-before-create match Docker. |
| UC-SEC-004 | `IpcMode`, `PidMode`, `UsernsMode`, `UTSMode`, and `CgroupnsMode` | Model each namespace independently. Private namespaces may map to the current isolated VM/container. Sharing with another container needs a pod or shared-sandbox identity and lifecycle. Docker `host` modes cannot be approximated by sharing with the Linux guest when Docker means the engine host. User namespaces require UID/GID mappings and ownership translation across rootfs, volumes, and binds. | Private, host, container-sharing, invalid target, target restart/removal, inspect, process/network visibility, volume ownership, events, and cleanup match Docker for every claimed mode. |

## DNS, hosts and networks

| ID | Rejected fields or operation | What implementation requires | Acceptance evidence |
| --- | --- | --- | --- |
| UC-NET-001 | `Dns`, `DnsOptions`, `DnsSearch`, and `ExtraHosts` | Add typed DNS and host-record fields to `ContainerSpec`. Translate DNS to tagged `ContainerConfiguration.DNSConfiguration`. Apply extra hosts through the guest host-table API before the user process starts, preserve image defaults and Docker special values such as `host-gateway`, and reconcile records without overwriting user changes outside the managed block. | `/etc/resolv.conf`, `/etc/hosts`, search and option behaviour, IPv4/IPv6, duplicate and invalid entries, rebuild, restart, inspect, and DNS failure match Docker. |
| UC-NET-002 | Network `Options`, `IPAM`, `EnableIPv4`, `EnableIPv6`, and non-`bridge` drivers | Extend `NetworkSpec` with plugin, plugin options, IPv4/IPv6 subnets, gateways, allocation ranges, and auxiliary addresses. Map the overlapping subset to tagged `NetworkConfiguration`. Add allocator and plugin support for Docker forms Apple cannot express. Driver names are capabilities, not aliases: a driver can be accepted only when the selected Apple network plugin provides the same connectivity and isolation. | Explicit and automatic subnets, IPv4-only, IPv6-only, dual stack, gateway, allocation, overlap, internal network, driver option, inspect, DNS, connectivity, and cleanup match Docker. |
| UC-NET-003 | `Attachable`, `Ingress`, `ConfigOnly`, `ConfigFrom`, and non-local `Scope` | These fields belong primarily to Swarm and cluster network control planes, which the pinned Dev Containers workflows do not currently emit. Full Docker API support would require a cluster-scoped network store, config-only networks, inheritance, ingress routing, attachability policy, and multi-node lifecycle. Until that product scope exists, keep the fields explicitly rejected and outside the north-star exit gate unless request-corpus evidence brings them into Dev Containers scope. | If brought into scope, multi-node create, attach, service ingress, config inheritance, inspect, failure, removal, and recovery must match Docker. Otherwise CI proves that the pinned supported clients never emit them. |
| UC-NET-004 | Endpoint `Links`, `IPAMConfig`, `MacAddress`, `DriverOpts`, and `GwPriority` | Map MAC address to tagged attachment configuration. Add allocator reservations for static IPv4, IPv6, and link-local addresses. Implement links as a scoped alias/host-record contract visible only to the linked container. Define supported endpoint driver options explicitly. Preserve attachment order and default-route selection for gateway priority. | Static addresses, MAC, scoped link aliases, scaled targets, collision, recreate, route selection, multiple networks, inspect, DNS, and cleanup match Docker. |
| UC-NET-005 | Network connect and disconnect after create | Add tagged Apple APIs that can hot-plug and remove a network interface from a running container, update guest routes and DNS atomically, and retain aliases and IP configuration. Do not emulate this by recreating the container because that changes identity, process state, events, and mounts. | Running and stopped connect/disconnect, force, alias and static IP, DNS, routes, inspect, events, target failure, service restart, and cleanup match Docker without container recreation. |

## Mounts and volumes

| ID | Rejected or incomplete surface | What implementation requires | Acceptance evidence |
| --- | --- | --- | --- |
| UC-MNT-001 | Mount `Consistency`; bind `Propagation`, `NonRecursive`, `CreateMountpoint`, `ReadOnlyNonRecursive`, and `ReadOnlyForceRecursive` | Extend `RuntimeMount` and persistence with each option. Prove whether Docker Desktop treats consistency modes as behavioural hints or no-ops before accepting them. Recursive and propagation semantics need a Linux mount namespace on the guest side and a tagged Apple/VirtioFS contract. `CreateMountpoint` can be bridge-owned only with safe, project-bounded path creation and rollback. | Nested mounts, host-to-guest and guest-to-host propagation, recursive read-only behaviour, missing source, consistency modes, inspect, cancellation, and cleanup match Docker. |
| UC-MNT-002 | Volume `NoCopy`, `Labels`, `Subpath`, and `DriverConfig` | Store volume labels and driver identity. Implement Docker copy-up for the default case and suppress it for `nocopy`. Validate and mount a subpath without path traversal. Driver configuration must select a registered volume provider and keep credentials out of state and logs. | Empty and populated image targets, `nocopy`, valid and invalid subpaths, labels and filters, driver options, persistence, sharing, inspect, `rm -v`, and leak checks match Docker. |
| UC-MNT-003 | Tmpfs `SizeBytes` and `Mode` | Carry size and mode in `RuntimeMount` and translate them to the tagged Apple tmpfs filesystem options. Preserve omission/defaults and validate Docker's range and octal mode rules before create. | Default and custom size/mode, full filesystem, permissions, inspect, restart, and cleanup match Docker. |
| UC-MNT-004 | Image-declared anonymous `VOLUME` lifecycle, NC-007 | Allocate a separately owned anonymous volume for every declared target, seed it with image content using Docker copy-up rules, and mount it before start. Persist container-to-volume ownership and implement remove, `rm -v`, auto-remove, commit/recreate, and crash recovery. Keeping the path on the writable root filesystem is not equivalent. | Multiple targets, pre-existing content, persistence, isolation, inspect, commit, rebuild, `rm -v`, auto-remove, failed create, service crash, and zero-leak cleanup match Docker. |
| UC-MNT-005 | Mount types other than `bind`, `volume`, and `tmpfs` | Classify every mount type emitted by supported client versions. An image mount needs a read-only image snapshot with Docker subpath rules; a cluster mount needs the cluster volume control plane described below; named-pipe mounts have no Linux-on-macOS equivalent and remain rejected unless a typed proxy is designed. Never treat an unknown type as a bind. | Each claimed type has create, filesystem, inspect, permission, failure, restart, and cleanup parity. Corpus evidence proves unused Docker-only types remain outside the Dev Containers gate. |
| UC-VOL-001 | Non-`local` volume driver and `DriverOpts` | Add a versioned volume-provider SPI with explicit create, mount, unmount, inspect, list, and remove operations. Bind driver configuration to a provider fingerprint, validate option schemas, and make provider loss or upgrade recoverable. The runtime-neutral core must not import `ComposeCore`. | Provider lifecycle, options, credentials, concurrent mounts, restart, missing provider, inspect, filters, forced removal, and cleanup match Docker. |
| UC-VOL-002 | `ClusterVolumeSpec` | This is a Swarm/CSI-style cluster volume contract, not a current Dev Containers requirement. Implementing it requires topology, capacity, access modes, availability, secrets, snapshots, publish/unpublish, node identity, and a durable control plane. Keep it rejected unless supported-client evidence brings it into scope. | If brought into scope, multi-node provisioning, attachment, failover, access-mode enforcement, secret handling, inspect, recovery, and deletion match Docker. Otherwise corpus validation proves it is never emitted. |

## Ports, interactive I/O and credentials

| ID | Rejected or incomplete surface | What implementation requires | Acceptance evidence |
| --- | --- | --- | --- |
| UC-IO-001 | Published-port protocols other than TCP and UDP, principally SCTP | Add the protocol to `PortBinding`, the Apple runtime transport, the host forwarder, conflict detection, inspect projection, and cleanup. SCTP requires guest kernel support and a host transport or proxy with equivalent association and multi-stream behaviour. | IPv4/IPv6 SCTP connection, association restart, port collision, dynamic host port, inspect, event, and cleanup match Docker. TCP and UDP remain regression controls. |
| UC-IO-002 | Attach stdin and attach resize | Interactive exec now uses a host PTY around the tagged Apple CLI and applies Docker `ConsoleSize` plus subsequent resize requests; non-interactive exec retains the typed direct process API. Completing attach still requires a tagged Apple session that exposes the running init process's real stdin and PTY, including resize, without starting a replacement exec. Preserve Docker framing, EOF, half-close, detach, cancellation, and exit semantics. | TTY and non-TTY attach, stdin, stdout/stderr framing, EOF, repeated resize, slow reader, disconnect, cancellation, and container exit match Docker. Interactive exec and VS Code lifecycle flows remain regression controls. |
| UC-AUTH-001 | `container-compose` provider `registryAuthentication` | Define a versioned provider credential contract using a keychain reference, credential helper, or private file descriptor rather than secrets in arguments or environment variables. Cover pull, build, and Feature resolution, token refresh, logout, and redaction. The provider must report `native` or `emulated` only when the selected implementation actually completes those flows. | Authenticated and anonymous registry, helper, expired token, wrong scope, build, pull, Feature, logout, diagnostics redaction, and cleanup match the Docker lane without credential retention. |

## Delivery order

The implementation order should close the largest number of real Dev Containers gaps without waiting for new Apple primitives:

1. **Bridge the tagged surfaces already available:** stop signal, shared-memory size, DNS, sysctls, read-only root, supplemental groups, ulimits, network subnets/options, MAC addresses, tmpfs size/mode, and lossless typed labels.
2. **Add bridge-owned lifecycle state:** stop timeout, restart policy, anonymous-volume ownership/copy-up, and exact recovery.
3. **Expose lower-level OCI controls through tagged Apple APIs:** no-new-privileges, seccomp, masked/read-only paths, namespaces, and guest cgroups.
4. **Add genuinely new runtime primitives:** GPU and general devices, dynamic network attachment, exact fractional resource controls, advanced mount propagation, and SCTP.
5. **Complete provider contracts:** registry authentication and optional external volume drivers.
6. **Certify each slice independently:** add exact negative fixtures first, then Docker/stock/provider positive parity, real VS Code coverage where the property is editor-visible, and repeated timing evidence.

The implementation must remain sliced by capability. A broad "pass Docker options through" change cannot establish parity because it obscures which semantics the selected runtime actually enforces.

## Completion checklist

- [ ] UC-API-001 classifies every request member emitted by the pinned supported clients.
- [ ] Every in-scope catalogue row is implemented or has an upstream tagged-runtime blocker with an owner and issue.
- [ ] Every implemented row has a Docker oracle and stock Apple differential fixture.
- [ ] Provider-only behaviour is fingerprinted and does not expand the stock claim.
- [ ] `/info`, provider descriptors, inspect responses, and the conformance ledger report the same capability state.
- [ ] Unsupported requests still fail before creating containers, networks, volumes, mounts, host files, or state records.
- [ ] Recovery, cancellation, event order, cleanup, and performance evidence are retained for each capability.
