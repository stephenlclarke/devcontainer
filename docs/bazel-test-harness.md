# Bazel test-harness replacement

Status: implementation in progress, not release qualification. The existing harnesses remain until their replacement covers their complete scenario inventory. The operator explicitly authorised replacing them on 17 September 2026. The new implementation must be simpler, faster, recoverable and at least as comprehensive; wrapping the existing lane-wide runner is not the destination.

## Execution boundaries

| Layer | Owner | Cache and recovery boundary |
| --- | --- | --- |
| Product unit tests | Native Bazel Swift test targets | Declared sources, profile and toolchain; retain the original XML and coverage before output reuse |
| Harness unit and socket-component tests | Native Bazel test targets | Deterministic fixtures; no Container, Docker, credentials, downloads or product builds |
| Process/service integration | Explicit native Bazel integration targets | Fresh execution with a host lease; retain process outcome, cleanup and coverage from that execution |
| Runtime parity | One case, lane and explicit campaign identity | Published release inputs only; never compile a missing reference or candidate; successful independent cases can resume within the same campaign |
| Performance campaign | Fresh uncached live cases under a quiet-host lease | Prefetch first; record all setup, operation and cleanup durations and invalidated samples; do not confuse cached test durations with fresh execution |

Bazel owns dependency scheduling, action reuse, test selection and reports. Small test probes and evidence helpers do not schedule builds, silently retry cases or implement another build coordinator. The first direct Engine probe uses standard-library Python solely as a test client and has no Docker CLI dependency, including in candidate lanes.

## Current replacement components

`Tools/testing/case_evidence.py` seals each case in a transactional SQLite record. Its identity binds the explicit campaign, fixture, lane, contract, harness, release set and runtime fingerprints. Reopening a completed case returns the same authenticated result, including failures. A changed result cannot overwrite it. An unfinished case requires worker/resource reconciliation rather than an automatic restart; other independent case identities remain available. Production admission, artifact fingerprinting, host leases and resource reconciliation must be connected before this evidence component can qualify live campaigns.

Results require separate monotonic setup/operation/cleanup durations, exact observations, explicit errors and cleanup status/resource inventory. Missing observations or uncertain cleanup cannot produce a passing result. The comparator rejects absent or duplicate lanes and mixed campaigns/contracts/releases. It reports ordinary duration ratios separately from functional parity; quiet-host paired performance qualification and its 10x/timeout gate remain to be connected. This component does not claim an arbitrary raw timing observation passed the performance gate.

`Tools/testing/engine_probe.py` replaces E01's Docker-CLI-based HTTP access with direct Unix HTTP. It preserves the five checked-in E01 observations: ping, prefixed API, HEAD ping, unknown-route error envelope and malformed-JSON error envelope. Socket component tests exercise actual HTTP traffic, invalid versions, negative responses, oversized replies, missing endpoints and timeouts. They are tests of the harness, not proof against the three real runtimes.

The shared HTTP reader also rejects early EOF when a declared `Content-Length` remains unsatisfied. Python's size-limited response read can return a short body without raising; a complete JSON record in that body is not proof that an image build or other mutating request completed. The regression sends valid JSON followed by an early close and reproduces the missing rejection at `4be157b5-0cb3-4159-b739-01e834f2d4c1`. With framing validation, all 346 native harness tests pass at `f02b1a6d-3a8c-4cf1-b6e7-fc6c12ab08f1` (4.554 seconds development elapsed), including exact-size-limit acceptance and existing HEAD behavior. Response-size bounds, phase deadlines and ownership/uncertain-state recovery are unchanged. These disposable socket tests touch no live runtime and do not complete E04 or qualify a benchmark.

`Tools/testing/archive_probe.py` ports E05's five archive assertions to direct Unix HTTP: exact content, a 1 MiB binary payload, long filename, exact mode and relative symlink target. It generates deterministic PAX input and inspects the bounded returned tar in memory, never extracting server-supplied paths onto the host. It requires an admitted full container ID and API version; the caller supplies an isolated disposable container, durable resource ownership, a whole-phase deadline and verified cleanup. It does not create a container, pull an image or run Docker. The released guest adapter now connects these boundaries; socket-component success is not live E05 parity. Request timings and sanitized failure types remain available to the case recorder.

`Tools/testing/guest_fixture.py` provides that probe's container ownership boundary. It verifies an already-prepared immutable image ID, refuses existing names, journals creation intent before the request, checks exact returned ID/name/labels/image, starts the guest and connects the archive operation. Removal is by verified immutable ID only; completion requires absent ID/name and an empty owner-label query. An interrupted create can be reconciled when the exact owned resource is observed; a missing response with no observed identity remains uncertain, even after a 404, because creation could still be pending server-side. Interrupted deletion resumes using its retained ID. Journal intent binds the socket and API version as well as the case/image identities. The released adapter supplies the admitted runtime/guest data, host lease, private durable journal and phase deadlines. Component tests exercise the sequence over a real test socket, not a Docker or Apple VM; live qualification remains required. No implicit image pull, compilation or broad cleanup is introduced.

Run the focused native harness checks with `make bazel-harness`. Individual tests and monotonic durations appear in Bazel's JUnit evidence, retained by the existing launcher. Scratch remains on the enrolled SSD. No Swift product compilation is in this target's dependency graph.

`make bazel-prepare-releases` connects the reviewed release-asset lock to executable payloads. Extraction stays on the enrolled SSD; archives and reusable verified executable trees are long-lived internal assets. The existing reference-store lease covers acquisition, preparation and publication. Preparation rejects archive traversal, links, duplicate entries, oversized payloads and missing executables. Apple installer packages are expanded with `pkgutil`, never installed; installer scripts are not executed. Every extracted file and mode is bound into a retained inventory. Executables publish under `~/Library/Application Support/ContainerFamily/retained/workflow/prepared-releases/<preparation-sha>/` with a durable pending intent before copying. Admission refuses pending publication; interrupted copying verifies existing entries, reuses complete files and finishes only owned incomplete files. File/directory durability and complete inventory verification precede removal of the pending marker. Sealed executable trees are never silently repaired or overwritten. Repeated preparation reuses them even after SSD extraction-cache eviction, without re-extraction or compilation. Package expansion does not establish signing/notarization acceptance or prove the complete runtime closure is ready.

This placement follows the long-term asset policy. An SSD-resident stock API executable stalled in `dyld` opening its own binary, while the byte-identical installed internal copy completed a launchd `--help` diagnostic in 0.268 seconds. That comparison is a host-location diagnostic, not parity or permission-policy proof: the later retained-tree integration attempt below still timed out. The workflow retains its own exact release tree rather than trusting the operator's installed tree or changing permissions. Runtime state, native logs, sockets and all disposable test roots remain on SSD.

### Dev Container reference tools

D01 image configuration proves environment, workspace, root user and post-create execution against the official reference and both candidate runtime profiles, with bounded execution and verified cleanup. Docker and stock candidate observations pass as recorded below. D02 Dockerfile configuration also passes reference and stock observations. Both three-lane contracts are handed off incomplete on enhanced guest-release admission. Tool preparation alone is not fixture completion.

Run `make bazel-prepare-devcontainers-cli` before the reference lane, or add `OFFLINE=1` for read-only reuse. `Tools/bazel/devcontainers-cli.lock.json` pins [Node 24.21.0's official distribution](https://nodejs.org/dist/v24.21.0/) and [Dev Container CLI 0.88.0's npm distribution](https://registry.npmjs.org/@devcontainers/cli/0.88.0). These prebuilt official distributions are an explicit exception to GitHub release-asset sourcing: the workflow neither rebuilds them nor installs global packages. CLI version and npm integrity must also match the existing parity manifest. The reference implementation belongs to the [devcontainers project](https://github.com/devcontainers/cli); these dependencies do not belong to the Docker-free candidate.

The shared reference-store lease serializes bounded HTTPS acquisition, integrity verification and publication. Node extraction retains only its executable and licence; it never materializes bundled npm links. CLI extraction retains the self-contained package and notices and rejects runtime dependency drift. Archives and prepared inventories are retained internally; disposable download/extraction state stays on SSD. A recorded interrupted copy can resume, a complete recorded copy seals without refetching, and changed sealed assets or foreign files fail closed. Offline mode cannot download, repair or complete pending publication.

Seventeen deterministic tests cover immutable reuse after SSD eviction, corruption, invalid pins/bounds, archive traversal/links/duplicates, interrupted publication and clean bounded download arguments. Native Bazel preparation gate `d7c1634e-2b12-415e-815b-1385fe8496d3` passes all 92 preparation tests. Focused missing-line trace covers 142/154 executable lines in the new preparation module and 26/27 in the selected Node extractor; this is not aggregate product coverage. Actual acquisition and offline revalidation return identical preparation identities (`3eab884b3727e17a20f96b5ac61ad353746fde7ceb8a494d2a1d8792bc24f26f` for Node, `58fd5a18056d9c6e7600e8e898b3e74f86539b844993bd934ffc0a185ab5076b` for the CLI). Clean-environment version probes report `v24.21.0` and `0.88.0`. No guest or Docker service was started for these checks, and no D01 runtime result is claimed.

## Cutover requirements

### C02 dependency health and service selection

The current stock package pair passes the unchanged four-observation C02 contract and zero-residue cleanup: campaign `c02-current-packages-20260920a`, harness `321063a`, invocation `82123a6c-9462-4f8c-83da-8e7cbced2d3d`. It reuses devcontainer package `2d579a4c-71bb-4407-ae60-c75cecdb2d3c` from `f744a85` and Compose package `94187a84-5393-4f1c-8849-0c8447ccb476` from `8acef00b`. The latter passes package smoke at `38331b7d-3557-44c2-8423-8b4a33362178`; its separate cached package target retains the archive without recompilation. Raw setup/operation/cleanup are 15.861627584/7.844498750/2.116091708 seconds; seal `75e0bce6c65831fcb340b8105d46c78b997fd6fcd1207370c007cbd99a5e1dae`. Recovery is clear. This is non-quiet functional evidence, not full foreground, enhanced-provider or release qualification.

**Earlier packaged stock proof:** `1d5b7c55-6f1b-4ec5-bf97-83654756afc1`, campaign `candidate-c02-quiescence-20260919a`, passes all four unchanged observations with zero errors and zero remaining owned resources. Source is `35ced5d68ed8c0776c824532a6591e9dd5cecdff`; Dev package `6c04245c-9ddb-4768-88a4-9c3f268b93ba` has archive SHA-256 `debe61cc289324faef91d017e43bb5011a2fa4ab340aaf47f2c14d5475cbc04d`. It reuses Compose package `0954dc90-72ca-4a6f-84c3-9e6b6ee272b2` at `6af61953b9c467bfbf06da228088ef7c48b1310f`. Seal `c98e4444f02af8f39e1db4ff9d5d06ee60ff073611bfd196c2bf3995261eb05a` authenticates setup/operation/cleanup of 11.708850750/7.050163500/2.114855959 seconds. The read-only report revalidated these results on 19 September. The campaign remains unqualified because Docker/enhanced matching is absent. This supersedes the earlier stock-current paragraph below, not its retained evidence; it does not prove live partial-failure recovery, quiet benchmarks or subsequent uncommitted I/O changes.

**Exact enhanced guest distribution:** on 19 September the published Compose 0.15.1 authority bundle was downloaded afresh and matched its release-asset SHA-256 `1185c113b662c356ff5e37844b6a7559ce41d4003cbad2e97a0e83ee632bb054`. The existing authority verifier passed for the pinned stack and receipt `700c3419435d41dc1a83af4d299304d3a9b922d13f752f855df0f1ab38ca3d8d`. The retained initialization archive matches its exact `9e18071a3c508d4376bd734fa0a22c61326907edd4ec35a74d84edce17062596` input. The expected GHCR tag still returned `MANIFEST_UNKNOWN`; restoring those unchanged bytes with preserved digests failed because the saved credential lacks the required package-write scope. No substitute guest was built or published. Credential correction and registry restoration/admission remain prerequisites for enhanced live qualification.

**Recovery development checkpoint:** receiver-side quiescence and failed-creation partial inventory are now implemented and component-tested. The private socket's version-1 recovery control freezes new mutations, tracks all handlers including health inspections, prevents new health probes after freeze and rejects proof after unobserved streaming/attach/detached-exec work or failed health completion. The native provider checks active lifecycle generations and the durable pending-create journal. C02 requires a nonzero CLI exit, stopped command groups and its captured engine epoch before sealing any partial inventory, then revalidates exact ownership/incarnation/membership on every cleanup pass. Missing protocol support, restarted engines, late native work, foreign resources and unknown completion stay quarantined. A partial receipt never changes the original failed case into a passing test.

Affected gate `d61b95d7-57fa-478a-98dc-9ef54b328dce` passes 375 Swift functions (model 25, state 11, core 15, API 113, native 211), plus the unchanged cached 887-test harness. Invocation time is 10.027 seconds; native 9.5 seconds, API/core/state 0.8 seconds each. The new recovery barrier covers 48/48 instrumented lines and native helper 14/14. Python test success is not coverage evidence: the generic Bazel coverage collector reports no Python coverage. Focused `77cfffaf-6088-49ec-9aed-12283b5da949` also passes. Earlier `a6b8aee2-1391-4df2-80a2-eb1badfa7446` exposed an async protocol-default dispatch bug; concrete and existential store regression checks now prove the real SQLite implementation is selected. Independent complete receiver/harness review found no remaining actionable source finding. Live fault qualification, new packaged-candidate proof, enhanced matching and release gates remain outstanding; the stock result below predates this recovery change.

**Current stock result:** live invocation `314e6af8-48cb-4f9a-b6bf-77715097361c`, campaign `candidate-c02-bind-policy-20260919a`, passes all four original observations, including `startup_dependency_dns`, with zero errors and verified cleanup of all owned resources. Runtime recovery reports `clear`. It uses signed source commit `498fec8aeab64b67b9761fc20595de254602f216`, Dev package invocation `fa632764-23f9-4e3f-92b7-43ce86c13b84` (archive SHA-256 `2169d0c7d7f81f05c9942ff02e0f299259fb7c4c94bac54c105e41b292515dba`) and unchanged Compose package `0954dc90-72ca-4a6f-84c3-9e6b6ee272b2` at `6af61953b9c467bfbf06da228088ef7c48b1310f`. Package/smoke qualification took 28.041 seconds; smoke 3.2 seconds. The authenticated case seal is `55c3d7ac9f3d2134a36bf266db7d6224af5d546c6269597d9e1ee04b4d9841cd`; setup/operation/cleanup took 12.015230375/7.176422917/2.383378292 seconds. These are single-run functional timings, not quiet paired benchmarks. The full campaign report deliberately remains unqualified because its enhanced and Docker lanes are absent. This supersedes earlier stock startup-DNS failures below without modifying them. Partial-create recovery, prepared foreground I/O, matched enhanced proof and release qualification remain open.

Prepared-gateway live invocation `f060a72a-624b-4bc6-a00a-b6311d0f90e5` (campaign `candidate-c02-gateway-20260919a`, case `91c14dfadeb6f056ad7f6361ab2b7b7c9e1cbc16831753cc713e498aca348a5a`) used Dev candidate `6766a6ea-5489-4f30-95ad-5f0be965f3f6` and Compose candidate `0954dc90-72ca-4a6f-84c3-9e6b6ee272b2`. Both dependencies started through the gateway, but app creation failed with HTTP 501 for `HostConfig.Mounts.[0].BindOptions`; startup DNS was not reached. Setup/operation/initial cleanup durations were 12.201860166/5.773468542/0.025493042 seconds, not quiet performance evidence. The failed case remains unchanged. Subsequent identity-verified operator recovery removed the exact two guests and project network, retained private diagnostics, stopped the captured engine and restored original services; the recovery report is clear. Partial-create automatic C02 recovery remains an open harness gap.

The bind-source candidate maps Engine `CreateMountpoint` to durable optional runtime policy and implements missing-directory creation at native mount preparation. It preserves the [Docker distinction between structured mounts and legacy volume syntax](https://docs.docker.com/engine/storage/bind-mounts/), validates native syntax without creating paths during preflight, preserves existing user contents and never treats the created directory as container-owned cleanup. Explicit propagation/recursive policies remain unsupported. Focused gate `0bc1e258-9eaf-4a13-b899-46f8a2991a47` passed eight new test functions. A broader gate identified an existing expected short-bind value that needed the newly explicit policy; `01f4ac47-1e0e-49ba-9be5-f4f971f3e64f` then passed all affected targets. Review additionally found the missing-source `=` parser ambiguity, now rejected before mutation with a no-residue regression. Final affected gate `8705dc39-032b-41cd-8a47-0702b95d21cf` passes 366 functions (model 25, state 11, core 15, API 105, Apple 210), reusing four unchanged target results and executing the updated Apple target in 10.1 seconds (20.245-second invocation including compilation). The new native policy helper has 25/25 instrumented executable lines covered; this is not aggregate product coverage. Strict changed-file SwiftLint, Markdown and diff checks pass, and the independent source re-review found no remaining actionable issue. A Swift Testing macro compile failure in intermediate `91e80f88-83f6-47ec-9f45-f849c2aa77cc` is retained, not presented as passing evidence. These are development candidates, not stable-release or full live parity evidence.

The final image-declaration review is clean. Its validation helper covers 18/18 instrumented lines; the matching Compose launch helper covers 33/33 lines. Strict touched-source/test and Markdown lint pass. Earlier component counts in this section describe preceding checkpoints, not the current complete gate.

Prepared Compose launch now negotiates the candidate's versioned `ContainerImageReference=1` extension. Create sends an immutable config ID in `Image` and the caller's historical declaration in `ContainerImageReference`; the gateway validates the config-ID form and resolved identity before allocation. Optional `requestedImageReference` metadata changes only inspection/list presentation, not runtime lookup, process defaults or image identity. Recovery uses the existing incarnation-checked metadata overlay; raw native observations and missing metadata never infer the declaration from labels or current tags. This is historical caller spelling, not independent proof that a mutable tag currently points to an ID. Legacy digest-only native alias validation is unchanged.

Final affected stock gate `fb8a4796-e98d-47e7-a7b7-ec43cec06d78` passes model/state/Core/API/native suites (358 functions; 29.844 seconds including compilation, target times 0.6/0.8/0.8/0.8/9.4 seconds). Tests cover a missing declared tag without re-resolution, pinned declaration, invalid display and immutable binding, provider mismatch, round-trip persistence and native metadata recovery. Matching Compose gate `75eeb643-9238-4e60-b5da-544a95b6bbbc` passes its full Engine adapter with old/unknown gateway refusal and create/start partial-failure checks. This cross-repository component proof does not yet close live C02; prepared foreground attached I/O remains a release blocker.

The gateway now persists finite `StopTimeout` values and applies them to stop/restart unless `t` explicitly overrides the policy. No configured value selects the Linux Engine ten-second default; zero remains an immediate timeout. Pinned Moby sources define [configured policy selection](https://github.com/moby/moby/blob/568f755ebeb1ac9c6a8febbda6cd371ea0a9630b/daemon/container/container.go#L564-L570) and [the Linux default](https://github.com/moby/moby/blob/568f755ebeb1ac9c6a8febbda6cd371ea0a9630b/daemon/container/container_unix.go#L20-L24). Malformed/overflowing strings return 400; negative or above-Int32 values return unsupported before allocation/signaling. Indefinite waits remain a parity gap: stock Apple's signed timeout is not a wait-forever primitive. Signal query overrides and stopped-container status parity are not changed here.

Focused timeout invocation `89e5b926-594c-4453-8f63-342928662f38` passes six test functions, including parameterized defaults, overrides, validation and model round-trip (7.160 seconds including compilation, target 0.6 seconds). The native recovery test uses a new runtime and the metadata-applied observation path; focused invocation `d39da389-56f9-469f-81a7-aed10f072cc0` passes. Final affected gate `15ed83a5-ed51-4b30-9094-4448512c540e` passes 354 functions: model 25, state 11, Core 15, Docker API 98 and Apple runtime 205. Invocation time is 9.447 seconds; unchanged first four target results are cached (0.4/0.7/0.8/0.8 seconds), native target 9.0 seconds. The timeout helper covers 24/24 lines; strict touched-source/test lint passes. Initial test compile and raw-observation recovery assertion failures remain retained at `fcb5f03e` and `bdcd6d8e`. These component runs do not establish live stop parity, quiet benchmark performance or C02 completion.

The direct Engine API now supports `Healthcheck.StartInterval` through strict request decoding, optional persisted model data and inspection. It accepts zero/default or at least one millisecond, uses the configured startup cadence while starting within grace, and anchors interval selection to the last accepted probe completion. A delayed inspection therefore cannot postpone a probe already due from startup scheduling. Source references are pinned Moby [health scheduling](https://github.com/moby/moby/blob/568f755ebeb1ac9c6a8febbda6cd371ea0a9630b/daemon/health.go#L237-L304) and [health validation](https://github.com/moby/moby/blob/568f755ebeb1ac9c6a8febbda6cd371ea0a9630b/daemon/container.go#L286-L306). Legacy Compose label policy v1 remains unchanged and rejects explicit startup interval; the evaluator remains inspect-driven and its immediate first-check behavior is unchanged. No background-monitor parity is claimed.

Focused health gate `68d42ed9-c8e0-46ca-9c6d-994e1443455e` passes nine functions, including default/explicit cadence, first-success and grace-expiry transitions, concurrent reservations, API round-trip, invalid values and old JSON decoding. Final affected stock gate `2bbf7abc-a4d6-4ddc-b103-fe0b09761828` passes 348 functions: model 25, state 11, Core 15, Docker API 92 and Apple runtime 205. Invocation time is 28.573 seconds including compilation; target times are 0.3/0.7/0.8/0.7/10.2 seconds respectively. Strict touched-source/test lint and independent final review pass. The initial missing-schema-allowlist failure is retained at `a7cc667d`. These are component timings, not quiet benchmarks, a live Docker oracle or C02 closure.

Engine environment removal is now distinct from an empty value. Bare `Env` keys suppress image values in the native process, remain bare keys in API inspection and survive metadata adoption; legacy CLI creation explicitly rejects removal rather than converting it to an empty string. The optional metadata field preserves older records. Component gate `ab0f5743-b2f4-4c0d-ab34-50ec3aea8320` passes 317 test functions (model 25, API 87, native 205; 15.480 seconds invocation, target times 0.5 seconds cached / 0.9 / 9.7). The new environment parser covers 20/20 lines. This is not live environment parity or a benchmark. Compose's process translator is still being integrated; no new C02 qualification follows from this correction.

The gateway candidate projects memory bytes, shared-memory size, read-only root filesystem, sysctls and Linux stop signal into descriptor-bound native creation, inspection and durable metadata. Invalid sizes/signals and conflicting Apple-forced sysctls fail; legacy CLI fallback rejects nonempty execution settings instead of dropping them. Missing legacy metadata adopts observed settings, while new requests retain requested defaults. VM memory allocation is not Docker cgroup accounting parity. Live enforcement and Compose routing remain unqualified.

Execution-settings component gate `c39402d8-6be2-46eb-b588-fb910b6fd44d` passes 310 affected test functions (model 22, Docker API 85, Apple runtime 203; 10.055 seconds invocation, target durations 0.5/0.7/9.2 seconds). Focused instrumented coverage is 29/29 model lines, 18/18 router conversion lines and 39/39 native projection/adoption lines. Independent final source review found no substantive issue. This is component evidence, not a quiet benchmark, live parity result or release qualification.

The gateway now distinguishes omitted/null entrypoint from an explicit empty array and the `[""]` reset sentinel. Process defaults are obtained from the descriptor-bound OCI configuration, resolved before native/CLI submission and persisted for subsequent inspection/recovery. Old metadata retains its previous inference through an optional marker. The reference policy is pinned Moby `568f755ebeb1ac9c6a8febbda6cd371ea0a9630b` (`docker-v29.5.2`): [image-default merging](https://github.com/moby/moby/blob/568f755ebeb1ac9c6a8febbda6cd371ea0a9630b/daemon/commit.go#L69-L76) and [entrypoint reset](https://github.com/moby/moby/blob/568f755ebeb1ac9c6a8febbda6cd371ea0a9630b/daemon/create.go). Empty CMD inherits only when the requested entrypoint has zero elements; `[""]` suppresses inheritance before clearing the sentinel. This source-backed component implementation is not a new live Docker oracle result.

Process-default component gate `90993d87-45c4-42e8-bbb5-36c3d5d275b6` passes all 297 affected test functions (model 17, Docker API 82, Apple runtime 198; 25.749 seconds invocation; 0.3/0.7-second cached model/API results and 11.0-second native suite). The resolver has 17/17 instrumented lines covered. Tests cover the entrypoint/command matrix, native executable/arguments, descriptor-bound defaults, actual creation metadata and reinspection after runtime recreation. Touched source/new tests pass strict SwiftLint. Compose gateway routing, live C02 and full release qualification remain open.

Gateway DNS configuration is now retained through create, native submission, CLI fallback, JSON/typed native adoption and Docker inspection. Optional persisted DNS settings decode older metadata; legacy absent settings use observed native DNS, while explicit empty settings remain explicit. IPv4/IPv6 nameservers and single-token search/options are validated before submission. Explicit nameservers override the BuildKit host-resolver fallback without dropping search domains/options. This removes a configuration-loss obstacle to the Compose gateway cutover; it does not connect Compose launches or establish live resolver parity.

Final stock Bazel component invocation `f12eb9e6-7f77-404e-9237-5d1f7fbefb1d` passes all 291 functions across the three affected targets (model 15, Docker API 81, Apple runtime 195; 24.818 seconds invocation, respective Bazel test durations 0.3 cached / 0.9 / 9.4 seconds). `RuntimeDNSConfiguration.swift` has 25/25 instrumented lines covered. Touched source and new standalone tests pass strict SwiftLint; final independent source review has no remaining findings after correcting typed-inventory and legacy-metadata DNS loss. Initial compile and test-fixture failures remain retained as development evidence, not product parity results. No quiet benchmark, aggregate SonarQube or release claim follows from this component gate.

Deterministic gateway startup-order proof: stock Bazel invocation `8195ce86-6eda-4377-96a5-d7f673c5645a` passes 36 creation/lifecycle test functions in one suite (10.074 seconds invocation, 1.2 seconds Bazel test duration). The new test reads the real managed backing file inside fake native `bootstrap` and `process.start`: a native-only database peer's distinct native hostname and service alias are present at both boundaries, an isolated-network peer is absent, and the app's allocated address appears only after bootstrap but before process start. File transfers are injected fakes, not live daemon calls. This protects gateway ordering, not Compose routing or live stock parity; the four-assertion stock failure below remains open. An earlier test-development invocation `55b8a311-3949-4b69-9b99-b40f1abe85d3` was stopped after the peer fixture reached a default live file client; that failed evidence is retained and the fixture now explicitly supplies its fake.

The strengthened C02 contract now preserves all three original observations and records the application's first `getent hosts database` result before sleeping. `overrideCommand=false` ensures the original command executes. The probe reads the saved result; later exec-time DNS repair cannot rewrite it. Docker passes all four assertions, while stock Apple fails only `startup_dependency_dns` with the unchanged product packages below. Both cases clean up with zero owned resources. A successful first lookup alone would still not prove installation before entrypoint: startup ordering also needs deterministic component proof because client exec during `up` can race with the original command.

| Lane / campaign | Retained case ID | Setup seconds | Operation seconds | Cleanup seconds | First lookup |
| --- | --- | ---: | ---: | ---: | --- |
| Docker / `docker-c02-startup-20260919a` | `ab4827ffe79639adbd839d7c186278547a783e364ef66546abbf17d0210611c3` | 20.599481042 | 3.614325208 | 3.370473916 | true |
| Stock / `candidate-c02-startup-20260919a` | `6e32fd56818f13e380dd4c600529d17892c2fd226307b8739eaa1c1907bfee5f` | 11.095968458 | 6.513630250 | 2.313965917 | false |

These are diagnostic functional timings, not quiet comparative benchmarks. Both cases bind contract SHA256 `830a44c8d633ebae56900e8e4b81b265d69d78e6bb4ef61516c86043d60a8650` and harness SHA256 `182cd36f1cfb1ec9aca90f828225b6202a7fe24a679c2fbfa159c42acdd8a18b`. The retained case identities contain each exact release-set and runtime fingerprint. Forty-two focused harness tests pass, including a counterexample proving later successful DNS does not replace failed startup evidence. This stronger oracle supersedes the three-assertion pass below for current C02 qualification. The next implementation must connect Compose creation to managed pre-start host installation without dropping service configuration; repeating exec-only reconciliation cannot fix this failure.

Current stock proof passes every unchanged observation (`dependency_dns=true`, `dependency_health=healthy`, `run_service=true`) and cleanup with zero owned resources: invocation `c63c4ae3-cab5-44b8-ae21-79a61ed0493b`, campaign `candidate-c02-fixed-20260919a`, devcontainer `64db1c6d581819caecb2702a389bdb66d8193dab`, unchanged Compose `c8a031de` / package `b0b1242a`. Devcontainer optimized package/archive gate `cdfeef29-d810-4adc-9424-5c869300be56` passes (28.024 seconds; smoke 3.1 seconds), archive SHA256 `cbbadeaf586fc52eb7ebee9a2dfa1114116875196d27ff475d1ff1585d9dc538`. The live case records setup 12.080025208 seconds, operation 7.833320125 seconds and cleanup 2.316374666 seconds; these include diagnostics and are not quiet comparative benchmarks. Case ID `b40a3bb600f60340245c11fa05b6a425d3f0c6d290374d82d08fce6f36b55279`, result seal `618b85d6eaac600d90dfac7957e0af7557b56c091018be08fc26459f4037a153`. Existing Docker reference proof is retained; enhanced qualification, startup-before-entrypoint behavior and full release gates remain incomplete. This supersedes failed/pending stock statements in the diagnostic history below.

Root cause and correction: traced stock invocation `242739fd-fa10-4c9e-9355-8e49cc27d96d` at temporary diagnostic head `015fe38` records `exec eligible=false` for every native exec. The HTTP layer's `RuntimeLabels.projectComposeLabels` synthesizes Docker mirrors; internal native snapshots do not contain them. The runtime identity helper incorrectly required those response-only labels. It now accepts complete native version-1/project/service/non-one-off identity, while rejecting any present conflicting Docker mirror. Native-only and absent-mirror regressions fail before correction (`b6cf5194-e3d6-4471-914f-023ba28d817e`); focused stock coverage `ddb0b277-1e7e-45dd-82e2-0c72a530d7f7` passes 12 functions across two suites, including real runtime exec preparation with native-only fake snapshots. The identity helper covers 17/17 lines; unrelated LCOV overflow warnings are not aggregate coverage evidence. Temporary tracing is removed. Corrected live proof remains pending; startup-time managed mounts and enhanced qualification remain separate requirements.

Latest diagnostic evidence (19 September): the `e8796ba` stock candidate/archive gate passes (`96d15fd5-7624-496b-a79b-0e922c593932`), but unchanged C02 still fails (`5b1c1d6b-d0dc-42dc-9c6f-05717bdfb90b`). Subsequent diagnostic-only invocations reuse that binary and the unchanged Compose `b0b1242a` package: `ddf0aba7-86a7-41bb-8765-688415cdbba0` retains all three running services, healthy database, shared observed addresses and an app hosts file with no managed block; `8ca8aae0-e558-4b99-92ba-1546b3600366` also proves the adopted app specification contains the correct user-defined network. In isolated experiment `13e861e0-ff61-4042-be85-7307f69fe419`, a direct gateway exec-create request returns 201 but still leaves that hosts file unchanged. This rules out a client dispatch bypass as the sole cause; the exact reconciliation branch remains unproven. Every run records passed cleanup with zero owned resources. These are failed functional/diagnostic runs, not quiet performance or release qualification. They supersede the older pending-live statements below.

The maintained harness retains selected state/network fields and post-probe hosts archives privately, without environment values or an extra exec. Known-stopped containers skip archive diagnostics; the current archive API does not provide an atomic never-start guarantee if the container exits between inspection and transfer. Candidate metadata reads are read-only and select only the owned app's identity, attachments and timestamps. Diagnostic failures do not replace original observations. Forty focused reference/native harness tests pass; independent review is clean with the archive race caveat above. The one-off forced exec experiment has been removed from maintained code. Next diagnosis must instrument actual reconciliation branch decisions instead of repeating uninstrumented tests or changing Compose exec routing.

Running-state reconciliation checkpoint: native Bazel coverage invocation `513f3d0a-5e63-45a2-9744-78db52601143` passes all 167 Apple runtime and 79 Docker API test functions (the unchanged API target is reused from cache), with a 9.386-second invocation. The new service-identity helper covers 17/17 executable lines; this is focused coverage, not aggregate SonarQube coverage. Focused `f91f887a-fe13-43d9-ac53-4477e43e7a40` passes 14 functions, including cancellation, stale incarnation/start generation, isolated projects, held-transfer process exit/listener cleanup and the unchanged two-read startup budget. Full source review and strict source/Markdown lint are clean. These are component results only; no new live C02 candidate or quiet timing comparison has run since the sealed failure below.

Status: active; Docker passes. The corrected stock candidate now starts all three services and verifies actual database health, but the unchanged guest probe fails service-name resolution. Cleanup passes with zero owned resources and recovery is clear. Full stock/enhanced qualification remains incomplete. The original three-service Compose file, devcontainer configuration and DNS/health/runServices probe are unchanged.

Stock invocation `1dca60f5-1fed-46fd-8978-61024e5f588a`, campaign `candidate-c02-native-20260919b`, uses devcontainer `c7fe7d238effaf5c145a772e1a45031ae0975d8b` and Compose `c8a031dedf1ab53d06848d8a744d194b5f25c2d1`. Startup/health verification succeeds; guest commands report `bad address` for `database` and `helper`, producing `dependency_dns=false`, empty `dependency_health`, and `run_service=false`. Setup/operation/cleanup are 11.928013917/8.077078834/2.587960000 seconds. Case `00a784a0ebef5a8d282512225d2362bb972edb78b766219fe0104cdac378e75d` has seal `25fac5b96c066cf0bb0cfc90bc03c14099f4c2580d01a26620b894adb03a466d`; its failed result remains immutable. The health correction is progress, not a C02 parity pass.

The native Compose CLI path bypasses the stock adapter's creation/start reconciliation. The current development change projects validated mirrored Compose service labels into managed hosts using actual shared-network addresses, protects target incarnation and observed restart identity, serializes updates, preserves unmanaged entries and reads complete inventory inside the queue. Ordinary list/record decoding does not trigger transfers; exec preparation reconciles only its requested target and propagates cancellation. Focused invocation `861bf545-57c3-43e7-9233-6f2ac26df0e2` passes 13 test functions, including the unchanged two-read startup optimization. This does not establish DNS before an externally launched entrypoint executes. Stock Apple 1.4.1's [network CLI contract](https://github.com/apple/container/blob/1.4.1/docs/command-reference.md) has no alias option; [upstream alias request 1839](https://github.com/apple/container/issues/1839) remains separate. The [remaining managed-file startup design](../DESIGN.md#startup-time-name-resolution-remaining-c02-implementation) must be implemented and exercised before C02 closure. Rejected preboot-copy experiments were never connected to lifecycle operations and are not shipped source.

Optimized candidate/archive checks pass: devcontainer `e77bcd2e-3cc2-4310-a066-b11befca22ce` in 28.154 seconds, archive `98ca71b13d76f869991bdbf9de3f8024c5fa0fbf2f90a20247c31610eae6ccb7`; Compose `b0b1242a-f8ce-4f99-a855-e4217c7817f6` in 41.120 seconds, archive `85eef2f570920759b2cc2bc300ae6530d118bee6721726c6660af22e79fff2b5`. Both are clean-source, admitted unsigned development candidates. Retained build comparisons report raw ratios of 1.0751 and 0.9948 to the preceding package runs, with `quietHostVerified=false`; these are not controlled performance results. Docker references were not rebuilt or rerun.

The stock candidate advertises `NativeComposeHealthPolicy=1` only for the explicit emulated capability. Matching Compose negotiates before resource creation and native launch, then replaces unsupported health flags with the bounded version-1 `com.apple.container.compose.health-policy` requested-policy label. Both typed and JSON native adoption validate the exact schema, command, duration and retry bounds; verified observed policy takes precedence over older requested metadata after restart. Malformed or unknown policy fails, and labels never assert an observed healthy state. Enhanced native health remains unchanged. Explicit `start_interval` is rejected, not ignored.

The existing inspect-driven evaluator uses actual guest exec results, preserves healthy status until the failure threshold, ends startup grace after success, uses the default five-second startup cadence and honors shorter configured intervals without clamping. Each in-flight result is bound to a unique reservation; stale or duplicate completion after reset, restart or removal cannot resurrect state or release a newer probe. Autonomous background scheduling, health events, persisted observation history and image-inherited health remain outside this bridge's qualified scope, not claimed Docker parity.

Component coverage invocation `9ed92b84-f2f2-4105-988e-9a390b3b7be8` passes the complete affected Apple-runtime (155 functions), Engine-API (79) and Compose-provider (12) targets in 9.898 seconds. The policy validator records 28/28 executable lines and the changed health-registry region 90/90; these are focused measurements, not overall coverage or Sonar acceptance. Earlier focused regression `53fa6935-354b-47a0-9436-368381524968` includes six delayed-completion cases across restart/reset/removal with present/missing start times. Compose coverage `99283d36-a683-4b66-819c-47a62c1441b8` covers 72/72 policy and 67/67 transport lines; preflight `d562e2f4-6f9a-485a-8069-16fa7bab038b` proves rejection before resources for up/create/run and offline dry-run. Full two-repository source review is clean. Corrected live proof and complete release gates are still required.

Docker invocation `2946308c-9f01-4f8c-b64e-0519ba571944`, campaign `docker-c02-native-20260919a`, passes all three observations (`dependency_dns=true`, `dependency_health=healthy`, `run_service=true`), cleanup and zero-resource checks at harness source `fe4ef0f`. Setup/operation/cleanup are 20.093952500/3.593916791/3.322737917 seconds; evidence seal `896cf5a66001e2fe323dbbea8710b4b4a76a87d251949453ab8d319d481d0f51`. This is functional evidence, not a quiet paired benchmark.

Stock invocation `79d52880-b2ca-4a6a-a681-55d8ba9a41d2`, campaign `candidate-c02-native-20260919a`, fails before container creation: Compose forwards `--health-cmd`, interval, timeout and retries to unmodified Apple Container, which exits 64. Setup/operation/cleanup are 11.028324333/1.077780000/0.023339000 seconds; the original failed result and unknown cleanup outcome remain immutable. Exact case `32dae2216c99406f574a8eaf8ce0995cc94617bb11402fe081c96d9b472a5367` was subsequently recovered under task-owned recovery authority: full container inventory was empty, the generation-bound unattached project network was removed, the captured Engine stopped, diagnostics/procedure retained, prior services restored and the disposable root removed. Recovery is clear. The next correction must preserve real health semantics through the stock provider; dropping health flags or weakening the fixture is not acceptable. Existing C01 candidates do not qualify C02.

The adapter records all three service generations and the exact default-network membership before checking database health. Sibling services are bound to the unique project, role, working directory, image and creation identity, while the app additionally retains the original devcontainer label and workspace checks. Cleanup revalidates the entire observed project before each exact-ID deletion, removes app/helper/database before the network, and resumes partial cleanup without recreating resources. Unknown creation, missing command completion, foreign or replaced identities, extra members and reappearance after closure preserve quarantine. The dependency-image pull participates in process shutdown and diagnostic recovery; no broad Compose shutdown is used.

Final native Bazel component invocation `a117c4cb-7efd-440f-b71d-b506582e40a8` passes 841 affected harness tests. Fifteen focused real-Unix-socket component tests cover the C02 boundary; standard-library trace records 149/159 executable adapter lines (93.71%). Full independent diff review is clean. These are development component results, not native DNS/health proof, aggregate SonarQube coverage, quiet benchmarks or release qualification. Run `make bazel-engine-case CAMPAIGN=<unique-id> LANE=docker CASE_FIXTURE=C02-compose-dependencies` for the reference; native lanes additionally require the existing `CANDIDATE_INVOCATION` and `COMPOSE_CANDIDATE_INVOCATION` inputs.

### Foreground init attachment (development)

**Initial terminal-size oracle finding:** `E14-compose-terminal-size` gives the admitted Compose CLI an actual 37-row/113-column host PTY on stdin/stdout. Its proposed assertion that the first guest `stty size` sees those dimensions **fails on both Docker and stock Apple**. Both retain the identical raw guest prefix `stty: standard input\r\ncompose-stdout\ncompose-stderr\n`, with zero owned residue and clear recovery. The pinned [Docker CLI start path](https://github.com/docker/cli/blob/v29.6.1/cli/command/container/start.go) invokes [terminal-size monitoring](https://github.com/docker/cli/blob/v29.6.1/cli/command/container/tty.go) only after `ContainerStart`; matching host dimensions before the first guest command is therefore not established as a Compose-reference guarantee. Neither failed result is converted into a pass. The robust CLI initial/ongoing-resize contract needs to distinguish this startup race from explicit Engine `HostConfig.ConsoleSize`, which the stock gateway still rejects when nonzero. No guest wrapper or Apple fork change is justified solely by this failed stronger-than-reference assumption.

Both runs use signed harness source `ccb46b444f74f56cfb2908ffccf9f53ade04b91d`, harness SHA-256 `1bd94122305de77d081f0062643485b07bcd5398782baba41c14d643f145ef6f`, and contract `90bf4f0210afb4bbdb0b1dbc2759d959e1b22ca76fa1ae6a25f918d712dfda00`. Docker campaign `compose-terminal-size-oracle-20260920a`, invocation `56c8b8aa-fee3-41dd-9570-377723fb0fc7`, case `63169f74e06a7ebcc439c6c9611b3c7050e81974cdb7da786b341dd5d41f1029`, retains result SHA-256 `d1bac32c98a2425e8ca2e31905dbaeaeac538d60597cb56ea1adc9f38e9e628f`. Setup/operation/cleanup are 15.069630791/0.144084875/7.802717833 seconds. Stock campaign `compose-terminal-size-stock-20260920a`, invocation `6b00071b-b6e9-46a2-8a1e-8acef85ee44b`, case `50ea6382f09833fa4f8946230ae432a02f58866cda67e6a4c5c9f3d910ac7dfc`, retains result SHA-256 `ad7ceb00ecd8486600606e38a053e1ff36d7bc3fdb824f9aa9f8a163e0939cc4`. Setup/operation/cleanup are 10.019745958/1.342623250/6.614844917 seconds. Stock reuses devcontainer package invocation `b25c3954-5cf7-4e58-bc83-2349fce044df` and Compose `bc01cea0-1db6-416d-9017-e74f482fe0d9`; their exact immutable source/archive identities are below. These failed functional durations are not completed-workload or quiet benchmark evidence.

The E14 harness retains exact initial CRLF and subsequent raw bytes, TTY inspection, exit and automatic-removal assertions. Host line discipline is raw to avoid adding echo or a second newline conversion. Capture stays on its exclusively created descriptor; oversized output records truncation while owned cleanup continues. Use `CASE_FIXTURE=E14-compose-terminal-size` with E09's lane/candidate arguments. Component invocation `bb93beb9-bea0-4cda-99d1-f484c145677d` passes 971 harness tests in 44.708 seconds (44.6-second Bazel target), including real host-PTY IO, wrong dimensions/exit, extra bytes, pathname substitution and overflow recovery. Independent complete-diff review is clean. Initial validation `47c6cb6b-898e-4c8d-bcb8-b47c1ab5b7ca` exposed invalid test expectations for unknown creation; corrected tests require quarantine rather than weakening recovery. Live failed evidence takes precedence over passing component simulations; full terminal/C02 parity remains incomplete.

Independent contract review found no pre-first-instruction host-size guarantee in `DESIGN.md`, `COMPATIBILITY.md` or Compose's coherent design. E14 version 2 therefore implements the corrected CLI contract without changing either retained failed result: preserve the first-query bytes/status; require convergence to inherited 37-by-113 dimensions within five seconds, without any harness Engine resize or host SIGWINCH assistance; then change the host PTY to 53-by-121, signal the verified CLI incarnation once and require those exact guest dimensions within a separate five-second bound. Every raw query, status and phase duration is retained with the exact transcript, guest exit and automatic removal. Convergence is not pre-entrypoint correctness, and observing initial resize is not acknowledgement that Docker has installed its subsequent SIGWINCH handler; that startup window remains visible if encountered. Explicit nonzero Engine `HostConfig.ConsoleSize` remains a separate unimplemented create-time contract. Version 2's live qualification is pending.

Version 2 component invocation `88d4d2c7-a615-4ec5-a785-e6abf9bd9899` passes 973 tests in 46.713 seconds (46.6-second target). Negative real-PTY tests prove that absent inherited-size convergence and missing host resize fail without guest/API assistance. Deterministic delayed-snapshot regression `0e2d6927-7a6d-42fd-9df7-fc71f6a4d242` fails before deadline enforcement; the corrected sampler checks time before submission, during observation and before accepting a complete response. Independent complete-source review is clean. These remain component results, not live qualification or aggregate coverage evidence.

The affected devcontainer runtime component suite also passes under the enhanced compile profile: invocation `c3e35ce6-637d-4c68-92c0-bec353bcabd1`, 262 functions in 30 suites, 39.677 seconds overall and 9.8-second Bazel target duration. This verifies the selected enhanced APIs and deterministic recovery tests, not the separate enhanced VM startup or live parity gates.

**Stock signal live result:** campaign `compose-signals-stock-20260920a`, invocation `347cf245-9f7e-4b75-9eb0-9f379701280b`, passes all five E13 observations (USR1 forwarding, continued identity, TERM forwarding, guest exit 23 and automatic removal), with zero owned residue and clear recovery. It uses signed devcontainer `e6ca215158d8881e6a06c2472bb64347ed4f4944`, signed Compose `4e5073a44e3ef8cab9a0c8b2b43a1fee66e00170`, the unchanged E13 harness/contract fingerprints below, and stock Apple Container 1.4.1. Setup/operation/cleanup are 10.120383042/3.018196125/1.338684958 seconds. Case `60bbd9ec827e29843d4aa471e43b73f1d6331d8408ac5cb57725190092add149` retains result SHA-256 `f7e95226d6fa79e961085d4d694b5d5209d4a399def7a7559c20eee7eb71a635`. Docker fails earlier on duplicate USR1, so its incomplete operation duration is not a comparable benchmark denominator. Stock success is not Docker parity, enhanced qualification or full C02 completion.

Both optimized packages and smoke checks pass: devcontainer invocation `b25c3954-5cf7-4e58-bc83-2349fce044df` takes 29.217 seconds (3.2-second smoke), archive `bc74b04f5694d2f6df31ff1f2383726401ce57fee57167d0d0bca99ec5e9ff6b`, preparation `c269fdbf3fc2711c37ecfd6307c06cf621ad8f1c768f5ebb7d08bc81e7864bf5`; Compose invocation `bc01cea0-1db6-416d-9017-e74f482fe0d9` takes 39.240 seconds (1.6-second smoke), archive `1cba2624fd123885db2ca19e5e96e7725c0f46dbbad4097b7b1cf91ee6b86b4e`, preparation `957dc122b8f68e24fbac2281425b027669a68dfee46a2806983d832373cf4a0d`. These are immutable local integration candidates, not signed/notarized stable distributions. Build durations are retained observations, not quiet-host performance qualification.

**Current signal implementation and oracle result:** prepared Compose foreground launch now installs HUP/INT/QUIT/TERM/USR1/USR2 forwarding around the actual attachment, targets the immutable created ID by symbolic signal name and preserves the registered guest exit when delivery races removal. Process-wide handler ownership is cancellable, and cancellation joins interrupted signal requests before restoring host state. Devcontainer keeps published listeners through non-exiting signals; attached process exits or incarnation-bound fallback observers close them. Restored/CLI-started containers retain observers through transport failures, stop on confirmed absence/restart, and join even replaced observers during shutdown. Dynamic-port SQLite restoration retains its existing inferred start baseline. No dependency or stock Apple source change is needed.

The real Docker E13 campaign `compose-signals-oracle-20260920a`, invocation `1a71c2a5-93ba-4333-a0d8-9be3f0debe85`, **fails** the exactly-once USR1 assertion: one verified host signal produces two guest `signal:USR1` lines. Cleanup passes with zero owned resources; the later TERM marker is cleanup, not successful execution of the fixture's TERM phase. The pinned [Compose run path](https://github.com/docker/compose/blob/v5.3.1/pkg/compose/run.go) and [CLI attached-start path](https://github.com/docker/cli/blob/v29.6.1/cli/command/container/start.go) both install forwarding for this non-TTY request, consistent with the observed duplicate. This is an open oracle/compatibility issue, not a normalized pass or permission to waive parity. The stock live result above passes the fixture, while Docker equivalence, enhanced live qualification and the wider C02 signal contract remain open.

That failed oracle uses harness source `4796d232ffee116c14530cd17671fcabeae0bfb2`, harness SHA-256 `6f0c5f80512826252a210cb72237f08944b39f47b0e1acd056ad928b2ad00679`, contract `cf6ff7c3e965325306e35df1668e1f0a145d2ae6eb97422c35a36fbacccafef5`, and case `e4f466fdc1deaa0382e50d05a85afe7ad1e725a8a4e51c4d4790dc14e64ff1df`. Retained result SHA-256 is `387007e81241857bc171cad04a037fc0c111acb4af6af62098cccfcd8491d8e9`; setup/operation/cleanup are 14.202229708/0.185633708/2.856546500 seconds. These are failed functional-run timings, not quiet benchmark samples.

Focused regression evidence: removing Compose forwarding fails 15 assertions (`6ed508bb-4518-47cb-b24c-145cad8ae86b`); removing queued-owner and active-handler cancellation fails five (`665a7055-50aa-4ee4-abdc-885c169ae15e`). Corrected Compose `f6df1380-6238-4bc4-b649-290f1bc4234c` passes 1,132 Core and 95 Engine runtime functions in 53.275 seconds, with unchanged results reused after mutation restoration at `489ea210-9704-4566-ae04-bf33c2fd6dd1`. Devcontainer's unconditional signal cleanup mutation fails three signal cases (`a7f0f4b5-0887-4c68-a7fa-fda2390cbc48`). New shutdown regression `ce6788bf-913e-4fe3-b080-a737c20b498a` reproduces the retired-observer leak; its two nil-start expectations were invalid test premises because running snapshots already infer creation time. Corrected `b9bd2f33-2da1-4b2b-8533-8eb1d3738ab1` passes all 260 runtime functions in 21.682 seconds (9.2-second target). Final visibility-only `ff7efc2c-c659-4d34-9a60-f512f0a36a51` keeps the forwarding helper internal and passes the same 260 functions in 24.489 seconds (8.9-second target). Independent complete-diff review is clean; touched Swift files pass strict lint. These non-instrumented component results do not certify 90% coverage, enhanced live behavior, full signal parity, or a release.

**Compose signal forwarding:** supporting fixture `E13-compose-signals` sends host `SIGUSR1` and then `SIGTERM` to the admitted Compose command, not directly to its guest or process group. Each send revalidates the unreaped child incarnation. The guest must emit its USR1 trap marker, continue with the same immutable container ID, then emit its TERM marker and exit 23; exact separate streams and automatic removal remain required. Command, signals, identities, diagnostics and duration are journalled under the existing cleanup/quarantine contract. Use `CASE_FIXTURE=E13-compose-signals` with E09's lane/candidate arguments. Component invocation `255aaa4a-5596-498e-9fd2-c314ec96f692` passes 961 harness tests in 28.705 seconds, including eight real-child signal regressions. Review corrected the deadline test to prove it expires after SIGUSR1, not during startup; the final complete-diff review is clean. Earlier `b71ddd46-bbd5-47df-ae7b-2580927391b2` rejected uppercase journal-entry names; corrected names retain the exact signal inside the payload. The later live Docker result above is failed and retained; the stock live result above passes and enhanced qualification remains pending. This does not establish all-signal, terminal, enhanced or full C02 parity. The pinned [Docker Compose run implementation](https://github.com/docker/compose/blob/v5.3.1/pkg/compose/run.go) installs forwarding before attached start and uses [Docker CLI 29.6.1's signal forwarder](https://github.com/docker/cli/blob/v29.6.1/cli/command/container/signals.go); the maintained executable oracle is the behavioural authority.

**Invalid Compose terminal input:** `E12-compose-tty-input` runs the admitted Compose CLI with piped stdin and explicit `--tty`. A network-free dependency uses the prepared immutable image; the job uses a deliberately absent image with `--pull never`. The fixture requires a running dependency, exit 1, the pinned reference's exact terminal-input diagnostic as the final stderr line, empty stdout, no one-off job and no prepared job image. It journals configuration, command, process identity and dependency identity, retains both output streams and removes only the verified dependency. Unknown creation or additional owned resources remain quarantined. Use `CASE_FIXTURE=E12-compose-tty-input` with the same lane/candidate arguments as E09. The first live Docker invocation `9cd397aa-9d7e-40f3-baa9-2b18a454fdc4` started the dependency and exited 1, but correctly failed because the expected older diagnostic differed from the actual pinned-release message: `cannot attach stdin to a TTY-enabled container because stdin is not a terminal`. Its cleanup passed with no owned resources; the failed evidence is preserved. The expected line now matches that observed reference, with strict complete-line comparison. Corrected component invocation `dbadc081-4822-4ff9-a0d4-b037edf39c47` passes 953 tests; independent review is clean.

Docker campaign `compose-tty-input-oracle-20260920b`, invocation `da981850-e558-4696-919c-31991a178627`, and stock campaign `compose-tty-input-stock-20260920a`, invocation `eb01d889-799b-4f55-bede-32b0623155b1`, both pass all five observations with zero owned residue and clear recovery. Both use harness source `98271ac9743a6832a73dadc52732a0238b1c03ea` and fingerprint `7a662736e8b44a47889ac463cdd282a97f4ca18ed5702c47a90f0f38a66b9d95`. Docker setup/operation/cleanup are 15.024693583/0.202405500/2.928434500 seconds, seal `5903d44ab11b0b99e3c6943255016cec486a0577c7e1d257ad5c4d20db1dd527`; stock records 9.961572584/1.830071125/1.507568083 seconds, seal `2e300ecd799dc33260f354a7dde43b91fe5def05e17d4e5e9babc37b630e24b5`. The raw operation ratio is approximately 9.04x, informational and not a quiet-machine benchmark. Separate campaigns preserve published-reference/local-candidate identities.

Stock uses signed Compose source `0873ff5b5d52be929de96a9e28d193f5ef2bbf45`, optimized package/smoke invocation `5fa1fdda-246b-4d33-a110-ffc964b756ec` (37.190 seconds; smoke 1.6 seconds), archive `557793e1b210abe3bdd5e77a332c7bb8c2bef1f859263c32cc190c5f17317933` and preparation `745f7d4389376d566f24a20913257b4f4a62c28a37a7e9b2b0b3957e65c109b6`, plus reused devcontainer `43a05aee822ccbe19414ed0e0684fa81f669f0ab`. Compose's component mutation `9d0685ca-6085-43dd-9bad-9b5b160fd48f` fails without input rejection/prepared dependency requests; corrected `32f46429-5af0-4a4d-85dd-245edf8cfdd1` passes 1,130 Core, 113 Plugin and 93 Engine functions. These component checks establish sequencing; live snapshots establish post-error state, not absence of transient image/job work or independent diagnostic-emission ordering. Enhanced qualification, other foreground boundaries, accepted coverage/SonarQube, full quiet benchmarks and stable release remain open.

**Redirected Compose terminal selection:** `E11-compose-redirected` supplies `tty: true` in its network-free service but invokes the admitted Compose CLI with redirected stdin/stdout and no terminal or interactive flags. It requires effective `Tty: false`, exact piped guest bytes, input round-trip, exit 17 and automatic removal. It shares E09's immutable command/configuration receipt, candidate admission and recovery, not a global Compose executable. Component invocation `0d15dddc-64a5-4267-833e-22a59a527d2e` passes in 26.117 seconds. Both Docker and stock now pass all five observations with zero owned resources and clear recovery at harness source `0cc8715bc81a1ec3123db74b1f9c5262c5bb5bd1` and harness fingerprint `308e49f2119c89020eaa067258a8d9b0ba97b1bb37f187488a5e649c43d8e44c`. This fixture does not cover every explicit terminal flag, terminal size, signal forwarding or enhanced qualification. Use `CASE_FIXTURE=E11-compose-redirected` with the same lane and candidate arguments as E09.

Docker campaign `compose-redirected-oracle-20260920a`, invocation `73971fd2-6de7-4cfa-ac67-b60a1a54c646`, records setup/operation/cleanup 14.008298833/0.199918042/2.793132583 seconds and seal `19dfefa77cf49f3b9760c20a0a855e1093417e3f96cf2f12b2a4654f8802a62a`. Stock campaign `compose-redirected-stock-20260920a`, invocation `ebfcd164-6a90-422a-8f37-6940e067cf02`, records 9.928944375/2.985906084/1.379791292 seconds and seal `1e7e1945a42848dd98ec5c1ed6560b50999e71dfab183e7d1005cf7ba31bfe3c`. Stock uses signed Compose source `02f9330222b434a1398cb9f757151619c9026ba3` and reused devcontainer `43a05aee822ccbe19414ed0e0684fa81f669f0ab`. Its Compose optimized package/smoke invocation `1104cdd0-11ca-4f4f-96b8-eb29d1d94db3` passes in 36.376 seconds; archive SHA-256 is `4d98cad384cc5994868b0f1197a254784351a91138e6468584c7915ec741b9a2`. The raw operation ratio is approximately 14.94x, an open performance concern, not a quiet-machine benchmark pass. Separate campaigns keep published-reference and local-candidate identities distinct. This is not enhanced qualification, complete foreground parity, accepted coverage/SonarQube evidence or a new stable release.

**Quiet Compose regression:** Bazel-only `E10-compose-quiet` runs the same admitted Compose CLI and five guest observations as E09, with `--quiet` added to the journalled, executed command. It checks that quiet progress does not disable piped input, guest output, exit 17 or automatic removal. Complete stdout/stderr and raw durations are retained; these five observations do not certify byte-for-byte equality of CLI progress diagnostics or full foreground parity. Use `CASE_FIXTURE=E10-compose-quiet` with the same lane/candidate arguments as E09. The maintained fixture passes on Docker and stock at harness `66989b3`, Compose `0a5759a7` and unchanged devcontainer `43a05ae`; enhanced qualification remains pending. The earlier standalone Docker diagnostic is not substituted for this evidence.

Campaign `compose-quiet-20260920a` retains Docker invocation `de91b9cd-37fe-46c4-ba3f-d30d7eb54d63` with setup/operation/cleanup 14.033865417/0.183482584/2.702635084 seconds and seal `873806dd15831fec81442694231b9440a77781961be60a3558b387573b80cdc5`. Stock invocation `48721ab7-bc61-4a94-9a63-8e2f435b5cd6` records 9.848377042/3.027546375/1.311230750 seconds and seal `d8b4ef4fe936a7352fc0e85e6b9b281a95595238f44d9fa1f9c68035fa6d73a5`. Both pass all five observations, leave zero owned resources and have clear recovery. The raw single-run operation ratio is approximately 16.50x and remains an open performance concern, not a quiet-machine benchmark pass. The report deliberately refuses a published-release comparison: the same harness fingerprint has different release-set identities because stock uses local candidates. Neither this refusal nor the missing enhanced lane is waived.

E10 component invocation `8a9b23dc-cc32-4fce-ad2c-122d9dd4b502` passes all 940 harness tests in 27.491 seconds including Bazel overhead. Thirteen focused real-child-pipe tests cover 125/128 executable probe lines (97.66%), with source SHA-256 `4ef4ceb5d19657854f3ae3b414d73c54a03138b1371199b3838de3d07ea85fcd`; annotated tracing is retained in `diagnostics/compose-quiet-e10/compose_foreground_probe.cover`. Full independent diff review is clean. This is harness-only coverage, not product coverage or live parity. The stock Compose release-config package/smoke invocation `20cdad56-ae15-4ec6-a077-3603dd9c31ca` passes in 33.824 seconds; archive `718097bc973300388ac4d7e4fe719750dfe103ee9d80e296e75148d8bd6c9085` is prepared for local candidate integration only.

**Current E09 stock proof:** signed Compose `b8aaa52581e54b9e7ee777ffec6e0f067f2e94b3` and devcontainer `43a05aee822ccbe19414ed0e0684fa81f669f0ab` pass all five real CLI observations at invocation `7ab7395e-ad88-47ba-ad82-ea08f424ca30`, campaign `compose-foreground-input-20260920a`. Compose's previous non-interactive default was the remaining failure after network-mode correction; its default/explicit input projection and detached stdin lifetime now match the pinned reference. Setup/operation/cleanup are 10.027357458/2.955054250/1.338451792 seconds, with zero owned resources and clear recovery. Seal: `26a1ecfe5f0972a323d48f25a1d38c7a9f35471f87643b03d4c25394ce1e5cde`. This proves default piped Compose input/output, exit 17 and auto-removal on stock Apple only, not full foreground parity, enhanced qualification, quiet benchmarks or a stable release.

**Earlier E09 diagnosis:** Docker invocation `2af1f65b-3cb3-44ab-90ca-8eedb7078976` at harness `c4c3fd0` passes all five observations; setup/operation/cleanup are 15.136621083/0.191552625/2.752644125 seconds, with seal `7e32d2aeab705c8c25426126aa5cba3516d84a2e3f664a010d814f03af23f18d` and zero residue. Stock invocation `985f04a6-c350-472b-aea6-7c40811ee2f0`, using the E08 devcontainer package and unchanged Compose package `94187a84-5393-4f1c-8849-0c8447ccb476`, fails its strict running-container inspection. The API hard-coded network mode as `default`. That failed result remains retained; exact captured CLI/Engine shutdown, empty native inventory and zero pending creates allowed safe operator reconciliation, after which recovery reports clear. The corrected adapter persists the accepted selector, including network wire IDs, independently of explicit endpoint attachments; legacy metadata still decodes. Omitted/empty/`default` selectors normalize to `bridge`, matching the pinned Linux Docker Engine. The probe now retains the narrow pre-validation inspection fields to diagnose future failures without exporting environment data. The corrected default-piped stock result is recorded above; enhanced and wider foreground qualification remain pending.

Pinned Docker 29.5.2 diagnostics `c5339dc9807ce2ed3f661fb515a6317f926ac55c9a0df0f351779be9b55aa987` and `8f00734019121f2f5ebb3f5b68041a1583c31eefd0bb39f01fe959af04b7be19` retain requests, before/after-start inspection, statuses, source and cleanup in `runtime-cases.sqlite` (campaigns `network-selector-diagnostic-20260920a` and `network-selector-diagnostic-20260920b`). Docker accepts `none` plus an explicit named endpoint and a named primary selector plus a different endpoint: inspection retains the selector while the explicit endpoint controls attachment. A proposed rejection was withdrawn after this live proof and [pinned Moby source](https://github.com/moby/moby/blob/568f755ebeb1ac9c6a8febbda6cd371ea0a9630b/daemon/container_operations.go) review. These diagnostic cases are not parity acceptance fixtures or quiet benchmarks. The normalization regression fails before correction in `c79c6d2a-69b9-4e11-9c3f-5243c28c1866`; the earlier `none`/`bridge` regression is `7097944c-8a72-4223-aef4-ec865368a175`.

E09's final component invocation `b0b3067a-6d44-46f8-904a-e6541896819b` passes 938 harness tests (27.324 seconds including Bazel overhead). Twelve focused tests exercise real child pipes and Unix inspection with a simulated backend. Source `59f5b757825e659e03d9cdf3006b53857386b8e6a6f9be3ca124205efca9e0a6` has 97% focused trace coverage; annotated evidence is retained in `diagnostics/compose-foreground-59f5b757825e/compose_foreground_probe.cover`. Independent full-diff review found and corrected missing process-reopen proof, mismatched diagnostic recovery paths and missing native executable selectors; the final complete re-review is clean. This is harness coverage, not aggregate product coverage or live Compose qualification.

**Current E08 proof:** stock invocation `f95f7fb2-d25f-4baf-b106-a497e23b5251`, campaign `foreground-terminal-route-20260920c`, passes all seven observations with zero owned residue and clear recovery. It uses signed devcontainer `6f37aefe7e2e59c356b33f3f0a4a83de8f191475`, package `0a1e6add-d56e-432e-9dd5-f87d15541a1e`, archive SHA-256 `b8eb63072520c47c346492d79eca88ec1f16929408f0b9e15b0d8740fc98ecf0`, and unmodified Apple Container 1.4.1. Result seal: `1a1d12c1c2b189fffc879d36ac90b0f3c9a58aa0c9904434629d0694155c87ef`. Setup/operation/cleanup take 10.066741958/2.270453125/1.326601250 seconds. The Docker operation below takes 0.157329958 seconds: this raw single-run ratio is about 14.43x, above the diagnostic threshold, and remains open for quiet paired measurement and optimisation. These non-quiet functional runs do not certify performance. Enhanced-profile declaration tests also pass (`ef73ce99-9925-4d1f-b14b-b40e0237ac35`), but enhanced live proof remains blocked by private-provider startup while the Mac is locked. Initial terminal size, arbitrary subscription races, engine-process reopen, full CLI proof and release gates remain incomplete. The earlier E08 failures and pending-stock statements below describe superseded checkpoints, not the current stock result.

**Actual Compose CLI probe:** Bazel-only `E09-compose-foreground` runs the admitted Docker Compose release or matching native Compose candidate directly, with no product rebuild or global CLI lookup. It prepares one network-free service using an already-present immutable image, starts `run --rm --no-deps --pull never -T`, observes its exact guest ID before supplying stdin, and verifies separated guest output, CLI status 17 and automatic removal. Both complete diagnostic streams and monotonic command/phase durations are retained. Cleanup first proves the owned command group stopped, then reconciles only the authenticated resource; reopened or unobserved creation stays quarantined. A real-pipe/Unix-inspection component suite covers positive and negative paths, including interrupted diagnostic retention. This supplements C02 and E08; it does not prove interactive TTY handling or remove their outstanding requirements. Run through `make bazel-engine-case CAMPAIGN=<unique-id> LANE=<lane> CASE_FIXTURE=E09-compose-foreground`; native lanes also require `CANDIDATE_INVOCATION` and `COMPOSE_CANDIDATE_INVOCATION`. Docker and default-piped stock results are recorded above; the enhanced lane and wider foreground contract remain pending.

After the AutoRemove fix, stock run `4fe614dd-35c8-47b7-a685-56dac3834cd3` creates and starts the terminal, receives its exact markers and acknowledges wait, but resize returns `501`. Cleanup succeeds with zero residue. The provider declaration omitted `engine.route.ContainerResize`, so the public gateway rejected the request before reaching the implemented generation-checked runtime method. The declaration now includes that route; no Apple fork change is required. The focused declaration regression fails before the fix (`478fcd66-4dfa-4ecb-a3a5-c05ed92bd2e4`) and passes afterwards alongside the attachment-generation tests (`c78d0da0-3cee-4c10-b4ff-38f40abcf882`). Full native E08 remains pending. The changed legacy process smoke test keeps its unavailable-route check using genuinely unimplemented pause instead; it is not claimed as executed here because it requires the separate host-Keychain lane.

The first live E08 oracle passes all seven observations against pinned Docker at harness `5f0588d` (invocation `95934f4d-4286-4407-8257-34ed04a26e47`, campaign `foreground-terminal-20260920a`). Setup/operation/cleanup took 14.954567000/0.157329958/2.735904375 seconds; these are non-quiet functional timings, not release benchmarks. Stock candidate `f744a85` (`6e5d4768-251c-449d-ba03-0e09cd30b2cd`) failed after create because inspection omitted `HostConfig.AutoRemove`. The strict cleanup check quarantined the case; exact ID, owner labels, image, command and created state were subsequently verified, the sole owned container removed, captured engine stopped and prior services restored. Recovery did not rewrite the failed result. The adapter now projects the stored automatic-removal policy; a three-case regression (true, false, omitted/default false) fails before the fix (`435d19ad-5d4b-4868-8197-821dcee22462`). A rebuilt immutable stock candidate must pass E08 before this failure is considered resolved at runtime.

The supporting fixture, Bazel-only E08, exercises raw init-terminal stdout/stderr, acknowledged `next-exit` wait before startup, running terminal resize, default detach keys, continued process identity, stdin after reconnect, status 17 and automatic container removal. It uses the existing pinned Docker oracle, immutable native candidate and ownership/quarantine path; all seven observations must pass in each lane with exact output and exit semantics. The command explicitly disables terminal echo and newline conversion rather than normalizing captured output. It captures a guest-kernel UUID once per shell process and requires the identical held value after reconnect, so restarting init inside the same container cannot satisfy the detach assertion. A 30-second interaction deadline and 90-second whole-operation deadline bound failure. Initial terminal size before the first instruction, arbitrary subscription races and engine-process restart remain explicit uncompleted C02 requirements, not waived by E08. Native E08 qualification is pending; no new stable support is claimed.

The AutoRemove fix passes 130 API component tests in instrumented invocation `94506250-84c0-4063-9e40-87a023886691` (10.722 seconds). The added projection line executes 44 times. The coverage tool still reports malformed counters elsewhere, so this passing instrumented test run is not valid aggregate release coverage. SwiftFormat, strict SwiftLint and the changed-document Markdown checks pass.

E08 component invocation `87fbca03-1836-4007-b329-37f112849f44` passes all 925 harness tests (26.948-second Bazel invocation), including 12 focused Unix-transport and error-path tests. Removing the post-reconnect incarnation check makes the same-container restart regression fail; restoring it passes. Tests reject ineffective/rejected resize, stopped init after detach, unacknowledged wait before startup, wrong/truncated/oversized exit receipts, invalid process markers, pending auto-removal and configuration drift before cleanup. Independent complete-diff re-review is clean. Focused standard-library tracing covers 153/154 executable probe lines (99.35%), source SHA-256 `bc6e625afefaf0608e837eaa668f51518717c19b2cf84c2e7e277a8a4826610b`; the annotated file is retained under `diagnostics/foreground-probe-bc6e625afefa/foreground_probe.cover`. This does not resolve the separate invalid Swift coverage counters. The legacy non-Bazel engine runner cannot execute E08 and must not claim full parity during cutover.

E07 now has eight observations, adding `combined_history_live` to the original seven. During the first generation it opens a second output-only `logs=1&stream=1` attachment after observing both startup records. It waits for those records on the observer itself before sending the 4-MiB binary input, then drains both attachments concurrently and requires byte-identical separated output. Merely acknowledging HTTP upgrade is not proof that saved-log replay is ready. Half-closing the output-only observer must not close the primary stdin. Exact observer lengths/hashes and route durations are retained. This proves a controlled saved-prefix/live continuation, not every output-during-subscription interleaving; TTY, detach, auto-removal and engine-process restart remain separate requirements. The older seven-observation results below do not qualify this extension.

The deterministic header-before-history regression fails before the observer-marker barrier and passes afterwards. The real Unix fake also sends upgrade before taking its history snapshot. Socketpair tests cover output-only half-close, premature input EOF, bounded output, deadline expiry and zero-length writes; duplicate saved prefixes remain failures. Component invocation `3bdbe1a7-23ba-4693-b2f3-2c0f8c795b32` passes all 912 tests (26.513-second Bazel invocation). Independent full-diff re-review is clean. Pinned Docker and stock Apple pass the eight-observation live fixture below; enhanced live qualification remains required. No product rebuild was needed for this harness-only extension.

Focused standard-library tracing of the 13 attachment tests covers 180/186 executable probe lines (96.77%). The retained annotated source is `diagnostics/attachment-probe-8f9d921f0b0c/attachment_probe.cover` under the internal workflow evidence root; source SHA-256 is `8f9d921f0b0c8105c78e98f95ac084d7e44e58b51dcebf0e94f82f68b8994346`. This is focused harness coverage, not aggregate Swift/Sonar coverage or timing evidence.

The eight-observation live campaign `init-combined-20260920a` uses clean signed harness `ec3da380f7b82b61d12b207033b6a896bdd43497`, contract SHA-256 `337aaef71ca81c941a877fbf12f7d1e33437d5a5d9052618d65145db55c09e7c` and harness fingerprint `7bf8b46b3069b8842c9f56dffd384b554805a92c90d051786f1f02348519159f`. Both lanes pass all observations and cleanup with zero remaining owned resources; recovery is clear. Docker uses the pinned downloaded release. Stock reuses devcontainer package `2d579a4c-71bb-4407-ae60-c75cecdb2d3c` from `f744a85` without rebuilding. The raw phase timings are functional observations, not quiet-host benchmarks or a complete three-lane qualification.

| Lane | Bazel invocation | Setup seconds | Operation seconds | Cleanup seconds |
| --- | --- | --- | --- | --- |
| Docker | `2b0ffd34-eca7-4a90-9e06-0d05a634e152` | 15.158114084 | 2.896404375 | 2.782528000 |
| Stock Apple | `94b55b2b-d28e-4983-8da9-39f252ff76f1` | 9.757396125 | 5.135569958 | 1.374941375 |

Retained result seals are `b51d40a9634ebecc31322bd1a67f05738fd87f79552a2eac9998bec27f3071e8` (Docker) and `a6bc3a3974628df5f9a0c5a2ec35e0eb04e14d67ad4a3d004c5fd728321b2b33` (stock). The enhanced startup blocker has not changed: a fresh desktop-control check still reports a locked Mac, and no equivalent enhanced runtime attempt is repeated without new host evidence.

The Bazel-only E07 init-attachment oracle supports the active C02 foreground contract. It uses one exact owned Alpine container and the same HTTP fixture in Docker, stock and enhanced lanes: register non-TTY attachment before each of two starts, transfer two distinct deterministic 4-MiB binary inputs with concurrent output drainage, close stdin, observe separate stdout/stderr and native status 17, then replay and compare complete accumulated history after each exit. It never substitutes exec output or reads merged native log files. Each connection has a 30-second deadline; the whole operation has 150 seconds and uses the existing generation/label/image-checked cleanup and quarantine path. Every HTTP operation retains monotonic duration; ordinary runs do not establish quiet comparative performance. A repeated-generation substitution regression fails at `79f14ae0-c4d3-4ee8-a6b8-a25ea67d244f` before distinct inputs and accumulated expectations are used. Corrected invocation `2e35cc3c-032d-4ffc-8629-18c458cbbf60` passes all 895 harness component tests (19.653 seconds of tests, 20.138-second Bazel invocation); independent full-diff review is clean. Live oracle qualification remains required. This fixture does not cover TTY, detach, auto-removal, engine-process restart or the atomic combined history/live boundary; those remain separate foreground requirements. The legacy non-Bazel engine runner cannot execute E07 and must not be used to claim complete parity during cutover.

Docker reference `4dd83f46-1b50-4f31-a015-2872b40bfa05` passed the first live 4-MiB transfer and status-17 checks but failed the initial raw-history assumption, with verified cleanup. Diagnostic reference `517761ef-ea97-4fbf-9d9d-7f8d56b8a3a0` confirms that history expands stdout from 4,194,330 to 8,388,634 bytes by replacing invalid UTF-8; stderr is unchanged. Both failures are retained. The second run's setup/operation/cleanup were 254.675742208/0.431772750/32.317894750 seconds, including slow Colima SSH readiness, not a quiet benchmark. The oracle now checks effective `json-file` logging with empty options and constructs expected history independently using pinned Moby's [line/16-KiB record assembly](https://github.com/moby/moby/blob/568f755ebeb1ac9c6a8febbda6cd371ea0a9630b/daemon/logger/copier.go) and [per-invalid-byte JSON encoding](https://github.com/moby/moby/blob/568f755ebeb1ac9c6a8febbda6cd371ea0a9630b/daemon/logger/jsonfilelog/jsonlog/jsonlogbytes.go). Actual candidate/reference output is never normalized. Live bytes remain exact; expected saved-log bytes reflect the driver's observable contract. The corrected model and socket tests pass invocation `7fa3f04f-6707-4785-8bf8-5e979cd8be4e` with clean independent review; the corrected Docker E07 reference passes at `28a39aef-089f-4ea9-99e0-019e3d171279` (source `5e7e867`, campaign `init-attachment-20260920c`). All seven observations and cleanup pass with zero remaining resources. Setup/operation/cleanup were 15.248188916/3.050729625/2.860054667 seconds; result seal is `f3441bde3eabf03775813200f9b5e367ba96c9a06e1d10a6b0ee85212d05c710`. This is reference functional evidence, not native or quiet-performance qualification.

Current schema-6 native capture retains raw evidence and separately projects pinned json-file logical records, with per-generation natural source EOF, independent finite log cutoffs, rollback-safe encoder state and bounded projected storage. Regression tests cover per-invalid-byte encoding, valid UTF-8 split across raw frames, logical 16-KiB boundaries, source/generation isolation, EOF after an earlier snapshot, cancelled/failed monitors, write/EOF failures, schema-5 refusal and provider policy after engine configuration change. Stock native E07 passes all seven observations and zero-residue cleanup at source `f744a85663086c4ab874ae10e15c0daf079fd785`, invocation `6108421b-44df-404b-ae80-4732aa5d5f90`, reusing package `2d579a4c-71bb-4407-ae60-c75cecdb2d3c`. Setup/operation/cleanup were 5.634863375/5.376193042/1.603365250 seconds; seal `e248c5675278bb7cd0f4de098f666167af45571de4bd746a3bf24d5e3189bb3c`. Enhanced package `f902dd51-8d1f-4cbe-bf12-3cea78902653` passes package smoke but E07 invocation `599ae7a2-6871-4f33-892c-ad7aec4b29c2` times out during setup, before observations, with successful cleanup. The startup cause and correction are recorded under [isolated test keychain](#isolated-test-keychain); enhanced live qualification remains required. These are non-quiet functional runs, not paired performance qualification. The [architecture and development limits](../DESIGN.md#source-aware-output-history) remain explicit; old schema-5 results below are historical foundation evidence, not proof of the current projection.

The final affected component revision passes stock invocation `554a8892-788a-4484-9415-179306455770` (22.225 seconds overall, native target 9.3 seconds, unchanged state/API/model cached) and enhanced `0f65700c-a7b5-40f6-80ac-6b11493ee5ce` (39.965 seconds overall, native 9.3 seconds, API 0.6 seconds, unchanged state/model cached). They cover 255/262 native functions, 129 API functions, 36 state functions and 25 model functions. Independent source review is clean after correcting provider-policy provenance and pre-encoding input bounds; strict touched Swift/Markdown lint passes. Separate focused stock instrumentation `2832d5f5-100d-4a9c-a998-c8520b460d20` passes with no retained coverage-merger warnings/errors: encoder 81/82 lines, journal 202/203, cursor 110/111, projection 59/60. These are focused dirty-development input snapshots, not clean-head full-project coverage or SonarQube authority. Non-quiet durations are diagnostic only. Failed invocation `f5c7cfe7` records two new fixture setup errors (missing guest command), corrected without altering the production contract or deadlines.

The previously-open-reader failure regressions fail at `5af8e167-78dd-492e-a5d1-20a84ae8ba3e` and pass after shared failure-state propagation at `1079c6ea-3529-4697-8990-1c409882d443`. Completion-update failure is also covered at `2218999b-78dd-43f0-aeb4-3fedfef95aab`. A real SQLite/native restart-and-reopen test exposed missing lifecycle-connection contention handling (`31ab6bad-e78e-4a5a-814c-62ca498f2405`); the same test passes with the bounded wait (`6f80bae9-6cad-4929-81b7-b93ba205b6c6`). Generation replacement before durable completion is reproduced by a held-completion test (`a290e28b-a188-45c7-9ffd-b5e296b7d786`) and corrected at `a1e12a95-57e9-49f6-a5f9-f4f1592594a3`. These are retained functional evidence, not quiet benchmarks or valid instrumented coverage.

The complete affected state, native-runtime and gateway targets pass stock invocation `fa66f598-0b3f-4b3c-bb15-fc38e395dede` (9.798095042 seconds overall; unchanged state and gateway targets cached) and enhanced invocation `c364ada5-2141-4178-a2df-f4f2c2fb7a68` (39.766801666 seconds overall; unchanged state target cached). Both invocation outcome records report zero build and evidence-validation errors. These final component runs cover the source-aware history implementation in both dependency profiles; they do not qualify live foreground parity, coverage, quiet performance or a release.

The native provider now advertises `ContainerExitWaitRegistration=1` only when direct process APIs are enabled. `POST /containers/{id}/wait?condition=next-exit` installs a generation-bound receipt before returning HTTP headers. The receipt retains the real native exit across auto-removal and is independent of output delivery or drain failure. Cancellation unregisters only that waiter, including a caller cancelled before waiting. A registration arriving after native exit cannot replay the old result: it joins the previous drain, revalidates immutable identity and subscribes to the next generation. Unsupported providers fail before a success header. Other wait conditions retain their existing path.

The held-drain generation regression fails at `f209e6e2-3fcf-4cac-829b-f404ee9be56e` and passes after correction at `d3c135db-c8e4-4bfd-b561-7decea0e4849`. A separate weak-reference regression proves pre-cancelled waiter retention before the fix (`9cc42765-9f3a-4fe4-997c-8ea8fcac8ac9`) and immediate release afterward (`19da764d-a647-4606-ae29-04877851c8da`). Final stock affected-component invocation `0bd3e77c-a776-42fd-85de-f064b78fea70` passes 244 native-runtime functions and 127 gateway functions (gateway reused from unchanged cache), taking 9.101 seconds overall and 8.9 seconds for the native target. These are non-instrumented functional timings, not quiet comparative benchmarks or released evidence.

Enhanced affected-component invocation `ba593c93-e280-4820-b996-8d7157f9738e` passes 251 native-runtime and 127 gateway functions (39.110-second invocation; 8.9/0.6-second targets). Both native dependency profiles therefore pass this registration boundary without a VM. The independent full source/test/documentation re-review is clean. Final matching Compose header-normalized component `91a7088b-513d-4660-9f9d-c5b58953b87b` passes the same 91 functions (17.390-second invocation; 0.8-second target); the complete repository licence-header check and changed Markdown checks pass. Header repairs retain original devcontainer attribution and do not alter product semantics.

The matching Compose implementation in PR 708 registers both attachment and acknowledged exit before starting init, drains output while start confirmation is pending, handles TTY/detach/resize and preserves the registered status after auto-removal. Shared client `c04ed07b8a324a996b9d62397278b90a389fe830` supplies the additive response-head callback. Stock Compose component `3b5ef433-e275-4509-aec2-29f5416287ba` passes all 91 functions (8.408-second invocation, 0.8-second target); cancellation after output EOF and TTY auto-remove races have retained fail-before evidence. This replaces the earlier unsupported-foreground client state below; native history and complete live qualification remain open.

The Apple runtime now connects generation-owned stdin/stdout/stderr descriptors during bootstrap, before starting the original entrypoint. Multiple live attachments share that generation without owning or killing init; detach and blocked/queued-write cancellation preserve other clients' stdin. Completion joins output EOF, input cleanup and accepted resize operations before the generation can be replaced. Slow subscribers fail explicitly rather than silently dropping bytes. A missing output EOF is a bounded error, not a successful exit.

Stock Apple 1.4.1 accepts terminal resize only after init starts. A pre-start request is retained and applied after start; an exact initial terminal size before the first guest instruction is not claimed. Native creation/start identity is checked for attachment and before resize. Unmanaged/external starts cannot adopt descriptors the engine never supplied. This does not provide atomic generation-bound native control against a concurrent external runtime writer; live qualification must use the isolated, exclusively owned engine. An ambiguous bootstrap/start fails without sending replacement descriptors. A host-preparation failure can reuse the same bootstrapped process without launching an entrypoint with incomplete hosts.

An ID-scoped closing barrier prevents actor-reentrant attach/start from replacing descriptors before earlier controls join. A caller's resolved creation/Docker identity cannot change across that wait, and pending I/O closure prevents recovery quiescence. Deterministic regressions observe the queued input/resize admissions and both waiting replacement callers before releasing their held operations. They cover same-incarnation replacement and changed-incarnation rejection. Completed closing records retire automatically.

The runtime foundation at `d6c08642c5099c07e30be230787a244fdf542a2a` passed stock component `6c2d4f56-e49f-46ff-8f09-f7f00f79d1f8` (235 functions in 28 suites, 9.133-second tests, 37.644-second invocation) and enhanced `bb0830f8-c9a6-4fd0-82bb-8607da173c42` (242 functions in 28 suites, 8.733-second tests, 25.664-second invocation). The following HTTP checkpoint supersedes these as current component evidence. These are development-source results, not released or VM-backed parity proof.

The HTTP adapter selects effective stdout/stderr/stdin streams, applies Docker's query booleans and TTY detach-key parser, preserves non-TTY `StdinOnce` EOF policy, and now reads one at-most-64-KiB chunk on demand instead of filling an intermediate queue. Output-only disconnect cannot close shared stdin. Quiet output-consumer cancellation and history failure join EOF policy before revoking the native subscription; EOF interrupts a blocked writer without killing init. The native live subscriber still has an explicit 256-frame bound. A requested-but-unavailable stdin-only stream ends immediately unless non-TTY `StdinOnce` requires waiting for exit. Resize targets the inspected immutable identity and an owned running generation. The reference is pinned [Moby attach behavior](https://github.com/moby/moby/blob/568f755ebeb1ac9c6a8febbda6cd371ea0a9630b/daemon/attach.go), with its stream implementation and vendored detach-key parser; source comparison is not a substitute for the remaining executable Docker oracle.

Native history is never synthesized by relabeling Apple's merged log. The router now uses `prepareContainerAttachment` to acquire history and live subscription atomically. The older standalone history SPI remains a compatibility default, not the router's preparation path. Native engine-owned generations use the schema-6 versioned log projection described above; existing general log endpoint behavior and imported/external history are not qualified by these attachment tests.

Quiet-consumer cancellation fails before the fix at `5aec91ea-50e8-442a-8cd2-01446374414a` (both stdin policies), then passes at `1d7750d9-7689-4f43-932a-1b885c0ff6b6`. The effective-stdin regression passes at `be608056-6ae3-449b-a133-96fff91f3f72`. Complete model/API/runtime component invocation `be75521c-3f10-4d4f-b680-6838e1309005` passes the enhanced profile (2.534-second invocation; unchanged native tests reused from cache), and explicit stock invocation `ad2c09a0-563e-49d7-85b7-101f8644f84b` passes (35.198-second invocation; unchanged model tests reused). The final independent full-source review has no remaining actionable finding. These non-instrumented, non-quiet build/test observations are not coverage, SonarQube or comparative benchmark evidence.

Instrumented stock invocation `e800ebe7-7dbd-4a69-bf9d-c58367bdd4f9` passes its 235 functional tests but emits invalid raw loop counters (`18446744073709551614`) at the then-current `AppleProcessSession.swift` lines 560/561/563/564. Its coverage is rejected, despite the passing Bazel command; no percentage from its reduced merged report qualifies the release. The earlier warning-free runtime checkpoint below is historical, not proof that the compiler counter defect is resolved. Preserve the retained diagnostics and pursue the existing minimal-reproducer follow-up rather than excluding or rewriting counts.

Remaining C02 work: qualify the implemented history boundary and exercise full public HTTP/CLI and prepared Compose foreground behavior against pinned Docker, stock Apple and enhanced releases, resolving observed differences without weakening the contract. The component-tested client has no CLI fallback or deletion by name; failed starts remain inspectable. Full valid coverage/SonarQube, fresh-machine enhanced guest distribution, live fault recovery, quiet paired benchmarks, package/install/release and generated documentation/VHS gates remain open. The exact retained enhanced guest archive is locally prepared, as recorded below; a missing local image is no longer the blocker. No new stable release or full foreground parity is claimed.

### Process I/O cancellation and coverage integrity

Committed runtime checkpoint `73938b6c45db3c1a2fa2bbcce21a0a488a0cf188` also passes the complete Apple-runtime component: enhanced invocation `904e8a25-dc7d-422e-962b-74495d08bab7` runs 226 functions in 8.534 seconds (9.746-second invocation), and stock `85380791-b2c3-4e16-853a-d6ed08ef2b59` runs 219 in 8.545 seconds (18.632-second invocation). Neither emits a coverage-merger diagnostic. This supersedes the earlier failure for this component, not the separate whole-project coverage gate. These timings are non-quiet development observations.

The remaining coverage diagnostic is localized by stock Compose CLI invocation `5625271b-0357-4f6e-80bb-52e4fdea9d67`: all 24 functions pass, but raw LLVM output has unsigned-underflow counts in `ComposeCommand.consumeGlobalOption` and `DevContainerConfiguration` executable/home-path selection. The raw files, before Bazel omits invalid rows, are retained at `retained/workflow/diagnostics/coverage-5625271b/raw-swift.lcov` (SHA-256 `6183012845fcb7109af6edb2176340e94b7a4f1b6035f7d2510c1cb56d310d4f`). Diagnostic `a3d50d40-4e23-424e-9f20-01de0598c67a` used `--test_arg=--no-parallel`, but the pinned rules_swift JSON-ABI runner does not forward that command-line setting and the test log proves execution remained parallel; it is not serialized evidence. Atomic-counter compiler experiment `9b7b84ec-5b5e-4367-8070-aeb678ff8640` still emitted underflow and was reverted. Its raw report is retained beside the first under `coverage-9b7b84ec` (SHA-256 `084cf8d4614cbd1e7b17a1effa89ac7343fa9a3b0ea3888f39efb6e79015458f`). The root cause remains unproven. Do not repeat these ineffective settings or rewrite/exclude counters. The next quality step is a minimal counter-emission reproduction, not another full-project rerun. These diagnostics are task-owned release-quality evidence; retain them until the defect is resolved and superseding clean evidence is accepted. This open quality gate does not prevent the remaining foreground-I/O implementation.

The subsequent direct-reader consolidation removes the duplicated blocking `DirectPipeMonitor` and routes all three process paths through the shared reader. A held-delivery regression proves that even an already-cancelled waiter cannot observe completion before the reader's finish callback. Restoring the previous direct reader fails the nonblocking output regression at `78dbaa51-fa09-4788-94c0-20dad5e26e57`. Restoring only the old terminal completion cancellation fails the real-shell exit-status regression at `98c70a1f-7265-4bc0-ba25-b13ef596509b` (synthetic 255 instead of 42). The initial shell fixture used an external sleep that deferred its TERM trap past the existing grace period; `e3bd73d8` records that rejected fixture, replaced with a builtin read without changing production deadlines.

Final focused stock `e80151f8-1795-4b72-bec2-7650d7d35c2e` and enhanced `a8f8bcb7-07f2-4b21-ae94-232f2d0e2fa5` each pass all 32 functions in the five affected suites, with no coverage-merger diagnostics. Recorded build/test invocation durations are 13.064 and 30.712 seconds respectively; these are non-quiet development observations, not performance comparisons. Independent complete-diff review is clean. These are focused component observations, not new full-suite coverage or live foreground parity. The existing direct-session idle-drain heuristic remains unchanged; this change proves cancellation/reader ownership, not lossless end-of-stream under every guest transport condition. Historical full-suite counts and rejected reports below belong to the preceding input-cancellation checkpoint.

This supports C02 foreground I/O but does not complete foreground attachment. `ProcessInputWriter` previously queued cancellation behind its own blocked write and used an unbounded readiness wait; terminal input could block inside a write. Cancellation now publishes a synchronized flag before queueing close, uses nonblocking descriptors and 100 ms readiness checks, and propagates Swift task cancellation. Descriptor closure stays on its owning worker. `ProcessPipeMonitor` now serves CLI-backed, PTY and direct-API output, replacing the duplicate direct reader. Its completion join cannot be skipped by task cancellation: descriptor closure and delivery callbacks finish first. Caller-owned handles are relinquished without closing them. Terminal cancellation preserves the real process-exit waiter rather than returning synthetic status 255. Invalid descriptor setup fails before writing.

Regression `55f437ad-3eaa-497e-9137-58d0f0bd4a46` restores the original implementation and demonstrates delivery of all 4 MiB after explicit cancellation and a 30-second task-cancellation timeout. Corrected focused run `2eb37b71-4aa6-48bd-a302-fe921dfbdbb5` passes 30 functions including real socket/PTY backpressure, no-reader cancellation, invalid handles, idle cancellation and complete output delivery. Full stock `42910f8a-93c8-48ae-ad3e-2710ef77c898` passes 217 functions but emits invalid unsigned coverage counters. Atomic-counter experiment `c74796d2-499e-491a-97d8-130c18c23cd0` did not fix them and its build settings were reverted. These are not clean release-coverage authorities.

Enhanced `d2aa6ae0-50e8-4f65-ac78-f870f7c60419` exposed missing test-double `disconnect` conformance; corrected `3176df36-8799-4ae6-9b0b-107752b837e3` then exposed a stock-only file-bind assertion. The test now verifies enhanced file acceptance versus stock rejection, preserves the file in both profiles and rejects creation beneath it. Instrumented `84ebf308-262d-46ee-8779-a4dc7d8f15b4` passed 224 functions but is rejected because source comments changed during the invocation; it also retained counter underflow. It is diagnostic evidence only.

The workflow rejects coverage-merger tracefile warnings/errors and out-of-range line counters instead of accepting the merger's reduced denominator. Retained export requires authenticated diagnostic logs for every test result and the validated inventory. Both default native and explicit consumer-policy gates validate counters. No counter is rewritten and no production line excluded. Independent review prompted fixes to missing-log and default-policy bypasses. All 199 shared helper tests pass through the digest-locked Compose consumer launcher. Final enhanced native functional invocation `512e6214-4fb1-4a80-b75a-fde40025695c` passes 224 test functions; strict touched-Swift checks pass. Resolving the instrumented counter defect remains a release-quality requirement; functional success alone does not prove 90% coverage.

Final stock functional invocation `5e562032-5c1b-496d-82db-10dcee3e577a` passes all 217 functions (9.325-second test run). Enhanced `512e6214` passes all 224; the 199 shared helper tests, touched Swift formatting/static checks and affected Markdown lint pass. These are component results, not live foreground parity or coverage qualification. Read-only revalidation also rejects earlier clean-source coverage receipts `0cb42d92-24fd-40f7-9794-f3cc7707703c` (devcontainer `8e12ed7`) and `ba2c8a11-ccb0-4801-b210-9a494c709f44` (Compose `4b9f1fc8`) because their retained logs contain the same invalid unsigned counters. The defect therefore predates this I/O patch and affects both projects; their historical reported percentages must not be used as release authorities. The original receipts are retained unchanged.

### C01 Compose service

Status: stock and Docker live proofs pass; handed off incomplete for the missing enhanced guest prerequisite. No full three-lane or release qualification is claimed.

On 19 September 2026, stock invocation `178bf269-d0d4-48e1-ac73-8ca375c2707d`, campaign `candidate-c01-native-20260919b`, passes all three unchanged observations and cleanup at devcontainer `aa56f00dc8038da7cd0d23ccfa3d0fa287084930` with Compose `1fe36f56d88a217f2dd95c5d5a0189cd1710afd5`. Setup/operation/cleanup take 7.059474833/4.164177458/1.628399333 seconds. Zero errors or owned resources remain, and runtime recovery is clear. Evidence seal: `ca9559cb705e150b8946f09759e5641a244372969ed32a25335a56e952247d5b`. The retained Docker result below is unchanged; separate campaigns are not a quiet paired timing comparison.

The exact stock packages are devcontainer invocation `93170c3a-fb3a-4504-a68c-94dab6b7554d` (26.185 seconds, archive `73e837e2189e746ca0140141b9a48af146bc1051d76cc8fb3ebced1cce08ca36`) and Compose `33d4f1ab-ade6-4147-bb1d-2fd212b6182b` (41.339 seconds, archive `e73450b403510a42ae95951adafed0125776087681ad158ab176565ecef06942`). Both pass real archive smoke tests and clean-source admission. The initial devcontainer package check `18cc7496` caught a stale unprefixed native-version expectation; correcting that exact assertion yields the passing package without changing runtime behavior. These are unsigned development candidates, not released assets.

The following implementation checkpoints preserve their original status and are historical; the result above is authoritative for current C01 stock progress.

The network/image correction is implemented in development source, pending a rebuilt matched candidate and live proof. API-created networks persist a server-generated wire identity separately from the native identifier; lookup, deletion, coordinator ownership and create-time attachments resolve that identity, and stale IDs cannot select a same-name replacement. Inspection projects Compose labels and actual observed endpoint membership with container Docker IDs, rather than requested attachments. Compose's stock resource path already calls this Engine API. Its matching PR 708 change supplies the logical network key independently of a custom runtime name in both real and dry-run creation.

For digest-qualified native image launches, Compose retains the original operand in a reserved creation label. Devcontainer restores only its spelling after validating the exact native descriptor digest and normalized repository; platform and image ID remain native-derived. Invalid labels, missing descriptors, different repositories and root-versus-child digest mismatches fail closed before metadata adoption. Existing metadata cannot overwrite the verified spelling. There is no added registry lookup, label-derived image identity or mutable-tag inference; tag-only launches are unchanged. The original fixture and cleanup assertions are unchanged.

Focused API/Core coverage `45f05da2-33ca-48de-b02f-845286240e16` passes, including 73 API functions. Apple-runtime component coverage `7f2c09e6-f554-42c0-b87f-5d42249e0c80` passes the complete stock component; the new image-reference validator records 17/17 executable lines. Compose logical-label coverage `22eed813-28df-4954-b8d4-e3a51cee6d3c` passes four selected functions, and final Engine-adapter coverage `240941b9-73b1-4e4a-b1c6-efa1f9b48fd1` passes all 28 functions, including valued/grouped short options and stock registry/DNS options before the image operand. These are retained development-source component results, not clean-head aggregate coverage, a successful native C01 result or release certification. Earlier failed C01 evidence remains immutable; the older candidate archives below do not contain these source corrections.

First native stock execution at `014abca`, invocation `8b876ed0-a97a-430a-92c7-2a6190517364`, campaign `candidate-c01-native-20260919a`, failed verification after successful CLI creation and post-create execution. Raw setup/operation/cleanup are 7.255948625/3.934196625/0.018201042 seconds. The project name lost its hyphens; inspected image spelling and network ID/labels/membership also differed. The exact owned service/network and spawn-time-authenticated Engine were subsequently recovered, previous services restored and the disposable root removed. Recovery is clear; immutable case `0b4034ea29cfcd658b6d8e0722e09256ca3ed46a84cc9ae54898ca34d4e36e78` remains failed, not a passing cleanup or quiet benchmark.

The project-name cause is confirmed in retained official CLI 0.88.0: its numeric Compose-version comparison treats native `0.15.1` as Docker Compose before 1.21 and removes hyphens. The native facade now qualifies only `version --short`/`version -s` with the truthful vendor prefix `container-compose`; it does not invent a Docker version. The official CLI's existing unknown-vendor path preserves modern project names. Explicit Docker probes and other version forms remain unchanged. Bounded capture preserves child failures and both diagnostic streams; malformed successful output fails closed before project state. Focused coverage `0eb79010-a29e-42f1-a911-c570b97a0713` passes 19 test functions, including the public entrypoint, and records hits on all 30 added executable lines. The LCOV merger warns about overflow counters on unrelated lines 249-251; this run is not authoritative aggregate coverage. Network/image fixes and a new candidate/live run are still required; no unchanged Docker oracle or archive was rebuilt.

The native harness now admits a separately prepared Compose candidate with an explicitly matching stock/enhanced profile. Admission checks the clean retained source snapshot, archive and product hashes, Swift/Go dependency identities, embedded build metadata and notices, and revalidates the complete Compose input before claiming successful cleanup. `DevcontainerComposeCandidate` preserves C01's original assertions and service/network ownership while invoking the packaged public CLI with isolated config/state paths and the selected native frontend. Preparation gate `83ce449e` passes 104 tests; runtime component gate `6f3c6c08-abe0-4d15-8a1b-95d8dec92abc` passes 819 tests, including artifact drift and incomplete-admission rejection. A supplemental 34-test trace covers 147/148 executable preparation-module lines and 13/13 native Compose-adapter lines; retained focused evidence is not aggregate quality or runtime proof. Initial failures `dac8fc1f` and `83ce449e` were test-fixture invocation reuse and duplicate mock cleanup, respectively; ownership/integrity guards were preserved.

Stock Compose candidate `1edbd835-d258-4bd1-bf69-004223d1004f` at `4b9f1fc8b364ff6f882140fba9672c1c2c1d02b9` is reused without rebuilding: archive `1a4d41af6e7ea938d3dd986131273b621b066d15080458d289c2d0a731868e03`, preparation `063f4a0e34930492dce533751a6e9266168319f7fe671e85ba448fe9ad3e373c`. Its runtime source and dependency locks are unchanged through Compose `10416a77`; later changes concern documentation/build tooling. The devcontainer selection correction is packaged once as `a52d8072-ae1f-4ed8-b12f-f73a2bbe21da` at `fd74630`, with archive `40bcc3468ad747bf401b53f7e5913eac0bea25248e8b9feab1d23605b44cb868`; package/smoke passed in 26.472 seconds. These are unsigned development inputs, not published release certification. Native live proof remains required; earlier admission-unimplemented statements below are historical.

```sh
make bazel-prepare-candidate CANDIDATE_INVOCATION=1edbd835-d258-4bd1-bf69-004223d1004f CANDIDATE_FAMILY=container-compose
make bazel-engine-case CAMPAIGN=unique-native-c01 LANE=apple-stock CASE_FIXTURE=C01-compose-service \
  CANDIDATE_INVOCATION=a52d8072-ae1f-4ed8-b12f-f73a2bbe21da \
  COMPOSE_CANDIDATE_INVOCATION=1edbd835-d258-4bd1-bf69-004223d1004f
```

Native selection is now implemented in development source: omitted Compose choices use the separately installed `container-compose`, explicit saved Docker choices survive, and native discovery/mutation receive the resolved socket and Container executable despite conflicting ambient settings. Regression `87994af7-3489-4e34-92a8-6a13d25664d6` fails before the fix; focused stock coverage `6637ba72-0826-4baa-940c-7bf861610699` passes 33 test functions, including both backend selections and configuration creation. All six changed executable lines recorded by LCOV are covered. Whole-file focused coverage is 302/326 (92.64%) for configuration and 375/425 (88.24%) for the Compose command; the latter is below the 90% aim and still needs the remaining command-error boundary coverage at the coherent quality checkpoint. This development run is not clean-head aggregate or runtime qualification. Independent source/test review and strict touched Swift checks pass. Native C01 admission/live proof and the enhanced guest prerequisite remain outstanding; earlier default-selection pending statements below are historical.

Docker reference proof passes all three unchanged observations, cleanup and zero-resource checks at source `e59a071`, invocation `4dcb1ff0-e06e-474e-95ec-7fe5fb671bd3`, campaign `docker-c01-native-20260919a`. Setup/operation/cleanup are 262.253397541/1.809207166/32.625809958 seconds; seal `f33130c70e8665e4eeb30bd3ddf5af81616d2d3849ec32cd88f78a9e29534886`. Runtime recovery is clear. Setup's VM user-session and SSH-control readiness delays are retained, not retried away or added to fixture operation time. All deadlines remained unchanged. This is functional proof, not quiet performance evidence. Native Compose admission, Docker-free default/provider selection and both Apple-lane observations remain outstanding; the earlier pre-live component checkpoint below is historical.

C01 is the active contract: preserve the original service environment, post-create hook and workspace observations from its checked-in configuration, Compose file and probe. Non-goals are additional service dependencies, resource options and lifecycle variants (C02-C04). The reference uses the existing published Docker VM, pinned official CLI and already-retained Docker Compose 5.3.1 GitHub executable; no reference or product is rebuilt. Candidate lanes must select the separate `container-compose` frontend with stock/enhanced Apple providers and must not fall back to Docker Compose. Native C01 admission deliberately fails before runtime mutation until that path is implemented.

The new reference adapter chooses a case-specific project name through the CLI environment, preserves fixture bytes and refuses an existing project or network. It verifies the service's full ID, case/project/service labels, pinned image and workspace mount; captures the default network's full ID, name, creation time, labels and sole-service membership before the probe; and removes only those authenticated resources. Whole-project inventory, final absence, immutable command diagnostics and the existing VM process receipts are required for cleanup. Unfinished commands, missing creation receipts, foreign members or changed identities preserve quarantine. A completed cleanup receipt rejects reappearance rather than authorizing another deletion. Completed-command recovery reuses this ownership plan without rerunning `up` or changing a failed result.

Focused component gate `292a77dd-421d-46f1-a91a-eb285887a05a` passes 802 tests after review regression `9c0566c6-cc41-4653-a04c-fadf34525b2c` proved that a reappearing closed network must be rejected. A supplemental 15-test instrumented trace covers 125/134 executable lines (93.28%) of the new adapter; this is focused development evidence, not aggregate product coverage. Live reference, native lanes and quiet paired timing remain required. Exact release assets are already authenticated by `releases.lock.json`; source and closure fingerprints bind the new adapter and original fixture files. The Docker-free native default/provider selection and enhanced published guest remain explicit dependencies. C01 is not Verified, and no fixture omission is counted as passing.

### D07 reuse and cleanup

Status: handed off incomplete on enhanced guest publication. Stock now passes all six unchanged observations, cleanup and zero-resource checks at source `53aafe4699d21e015bcd143ea40455282752846a`, invocation `d3a775e0-6b1f-4bc5-ad82-18d86a911e32`, campaign `candidate-d07-native-20260919c`. Raw setup/operation/cleanup are 7.019353292/6.651025458/1.298868042 seconds; seal `108b9fd87eadef90e0386bf6ac82d435d6ef80c000de6b242bf53bb386de0031`. There are zero errors, every expected observation matches and runtime recovery is clear. The retained Docker proof below remains valid; separate campaigns do not form a quiet paired benchmark or complete three-lane qualification. C01 Compose-service configuration is next.

One release-mode candidate package (`1e56eae2-70d3-40be-9e25-62025884acbf`) passed archive smoke checks in 26.240 seconds, with 1,239 action-cache hits and 24 executed actions. Archive SHA-256 is `0e85944ae69d4a6e2b433635ac73fca59451496bf9b95640bdb00a7843758cca`; preparation `2d9fcd5177871fd1b11a339dd907c8b0350d097d205fa4d78a453b7d4637e14d` was performed once and reused by the live run. This is an unsigned local candidate and a functional build-time observation, not a quiet build benchmark or stable release. Earlier failed checkpoints and pending-proof statements below describe the historical development sequence.

Current stock checkpoint: product `b3e6cf9`, archive invocation `3477c125-a7db-4187-ab1f-d2cd508fa9ae`, permits initial creation and reuse with unchanged 1/1 hook counters. Live invocation `87b56dd6-7499-48e3-84eb-653f135714ac`, campaign `candidate-d07-native-20260919b`, fails at the official CLI's `rm -f <full-container-ID>` before replacement. Raw setup/operation/cleanup are 6.770235166/3.913581708/0.015700792 seconds. These are failed functional diagnostics, not benchmark evidence. The original owned container, named volume, attachment and captured Engine were subsequently reconciled; services restored and recovery reports clear. Private recovery case `18fd54325443c15b6e481a031e61170594965252b525f3a78c4a0ee936f84d3f` retains the reviewed procedure and diagnostics. The sealed failure and its original unknown cleanup outcome remain unchanged.

The focused correction implements only `rm -f/--force` with one complete hexadecimal or native UUID ID, selected-socket forced deletion, no volume deletion and no Docker fallback. Missing/ambiguous/unsupported arguments fail before mutation; failed HTTP requests and unexpected response bodies cannot print success. Red regression `0847d062-adb5-426e-ae1c-c30f1121f907` becomes green at `42086676-4ae6-4e0e-9bc7-913507c7ed41`: 75 frontend test functions including real executable/socket deletion and 404 propagation. Instrumented frontend and command files cover 95/95 and 161/161 lines respectively, not aggregate product coverage. Strict lint then required extracting unchanged version-response logic from the enlarged dispatcher. Final coverage `ba2c850f-2420-46e1-aba8-5aa6dacb0f9d` passes the same 75 functions, with 98/98 frontend and 161/161 command lines. Touched-source formatting, strict lint, Markdown checks and complete independent review are clean. A new exact-source package and corrected live stock proof remain required; earlier named-volume chronology below is historical.

D07 Docker passes all six original observations with cleanup passed, zero errors/resources and clear recovery at source `da0afc0`, invocation `be380dd2-e11c-44d3-8297-56b03212fb23`, campaign `docker-d07-native-20260919a`. Setup/operation/cleanup are 17.709250667/1.903599583/2.977283459 seconds; seal `76a78dd352fe1b65a1ba43aabca7d3a37f50da931380b22143d1801f969bb715`. No product or workload was rebuilt. These are functional observations, not quiet paired benchmarks.

Stock invocation `f3177942-1ce6-460f-b395-93d14519ec00` fails before container creation because the frontend only accepts bind mounts. The failed case and unknown cleanup result remain immutable. The captured named-volume request is now projected as a typed `HostConfig.Mounts` entry through the existing native Engine API. Bind defaults remain unchanged; anonymous volumes, advanced volume options and quoted CSV remain explicitly unsupported. Named sources follow [Moby's restricted name grammar](https://github.com/moby/moby/blob/master/daemon/names/names.go), not filesystem paths. Red frontend gate `4fd508f6-8b28-4e16-8485-e5dc543058e1` becomes green in coverage `f1e37f6c-28b8-4416-910b-c857ac28840d` (71 test functions), including a real executable/private-socket named-mount request. Exact recovered cleanup, final source coverage and corrected stock live proof follow; no stock pass is claimed yet.

D07 is Active. The Bazel adapter preserves the original pinned Alpine configuration, fixed volume name, hooks and probe. Three bounded official-CLI `up` operations prove initial counters 1/1, reuse of the exact container with unchanged counters, then replacement by a different container with the original absent and persistent volume counters 2/2. Exact container and volume absence establish the sixth cleanup observation. Unexpected output, any changed identity, repeated hooks or missing persistence fails; no nonzero command is treated as success.

The fixed named volume is explicitly created with the case owner label inside the exclusively owned engine, without modifying the fixture. Existing names are refused, never adopted. Its creation identity, labels and mount are revalidated before container deletion; the existing network/volume resource owner removes it without force only after guest removal. Every CLI generation has separate immutable intent, result, diagnostics and exit receipts. Native foreground attachments from all three `up` calls remain owned until resource deletion closes their streams. Unknown completion quarantines the case; completed Docker commands permit cleanup-only recovery without replaying `up`. Interrupted native phases still require explicit reconciliation.

The first Bazel analysis (`267347ed-0030-4d9a-aaf9-483c08ec9ce6`) exposed omitted root exports for the hidden configuration. Adding all three D07 exports corrects it. Component gate `b19872df-1cad-4c94-904f-e71f8751ca68` passes 774 tests in 15.134 seconds Bazel elapsed, including real Unix-socket ownership/failure cases and real native command attachments. Live Docker and stock proofs remain required; the retained `295489b` product will be reused without rebuilding unless product behavior needs correction. Enhanced qualification remains blocked on its exact guest image. Functional durations are retained; quiet paired benchmarking and release qualification remain later gates.

Documentation disposition: README, DESIGN, COMPATIBILITY, DocC Testing and issue/PR handoffs now describe the candidate named-volume boundary and its pending corrected native proof. The original fixture, public lifecycle CLI options, install instructions and stable compatibility claims remain unchanged. The harness-development chronology above describes pre-live checkpoints; Docker proof is now recorded, and the product correction invalidates reuse of `295489b` for corrected native execution. The final documentation phase still owns whole-release DocC, benchmark and installation-first demonstrations.

Final focused fault additions cover lost rebuild receipts, a surviving old ID with changed ownership, an unobserved creation outcome and reappearance after deletion. Component gate `24425c8f-8e85-4ede-a4e7-7af15d981181` passes all 782 tests in 15.294 seconds Bazel elapsed. The earlier focused 28-test trace measures 116/121 adapter lines (95.9%), retained under `focused-coverage/d07-reuse-24425c8f`; temporary trace files were removed after byte-verified retention. This is adapter coverage, not product-wide coverage or live parity.

Independent review found that an absent replacement with a lost rebuild result could incorrectly rely on earlier-generation IDs. The focused regression fails against the original adapter. Recovery now requires an observed replacement or its distinct delete receipt; empty inventory, changed-owner replacement and only the original generation remain quarantined without deletion. A separately journalled replacement-delete receipt still permits cleanup-only completion. All 786 component tests pass in `663af5c3-ecfe-4011-bc37-d066fa6b31a5` (16.231 seconds Bazel elapsed). This corrects the harness before live execution; no product rebuild or fixture relaxation is involved.

The final complete independent review is clean. A 40-test focused trace executes 125/127 adapter lines (98.4%), retained separately under `focused-coverage/d07-reuse-663af5c3`; its owned temporary directory was removed after byte verification. Touched Markdown and diff checks pass. This supersedes the pre-review coverage measurement without rewriting that historical evidence.

Product review exposed bare `source`/`src` being interpreted as the volume name `true`. Red regression `b67be500-2532-4953-ab1f-2287ca443a63` reproduces it. Non-boolean fields now require explicit values; bare `ro`/`readonly` remains supported. Final frontend coverage `dd4683df-4790-4f4a-9937-e731a39c641b` passes 71 test functions and executes 178/178 run-parser lines. Both profile inventory minimums are 71. Touched Swift formatting/static checks and complete independent product review are clean; this is focused development coverage, not aggregate release qualification.

The failed stock case was recovered under the operator's task-owned recovery authority after independent review of a one-case procedure. It re-admitted the exact original bundle and guest, verified the recorded parser rejection before create, absent CLI process groups, empty whole-engine container inventory, exact volume identity and captured Engine incarnation. Diagnostics and authorization were retained before non-force volume removal. One TERM was submitted to the captured Engine; a transient exit identity caused conservative rejection during observation, then the normal recovery report proved it stopped and allowed service restoration. Recovery case `94f322a78abd02a4774a658a62f9539ddc0cee02dd1795155410456bf2e38172` is restored and the guard is clear. The script is retained under `operator-recovery/<case-id>`; disposable runtime/script roots were removed after diagnostics retention. The original failed result is untouched. This does not claim general unattended recovery of unknown native outcomes.

### D06 published ports

D06 is handed off incomplete on enhanced guest publication after passing all four original observations in Docker and stock Apple. Stock invocation `ecf30eb5-335e-4144-8747-65c91e32975b`, campaign `candidate-d06-native-20260919a`, tests source `295489bc8324a96e14da995ec1935e16b6b74dc1` with setup/operation/cleanup 7.883461417/3.893173000/1.586567625 seconds and seal `e48a71a95731a4c3e0610c57ae421ed0c84d134dff569a3ea77dda4df1b09199`. Observations match, cleanup passed, zero errors/resources remain and recovery is clear. It reuses package invocation `15d10e7d-5224-4acc-930e-31a89414bcde`, archive SHA-256 `f4bdce2495bd64a43db233d45335f3d5e5d4e7468484a7c7c0e55998be49d062`, prepared once without a test-time rebuild. The package smoke passed in 25.488 seconds Bazel elapsed, with 1,236 cache hits and 27 actions. These are ordinary build/functional observations, not quiet paired benchmarks. Separate campaigns correctly report incomplete three-lane evidence. No stable release is qualified. The implementation chronology below records earlier pending proof, now resolved for stock only.

D06 Docker reference passes all four original observations with zero errors/resources, cleanup passed and recovery clear: invocation `524dce17-469a-4819-9258-a210a414d7cb`, campaign `docker-d06-native-20260919a`, harness `b5b60e0efb5b76266a2190a6e530e067a6822527`. Setup/operation/cleanup are 18.104937083/1.007004959/2.990339625 seconds; seal `9296dc7ce677be2c1178046745dab8d7bae5e73c840c4367014180b9ff97b030`. These are functional observations, not quiet paired benchmarks. No source or workload image was built for this reference run.

The captured `run -p 127.0.0.1:49277:8123` exposed the native frontend's unsupported option. Regression `5a94e51a-f5da-46a9-a99e-3efba2957adc` also proves the missing native inspect `HostConfig.PortBindings` projection. Explicit IPv4/fixed TCP parsing now sends both publication and exposure fields through the existing transport; inspect derives bindings from the recorded runtime specification and excludes expose-only ports. Dynamic/range/IPv6/UDP syntax fails before mutation. Coverage gate `8b71d8e8-08d3-4064-af8e-7103a9d3f070` passes 69 frontend and 64 API test functions, including actual executable/private-socket projection, malformed inputs and omitted-option preservation. `DockerRunCommand.swift` covers 157/157 lines. The extracted inspect DTO covers 1/2 instrumented lines; the unchanged `networkMode` constant's synthesized initializer counter stays zero despite the existing direct-value/encoding test, an explicit instrumentation limitation rather than missing behavior hidden by a gate. Touched Swift static checks and independent product review are clean. Native live proof and enhanced prerequisites remain open.

D06 is Active. Keep the original fixture configuration/probe and all four observations: actual CLI `forwardPorts`/`portsAttributes` metadata, guest HTTP, Mac loopback HTTP and rejection of a second owned container publishing the occupied port. The pinned Python 3.13 Alpine index is `sha256:399babc8b49529dabfd9c922f2b5eea81d611e4512e3ed250d75bd2e7683f4b0`; its admitted ARM64 manifest is `sha256:0515d7a37d0febc8bd7d88b4879b8598f4e1a1aae16307c733fd34f36be18f15`, config `sha256:8916b0d1897684408d57200dbe62d6b718655110b8911a66bc8eeec3c8a6e3e4`. No product or workload image is built for the reference. Arbitrary port ranges, IPv6, TTY and VS Code UI are non-goals of this fixture.

The adapter requires `up --include-configuration` to return the original forwarding configuration; the legacy probe's constant `forward_metadata=true` alone is insufficient. The host probe uses direct loopback TCP, an 8 KiB response cap, exact status/content/framing and one ten-second readiness/receive deadline, including slowly delivered headers/body. A listener already occupying the port blocks admission; no unrelated listener is stopped. The second container uses a distinct journalled owner, an admitted immutable image and exact port bindings. It must be created, then fail start with a specific port-allocation error, remain stopped and be removed before the primary container. Unknown errors or changed resource identity cannot be accepted as parity or cleanup.

The Docker-only VM enables its existing SSH port forwarder for this fixture and prepends a single `127.0.0.1:49277` TCP rule followed by an all-port ignore rule in its private Lima configuration. All other fixtures retain disabled host port forwarding. This uses [Lima 2.2.0's documented first-match rules](https://github.com/lima-vm/lima/blob/v2.2.0/templates/default.yaml) and [override-first merge order](https://github.com/lima-vm/lima/blob/v2.2.0/pkg/limayaml/defaults.go), within the existing VM/SSH ownership, process receipts and cleanup-only recovery. The original loopback publication contract is not changed to a VM address or public interface. Native candidates do not start Docker, Colima or this forwarding path.

Component checkpoint `9f8d4139-5a89-47c7-a67d-489526d275ea` exposed Python 3.9's distinct `socket.timeout` type in the real slow-response regression. Explicit handling fixes it; ten focused HTTP/forwarding/recovery tests pass, followed by all 741 component tests in `5eb873b1-196f-43f9-ba23-0a196cc2f1af` (14.498 seconds Bazel elapsed). A focused 78-test trace executes 141/147 D06 adapter lines (95.9%); this is not whole-product coverage. The prepared Python OCI archive has SHA-256 `1ec252253304751e744fd1ffab3b1e2cf0c718b854588055af317f903a180afe`. Docker proof and frontend publication implementation are now recorded above; native live proof remains required. Enhanced qualification remains blocked by its exact guest input. Phase durations are retained as functional observations, not quiet paired benchmarks.

The fallback host forwarder also misclassified an occupied TCP listener as a container conflict (HTTP 409). Real-socket regression `b1acb147-7f65-4df1-8bd8-ac29e5a388a8` reproduces it; typed `EADDRINUSE` now maps to runtime allocation failure (HTTP 500), matching the captured Docker start phase, while unrelated errors retain their classification. Coverage `5b170ff3-4d9d-4b73-afbe-07f2c411347d` passes all 147 stock runtime test functions and verifies that collision cleanup leaves the original listener intact. Final combined coverage `1e370fe5-160f-4988-854a-e2669c624cd6` passes all 280 affected functions, reusing unchanged frontend/API results; `PortForwarding.swift` executes 108/108 lines. Touched static checks pass. This tests the fallback path, not the stock native publisher's live behavior.

Documentation disposition: README, DESIGN, COMPATIBILITY, DocC Testing and the issue/PR handoffs now describe the unreleased publication boundary and distinguish reference evidence from pending native qualification. Install instructions and public lifecycle CLI help remain unchanged: no release is published and no public lifecycle option is added. The internal frontend's usage errors document its bounded syntax. The final documentation phase still covers the complete release.

### D05 locked Features

D05 is handed off incomplete on enhanced guest publication after passing all four unchanged observations and cleanup in Docker and stock Apple. Both lanes use harness `f3841fb394edbacbb7c23abc467ad556cf8a1e2a`. Docker invocation `9cd52ed5-55cc-4245-8058-8671ba0e948d`, campaign `docker-d05-native-20260919b`, records setup/operation/cleanup 19.239489125/25.160549542/3.062568708 seconds and seal `3ce6775955ae47a2fecb8caa0ac33d0ddba0e98405094798ad93f465c5e120f9`. Stock invocation `f80ebfc8-b5c8-45d1-b155-b6a1e9006e2e`, campaign `candidate-d05-native-20260919a`, records 10.866535791/41.344849292/2.752595542 seconds and seal `671a24a48901988895fa0da1249c62c9316233769ac7869412d9299d91d5def0`. Stock reused retained product `533f9a7`/archive `f3afc1a2-9e75-4464-ba17-5386abd1e8fe` without rebuilding. Both have zero errors/resources and clear recovery. These separate functional campaigns are not quiet paired benchmarks or three-lane qualification; the report correctly remains incomplete. The 19 September 05:39 BST registry check still reports `manifest unknown` for the exact enhanced guest tag. Do not rerun unchanged successful cases as a substitute for that dependency. D06 ports is next.

The final component gate passes 693 tests in 12.441 seconds (`bf4a2f55-bb44-4039-a4bc-f8ead1fd4a96`). A focused 48-test standard-library trace at the live harness source covers 92/96 executable adapter lines (95.8%), not aggregate product coverage. The complete final independent review is clean. README, harness instructions and issue/PR handoffs are updated; DESIGN, COMPATIBILITY, CLI help, install instructions and DocC remain unchanged because no product binary, public API, stable-support claim or invocation interface changed. Full release-quality, enhanced parity, unattended fault recovery, quiet performance and publication gates remain open. Earlier implementation/failure checkpoints below are historical.

The first Docker invocation `867e99e6-a7cc-417d-9d1c-71c3a33b2516` at `70320f48fb320ae42e5c0bfdf62bea6e65eac4e7` correctly rejected the missing lockfile, but exposed a harness assumption: `--log-format json` does not turn Node warnings or the CLI's final stack trace into JSON. That failed case remains immutable; cleanup passed and recovery is clear. A deterministic mixed-stderr regression reproduced the parsing failure. Rejection now independently requires the exact structured stdout `message` and a complete matching plaintext error line, as well as exit 1/error outcome/no created ID; unrelated errors still fail. Component gate `bf4a2f55-bb44-4039-a4bc-f8ead1fd4a96` passes. Positive installation has not yet run; a fresh reference case is required.

The unchanged fixture must install `git` and `jq`, retain its checked-in lockfile and reject a missing frozen lockfile through the pinned official CLI. It uses Ubuntu 24.04 index `sha256:4fbb8e6a8395de5a7550b33509421a2bafbc0aab6c06ba2cef9ebffbc7092d90`; the admitted ARM64 leaf is `sha256:7f622ca8766bccb22f04242ecb6f19f770b2f08827dc4b8c707de5e78a6da7ab`, config `sha256:ea17ec341c4211d1dd7f184a0dedf7dcb7945e92db20a5dde20544262214b84f`. Common Utilities 2.5.9 and Git 1.3.8 retain the original checked-in OCI digests/options. The [Dev Container Features project](https://github.com/devcontainers/features) and [reference CLI](https://github.com/devcontainers/cli) define these inputs; no candidate-side replacement installer is introduced. Arbitrary Features, TTY, Compose integration and unrelated runtime changes are non-goals of this fixture.

The adapter reuses D02 generated-image ancestry and ownership checks. Expected metadata is projected from the two digest-authenticated Feature manifests using the pinned CLI's metadata fields, not accepted merely because a label exists. Both positive and negative `up` commands use `--frozen-lockfile`. Before positive creation, only the disposable workspace's copied lockfile is renamed; the negative command has its own journal identity and must exit 1, return an error result and emit the exact missing-lock diagnostic. Network failures, signals, an unexpected container or regenerated lockfile cannot satisfy `frozen_lock`. The original copy is restored without overwriting an unexpected replacement. Unlike the legacy negative test, no existing container is removed. Positive execution must leave the lockfile bytes unchanged. The probe supplies the other three observations; configuration is never treated as execution proof.

The reference retains its private Docker VM/legacy Engine-build path. The candidate uses its authenticated private bundle and admitted native builder, with no Docker fallback. The negative command participates in command-completion, process-stop, diagnostic retention and cleanup-only recovery checks. Interrupted native phases remain quarantined rather than replayed; full unattended recovery is still an open programme gate. Negative execution has a 120-second deadline, positive Feature installation 900 seconds and the operation an outer 1080-second deadline. Timings include both runs and are recorded as functional observations; quiet paired benchmarking remains later work.

Focused component gate `3aa51759-293c-4a61-90ca-a1f0255a8b0b` passes 689 tests in 12.500 seconds. The initial gate `411c3c90-082e-49a5-ac85-b20838c6535f` exposed positional assumptions in image-lock tests; these now select images by name. Independent review also found the native diagnostic filename difference and a missing negative-command recovery entry; both are corrected, with real subprocess and diagnostic-closure regression tests. Live Docker oracle capture is next, followed by stock execution using retained product `533f9a7`/package `f3afc1a2-9e75-4464-ba17-5386abd1e8fe` unless the oracle exposes a product gap. Enhanced qualification remains dependent on publication of the exact enhanced guest. No release or complete parity claim follows from component success.

### D04 lifecycle hooks

D04 is handed off incomplete after reference/stock proof, on the same enhanced guest-publication dependency as D01-D03. Its unchanged image-based fixture returns `host_hook=initialize` and `order=onCreate,updateContent,postCreate,postStart,postAttach` through pinned official CLI `up` and `exec`. `initializeCommand` writes into the disposable mounted workspace; subsequent hooks append the guest's lifecycle file. The harness neither creates the host marker nor manufactures/normalizes hook output. Repeated reopen/restart semantics, parallel/failed-hook cases, Features and TTY are outside this fixture.

Both adapters reuse D01 immutable image/owner/workspace checks, bounded commands and normal owned cleanup. Docker additionally supports cleanup-only recovery after verified CLI command completion. Interrupted native phases still require explicit reconciliation and remain quarantined; the adapter does not establish fully unattended recovery. Native D04 needs the authenticated private-runtime candidate but no builder; missing admission fails before runtime/keychain mutation. Exact hook configuration, probe and adapter bytes participate in shared fingerprints/runfiles. Neither adapter availability nor component tests establish three-lane parity. The stock run reused the existing `533f9a7` product archive without rebuilding. Enhanced guest publication remains blocked; quiet paired timing belongs to the later performance phase.

The first D04 Docker run (`9dd92996-0220-49a9-b668-d532efee6976`) returned both original expected observations and cleaned up, but its gate failed: the common contract loader incorrectly lowercased all expected strings, changing the hook names. That failed result remains immutable. Shared expected-value serialization now preserves string case and only converts JSON booleans to their lowercase wire spelling; native execution, Docker execution and report loading use the same helper. Regression `07d0191d-32af-47da-a7e2-2c893160df27` reproduced the bug; `707b9f03-8a13-4e1e-9cc3-e2ce1f031331` passes all 639 component tests. A genuinely lowercased observation still fails. Fixture assertions and actual output are unchanged; the new reference run below validates the corrected gate.

At harness source `57ab04ce674a5ffe894dd151afd0b00ea947f2dc`, Docker campaign `docker-d04-native-20260919b` passes (`7c41a35a-01bc-4aaa-b866-2a580cb3c560`) with setup/operation/cleanup 18.216034667/0.736704542/2.895339167 seconds and result seal `9d64ef5c2fd1747ecc2e544a14decf5db628cda24841d4078b875dfef907f450`. Stock campaign `candidate-d04-native-20260919a` passes (`76ebd27c-8b98-4df1-969b-fa12c3f2e159`) with 6.548827792/2.733246333/1.346782375 seconds and seal `95583026d1c165b3e4387c676ab9c9076e213ed24d2c5156db20ea3bd1003dca`. Stock uses candidate `f3afc1a2-9e75-4464-ba17-5386abd1e8fe` from product `533f9a7`, not a new build. Both runs have zero errors/resources, passed cleanup and clear runtime recovery. Separate functional campaigns do not establish quiet paired timing or full three-lane release qualification; the report correctly remains incomplete. D05 Features is the next independent fixture.

### D03 users and environment

D03 is handed off incomplete after reference and stock qualification, on the same enhanced guest-publication dependency as D01/D02. The unchanged D03 Dockerfile creates `vscode` UID 1000. Its official-CLI `up`/`exec` probe observes `container_env=container-value`, `expanded_env=container-value`, `remote_env=remote-value`, `home=/home/vscode`, `post_create=user-post-create`, `uid=1000` and `user=vscode`, with verified cleanup. UID remapping, TTY, Features and the full lifecycle matrix are outside this fixture. D03 retains the released Docker closure and CLI 0.88.0, local legacy build path, bounded execution and raw phase timings; authoritative quiet comparisons wait for the performance phase.

The reference adapter reuses D02 build ancestry and completed-command recovery. Image metadata must equal the fixture's post-create, container user/environment, remote user/environment and disabled UID-update settings; raw variable expressions are not rewritten into expected results. This matches the pinned CLI's metadata field projection. Only the checked-in disposable fixture workspace becomes readable (directories `0755`, files `0644`) by the non-root guest; the private parent, control state, logs and credentials retain restrictive modes. Admission binds Dockerfile/configuration/probe bytes into the shared comparison identity before any VM work.

Docker D03 passes all seven observations and cleanup at `d0ad0aa57ad418a1aba5be6dd5ae61d712d80d81`, invocation `caae5f4d-2d1f-4cb2-b9ae-3d54b5b13bde`, campaign `docker-d03-native-20260919a`. Setup/operation/cleanup took 257.331951208/1.076954209/32.420980042 seconds; these are functional-run observations, not quiet paired benchmarks. No owned resources remain, the disposable root is removed and runtime admission is clear. Result seal: `42d5839f03cf3c6ccf31acb7b18354a519f197a4a3b3063fc098c682d67e2ca6`.

The captured reference startup uses `-u vscode`. The unreleased native frontend forwards `-u`/`--user` as the Engine create `User` field; malformed or duplicate values fail before mutation and an omitted user preserves image defaults. Candidate D03 reuses D02's admitted builder, pre-build identity/DNS checks and guest-before-builder cleanup, plus the same seven observations and non-root-readable fixture subtree. A prepared candidate archive is mandatory. Enhanced guest admission remains required; neither component tests nor reference/stock success establish complete three-lane parity.

Stock D03 passes all seven observations and cleanup at `533f9a7e268c894c3e86f5dde0ca78b570a6628f`, invocation `495adb97-3b5e-435f-83ec-2e9bccd9b674`, campaign `candidate-d03-native-20260919a`. Setup/operation/cleanup took 11.146059708/6.883518042/2.382704250 seconds. Result seal: `8dd3e3777e1a3e34130d8fa275ece730184b458ce2aeecf8997a309e7c265ae5`; zero errors or remaining owned resources, runtime recovery clear. The immutable stock archive (`f3afc1a2-9e75-4464-ba17-5386abd1e8fe`, SHA-256 `38e3080df0fb733af7e74485cddfd37f12b21e45c697c64302755ba813ef5c9a`) passed package checks and was reused without rebuilding. Build wall time was 25.707116042 seconds, with quiet-host verification false. Reference and candidate ran in separate functional campaigns, not a paired performance experiment.

The 19 September post-run registry check still reports `MANIFEST_UNKNOWN` for the pinned enhanced guest tag, and Compose 0.15.1 still has no guest OCI asset. No substitute guest or repeated unchanged live attempt is used. D03 remains incomplete for three-lane qualification; the next independent fixture is D04 lifecycle hooks.

Native harness gate `14c62e5f-9070-4290-bafd-410648ddb938` passes all 585 component tests. Focused standard-library tracing executes 26/26 lines of the new reference adapter across 15 tests; this is diagnostic adapter coverage, not overall product coverage. Independent final-diff review is clean. Initial analysis failure `122f09de-e4e2-4425-b16c-4a15add67d2e` identified a missing Bazel fixture export, now corrected; it did not start a VM.

### D02 Dockerfile-configuration reference

D02 is handed off incomplete after passing reference and stock execution, while the enhanced lane remains blocked. The 19 September 2026 02:06 BST anonymous registry check still returned `manifest unknown` for the exact enhanced initialization tag; Compose 0.15.1 still exposes no guest OCI archive. No stock substitution or repeated unchanged runtime attempt is permitted.

`make bazel-engine-case CAMPAIGN=<id> LANE=docker CASE_FIXTURE=D02-dockerfile-config` uses the existing private VM, official CLI and immutable Alpine pin. Setup copies the unchanged configuration, Dockerfile and probe and pulls the base. Operation measures CLI `up` (including the fixture image build) and `exec`; assertions remain `build_arg=from-devcontainer`, `build_target=development`, `post_create=dockerfile-post-create` and the exact workspace. Product binaries are not rebuilt. Generated image ownership requires the case label, workspace mount, immutable image identity, pinned base-layer ancestry and exact lifecycle metadata before guest deletion. Build outputs/cache belong to the disposable VM and disappear with its verified shutdown/root removal; nothing in the operator's Docker store is adopted or deleted.

D02 explicitly selects the official CLI's `--buildkit never` mode with `DOCKER_BUILDKIT=0`, so the pinned Docker client uses the Engine build endpoint without using an unpinned global Buildx builder. The shared upstream `exec` path still probes Buildx availability; no plugin exists in the pinned Docker client's global search directories on this host. A fully hermetic plugin-discovery boundary remains reference-harness hardening work. D01 arguments remain unchanged. This is the legacy Engine-build reference path, not evidence for Buildx-specific behavior; fixture configuration and assertions are unchanged.

The Docker reference passes all four exact observations and cleanup at source `05cae9ed0a9f0f6e14c87f0af3cb7d02d0b531e7`, invocation `0686509c-bf9c-4ba3-a405-02fb0ebca041`, campaign `docker-d02-native-20260919a`. Setup/operation/cleanup durations are 17.683057833/1.214592000/2.948016291 seconds. No owned resources remain and runtime recovery is clear. These are functional observations, not quiet benchmarks or three-lane qualification. The earlier reference component gate `a7a37ae6-0efd-4aa6-9bca-bce6e1fb678e` passes 546 tests; focused tracing covers 56/56 new reference-adapter lines in 15 tests, not aggregate product coverage.

Candidate D02 requires a prepared private-runtime archive containing the native build frontend, plus the already released builder image. It reuses the candidate command/attachment lifetime and D02 reference ownership/probe logic. Setup admits and provisions the private worker; before `up`, it revalidates worker identity and host DNS. Cleanup verifies and removes the guest before stopping the builder; image/cache data stay within the disposable provider root until verified provider shutdown. Missing or changed ownership keeps quarantine. Native `RootFS.Layers` comes from the same descriptor-bound OCI configuration as the image ID, not fabricated test data. No Compose-core dependency or Docker fallback is introduced.

Stock D02 passes all four observations and cleanup at `106144abaf5c68e21acb839211bf2c6a5f5b2280`, invocation `3530b684-a77e-42fb-b1a0-11e4c507e401`, campaign `candidate-d02-native-20260919a`. It uses the package from `395f1178-2659-455c-9fb0-0bcb6e2671b4` (archive SHA-256 `aaeb1290b4a8803d6301d445e696422cbf752ec0ca6a72de4da52bbe51c0bcb9`) without rebuilding during preparation or execution. Setup/operation/cleanup took 11.082251250/5.716879250/2.264501042 seconds. The result seal is `436066ed0b93478fe0817fa23190a1074896b020296f27d817cbebd8f95c80a2`; no owned resources remain and recovery reports clear. These are functional observations, not quiet paired benchmarks or release qualification. The campaign report correctly remains incomplete rather than treating evidence from separate campaigns as a complete three-lane run.

Affected component gate `5d2219b7-fb30-4a10-b66b-f7204c120f3e` passes native image/API/model tests, 564 harness tests and the frontend suite. Final frontend gate `b482306a-a3c9-4813-b4f6-f1e77792bc0f` passes 65 tests, including real executable SIGTERM cleanup and both public-client/executable socket build success and late HTTP-200 failure. Focused line coverage is 96.5% archive, 100% build parser and 98.3% build stream; native identity is 91.1% in the component gate. Earlier signal-crash failures remain retained. These measurements are development evidence, not aggregate product coverage or live candidate qualification.

The existing completed-command Docker recovery supports D02 without replaying `up`/`exec` or rewriting failed results. Unknown creation, interrupted native builds and unresolved attachments still require conservative reconciliation; the new adapter is not a claim that all interrupted native phases recover automatically. The frontend's current build limits and explicit `.dockerignore` rejection are documented in the README. Full fixture parity, enhanced inputs, quiet timings and release gates remain open.

### D01 image-configuration reference

Stock candidate D01 now passes all four exact observations with verified cleanup and zero remaining owned resources: campaign `candidate-d01-native-20260919b`, invocation `46a33e35-686a-45f4-9168-80ba6ecafa31`, harness `e346eede2c0a5c2e615152dc7cb5568f7adad67d`, retained product `f8dc210688774a381d4ea3040870ee836c18d5ca`. Setup/operation/cleanup took 6.772554542/2.210318375/1.615386041 seconds. Recovery is clear. This result reused the existing archive and did not rebuild products. It is not a quiet paired benchmark or coherent three-lane campaign; the report correctly remains release-unqualified because matching Docker/enhanced cases and the other fixtures are missing from this campaign. Earlier checkpoints below remain historical.

The first stock candidate run (`85a573e8-329f-4b22-9da5-26f3d7771982`, harness `c87438a`, retained product `f8dc210`) started the container and completed post-create but failed before probe observations: the upstream CLI intentionally leaves a foreground attachment alive after `up` exits. The harness now reconciles completed non-attachment commands, deletes the verified owned guest, then verifies attachment closure and retains final diagnostics under one outer cleanup deadline. Real subprocess and actual cleanup-wrapper regressions pass with all 525 component tests (`18429076-b9d2-49e8-b7e4-f5ea344f049c`); independent source review is clean. The original failed case remains failed; its exact guest/helper/Engine were safely closed and prior services restored. No product rebuild was necessary. Live candidate qualification remains outstanding.

Status: Docker reference passes all four observations and zero-residue cleanup at source `c6daa35fde14da9de59f080136865728c92841ac`, invocation `83e7687b-5db8-4788-8dc2-5cf3b33ca656`, campaign `docker-d01-native-20260918c`. Observed environment is `image-config`, workspace `/workspaces/devcontainer-parity`, post-create marker `post-create` and UID `0`. Setup/operation/cleanup take 20.340733000/0.744793291/6.458950417 seconds. Recovery reports clear. These are retained functional observations, not quiet paired performance or three-lane parity. The full harness passed 500 tests at `006b5789`; the final argument correction passed 12 focused tests and this live run. A final 68-test instrumented component run measures 95% rounded line coverage in the new reference adapter, not aggregate product coverage.

`make bazel-engine-case CAMPAIGN=<id> LANE=docker CASE_FIXTURE=D01-image-config` executes the unchanged D01 configuration and probe through the prepared official CLI and Node, using only the admitted private Docker socket/client. A unique case label identifies the created resource. Workspace and CLI data remain in the private SSD root; no global Docker context, npm installation or SwiftPM fallback is used. Setup explicitly pulls the fixture's unchanged immutable Alpine index digest into the private VM. The CLI reference may use Docker; candidate installation must not depend on it.

The native candidate adapter selects `LANE=apple-stock` or `LANE=container-compose` with `CANDIDATE_INVOCATION=<prepared-schema-2-archive-invocation>`. Admission authenticates the private Node/CLI bundle and native executables before any service mutation. It reuses the exact public packaged CLI, provider isolation, fixture content and ownership assertions; native UUIDs are explicitly accepted without rewriting observed identities. Image pull remains in setup, and operation measures only `up` plus `exec`, matching the reference phase boundaries. Each command journals intent before spawn, preserves stdout/stderr separately, records exit and verified process-group closure, and retains bounded diagnostics before cleanup. Missing exits, surviving groups and uncertain creation preserve quarantine; no command is replayed. Interrupted native D01 resource deletion requires explicit reconciliation, not the Docker-VM recovery path. This adapter passes 522 component tests (`e34f5f9c-6e7f-43dc-8e73-56fc4f7fbd26`); a focused 71-test trace executes all 86 candidate-adapter lines. Neither result is live candidate parity or a quiet benchmark.

Setup, operation and cleanup have whole-phase limits of 180, 195 and 45 seconds respectively, inside the existing VM lifecycle bounds. CLI pull/up/exec commands separately allow 120/120/60 seconds and record monotonic durations, process and exit receipts. Cleanup checks full container ID, case label, immutable image and workspace mount, then deletes by ID and verifies absence. Missing command completion, uncertain creation, replacement resources and ambiguous ownership preserve quarantine. Private bounded CLI logs and metadata are retained before root removal, including nonzero command failures; public case observations do not expose those logs.

The first live reference attempt (`94cc76fa-ba5b-48a0-92df-830aa28c1d7c`, source `97a8b46`) completed CLI `up` and verified cleanup, but failed before observations because combined stdout/stderr placed a Node deprecation warning beside the result JSON. The command runner now captures and retains both streams separately; strict result/probe parsing consumes stdout only. It does not suppress warnings or filter unexpected fixture output. A real subprocess regression proves stream separation and retention. The original failed result remains failed; the subsequent corrected pass is recorded above.

The second attempt (`9217930a-b5c9-4d18-aa86-8b525d369ab0`, source `60490e4`) completed both CLI commands and cleanup, but CLI `exec --log-format json` wrapped the probe output into stderr log events. The adapter now uses JSON mode only for `up`, retaining ordinary `exec` stdout behavior as in the original fixture runner. All four observed values in the private diagnostic were correct, but that failed attempt is not promoted into a pass; the corrected invocation must produce fresh evidence.

For a completed-command interruption, `make bazel-recover-runtime` can report `ready-to-clean-completed-devcontainer`. Apply with `make bazel-recover-runtime-apply CASE_ID=<reported-id>` reauthenticates original released inputs, command closure, VM process identities and resource ownership before cleanup and shutdown. It never replays `up`/`exec` or rewrites a sealed failed result. Unknown command completion or interrupted VM shutdown still requires explicit reconciliation. The component gate passes at `6160f0b0-1ba9-4b96-91a9-a6d2523dc302`; source review found and corrected omitted CLI diagnostic retention and missing normal-phase deadlines. A real slow-response socket regression verifies bounded cleanup without clearing ownership. Both candidate D01 lanes and quiet paired performance remain outstanding.

### Bounded service HTTP probes

The explicit `DevContainerServiceIntegrationTests` lane uses `ServiceTestHTTP`, backed by the existing `ProcessRunner`, rather than waiting for curl to exit before draining pipes. Each request has a five-second maximum (or an earlier enclosing deadline), drains stdout/stderr concurrently, retains at most 64 KiB per stream and rejects truncation, helper exit failure, malformed UTF-8/status and unexpected HTTP success requirements. Curl's implicit configuration and proxies are disabled; only the Unix socket is selected. Deadline return waits for owned child termination and reap. The bound does not mean abandoning cleanup at five seconds.

The deterministic service suite includes fake-executable probes for large stdout/stderr, exact request arguments, malformed responses, failed helper exit despite HTTP 200 text, deadline/reap and expired-before-launch behavior. These are no-Keychain component tests, not live HTTP parity. The evidence policy requires all 23 service test methods in both dependency profiles, including provider-declaration contracts below. Building the declared host-integration target proves compilation, not execution or Keychain approval. Host integration remains opt-in and still requires its own exact-head result.

Provider declaration tests exercise the same pure projection used at startup: exact build/runtime provenance and resource ownership, compile-profile-specific handoff advertising, unavailable/emulated capability mapping, route inventory, deterministic fingerprints and rejection of empty runtime provenance. They neither open Keychain nor start a server. Positive execution in the production projection is required by the coverage policy; this does not qualify the remaining persistence, enrollment and real-server startup paths.

### Published Docker oracle inputs

`make bazel-prepare-docker-oracle` uses `Tools/bazel/docker-oracle.lock.json` with the existing leased GitHub acquisition and recoverable preparation path. It downloads published binaries, never source builds: Colima 0.10.3, Lima 2.2.0 (including the ARM64 Linux guest agent), and Colima-core 0.10.4's compressed Ubuntu 24.04 ARM64 Docker image. Extraction and copying use SSD scratch; verified reusable payloads and inventories remain internal. `OFFLINE=1` verifies and reuses the retained inputs. The image stays compressed, non-executable data; no VM, Docker context, service or existing Colima profile is created or changed.

The version numbers intentionally differ: [Colima's pinned image manifest](https://github.com/abiosoft/colima/blob/00f6c297e92a82c04a4ab507db0a61435650d7e8/embedded/images/images.txt) selects Colima-core 0.10.4. Its [build source](https://github.com/abiosoft/colima-core/blob/078088ada3cce8835b8c42710ab44d17a935b195/Makefile) specifies Docker Engine 29.5.2. The acquired image also matches the manifest's SHA-512 `32242674b046b5057e60c4aba334b51e3665f05412cda89ed081cc2de153ae5c41f6b105b5c442cbe48d78e2cc21e9ba1950e406b6fb4fc2fd1dd2259240abbd`. That is published-input evidence, not verification of a booted Engine's version or executable hash.

The Lima archive contains one documentation-only symbolic link, `share/doc/lima/templates` to `../../lima/templates`. Preparation records this exact version-specific omission in its hashed layout and materializes no link. Missing, changed, duplicate or additional links fail; all runtime files still require regular files and full inventory authentication. Colima's executable and the compressed disk image use separately reviewed raw destinations and permissions.

On 17 September 2026, all three actual GitHub assets were acquired, prepared, retained and reused offline with unchanged inventories. The retained executables report Colima 0.10.3 and Lima 2.2.0. Their preparation keys are:

| Input | Preparation SHA-256 |
| --- | --- |
| Colima | `a0f39d996717ab85b87f9c9f84b1963e6429c4e10e8fddf1785833bb62131590` |
| Lima | `437aba2ea0f1326026acf732350e2dc4516d106ad720960d3fedffbc2ce66b32` |
| Compressed Docker image | `0ec4c7c78215194ec038d7177d9303d3a335a4198b5d857c74e2e8ac4e4fb9c4` |

`make bazel-prepare-docker-cli` obtains the existing parity manifest's Docker 29.6.2 Homebrew bottle from public GitHub Packages, not an invented `docker/cli` GitHub release or the operator-installed 29.8.0 client. `Tools/bazel/docker-cli.lock.json` pins the registry manifest, bottle size/hash and executable hash; validation binds the latter values to `Tests/Parity/manifest.json` before writes. Skopeo downloads anonymously by manifest digest with mandatory TLS, private HOME/registry/certificate configuration and an explicit acquisition-only policy restricted to the Docker bottle repository. Content hashes authenticate the downloaded manifest, bottle and executable; no registry-signature claim is made. The policy does not change the operator's configuration. The [Skopeo command implementation](https://github.com/containers/skopeo/blob/main/cmd/skopeo/main.go) and [registry configuration selection](https://github.com/containers/image/blob/main/pkg/sysregistriesv2/system_registries_v2.go) explain why a private per-user configuration is needed instead of merely supplying `--src-no-creds` or `--registries-conf`.

Acquisition is serialized by the reference-store lease. Temporary data stays on SSD; the bottle, prepared client, licence/notices and authenticated inventories remain internal. Raw publication records intent before copying and seals only verified durable bytes. An interrupted copy can resume; changed sealed bytes, aliases and unregistered files fail without repair or adoption. `make bazel-prepare-docker-cli OFFLINE=1` is read-only and refuses incomplete publication, even a leftover pending marker; explicit online preparation revalidates and finishes recovery. Complete prepared assets are reused after SSD extraction-cache eviction without re-extraction. The command does not invoke Brew, install a package, run Docker, start a VM or read an operator Docker context.

On 17 September 2026, real acquisition and offline reuse returned preparation SHA-256 `405e13476fdedc5732e5d7a048c81d9a2a273c00dd88f56d232dc2b634057983` and inventory SHA-256 `1b38f210632788a243e68974995f9c14207bdc941e25ae6272589b4d7535e6fc`. A separate client-only version check reported `Docker version 29.6.2, build dfc4efb1e2`; the executable matches the existing `eade1c3a5dda47534dc776f2f534c99cc94cfcf9ce07c4bf09e98258d13e7d7a` pin. Thirteen focused regression tests pass; standard-library trace reports 99% rounded line coverage for `prepare_docker_cli.py`, not overall product or SonarQube coverage. Tests cover offline reuse, SSD eviction, pin drift, corrupt archives/receipts/executables, interruption recovery, foreign files, storage separation, TLS and isolated downloader configuration.

The complete oracle is still unqualified. The preparation commands deliberately return `runtimeReady: false`. No historical parity pin, support claim or release gate is upgraded by preparation. README, launcher help and PR handoff describe the acquisition change; product CLI/API, installation, DocC support claims, compatibility fixtures and release notes remain unchanged because no product behavior or released support changed.

The opt-in `make bazel-engine-case CAMPAIGN=<id> LANE=docker` now selects `released_docker.py` and `docker_vm.py`. E01 uses the same protocol probe; E02, E03, E05 and E06 use the same guest assertions and fixed API 1.53 as the Apple adapters. Preparation is separate and admission is offline. The runner precreates private SSD `COLIMA_HOME`, `LIMA_HOME`, Docker configuration, caches, temporary directories and an explicitly mounted workspace. It does not mount its control HOME, activate a Docker context, forward host TCP ports, use an SSH agent, enable Kubernetes/Rosetta, or rebuild/download an oracle. Lima dependency provisioning checks existing tools instead of implicitly installing guest packages. Live verification must still prove that the pinned image satisfies this configuration.

The shared host lease and durable quarantine precede VM startup. A private SQLite journal records each command intent, process, exit and duration. Startup checks the exact private instance, Docker version/commit/API/architecture and guest `dockerd` checksum. Before Colima may stop anything, the runner authenticates only the pinned Lima hostagent/VZ/usernet PID-file paths, their exact prepared executable and owned-instance arguments, and captured process incarnations. A process merely using the same binary or mentioning the case directory is not shutdown authority. Successful cleanup requires stopped VM inventory, no captured or newly scoped helper, an unreachable Docker socket and retained diagnostics before removing SSD state. Reused PIDs are distinguished by start time.

Make selects the separate `//Tools/testing:released_docker_engine` target for `LANE=docker`. Its 3,600-second outer Bazel timeout accommodates the permitted VM setup, E03 operation and cleanup budgets (at least 1,035 seconds together), plus evidence retention. This is a final cancellation boundary, not an expanded fixture deadline: startup remains 300 seconds, ordinary VM commands 60 seconds, E03 360 seconds and VM stop 90 seconds. The existing Apple target retains its 900-second outer timeout. Neither value is a performance acceptance threshold or permission to retry a failed case.

An interrupted command, startup without ownership capture, changed PID role or uncertain guest deletion preserves quarantine and the owned root. Report/apply recovery now dispatches Docker cases separately from Apple service restoration, but only after an authenticated `docker-vm-closed.json` proves verified shutdown. It rechecks the private plan, captured process incarnations, command PID/groups, scoped helpers and unreachable socket without starting or signalling anything. A durable directory-incarnation receipt supports interrupted normal cleanup, partial deletion and a crash after deletion but before guard clearance. Read-only reports perform no journal writes; apply requires the exact case ID, and the original failed case is never rewritten. Interrupted VM startup/shutdown remains blocked: do not manually clear its guard or treat this stopped-state recovery as automatic orphan termination. Independent non-runtime work can continue while reconciliation is pending. Historical standalone Docker/Apple records have different fingerprints and cannot be merged into a parity claim. New published campaigns use `campaign_identity.py`: all six shared release/guest/oracle inputs and the complete shared runtime/preparation helper set are bound identically across lanes; selected runtime identities remain lane-specific. Missing cross-lane locks fail instead of silently narrowing the comparison. Local-candidate receipts remain explicitly separated from published-release campaigns. The comparator still requires three matching complete cases and makes no quiet-timing claim. The full live matrix remains open.

Focused development validation passed 299 harness tests and the preparation target (`7fa96ea2-b9d1-4cbc-beb0-75e5fce8f65b`; the preparation target reused its unchanged passing result). Standard-library trace reports 92% rounded line coverage for `docker_vm.py` and 97% for `released_docker.py`, not overall product/Sonar coverage. The preceding `9a00a20f-c65f-4a88-86e8-46cdc66c0c7e` exposed Bazel's long sandbox path in VM-free configuration tests; those tests now isolate socket-path validation and separately prove the real macOS limit, without relaxing production admission. Independent final-delta source review, shell checks and affected Markdown lint are clean. These results do not establish a booted Docker Engine, quiet timing sample or release acceptance.

Bazel invocation `6e2f7ed1-3ed3-473d-8401-6cb90be624bb` passed all 70 preparation tests. The preceding `1d480944-d6c4-4074-9e27-fae77424f5a0` exposed a negative test fixture whose requested 0755 directory was made private by the runner's 0077 umask; the fixture now explicitly sets the prohibited mode before checking rejection. No production permission rule was weakened. Shell parsing, ShellCheck and affected Markdown checks pass; independent source/documentation review is clean. This is focused development evidence, not the immutable-head release gate.

Focused validation passes 31 release-preparation tests and 20 service-switch tests. Standard-library trace with missing-line analysis reports rounded line coverage of 91% for `prepare_releases.py` and 94% for `service_switch.py`; these are component measurements, not SonarQube or overall product coverage. Independent complete-diff source review found no actionable issue. README, preparation guidance and PR handoff are updated; product CLI/API, installation, DocC support claims, compatibility fixtures and release notes are unchanged because no product behavior or released support changed. Live oracle/parity and quiet-host performance evidence remain required.

### First live Docker qualification

The corrected E01 campaign `released-common-e01-20260918b` at `2b1a920` has authenticated matching identities across all three lanes and zero functional differences. Docker invocation `d0bfa4ac-4c72-4950-b0c1-58dc1a45c087`, stock `ba78bbc1-63dd-410a-b60b-ab9bf761a940` and enhanced `8533fef8-9196-4bb1-97b1-3d45c46ecbdb` all verified cleanup. The [generated timing table](evidence/released-common-e01-20260918b.md), [JSON evidence](evidence/released-common-e01-20260918b.json) and [JUnit projection](evidence/released-common-e01-20260918b.xml) preserve the exact fixture scope. Raw operation ratios are 12.302x stock and 206.606x enhanced versus Docker; these observations require investigation and do not qualify the quiet paired performance gate. No full-matrix or release claim follows from E01.

`make bazel-parity-report CAMPAIGN=<id>` reads one consistent read-only evidence snapshot and authenticates case/artifact seals. By default it requires the whole manifest; `CASE_FIXTURE=<id>` explicitly selects a subset without claiming complete coverage. Missing cases report failures; unfinished, corrupted, ambiguous or mixed-identity evidence refuses a report. Shared harness/release identities must agree across fixtures, not merely within one fixture. `REPORT_FORMAT=json|markdown|junit` renders the same records without runtime operations, retries, compilation or evidence mutation. Public output retains fingerprints, phase timings, status and payload hashes/counts; private logs, errors, resource paths and unexpected observations stay internal. The reporter does not certify quiet timing or releases. Focused report tests cover those boundaries, including cross-fixture drift and launcher path resolution; the component gate passes at `93ae340d-2ae1-4733-a456-de01f386b444`, all 148 workflow-helper tests pass, and focused trace coverage before the final identity guard was 97%. Independent final review found no remaining actionable reporting issue.

The initial shared E01 campaign `released-common-e01-20260918a` at `2d2d899` passed each lane with verified cleanup, but its records are not a valid comparison: Python resolved the Docker imported module to the workspace while the Apple entry script retained its Bazel runfiles path. Directory globbing therefore hashed extra unit-test files for Docker. The comparator correctly refuses the differing harness identities. The correction enumerates the complete 17-module runtime closure, hashes every member, and fails on a missing member; a symlink-runfiles regression verifies workspace-only tests cannot change that execution identity. Original records remain immutable. Focused Bazel validation passed at `5a4d7d9b-827d-4f83-b68c-22430f7639e4`; a new live campaign is required.

At committed harness `c8580a9`, E01 passed against the real downloaded Docker VM (`88799304-cedb-4adb-ab41-61d3120d27fb`): setup 14.240 seconds, operation 0.009 seconds, cleanup 2.793 seconds. Exact Engine version/commit/API and executable hash matched; verified teardown left no owned VM/helpers and clear admission. E02 (`d179f84a-a44b-4320-8c82-70aae12e5b23`), E03 (`2a097aab-9942-42bc-bb51-2f4e0d2e69ec`), E05 (`c8af992a-7033-46ae-84d4-40ea644b67c1`) and E06 (`9cbb9f31-fc0a-4dbb-a9e4-4a183b2d1861`) all failed at image admission after successful loading, before any fixture operation; all cleaned up successfully. E02 also showed two 120-second guest SSH readiness delays with hostname-resolution warnings (254.005-second setup and 32.404-second cleanup); the other three setups took 13.538-14.620 seconds. These are immutable functional diagnostics, not quiet comparative benchmarks. The startup variance remains to be investigated.

The image-admission failure exposed a harness assumption: the pinned Docker containerd store returns its OCI target digest as `Id`, not the config digest. See [exact pinned Moby inspection source](https://github.com/moby/moby/blob/568f755ebeb1ac9c6a8febbda6cd371ea0a9630b/daemon/containerd/image_inspect.go). Docker admission now requires the already-verified single-platform manifest digest in both `Id` and `Descriptor.digest`, plus Linux/ARM64, and privately retains the unmodified inspection. The shared guest adapter accepts only a config or manifest ID already bound by its admitted OCI closure; all resource ownership comparisons still use that exact ID. It does not rewrite either image pin, normalize returned fields, change fixture assertions, or claim config-ID versus manifest-ID parity. Stock's config-ID selection is unchanged. Regression tests cover wrong manifest/architecture, unrelated IDs and unchanged config pins; the 301-test harness and preparation target pass (`9d4ce713-8089-48fe-b2cf-69311ac07a60`). The preceding focused run caught an incomplete legacy synthetic fixture, now supplied with its required pinned image identity. Live guest qualification remains required.

With that correction committed as `dde586f`, all four Docker guest cases passed, including exact 4 MiB duplex, archive mode `0750`, alias DNS, persistent volume sharing, read-only binds and tmpfs. Each used the downloaded tools/image/client, retained exact Engine identity and verified zero owned resources after shutdown. Functional diagnostic timings are retained below; these are not quiet paired benchmarks and do not qualify the other two lanes.

| Fixture | Bazel invocation | Setup s | Operation s | Cleanup s |
| --- | --- | ---: | ---: | ---: |
| E02 lifecycle | `be4b855b-8b4c-4fbf-9c51-c2a74e87fb47` | 14.908 | 2.288 | 2.862 |
| E03 streams | `f2d89b90-2f76-433c-b20c-c17da6c5fb11` | 15.114 | 0.246 | 2.814 |
| E05 archive | `967611f0-ea52-4666-b477-c08af68da720` | 14.821 | 0.135 | 2.796 |
| E06 network/volume | `0d7bb25b-9a99-42df-a160-e21c208d63b3` | 15.347 | 0.407 | 3.062 |

The common published-campaign identity follow-up passes the 304-test component harness and preparation target (`a5949762-b735-45cc-8f96-f8354c84688a`). Tests prove distinct runtime lanes can compare, every shared lock/helper change invalidates the campaign, and missing inputs fail. Independent final-delta review is clean. Historical runs remain under their original identities; no new three-lane claim is inferred from this change.

### Pinned guest image preparation

`make bazel-prepare-guest-images` reads `Tools/bazel/guest-images.lock.json`. Registry-backed entries download immutable public manifests with Homebrew Skopeo (`/opt/homebrew/bin/skopeo`) and create deterministic OCI archives. The enhanced initialization entry instead requires the exact published archive bytes through the import command below; it cannot fall back to registry repacking. On a fresh machine, preparation stops at this first entry until its archive is supplied. Preparation does not use a Docker daemon, start a VM, load images into an operator store or compile products. Acceptance independently checks the pinned manifest/config digests and every referenced blob. Downloads and source snapshots use owned SSD staging; completed archives, pending publication records and receipts reside in private internal retained storage. The shared reference-store lease serializes publication. Interrupted publication resumes only matching intent; a sealed archive is never silently repaired. `make bazel-prepare-guest-images OFFLINE=1` verifies existing complete archives without downloading. Offline preparation can finish a verified receipt's leftover pending marker; runtime admission instead uses `require_image`, which performs no writes and refuses every pending publication.

The OCI closure checker is reused unchanged from Compose commit `c716f013f6380941fd5fd04168ce4fbff5b61027`; provenance and license are in [NOTICE.md](../NOTICE.md). The wrapper bounds archive inventory/size and binds one linux/arm64 image plus its config digest. Multiple aliases must identify the same pinned root; nested indexes must each select exactly one image. The stock initialization pin is the arm64 image published for Containerization 0.45.0, the dependency of [stock Container 1.4.1](https://github.com/apple/container/blob/1.4.1/Package.resolved). The workload pin is the arm64 manifest within the already-pinned Alpine fixture index `sha256:14358309a308569c32bdc37e2e0e9694be33a9d99e68afb0f5ff33cc1f695dce`, not a fresh resolution of `latest`. The old Engine runner's mutable image references are not imported into the new path. Fresh-machine enhanced image distribution and live qualification of per-lane image loading/guest cleanup remain outstanding; prepared files do not establish a complete runtime closure or live parity.

Import an existing exact enhanced archive without modifying or repacking it:

```console
make bazel-import-guest-image GUEST_ARCHIVE_NAME=enhanced-vminit GUEST_ARCHIVE_PATH=/absolute/path/to/container-vminit-arm64.oci.tar
make bazel-prepare-guest-images OFFLINE=1
```

The source must be an owned canonical single-link regular file, readable by its owner, with no executable/special bits or group/world write permission. Read-only `0444` release archives are accepted unchanged. Retained archives and receipts remain private `0600` files. Import requires the lock's exact `archiveSHA256`, stages a byte-for-byte snapshot on SSD and verifies the OCI closure before publishing a durable receipt; the source is never permission-modified or deleted.

On 19 September 2026 the original enhanced archive was imported and all five locked images verified offline. Its SHA-256 remains `9e18071a3c508d4376bd734fa0a22c61326907edd4ec35a74d84edce17062596`, size 111,155,200 bytes; the retained preparation key is `9059281e10c47b9193f6018fb282057063900c61dd3c2f6115a3bc4d8baf1754`. The source stayed `0444`, the retained copy is `0600`, and no pending publication remained. Focused Bazel invocation `e61fe894-8266-4ff7-ab8b-b68c149e269a` passed 111 preparation tests after the read-only-source regression failed at `589e730e-b35b-4936-8ebe-4b2f46752315`. Independent source review is clean. This removes the local missing-archive prerequisite recorded by earlier fixture histories; it is not enhanced guest execution, quiet timing evidence, fresh-machine distribution or release qualification.

`make bazel-prepare-guest-kernel` uses `Tools/bazel/guest-kernel.lock.json` and the same leased release acquisition/preparation path. It pins the Kata 3.32.0 ARM64 GitHub archive recommended by [stock Container 1.4.1](https://github.com/apple/container/blob/1.4.1/Sources/ContainerPersistence/ContainerSystemConfig.swift), also referenced by the enhanced Container revision embedded in Compose 0.15.1. The archive SHA-256 is `8736c054d9223974735394f822000823baef509e1c33405ec798240fa9b6e4b5`. Only `opt/kata/share/kata-containers/vmlinux-6.18.35-197-debug` is extracted; the full Kata tree is never unpacked or installed. Extraction requires Homebrew Zstandard (`/opt/homebrew/bin/zstd`), has a 60-second deadline, and rejects warnings, wrong length or a missing ARM64 image header. The kernel is retained as non-executable data with a complete digest/mode inventory. `OFFLINE=1` reuses authenticated retained data without a download or re-extraction. Preparation does not install the kernel, register services or boot a VM.

On 17 September 2026, the real kernel archive was acquired, prepared and reused offline with preparation key `ba83aef23824a8f8039847c491bbdbb9848cab95be1b702e33422d6da4115f60` and inventory hash `3007998291daff4475d5b392add946eff4f1dfc4d319aa564ee49bb025055953`. An initial extraction failed because the clean environment omitted Zstandard's location; the explicit extraction PATH now includes it. This is acquisition/preparation evidence, not guest execution or parity.

Focused kernel-preparation validation passed 24 preparation tests and 113 workflow-helper tests. Standard-library trace reports 90% line coverage for `prepare_releases.py` (rounded diagnostic coverage, not SonarQube or overall product coverage). Bazel invocation `c170cbfc-0387-42f1-8bfc-b4d7228865ac` passed both preparation and case-evidence targets. Tests cover cached and SSD-evicted reuse, immutable retained admission, invalid headers, duplicate-length output, extraction warnings and missing/executable data files. These are development-worktree component checks; immutable-head release gates and live runtime qualification remain required.

The enhanced initialization tag `ghcr.io/stephenlclarke/containerization/vminit:7e066a3101bc84fa0f7231daf6a03aa9ef62a567` returned `manifest unknown` during the earlier inspection, and Compose 0.15.1 did not expose the OCI archive among its GitHub assets. The imported archive's hash matches `guestInitImageSha256` in that release's authenticated `stable-release-authority.tar.gz` (asset SHA-256 `1185c113b662c356ff5e37844b6a7559ce41d4003cbad2e97a0e83ee632bb054`). The existing Compose authority verifier passed again before import. Local admission does not make clean-machine acquisition functional. GitHub reports release 0.15.1 as immutable, so the existing Compose asset-backfill path cannot repair that release in place. Preserve its assets; exact-image distribution and enhanced live qualification remain required. No stock-image substitution or reference rebuild is permitted.

### Released guest-case integration

`make bazel-engine-case CAMPAIGN=<id> LANE=apple-stock CASE_FIXTURE=E02-container-lifecycle`, `CASE_FIXTURE=E03-exec-streams`, `CASE_FIXTURE=E05-archive-copy` and `CASE_FIXTURE=E06-network-volume` connect the lifecycle/exec/archive/network-volume contracts to `guest_runtime.py`. Omit `CASE_FIXTURE` for the existing E01 negotiation case. These opt-in guest paths are implemented and component-tested, not live-qualified. The enhanced lane refuses its missing initialization pin before selecting services; passing stock admission never substitutes for enhanced admission.

`network_volume_probe.py` preserves all six E06 observations: alias DNS, network/volume inspection, cross-container volume persistence, read-only bind behavior and tmpfs. Three separately labelled guests share a private durable journal through role-scoped records. A sleeping third guest reads the volume using exec rather than depending on an auto-removed one-shot container; it still proves a distinct consumer reads the first guest's data. Bind input lives exclusively in the owned SSD root, is read before the rejected write and remains unchanged afterward. Tmpfs additionally requires an inspected tmpfs mount, so an ordinary writable directory cannot pass. The whole operation is bounded to 180 seconds. Component tests exercise real resource HTTP but substitute exec results; E03 separately covers exec transport. This is not real filesystem/DNS or live runtime proof.

E06 journals intent before each resource mutation. Network deletion uses the verified returned ID; volume deletion uses its verified name/labels/creation metadata and is never forced. Observed driver and creation metadata are retained unchanged and compared before cleanup; the released Apple adapter's plugin name is not rewritten to `bridge` or presented as driver-field parity. Cleanup removes guests in reverse order before network/volume providers, checks absence and owner-labelled residue, and writes the aggregate removed record only after every resource closes. Existing/foreign/replaced/renamed/reappearing resources, uncertain unobserved creation, live attachments and failed deletions preserve quarantine. Report/apply service recovery refuses an incomplete E06 aggregate marker; automatic interruption reconciliation remains unfinished.

`exec_probe.py` preserves all six E03 observations through the [Docker Engine 1.54 exec API](https://docs.docker.com/reference/api/engine/version/v1.54/): stdout/stderr separation, exit status 7, environment/workdir/root-user selection, TTY output and an exact 4 MiB binary stdin/stdout round trip. A bounded Unix connection concurrently reads and writes so server backpressure cannot deadlock a send-before-read client. It half-closes stdin, decodes non-TTY frames strictly, preserves raw TTY bytes and retains sanitized request timings. A private journal records intent before exec creation, the returned identity before starting, and completion hashes only after matching exec/container identity and final exit status. An uncertain command is never repeated; the existing owned-container cleanup closes any remaining exec resources. Socket tests cover fragmented headers, simultaneous backpressure, malformed frames, invalid identities, interrupted requests and output/deadline limits. These are harness tests, not an executed Docker oracle or live Apple parity proof. E03 retains its original 300-second binary-transfer limit, with ten seconds for each other attachment and a 360-second whole-operation deadline; its idle parent lives for 600 seconds. The released-case outer deadline still includes setup and verified cleanup.

Admission is read-only: verify each retained kernel/archive and immutable receipt, reject pending image publication, require the selected release's exact initialization reference, and include every guest input plus both locks in the case fingerprint. After selecting the private provider, journal each setup command before launch, install the local kernel with `system kernel set --binary`, verify the private default kernel points at the admitted bytes, and load only the admitted initialization/workload archives. Setup uses a 190-second whole-phase bound and 60-second command bounds. No `system start`, recommended-kernel download, registry pull, product compilation or operator-store adoption occurs. Engine and setup clients receive the same explicit private Container app/log roots and selected installation root, rather than inheriting operator configuration. Command logs remain in the private internal journal as bounded 1 MiB snapshots with explicit truncation metadata; setup command durations and per-request timings are retained. A log-retention failure is retried during cleanup and cannot discard its only copy. Bazel's outer timeout is 15 minutes so shorter phase deadlines leave time for cleanup; this does not extend any individual parity operation's bound.

The common 1.53 Engine contract is used for guest operations and checked against each server's advertised range before mutation. E02 and E05 each have a 90-second whole-operation deadline, preserve their five existing observations, and use the admitted workload config digest rather than a mutable tag. Guest deletion and empty-owner checks run before Engine/provider shutdown with a 45-second cleanup bound. If guest removal or setup-process ownership is uncertain, retain the root, Engine/provider and host quarantine; do not restore unrelated services over a potentially live guest or delete its evidence. Report/apply service recovery refuses unresolved guest intent or a setup command without verified process closure. Automatic recovery of an interrupted guest/process is still unfinished and requires explicit reconciliation; this deliberate refusal is not a completed recoverability claim.

Missing provisioning-log retention is recoverable without reconnecting to a live guest. Once recorded guest removal, command closure, client-process absence, root ownership and original-service checks pass, report mode validates a read-only diagnostic plan and returns `ready-to-retain-and-restore`. Apply durably retains the bounded snapshots and their digest/truncation records before any service restoration or scratch deletion. A crash between payload and metadata writes resumes only when the retained partial bytes still match; corrupt complete evidence is never repaired. Snapshot inputs must be canonical singly owned regular files, never symlinks, hardlinks, FIFOs or group/world-writable files. Missing, changed or unreadable diagnostics keep quarantine. The original failed/interrupted case remains unchanged.

The initial released-guest component checkpoint on 17 September passed 188 case/runtime/socket tests and 16 image-preparation/admission tests. Focused standard-library trace reported rounded line coverage of 99% for `guest_runtime.py`, 99% for `host_runtime.py`, 95% for `released_engine.py`, 94% for `recover_runtime.py` and 98% for `prepare_guest_images.py`. The reused OCI validator remains at 54% under the image tests; that existing coverage gap is not hidden or counted as meeting the overall quality bar. Actual read-only stock admission verified the retained kernel inventory and both real image archives without starting services; report-only host recovery remained clear. Full-diff independent review found and resolved a provisioning-log retention defect; transient/permanent write failures and oversized output now have teardown-level regressions. These are scoped development checks, not live three-lane parity, benchmark results, SonarQube or release qualification.

The subsequent recovery/log checkpoint `b26c4a3` passed 194 component tests under exact-head Bazel invocation `f179eb0e-2467-4018-b9f4-569c99353255`. The E03 addition passes 20 focused socket/ownership tests and the 15-test guest adapter suite; standard-library trace reports 98% focused line coverage for `exec_probe.py`. Independent review found and resolved missing Bazel contract export and released-provider identity assumptions. Exec IDs accept complete Docker hexadecimal IDs or released UUIDs; a safe native parent alias must resolve to the exact owned Docker ID/name/labels/image. After stream EOF, terminal-state publication has a separate five-second bounded observation window without restarting commands or retrying HTTP errors. The subsequent exact-head E03 checkpoint `1c56ef5` passed 215 component tests at Bazel invocation `fa1d41dc-cf87-4357-a575-3b2fcf6daaf9`. E06 adds 20 focused resource tests and passes both expanded 17-test guest suites; trace reports 94% focused line coverage for `network_volume_probe.py`. These are component evidence, not real DNS/storage execution, controlled benchmark timings or SonarQube results. All live three-lane qualification and overall quality gates remain open.

GitHub review `4037254342` identified a restore-time definition race. Original service definitions are now re-read and compared with the complete journalled bytes/hash after bootstrap and inspection, including already-registered originals. Matching label/path/program alone is insufficient. Regression tests replace only environment or Mach-service fields, verify restore fails without a completion record, and preserve the changed definition for explicit reconciliation. This is a deterministic recovery check, not new live-runtime evidence.

On 17 September 2026, actual public-registry acquisition verified stock initialization archive SHA-256 `c90243296d1641eb2306a3a2d0c5b150512231156132b3362886c5c4578173f1` (69,089,280 bytes) and Alpine archive `92568fc6275de7c7a894c13d13c47c3f8acf56452c4933a0cb96e821f653a468` (4,136,960 bytes). The offline repeat returned identical receipts. Fifteen focused wrapper tests cover immutable identity, blob corruption, bounded metadata, anonymous transfer, offline reuse and interrupted publication; trace line coverage is 97.9%. The unchanged general-purpose Compose validator measured only 54.3% in these single-image preparation tests; multi-index/artifact/general-validator coverage remains a separate unqualified gap, not an excluded or passing product-quality claim.

GitHub review `4036590993` exposed a further recovery case: an unchanged original service definition is not proof that an old captured process belongs to its current registration. The exemption now verifies live PID/start/program identity and membership in the currently registered service process tree. Stale roots and detached descendants are refused before report/apply mutations and before clearing quarantine after a previously removed scratch root. Forty-one runtime/recovery tests pass, including a replacement launchd PID and the unchanged registered descendant case, with focused trace coverage of 94.5% recovery and 96.7% runtime service handling. The guest preparation checkpoint passed native Bazel invocation `60a61242-fbcc-4a12-a2fe-056908253db2` and all 110 workflow-helper tests; the combined final gate after the recovery correction passed at `eecf1b68-388b-47a4-906e-ffc8d5edfd56`.

### Image-build contract

Status: Docker live proof passed; Apple qualification pending.

For new Apple E04 cases, `recover-runtime --stop-client` admits only a spawn-time captured Engine incarnation after build resources, helper processes and their diagnostics are closed. `--apply --case <exact-case-id>` records intent before one bounded `TERM`; it rejects reused PIDs, changed binaries, same-group or recursive descendants, and uncertain prior signals. It does not escalate, resend a signal, restore providers or remove roots. Once the Engine is absent, bounded final diagnostics are retained privately with integrity/truncation metadata; ordinary service/root recovery requires this retention and resumes partial writes safely. Legacy PID-only evidence never acquires automatic signal authority from a later observation. The specific historical stock case was recovered under explicit operator authorization; its original failed result remains unchanged.

Stock candidate `ca06dd6` then passed all three build observations in `442cfce7-4b44-43b0-b04c-097afb2ea102`: positive build, actual intended failing command and image label. Its output removal was verified, but the overall case failed during residual-image checking because `/images/json` ignored label filters and returned the unrelated base image. The product now applies conjunctions of label predicates (including presence, exact empty values and values containing `=`) and projects original image labels. Regression `594d448b-2d51-416a-a8cb-60b19516931d` fails before the correction; `ec16f7d2-688e-4488-86c6-c63211cc2e95` passes afterward. This is not a retroactive passing live result.

`Tools/bazel/run.sh recover-runtime --build-resources` reports the narrow completed-Apple-build recovery state. Adding `--apply --case <exact-case-id>` reconciles only already-deleted image outputs and the captured private builder, leaving the Engine, service selection, root and quarantine intact. The report may retain journalled read-only inventory diagnostics (`diagnosticsRecorded`); it does not mutate workload resources. Admission revalidates the original candidate/release closure, base image, creation snapshot and every prior helper's stopped-process/diagnostic receipts. An uncertain helper, unrecorded builder, surviving image or changed input blocks cleanup. This does not repair the failed test or waive its image-filter bug. Legacy live Engine records without a captured process incarnation still require separate operator reconciliation. New cases retain that identity immediately after spawn while the original child handle is available; missing capture remains fail-closed.

The published Docker E04 lane passes all three observations, including actual failed-command execution and zero-residue cleanup (`8281d69f-8045-4b52-b816-a4de3cf2745a`, source `7696147`). Setup, operation and cleanup took 15.902431334, 0.688755917 and 3.008522584 seconds respectively. The corrected stock candidate at `378a5b0` also passes all three observations and cleanup (`0acf86ea-3bc4-47e5-8468-fba047e51352`, campaign `candidate-e04-builder-20260918c`): setup 10.207588292, operation 4.016804750 and cleanup 2.824603500 seconds. Its package/smoke invocation is `a30e9695-8948-49f9-8f8a-0b335777c1da`; the package SHA-256 is `e5b6d9c300acd05cc13da2eea0cfbab92c1b263219f9ab3f39a5941950b246d5`. Original services are restored and recovery is clear. These separate functional runs are not quiet paired benchmarks, not a single coherent three-lane campaign and not published-candidate qualification. Enhanced E04 remains blocked by the recorded host access and matching initialization-image admission; no stock substitution is permitted.

The Apple adapter now projects the selected manifest's repository-qualified digest separately from the configuration ID. Named image deletion uses native `ClientImage.delete` with garbage collection disabled; public digest-addressed mutations still fail closed. Private E04 cleanup explicitly journals its `private-native-tag` mode and authenticates the sole native tag, owner label, config ID and repository manifest against independent lookups before removal. This mode requires the exclusive disposable Apple image store: it is not atomic compare-and-delete, not general Docker image-ID deletion parity, and never a fallback after failed ID deletion. Unknown completion, changed manifests sharing a config, foreign/shared references or unreferenced labelled intermediates preserve quarantine. Underlying unused content is confined to the private root and removed only after verified provider shutdown. Docker keeps full-ID cleanup. Component evidence `2c958e33-e199-4db1-ac1d-e9e84c666ee5` passes 431 tests without skipped cases; both profile image-identity/deletion regressions pass.

Completed E04 Docker builds have a cleanup-only recovery path: the default report authenticates the original admission against the current offline release closure, the original Lima PID incarnations, the fixed profile/socket and retained terminal HTTP responses. It reports exact output-image identities without mutation. Explicit apply with the reported case ID repeats admission, deletes only owned image IDs without force/pruning, stops the owned Colima profile, retains diagnostics and delegates root removal to verified closed-VM recovery. It never reruns a build or changes the sealed failed result. A system VZ XPC worker is not adopted or signalled; only the authenticated profile controls its VM. Unknown build completion, replaced/foreign images, changed tools/processes and interrupted shutdown remain quarantined.

The live Docker build at `142e025` completed successfully, but cleanup rejected Docker containerd's local `RepoDigest` (invocation `4b375881-126d-4d38-af1e-1ded0ec11d69`). Admission now permits only the exact owned repository plus the observed content ID, with a matching descriptor; foreign or extra digests still fail. The failing command was never submitted, so this record is not an E04 pass. Recovery retains that distinction. Classic-builder intermediates must also appear in an authenticated terminal response, have the exact case label and descriptor identity, and have no tags, repository digests or container users. They are removed child-first by full ID with individual journal receipts, never by broad pruning. Unmatched residue remains quarantined. Final component evidence passes 424 tests (`9e74606b-a0db-49d9-b96b-d7d97ddb5f83`); standard-library trace with missing-line accounting reports rounded 95% image transaction, 100% completed-build recovery and 94% common runtime recovery coverage. These are focused component metrics, not aggregate product or release qualification.

Explicit recovery of case `0ba0d25b2e360362636022336e785f147b705f1a766d087c229c7cc08715e1f6` then succeeded: owned outputs/intermediates were removed, the original VM stopped, private diagnostics retained and its SSD root removed; the recovery report is clear. The sealed case remains `failed` with its original `unknown` cleanup result. The separate recovery journal establishes later cleanup, not retroactive test success.

The unreleased E04 candidate also corrects failed-build projection: completed Apple builder stdout/stderr is preserved before the provider stream throws; the Docker response converts that terminal error to one JSON `error`/`errorDetail` record after the operation journal records failure. Preflight errors remain request errors, while cancellation and abandonment remain failures rather than successful end-of-stream. Both profile regressions pass. The full affected stock components pass at `b0e96bee-a751-406e-b28c-4e74bcba40e3`, with 34/34 executed lines in the new lazy result adapter. This injected-boundary evidence does not prove that a real builder reached the intended failed `RUN`; live E04 qualification remains outstanding.

`Tools/testing/build_probe.py` implements pure E04 inputs and observations based on the [Docker Engine API 1.53 build contract](https://docs.docker.com/reference/api/engine/version/v1.53/). A deterministic in-memory context retains the original build argument, successful `RUN`, parity label and separate `RUN false` case, replacing a mutable base tag with a digest-qualified reference. Request arguments are URL-encoded, successful/failed output tags are distinct and tied to the complete case identity, and image observation requires a full digest, exact sole tag and owner label. No helper starts a builder, sends HTTP, creates an image or authorizes cleanup by itself.

The bounded JSON-line decoder preserves build errors even if later progress appears, rejects duplicate fields and malformed records, and does not count an HTTP request rejection as a qualified failed `RUN`. Its `failed` flag records only an observed error: the live adapter must still prove the intended command reached the expected failure phase rather than accepting builder startup or infrastructure failure. Eight focused cases cover deterministic metadata, retained Dockerfile assertions, query encoding, injection/mutable-base rejection, late errors, malformed/oversized responses, ownership replacement and foreign tags. All 325 native harness component tests pass (`eddb9bdb-8c8c-486e-99a5-869655c8b190`, 4.197 seconds development elapsed); independent review is clean. This is not a benchmark or live parity result.

`Tools/testing/build_images.py` adds the output-image transaction around those pure helpers. It admits the prepared base's exact configuration ID and repository digest, checks both output names before journalling their context hashes, and records each submission before the caller sends it. A complete validated HTTP response is retained privately before image observation. A missing terminal response, including an absent tag after interruption, cannot authorize cleanup. Recovery reopens the durable journal, checks runtime/context identity and exact image ownership, and deletes only the full image ID with `force=false&noprune=true`. Foreign tags, repository digests, base-image aliases, ID replacement, tag loss, reappearing outputs and remaining labelled intermediates stop cleanup. Per-image closure also prevents later submission when overall cleanup has failed. The component never submits builds or prunes caches; its `response` method does not prove the intended failed-`RUN` phase.

Nineteen additional tests exercise this transaction through a disposable Unix HTTP endpoint and reopened SQLite journals. They include lost deletion responses, interrupted result inspection, unknown completion, duplicate submission, partial cleanup, conflicting identities, input mismatch, shared images and intermediate residue. All 344 native harness tests pass (`e5462fc4-1ec8-4883-a4ce-3d0d2067ce91`, 4.591 seconds development elapsed), and final independent review is clean. These are component tests, not real runtime image builds, quiet benchmarks or measured coverage. The previous pure-helper evidence remains historical rather than proof of this newer transaction.

E04 is now registered as an opt-in native harness fixture, not a qualified compatibility claim. `make bazel-prepare-builders` admits the published stock/enhanced builder images in `Tools/bazel/builder-images.lock.json`; `OFFLINE=1` verifies retained archives. No reference runtime or builder is rebuilt. Stock Container 1.4.1 selects builder 0.13.1; the Container commit embedded in Compose 0.15.1 selects builder `5373d9b4363c`. The private runtime's supported `build.image` setting selects the exact local tag loaded from the verified linux-arm64 archive: stock lookup does not treat its descriptor digest as a local alias. The imported native index is authenticated through its SHA-256 content, pinned leaf manifest and pinned configuration, then bound into the builder snapshot. Only this disposable configuration is changed; no operator configuration is modified.

`build_fixture.py` sends the same bounded binary context in all lanes and records monotonic per-request timing. The negative Dockerfile now prints a case-unique execution marker before `false`; passing the negative assertion requires actual guest output plus exit code 1, not merely a command listing, HTTP error or unavailable builder. `build_runtime.py` journals the private builder before startup, refuses pre-existing containers, verifies image/configuration/labels/export mount identity, and stops/deletes only that worker after image-output reconciliation. Startup uses the same captured host DNS arguments as the product build command; resolver changes fail before submission instead of silently recreating and adopting another builder. A lost deletion response is reconciled by observed absence without resubmitting deletion. Uncertain build/start completion, foreign resources and unresolved residue preserve the quarantined runtime. Cache and intermediate data remain confined to the owned provider/VM root and are removed only after verified shutdown. Automatic reconciliation of every interrupted live-build phase is not yet complete.

All 409 native harness component tests pass at `0d4d83d5-afc2-424f-a2a1-d9e23058c84b`; independent final source review is clean. Focused standard-library trace reports line coverage of 100% for `build_fixture.py` and `build_runtime.py`, and rounded 99% for `build_probe.py` and 97% for the guest adapter. These are component results, not live parity, aggregate product coverage or quiet benchmarks. The complete unchanged 19-fixture matrix and stable-release gates remain required.

The first live Docker attempt (`b54b87db-8128-4cb3-92be-c90f35080029`, source `bf2ef0879fa0c98f2626dd0baf238ff333953cdd`) rejected base admission before submitting a build: Docker reports the pinned official Hub image as `alpine@sha256:...`, not `docker.io/library/alpine@sha256:...`. Both identify the same repository/digest. Admission now accepts those two exact spellings while still rejecting other registries, namespaces or digests; no parity observation is rewritten. The regression fails at `689e3d40-d899-4bb3-8d82-b9b1308c823b`, and all 411 component tests pass after correction at `74eaae0e-2b56-4fee-ba85-7530fac145fa`. The live failure's cleanup passed with zero owned residue and clear host recovery; it remains a failed case, not successful build evidence.

### Native F01 fault and race adapter

`CASE_FIXTURE=F01-fault-recovery` reuses the released-runtime admission, owned guest transaction and evidence store. Four simultaneous start requests target one signal-aware container; a separate, never-started `true` container retains the original four-way removal race. Readiness precedes TERM so exit 42 must be observed by both wait and inspection. The bounded event request must terminate without matching events; the missing-backend probe uses a nonexistent path inside the disposable case root rather than a shared `/tmp` name. Each resource has independent creation/deletion receipts and cleanup; provider recovery requires the composite closure receipt.

Concurrent requests have an absolute 30-second socket deadline in addition to inactivity limits. Expiry shuts down and closes only that request's captured socket, including an expiry during connection setup or a close-delimited response, and every worker is joined before resource cleanup. Real socket regressions exercise trickling headers, fixed-length bodies and close-delimited bodies. Status/error bodies and monotonic request/phase timings remain in private evidence; no runtime is rebuilt, no failed mutation is retried and no fixture assertion is skipped. Component proof alone does not qualify the live three-lane F01 contract.

The 481-test component gate passes at `1050a77f-a019-416a-bfca-9c123d6464d4`. A separate 64-test missing-line trace reports rounded 100% F01 adapter, 98% HTTP probe and 97% guest-runtime coverage; the retained record is `component-traces/f01-native-20260918` under the workflow evidence root. These are focused harness metrics, not overall product coverage or a live parity result.

The published Docker F01 case passes all five observations and cleanup at `17d5908b-93f2-49db-9a1c-6db8f2778157`, harness source `103dc2a`. Setup took 256.012880625 seconds, operation 1.794038333 seconds and cleanup 32.444049584 seconds. The unusually slow VM startup included two 120-second readiness phases; these raw functional timings are retained, not normalized away or treated as a quiet benchmark. The first stock candidate F01 (`3dfb2d73-bcfc-484d-a2bc-5cbf852994f8`) failed after wait/inspection; owned cleanup passed. A separate API defect leaked native `stopped` instead of Docker `exited`; the adapter now projects the Docker spelling without changing native state, IDs or exit codes. Regression `ba72830e-7a2d-432d-ac3f-80f8dc849a10` reproduces that defect, and the corrected stock API suite passes at `16b81bc5-2957-46d1-a557-0b656aaecadf`.

That spelling correction was insufficient: diagnostic run `a63ac21f-4b33-4cfd-8402-16d6b2eea5d9` captured the same container still reporting `running`/0 after process wait. Apple's exit monitor updates native state after process completion; the runtime must wait for that authoritative stopped state before returning the recorded exit code. A red/green lifecycle regression (`404f3135-f2fe-43e3-b25e-482b32085398` / `e6486f0c-f3c2-4a61-a00a-0b54a5c77c4f`) proves this ordering and a deadline regression rejects a native state that never converges. The private journal retains only selected identity/image/state fields before each F01 assertion. These component results do not rewrite failed evidence. Subsequent stock live qualification is recorded below; enhanced live qualification remains open.

Independent review identified a removal race in that barrier: automatic deletion could erase the cached exit before reinspection. The waiter now retains its observed exit and snapshot identity, discards it on an observed replacement, and preserves the real exit when removal wins inspection. Its regression fails at `582ea484-c9db-493b-ae74-471bcd8dc6e6` and passes at `efa5188b-6311-4bc6-839b-d880dfc9c9f5`. The full stock runtime component passes at `daa193d5-051b-41eb-b727-971e37cbc86c`; the enhanced component passes with coverage at `b0f37825-c684-496e-bd44-a8c5b5baa83f`. The changed wait and finish helper (source lines 639-702 at `a16379b`) measure 56/57 executed lines (98.2456%), not overall product coverage. The earlier 63/64 count included adjacent unchanged helper lines.

Stock F01 now passes all five assertions and zero-residue cleanup at `d996ac7c-d3d4-494e-a26a-793dfa800c89`, source `a16379ba812322423dc623d05e706bc6080d0e69`, campaign `candidate-f01-native-20260918d`. The package and install-layout smoke test passed at `676e2cd7-8af7-4c50-b9e6-5ce2b2f370bf`; its archive SHA-256 is `2c6d6d477e5167234d623427aad5f284640de3a60c61efb986ca4a007eebde3b`. Setup took 5.335279500 seconds, operation 2.733305750 seconds, and cleanup 1.296612333 seconds. Recovery reports clear. These are retained candidate functional timings, not a paired quiet benchmark, published stable release, or enhanced-lane qualification. Enhanced live input/access blockers remain open.

### Lifecycle component

The archive observer compares the five E05 fields rather than imposing an additional exact-inventory contract. Unrelated safe members and an omitted directory header are allowed; missing or duplicate expected file paths and unsafe absolute/traversal paths are rejected. Equivalent `./` and repeated-slash paths match the same expected file and cannot conceal a duplicate. Returned data is still bounded and never extracted onto the host. Recovery readiness likewise now checks captured original-process closure before any service mutation, and rejects an unregistered or identity-changed CI listener. An unchanged registered original is permitted, but an active worker or guest is not. These corrections address GitHub review comments `4036054539`, `4036054555` and `4036282172`; all 65 affected tests pass. The corrected native Bazel checkpoint is `ba73c4ba-bf70-4885-82a0-51d60be18876`; focused trace coverage is 100% archive, 94.5% recovery, 94.2% guest ownership and 98.6% lifecycle. These component metrics are not the product-wide quality gate.

`Tools/testing/lifecycle_probe.py` retains all five E02 contract observations: created state, restarted state, wait status, inspected exit status and repeated-removal behavior. It uses direct Engine HTTP and the same journalled ownership boundary as archive copying. The guest command is bound into ownership intent; create, start and ordinary removal are separately observable operations. Startup logs carry a persisted generation counter so a previous process's log cannot falsely prove the restarted shell has installed its TERM handler. Log framing is validated, readiness is bounded, and request failures are not retried. Actual exit codes and states are preserved for contract comparison rather than normalized into expected values.

The caller must still admit the full guest/runtime closure, hold the runtime lease, enforce whole-phase deadlines and invoke verified cleanup after success or failure. This component does not pull images, build products, start services or qualify a live lane. Fifteen lifecycle tests plus the sixteen guest-ownership tests passed; focused trace coverage measured 98.6% for `lifecycle_probe.py` and 94.2% for the expanded `guest_fixture.py`. The native Bazel harness passed at invocation `0b7cbc92-4af6-4ee6-ab66-a85b8f885a61`. Independent review caught a client deadline shorter than the requested restart grace: restart now allows twenty seconds for its ten-second shutdown grace plus startup, still bounded by the caller's whole-operation deadline, with a transport-forwarding regression. These are test-server results, not Docker/Apple runtime parity or product-wide coverage.

### First released-service integration target

Development builds use the same isolated lifecycle through an explicit, non-release boundary: `make bazel-prepare-candidate CANDIDATE_INVOCATION=<build-id>` restores the exact retained native archive, verifies successful clean unchanged source and matching dependency-lock identity, extracts on SSD, and publishes the verified executable tree internally through the existing resumable preparation journal. `make bazel-engine-case CAMPAIGN=<id> LANE=apple-stock CANDIDATE_INVOCATION=<build-id> CASE_FIXTURE=E02-container-lifecycle` then performs read-only candidate admission without compilation or repair. The runtime profile must match the selected lane. The released native provider, kernel/init/workload and runtime lock/recovery contract are unchanged. Candidate metadata/product digests and the complete preparation inventory are rechecked before and after execution; missing, changed or pending inputs fail closed.

Candidate evidence is labelled `local-candidate-integration-only` in output, JUnit and the sealed admission manifest. The candidate identity and scope also change the comparison's release-set fingerprint, so it cannot be mixed with published-release records even in the same named campaign. A successful local candidate case is implementation feedback, not release or full-parity qualification. Final parity continues to use downloaded published binaries. Component tests cover immutable reuse after all SSD scratch disappears, rejected dirty/changed/failed/corrupt inputs, profile mismatch, unfinished publication, tampering and separation of candidate/release comparison identities.

`make bazel-engine-case CAMPAIGN=<explicit-id> LANE=apple-stock` selects the manual, local, uncached `released_engine_negotiation` target. The enhanced alternative is `LANE=container-compose`. Both use only preprepared release payloads; missing or changed assets fail admission without downloads, extraction or builds. The first adapter explicitly supports the reviewed devcontainer 1.0.1 command interface and does not pass newer service flags to an older binary. The default remains E01 metadata/protocol integration, not a full guest-runtime campaign or a substitute for the Docker oracle; explicit E02/E03/E05/E06 selections use the guest adapter described above. Docker-lane admission, the remaining adapters and live qualification of the complete 19-fixture inventory are unfinished.

The enrolled SSD must enforce file ownership: launchd rejects private service plists on an ownership-disabled volume even when they appear user-owned to the caller. Check with `diskutil info /Volumes/SSD`; an administrator can enable it with `sudo diskutil enableOwnership /Volumes/SSD` after verifying the enrolled volume UUID. The case checks this before suspending services, binds the volume UUID/mount/ownership into its fingerprint, and revalidates it at cleanup. It never changes the mount policy or asks for elevated privileges itself.

The case binds the complete release lock, extracted inventories, actual command versions, OS/architecture, harness and expected observations. Setup and cleanup have separate timings from the conformance operation. The process receives an isolated SSD home/temp directory and a minimal environment. Whole-phase deadlines supplement socket timeouts. Bazel SIGTERM enters cleanup; an interrupted process creation is recorded as uncertain, never reported as no child. The internally retained case seal binds both the result and the exact name/digest inventory of attached diagnostics and ownership records, so missing or changed attachments cannot be reused as a passing result.

Stock Apple Container 1.4.1 connects to the fixed per-user `com.apple.container.apiserver` service; changing `HOME` does not isolate that XPC service. The adapter now selects that service transactionally through `runtime_services.py`: reject active CI workers/container commands/guest processes, snapshot explicitly authorised family jobs, suspend them, prove outgoing process closure, and register the selected prepared API with private SSD application/log/temp roots. The service definition follows stock `SystemStart`'s launchd contract without kernel installation or image pulls. Readiness and cleanup require the exact selected executable and running PID; those fields are attached to the case seal without incorporating transient PIDs into the reusable case identity. No operator provider keys or state are copied or changed. This metadata adapter does not establish the complete guest/runtime closure.

Each protocol request retains its method, route, status or safe exception class, and monotonic duration in `requests.json`, bound into the case seal. Request/response bodies and exception text are not included. This identifies the exact failing endpoint without rerunning a failed case or exposing operator credentials.

Cleanup captures diagnostics even when stopping the owned process fails. `process-cleanup.json` explicitly records whether termination was verified; when false, `engine.log` is a partial snapshot, not a claim that the process stopped. Quarantine remains in place. Engine log retention reads at most 1 MiB plus one truncation-detection byte; `engine-log.json` binds the retained byte count, SHA-256 and truncation flag. Excess output cannot exceed the case artifact limit or force an unbounded read. The Bazel test wrapper sets `BAZEL_TEST=1` itself, so host opt-in checks and declared engine runfiles do not depend on an inherited marker. Negative invocation `3af531b2-2a76-439f-ba53-e766ee0a94e1` verified that both explicit service integration tests fail before their Keychain operations when opt-in is zero; it is deliberate rejection evidence, not a passing integration result.

The adapter acquires the existing per-user Compose runtime lock. Its established `/private/tmp/container-compose-runtime-<uid>.lock` inode remains the shared IPC coordination point; payloads and disposable work stay on SSD. Before launching a service, the adapter writes an internal `runtime-admission.json` ownership marker. It clears that marker only after verified process cleanup, evidence retention, input revalidation and owned-root removal. A surviving descendant, uncertain spawn, hard-killed worker or other unverified cleanup leaves the marker and blocks all later admissions through the new adapter, even with a different campaign ID. Do not delete the marker or invent a fresh campaign to bypass it.

Automatic hard-kill/resource reconciliation is not yet implemented. Legacy Compose/runtime entry points share the lock but do **not** enforce the new quarantine marker: while it exists, keep those entry points suspended too until the owned process/resources are reconciled. Family-wide guard integration is required before unattended live cutover. An ordinary terminal result can be reopened unchanged within its original campaign; failed results cannot be overwritten by a retry.

`service_switch.py` snapshots explicitly authorised definitions before removal, journals every mutation, refuses unknown workloads/foreign replacements, verifies the loaded program and unchanged plist after bootstrap, and restores the original unchanged definitions after partial removal or interrupted registration. Consumers/listeners stop before providers; providers restore before consumers. Process closure is checked before selecting a new provider and before restarting originals. `service_journal.py` supplies an append-only, transactional, owner-bound private SQLite journal with corruption checks. It retains original definitions and bounded API startup-log snapshots in mode-0600 files inside mode-0700 internal storage. Original service environment bytes and API logs can contain credentials: they are never attached to public Bazel evidence; only the journal's hash/count receipt is attached. Original service restoration is attempted even if public diagnostic retention fails after verified engine termination. Root removal and quarantine clearance still require all cleanup and retention to succeed.

Because launchd removal can finish before its processes exit, closure permits a bounded 15-second wait only for disappearing owned processes/registrations. Changed ownership, active unrelated commands and CI workers are not retried. Shared interpreters are tracked by the captured job/descendant PID and start identity, not executable-name equality; `exec` or process-group changes do not end ownership. Homebrew's explicitly registered `sh.brew.container` root process is admitted for suspension only when its definition is exactly `container system start` and the captured PID/start/program still match. No unrelated container command or Actions worker receives that exception.

Simulated-service tests cover successful restoration, unknown workloads, changed/foreign definitions, interrupted removal/registration, a fresh recovery object, parent-driven plugin removal, replacement during bootstrap, outgoing-process survivors, private bounded logs and diagnostics failures. A real subprocess exits mid-SQLite transaction to verify crash rollback and reopen without touching host services. The persistence/switch primitives have focused standard-library trace evidence above 90%; this is diagnostic coverage, not an aggregate product or SonarQube result. Live qualification remains outstanding; do not equate these fault tests with a passing runtime lane.

### Operator service recovery

Run `make bazel-recover-runtime` to inspect the current quarantine under the shared runtime lease. This command does not start Bazel, build a product, change services or execute a parity operation. It verifies the admitted case/ownership artifact, private journal, enrolled SSD and remaining process inventory, plus original service definitions for Apple cases. A clear host returns `clear`; a reconcilable journal returns `ready-to-restore`, `ready-to-retain-and-restore`, `ready-to-remove-stopped-docker` or `ready-to-clear` and its exact case ID. Apply only that identified case with `make bazel-recover-runtime-apply CASE_ID=<reported-case-id>`.

Apply removes only journal-owned selected services, waits for process closure, restores unchanged original registrations, retains bounded native logs privately, and writes a durable cleanup authorization before removing the marked case directory. The authorization binds its device, inode and macOS creation time: a crash during recursive removal can resume even if `owner.json` was already removed, but a replacement directory cannot inherit that authorization. A crash after complete directory removal can clear the guard only after original service registrations and process closure are checked again. Completed case results and seals are never changed; an unfinished case remains unfinished rather than gaining invented phase timings or a passing result.

The command deliberately refuses a live recorded Engine PID/process group, an ambiguous spawn intent without a recorded handle, an unknown client, active CI workers/guest workloads, changed service definitions, missing/corrupt evidence, or older journals lacking the new runtime context. Engine spawn intent is now retained before launch so a worker dying before handle capture cannot be mistaken for a case that never launched a client. These conditions retain quarantine and require explicit process/evidence reconciliation; automatic orphan termination and family-wide guard integration remain unfinished. Original registered service roots can be recognised during resumed cleanup, but their active worker/guest descendants are never exempted. No provider keys are edited and no broad service-stop operation is used.

Focused development evidence: Bazel invocation `9100f660-7220-457e-bc28-e0e92b46fba3` passed the recovery/component target and reused the unchanged release-preparation target. The component suite contains 121 tests; standard-library trace measured 94.4% line coverage for `recover_runtime.py`, 95.5% for `released_engine.py` and 96.6% for `runtime_services.py`. The 88 workflow-helper tests, ShellCheck and report-only command also passed. Independent review found and prompted fixes for partial root deletion and active-worker exemptions, then reported no further actionable source findings. These are focused development results, not overall product/Sonar coverage, actual crash-recovery qualification on live services, or a stable-release gate.

The subsequent live service-recovery drill at `b32c05a009d4738bc91e09ee531a7b7aa1a48cc5` deliberately exited its worker with status 19 after selected-service startup and before any Engine spawn. Campaign `operator-recovery-controlled-20260917a` retained interrupted diagnostic case `6c247d221e8889a40c1c6482dcc7f964e19fa411504f102f9a70fcf991372723`. The operator report returned `ready-to-restore`; exact-case apply returned `restored`. Independent post-recovery checks verified all eight original definitions/registrations, absent scratch and guard, and the unchanged case seal `dd000f02aebb9bda6a47003da8f8e7a54ca35b1d2026375cfb6b12f54d35189b`. The diagnostic source is retained in case evidence and the recovery receipt remains private. This proves that concrete service-restoration path, not orphan termination, Engine behavior, guest workloads, full fault coverage or parity.

### Full harness cutover

The existing inventory contains 19 fixtures, each required in all three lanes: E01-E06 Engine semantics, D01-D07 devcontainer configuration/lifecycle/features, C01-C04 Compose projection, F01 failure recovery and V01 real VS Code. None may disappear because an adapter is incomplete. The release result must enumerate every expected fixture and assertion and reject omissions, skips, duplicate results or retry-only passes.

Before removing old entry points:

1. Connect the complete verified release/runtime closure, including guest images, Docker VM/oracle, official CLI and VS Code pins. Download GitHub releases; do not invoke SwiftPM or another product build in a parity case.
2. Add strict preparation and one-case adapters for all 19 fixtures, porting useful assertions without retaining lane-wide rebuilds or destructive evidence resets.
3. Connect the shared runtime lease and marker-protected SSD roots; journal owned resources before creating them. Recover an interrupted case only after confirming worker identity and reconciling its owned resources.
4. Retain case evidence internally before cleaning SSD scratch. Generate JSON, JUnit and the human comparison matrix from those same records.
5. Exercise cancellation, deadlines, crash-before/after-seal, corrupt receipts, changed artifacts, cleanup failures and repeated no-change unit requests. Prove completed cases are not unnecessarily re-executed and failed attempts remain visible.
6. Run exact-inventory differential parity against real Docker, stock Apple Container and the enhanced stack. Run at least five quiet paired timing samples; preserve timeout and 10x regressions. Retire the old harness only when the replacement meets these gates.

The present work does not change a support claim, lower the 90% coverage target, qualify a stable release or replace protected CI checks.

## Focused implementation evidence, 17 September 2026

Bazel invocation `f28f40cb-ef90-4bd2-815d-0613ca69ea52` passed both harness targets: 26 case/socket/JUnit tests and 13 release-preparation tests, with zero outer-suite failures, errors or skips. The helper suite passed 84 tests. These are development-worktree results, not immutable-head release qualification. Focused standard-library trace coverage measured 90% for `prepare_releases.py`; it is diagnostic evidence, not a SonarQube result or a substitute for integrating coverage into the new quality gate.

All five real locked assets prepared offline and then reused with identical inventory hashes. Direct version checks reported devcontainer 1.0.1, stock Apple Container 1.4.1, container-compose 0.15.1 and Docker Compose v5.3.1. The enhanced Container shipped inside the Compose 0.15.1 release reports `homebrew-main-352-780a86b995ac`, Container commit `780a86b` and Containerization commit `7e066a3101bc84fa0f7231daf6a03aa9ef62a567`; its embedded runtime revision is not the enclosing Compose release commit. Live admission must bind those actual component identities. No services were started and these version checks are not runtime parity evidence.

### First live attempt and diagnostic outcome

Follow-up development-worktree validation passed 53 case/socket/process/JUnit tests and 14 release-preparation tests (Bazel invocation `0ddc3afb-a7ee-4d62-8ed6-aebc2d22f8f4`; preparation reused its unchanged passing result). The 87 workflow-helper tests and focused core Swift target passed, including SSD symlink rejection and the corrected legacy host-test opt-in (core invocation `7614812a-ec74-4a5a-b7ea-c5b484f5dc5c`). These are scoped implementation checks, not full product coverage or parity. Independent complete-source review found no additional actionable findings; exact-head hosted quality and broader cutover gates remain required.

The first released E01 attempt (`released-e01-20260917a`, Bazel invocation `592dbc57-f4a2-4930-b76e-011e514c1164`) timed out after 5.003 seconds in the operation phase; setup took 0.531 seconds and verified cleanup took 0.254 seconds. Its immutable failure remains retained and is not stock parity evidence. Diagnosis found that the fixed API service pointed to the existing Homebrew enhanced runtime, not the prepared stock release; launchd reported exit 1 and repeated provider-binding/key mismatch errors. A bounded direct `system version` diagnostic also timed out after 10 seconds. This motivated the exact-service admission check above. No provider keys or existing state were removed, no service was replaced, and neither a new campaign nor a longer timeout is used to hide this failure.

The first controlled service transaction at `e929062ff5756ed0fb3c8e7563b047e1b34335c8` (campaign `released-e01-controlled-20260917a`, invocation `19ea20bd-8178-450c-97fb-dd301fb1e452`) stopped before selected-service registration or any parity operation. Immediate closure checks observed asynchronous shutdown and retained quarantine. Journal-led recovery subsequently proved the recorded processes had exited, restored all eight original registrations, verified every original definition hash was unchanged, removed only that owned SSD root and cleared its guard. The failed case remains unchanged; a separate private journal record proves recovery. This led to the bounded shutdown wait above, not a retry or a waiver of the failed case. The automatic operator recovery command remains unfinished.

The next committed adapter (`5baa6a043dbfe42c318f81727e77a7eec9e47fab`, campaign `released-e01-controlled-20260917b`, invocation `edb8a7dc-eb5a-4770-8a68-a58045b93caf`) reached selected-service registration, but launchd rejected its SSD plist for bad ownership/permissions. Setup took 0.750 seconds, no parity operation ran, and automatic original-service restoration/cleanup passed in 0.418 seconds. The enrolled APFS volume had ownership disabled; it was enabled with existing administrator authorization, and the preflight above now catches that prerequisite before disruption. This immutable setup failure is not a product parity result.

With ownership enabled, `3d9e076fd0ff1561432c5ee01be611c57b96f2d7` registered the selected job but timed out awaiting startup (campaign `released-e01-controlled-20260917c`, invocation `3c1b7d3d-c85a-4784-b516-a589274f5d6f`); automatic cleanup passed. An isolated disposable launchd probe established that SSD `StandardOutPath`/`StandardErrorPath` caused `EX_CONFIG` (78), even with precreated files or a Bash process, while the same trivial executable without redirection exited successfully. The adapter now mirrors stock `SystemStart`: omit launchd stdio redirection and use `CONTAINER_LOG_ROOT` for service-owned native log files. Those bounded API/core-image/machine log snapshots remain private journal entries. Every diagnostic probe job/root was removed; no existing service was modified by those probes.

The subsequent run at `3dc902bdf3257c4fcbc928b86b365a608a86d401` (campaign `released-e01-controlled-20260917d`, invocation `6d8a302a-9673-465b-947d-c42a880cb35b`) passed `/_ping` but timed out on `/version` after 5.002 seconds. Automatic cleanup passed in 0.598 seconds. No native API log was created; the recorded launchd PID appeared in unified logs as `xpcproxy`, so registration plus launchd's `running` state was insufficient startup evidence. Admission now also verifies that the live PID's executable is the selected released API binary. This rejects a launch intermediary, another binary, or an exited process; it does not claim application readiness. The focused Bazel harness passed (`fa224434-7942-4a9c-ad14-35c33b57e227`) and an independent source review found no actionable issue in this readiness correction. The failed case remains immutable; stock E01 is not yet qualified.

At `474d74f`, campaign `released-e01-controlled-20260917e` (invocation `cf8c744e-b064-4f40-bbb1-b0b718d00360`) verified the actual executable after 5.365 seconds of setup, then timed out on the version operation after 5.003 seconds; cleanup passed in 0.585 seconds. A separate bounded, private-journalled startup sample located the selected executable in `dyld` opening its on-disk binary, before API initialization. An isolated launchd `--help` probe through Bash also failed to finish within ten seconds. Both diagnostics removed their registrations and disposable roots, and the API diagnostic restored the original services. These observations locate a host launch/storage boundary but do not yet prove its cause. A read-only permissions check confirmed Bash has Full Disk Access; UI inspection could not check for a pending approval because the Mac was locked. Unlocking the interactive session is the next diagnostic step, not a reason to increase or waive the parity timeout. No new stable release is qualified.

### Retained executable and archive-probe checkpoint

At signed commit `b8a48ae7ea0b4e2feea145cba66dfe754fd73044`, campaign `released-e01-retained-20260917a` (Bazel invocation `0c08308e-99e3-47e3-9ad5-8e1c4c38dfde`) selected the verified internal stock API tree. Setup took 5.936 seconds; ping passed but `/version` timed out after 5.001 seconds. Cleanup passed in 0.537 seconds, the child was verified stopped, original services were restored and the recovery report was clear. No native API logs were created. The immutable failure is case `c77f66e0ee1cee63b9360f4541eba66389dc05e6d6f7996389e34e2d854d7b2d`. Relocation did not resolve startup; further equivalent attempts are paused pending new host evidence. It is not parity, a benchmark or release qualification.

The retained-publication change passed both focused Bazel targets (`11b33ecd-fc09-4a90-94a7-bec6113ca36a`) and independent complete-diff review. Its 21 preparation tests measured 90.0% trace line coverage. The subsequent archive-probe component checkpoint passed both targets (`7e2f95c8-faa0-4d47-8246-dc147f8dfadc`); 11 focused archive tests measured 100% trace line coverage for that probe. These scoped development measurements are not overall product coverage or SonarQube results. Guest-backed E05 and the complete three-lane matrix remain required.

The guest ownership integration passed both focused targets (`2aab82e3-97d4-4df5-91c6-211184aca94a`). Sixteen dedicated socket/recovery tests measured 94.9% trace line coverage for `guest_fixture.py`, including lost create/delete responses, uncertain in-flight creation, renamed/foreign/reappearing resources, failed starts, leftover resources, endpoint changes and journal-write failure before creation. This evidence qualifies the component boundary only; it does not clear the host startup failure or establish live parity.

After the operator unlocked the Mac, stock campaign `released-e01-unlocked-20260917a` at `975423cb39f47c415e9cdd1791aaecf57dad4964` still timed out on `/version` (invocation `579defe3-6db7-4b12-b5a8-24942c8272cc`). Setup took 0.874 seconds, operation 5.002 seconds and verified cleanup 0.625 seconds. A separately retained startup diagnostic (`unlocked-api-stack-20260917a`, case `1d618fc5ffe7fccf1deab9d5a941fccca00b8f28a13a5366562c34dfab34883b`) sampled the selected stock API inside `ServiceLogger.bootstrap` / `FileLogHandler.init`, waiting in Foundation file creation and `__open`. Unlike the earlier loader sample, application startup had begun. The native SSD log remained absent. The private journal retains the stack; the diagnostic source and failed result are sealed in case evidence, with successful cleanup and no remaining guard. Read-only/UI checks confirmed Bash and the app have Full Disk Access, but the stock API has no separate grant. A narrowly identified access test has been proposed to the operator; neither its approval nor its effectiveness is assumed. No permission change was made by these diagnostics.

The subsequent operator-approved Full Disk Access grant for the exact retained stock API executable resolved that first logging stall: campaign `released-e01-api-access-20260917a` (invocation `55235b65-b10c-4f01-8689-170989039f5e`) produced its native SSD API log and initialized plugins/network services. Setup took 0.851 seconds, `/version` still timed out after 5.002 seconds, and cleanup passed in 0.645 seconds. A separate retained diagnostic, `plugin-request-stack-20260917a`, sampled the owned processes after the request timed out. The network helper, not the API, was then waiting inside `ServiceLogger.bootstrap` / `FileLogHandler.init` / `__open`. Original services were restored and cleanup passed. Separately launched helpers do not inherit the API executable's grant; additional helper grants require explicit operator approval because Full Disk Access extends beyond the test folder. These startup diagnostics are neither parity passes nor quiet-machine benchmarks; the live gate remains open.

### API readiness and interrupted startup

A selected launchd registration, PID and executable prove process identity, not API readiness. New campaigns first run the selected released `container list --all --format json` once against the empty private provider root. The command must finish within ten seconds with exit zero and a complete, untruncated empty JSON inventory. Service identity is checked before and after the command. The durable `service-ready` receipt is written only after this RPC check; no fixture mutation or Engine startup precedes it. This admission probe is not retried and does not turn a failed conformance request into startup polling.

Probe intent, PID, verified process-group stop and bounded output remain in the private service journal. An interrupted spawn or missing intent-bound stop receipt preserves quarantine and prevents service restoration or root deletion. Recovery can finish retaining a stopped probe's diagnostics without running the probe again; it preserves the original failed case. Historical journals without this additional probe remain readable but do not acquire new readiness evidence. The regression first failed against PID-only admission (`ab610f5e-ad11-4718-9f0b-34cac59e7dbd`); the corrected native harness passes all 374 component tests (`c031ffce-d20a-4507-95b6-01aa2a6eeea8`). Live startup and parity qualification remain separate.

At `b2d43447d0ad0211453035206cdca532b41930b7`, released stock E01 passes the new readiness check and all protocol observations (`6afca16c-2a9b-4b41-ad10-1aa077aae463`). The enhanced released runtime fails setup before any operation (`40a6cebb-7c77-4e7d-98ca-eb49e6976491`), with successful cleanup and clear recovery. A single additional diagnostic run (`13a44c38-c268-4c1c-a30d-9154ab58b990`) samples the exact selected API executable blocked in `ServiceLogger.bootstrap` / `FileLogHandler.init` / file creation / `open`, before any API log exists. This locates the startup boundary; it does not establish permissions as the sole cause. Broader macOS access remains subject to operator approval. These are startup diagnostics, not quiet benchmarks or full parity. The retained private sample SHA-256 is `3832671688442f1a7c9dfb524b59016ae6816d9ee6cafc4cd2e93522e49b7eb0`.

The live failure also exposed a reporting distinction: Python's subprocess timeout was classified as a generic failed setup. New readiness attempts translate that specific deadline into a sanitized `TimeoutError`, so public evidence records `timeout` without exporting subprocess arguments/output. Historical results stay immutable. The full case-store regression fails before the correction (`a983318a-bead-4e32-8f79-d516a49c7b31`); all 375 component tests then pass (`4af4ff48-1ae2-44c7-b858-1662dc3c0514`).

### Approved helpers and first stock protocol pass

After the operator manually approved the exact retained stock API, core-images, network-vmnet, machine-apiserver and runtime-linux executables, E01 passed all five observations at `f45820b89c00a839515ee3fe3ae13c260c0c86bd`: campaign `released-e01-helpers-approved-20260917a`, invocation `b15eab82-69dc-4646-8a24-e6a58be9c212`. Setup was 0.954 seconds, operation 0.281 seconds and verified cleanup 0.740 seconds. No additional prompt appeared and recovery reported `clear`. This establishes the narrow stock protocol check, not unattended release/update recovery or full parity.

E02 then provisioned its kernel and images but failed before guest creation: `GET /v1.54/images/sha256:2c15e55df5d63efb31b629a557df305130612a16feb029c93447e54dda2c4189/json` returned 404. Campaign `released-e02-helpers-approved-20260917a`, invocation `c2109dda-a3ac-4481-a568-fc4347157c0d`, retained setup 5.098 seconds, operation 0.036 seconds and successful cleanup 1.229 seconds; recovery was clear. Both attempts remain immutable. These durations are functional diagnostics, not quiet-machine comparative benchmarks.

The source adapter previously exposed Apple's index identifier as a Docker image ID. The correction resolves the selected OCI manifest's original config digest through stock `ClientImage.manifest`, groups tags by that config ID, and keeps repository-qualified digest lookup separate from tag matching. Do not hash re-encoded config JSON. The draft now creates containers through the native API with captured descriptor/platform, verifies identity before metadata publication, and validates inputs before allocating volumes. Tests cover image defaults, non-root USER inheritance, network/security projection, no CLI fallback and no allocation for invalid requests. Live transport, candidate admission and E02 remain unqualified. Tag/delete by digest still fail closed pending alias-safe native semantics; deleting one alias cannot claim image deletion. The explicitly disabled direct-API path also refuses digest creation. No refusal closes a parity requirement.

The focused enhanced coverage run `34da6376-005a-4c09-8c91-56bf71eb6879` passed 127 tests but covered only 94/174 lines (54.0%) in the new create file: pure projection and fake-client tests do not exercise live XPC transport. This shortfall remains open against the 90% target and must be closed with meaningful transport/integration coverage; it is not waived or presented as release-quality coverage. The later stock focused run `e8c14e8e-3ddf-4053-a337-8de22422f6d9` passed after the non-root default correction.

## Upstream-first scenario reuse

### Native-candidate follow-up evidence

Candidate `cd32901cea7e93af040b4e3297b4a5197b753f5e` was built once (`44fff728-8aed-4008-a07b-031f6a6d8b3a`) and admitted from retained assets. Campaign `candidate-e02-descriptor-20260917a`, test invocation `aec37c4b-66b9-441f-a86b-413b1e3b104d`, timed out in engine setup after 24.976 seconds, before fixture operations. Cleanup passed in 1.084 seconds and restored the original services; recovery reported clear. The retained engine log reached provider selection but not socket readiness. A SecurityAgent appeared during startup; causation has not been established. This is a failed local candidate setup, not a benchmark or E02/parity pass. Further equivalent runtime attempts require a diagnosed startup change.

The pending native-create journal introduced after that attempt is separately covered by focused stock/enhanced runtime/state tests. It preserves uncertain intent, checks final native identity, blocks archive/lifecycle bypass and rejects inventory adoption during creation. Its operator reconciliation interface and live interrupted-create proof are still required; these unit tests do not clear the startup blocker. See [native creation recovery](../DESIGN.md#native-creation-recovery). Subsequent startup diagnostics add two fixed, payload-free markers after runtime recovery and handoff identity enrollment, so a bounded attempt can distinguish those phases without logging keys or guessing the cause.

### Isolated test keychain

The diagnostic candidate at `2c4326350e427c70acacb361c818ae2d3f834c9a` reached runtime recovery but not handoff identity enrollment (campaign `candidate-e02-startup-phases-20260917a`, invocation `1d59728a-7634-42e6-a33c-fe6cc05c45b2`). Setup timed out in 24.814 seconds; no operation ran and cleanup passed in 1.041 seconds. A separate read-only default-keychain query succeeded in the operator HOME but returned Security OSStatus `-25307` (no default keychain) in the isolated test HOME. This explains the reported missing-keychain dialog; the operator login keychain was neither missing nor reset.

The harness creates an ephemeral, random-password keychain inside the owned SSD case HOME before either the selected Apple API server or Engine starts. Its preparation callback runs after durable service snapshots and verified outgoing-process shutdown, before launchd can start the selected provider. A dedicated isolated child disables Security authentication UI and never sets the operator default/search list. Passwords remain in child memory, not arguments, environment or logs. Cleanup removes only the validated case keychain after both Engine shutdown and selected-provider shutdown/restoration are verified; the final service-journal receipt includes keychain deletion. Uncertain helper completion keeps quarantine and blocks restoration until its process is reconciled. A completed helper failure remains failed evidence but permits restoration when its shutdown is verified. The real isolated create/default/delete probe succeeded without changing the operator default/search list; it is not itself lifecycle or parity proof.

Enhanced E07 startup diagnostic `b64b70e5-c444-46ca-8a63-3bab7055b992` at `f744a85` samples the exact released API process in `ProviderHandoffProviderKeyStore.loadOrCreate` / `SecItemAdd` / `makeLoginAuthUI`: the enhanced provider needs its default keychain during API initialization, earlier than the former Engine-only preparation. The private stack SHA-256 is `4a27915881282626bddfa9caf9fe30a007f8cf47f9ce2c79c89bc1d5fc20f4bd`. Setup timed out after 11.083894292 seconds, operation never began, and cleanup passed in 1.159492833 seconds with no residue. This evidence identifies startup ordering, not an additional operator-approval requirement. The callback-order and cleanup regressions fail before the fix (`43f765df-d026-4abc-9e2a-e0cfb25f73a1`); the corrected harness passes all 906 component tests (`0f70782b-5a5f-4882-ba3f-4e2525d7be2e`, 25.934-second Bazel invocation). Deadlines, personal-keychain settings and provider binaries are unchanged. A new live enhanced run is still required.

The corrected signed harness `ef4a371e7adea38d378b93c842476b1cba56e676` reuses the existing enhanced product package. Live invocation `de2cc54a-be13-43db-b564-342399cbe72c` confirms the private keychain, API readiness RPC and kernel provisioning succeed, then the separately launched image helper times out during initialization-image loading. Setup/operation/cleanup were 61.773100166/0/1.325023666 seconds; the case records `failed` with `setup: TimeoutExpired`, not a passing operation. Diagnostic attempt `ab1d1df0-bfbb-4701-9683-7fcc6a5ff5c8` instead times out at API readiness before an API log is created (15.595249708/0/1.285165708 seconds); it never reaches the intended image-helper sample. Both runs restore original services, delete the disposable keychain/root, leave zero owned resources and clear recovery. Read-only before/after checks confirm the operator login-keychain default/search list is unchanged. UI inspection reports the Mac locked; further equivalent enhanced retries are paused pending host-access diagnosis after unlock. Stock-lane work may continue. Neither failure is waived or treated as performance evidence.

Every create/delete helper, including recovery deletion, has a durable private-journal intent before spawning, a PID record and an intent-bound verified process-group stop receipt. Missing stop proof blocks recovery even when the PID or keychain file is absent. A bounded private log snapshot and integrity/truncation metadata survive scratch cleanup; interrupted retention resumes before further helper work. Failures preserve the host guard and owned root. Never repair this setup error by resetting the login keychain, changing global search lists, extending parity deadlines or approving a dialog for the disposable test HOME.

The focused Bazel harness passed 263 tests (`e8c3c670-40bd-40d8-8b17-5c32b7ba92f8`), including uncertain spawn, worker death before PID/result, recovery-delete interruption, failed creation followed by successful cleanup and partial diagnostic retention. An earlier run exposed a fixture-only empty HTTP 204 write racing a closed client; the server now omits that empty write, with a deterministic regression. These component results do not establish a new live E02 pass, quiet benchmark, Sonar result or release qualification.

The subsequent live run at harness `0059999` reused the existing `2c43263` product candidate without rebuilding (campaign `candidate-e02-private-keychain-20260917a`, invocation `32d40362-54b3-4cf1-a390-8862db8f2256`). Engine startup completed in 6.098 seconds, including guest preparation, and logged handoff readiness plus socket listening. The first operation failed in 0.011 seconds with HTTP 400; verified cleanup took 1.192 seconds. Private keychain creation/removal both succeeded, the operator default/search list stayed unchanged and recovery reported clear. Thus the missing-keychain startup failure is cleared, but E02 has not passed.

That request used `/v1.54/` despite the [documented implemented API envelope](../COMPATIBILITY.md#docker-engine-api-bounds) ending at 1.53. Guest fixtures now use fixed API 1.53 for all lanes, validate that `/version` declares a range containing it before mutation, and retain the observed range privately plus the requested version in the runtime fingerprint. The Docker oracle remains the pinned API-1.54-capable engine; this does not change its version or waive a fixture assertion. It tests the existing claimed common API contract, not 1.54 support, and does not dynamically downgrade each lane. The API range regressions and full component suite pass (`c365adc0-1c5d-47a7-bee3-11e6880d1cd6`); live lifecycle proof remains outstanding.

### First stock candidate lifecycle and stream passes

Harness `0dd7ca8` reused candidate `2c43263` and the same prepared published stock provider/guest inputs, without rebuilding. E02 passed all five lifecycle observations (`44e8742f-c457-43cf-8716-8e575b297534`): setup 5.101 seconds, operation 4.400 seconds and cleanup 1.239 seconds. E03 passed all six stream observations, including exact 4 MiB duplex content (`3fd03ab2-ab88-470e-aa61-072c36f7bb1c`): setup 4.847 seconds, operation 1.290 seconds and cleanup 1.334 seconds. Both left zero owned resources and clear recovery. These are local-candidate functional results, not quiet comparative benchmarks or final downloaded-release parity.

E05 then failed exactly one observation (`0d8d52e1-f8be-4c67-aeb7-e24742a3ff56`): content, large file, long path and symlink matched, but mode was `0700` instead of required `0750`. Setup was 4.847 seconds, operation 1.157 seconds and successful cleanup 1.347 seconds. The service extracted uploads without preserving permissions, applying the harness's private `077` umask. The correction uses permission-preserving extraction inside a separate private parent so archive root-directory modes cannot expose staging. Focused stock (`bc93f91d-3b79-4feb-a4d8-306ff974f8e6`) and enhanced (`5b2a7242-26a6-4d34-8f96-079c53cdf084`) tests pass; E05 requires a rebuilt corrected candidate and live proof. Its expected mode is unchanged.

The corrected archive source was committed as `805940f55b04098dd20bfdb201bbc757e5fe8439` after independent review, focused enhanced lifetime-guard verification (`4e61765a-a2c9-4739-adb9-9e03188b256b`), SwiftLint/SwiftFormat and Markdown checks. One optimized candidate build (`43f27f56-7051-4de7-a001-6495432a35a3`) took 25.825 seconds, with 1,228 action-cache hits and 20 actions executed. Its retained archive SHA-256 is `306c1d7906f47784093041e522f01f990fcc92f16ed90ccf6804640ee376c53f`; preparation and both following live cases reused it without compiling again.

| Stock candidate fixture | Retained invocation | Setup seconds | Operation seconds | Cleanup seconds | Result |
| --- | --- | ---: | ---: | ---: | --- |
| E05 archive copy | `f21b6d91-ca27-4814-87aa-45f7451a0b84` | 5.390 | 1.288 | 4.513 | All five unchanged observations pass, including `0750` |
| E06 network/volume | `69e041fe-bb07-4924-948f-5412fbb6aa24` | 4.786 | 3.500 | 4.967 | All six unchanged observations pass |

Both cases report zero remaining owned resources; recovery is clear. Together with the earlier E02/E03 results this clears four implemented stock candidate fixtures, not the complete matrix: E02/E03 used the preceding candidate, the Docker/enhanced lanes and remaining adapters are unfinished, and final qualification must use published downloads. These raw functional/build durations are retained diagnostics, not a quiet-host paired benchmark or a performance improvement claim.

### Scenario sources

The replacement should import version-pinned upstream scenarios and assertions wherever possible, rather than inventing an independent definition of Docker or Dev Containers behaviour. The 17 September inspection identified these concrete sources:

| Source | Reuse boundary | Necessary adaptation |
| --- | --- | --- |
| [Docker Compose scenario framework](https://github.com/docker/compose/blob/34d0f701846c9b0d9b7e0903de43255260277311/pkg/e2e/SCENARIO.md) | Fixture projects, step intent, state checks, bounded polling, step timings and diagnostics; `TestRestart` checks that restart preserves container identity | Build the test harness with Bazel, not the product; supply pinned published executables. Replace command dispatch and inspection for native candidate lanes. Do not inherit or print the operator's Docker configuration or contexts. |
| [Moby ping integration tests](https://github.com/moby/moby/blob/568f755ebeb1ac9c6a8febbda6cd371ea0a9630b/integration/system/ping_test.go) | HTTP GET/HEAD, API-version and cache-header assertions at the existing Docker Engine oracle revision | Reuse API assertions without the internal helper that starts a fresh dockerd. Keep daemon-internal/Swarm scenarios explicitly accounted for; do not count an upstream skip as a pass. |
| [Dev Containers reference tests](https://github.com/devcontainers/cli/tree/f683c29f64a20109b4453e5149807e390ff65133/src/test) | Configuration, Features, lifecycle, user/environment and Compose scenarios at the pinned reference CLI revision | Prefetch the released CLI and fixture inputs. Replace source-tarball installation and hardcoded Docker cleanup with the lane adapter; preserve assertions. |

These are inspected reuse candidates, not imported or passing suites yet. Before adoption, record upstream test IDs, immutable revisions, licence attribution and any operational adapter patch. A newer scenario-source revision does not silently upgrade the pinned reference binary. Keep ordinary Go/Python/TypeScript test bodies under Bazel; retain only the project-specific artifact admission, host isolation, recovery and evidence layer. Candidate execution must not acquire a Docker runtime dependency through a copied upstream framework.
