# Change handoff: align devcontainer with lifecycle identity inspection

## Summary

Pin the Container package to merged revision `e76a28de2dcf2c3650871d8e5240d41d6a36cf12` in both the package manifest and resolved graph.

## Motivation

The lifecycle handoff in devcontainer relies on the same immutable identity authority consumed by Container Compose. The previous pin predated the final atomic discovery and native inspect correction, so it could not serve as the exact dependency revision for the 0.12.0 matched-stack checkpoint.

## Validation

```console
swift package --scratch-path /Volumes/SSD/github/devcontainer-lifecycle/.build resolve
swift test --scratch-path /Volumes/SSD/github/devcontainer-lifecycle/.build --filter AppleContainerRuntimeIdentityLifecycleHandoffTests
git diff --check
```

The graph resolved at the requested revision and all eight focused identity/lifecycle handoff tests passed.

## Compatibility

This is an exact dependency update. The Container change adds native CLI identity resolution without changing the Container API used by devcontainer. Runtime-neutral core boundaries and the separately installed Compose provider remain unchanged.

## Linked work

- Devcontainer pull request: [#65](https://github.com/stephenlclarke/devcontainer/pull/65).
- Container dependency: [stephenlclarke/container#135](https://github.com/stephenlclarke/container/pull/135).
- Parent parity issue: [stephenlclarke/container-compose#274](https://github.com/stephenlclarke/container-compose/issues/274).

## Remaining risk

The complete native, Compose, and devcontainer integration matrix remains the single coherent 0.12.0 release checkpoint after all exact dependency pins merge.
