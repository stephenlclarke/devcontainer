# Pull request 83: native Bazel build and evidence retention

## Motivation

Implement the native compilation/test boundary of the approved build-workflow redesign before replacing production CI and release paths. Tracked against [the workflow issue](ISSUE-build-bazel-workflow.md) and [PR 83](https://github.com/stephenlclarke/devcontainer/pull/83). This is continuing implementation of the complete system, not a claim that the initial qualification finishes the goal.

## Implementation

Host-service HTTP probes now reuse the existing bounded process runner: concurrent pipe draining, five-second or tighter enclosing deadline, 64 KiB per stream, strict response parsing and curl configuration/proxy isolation. The previous unbounded Foundation wait/capture implementation is removed. All 19 discovered service methods are now required by both profile policies. Initial fixture failures (`572dcffa-fa8e-4411-92c7-c43895280c05`) and a Swift Testing macro compilation failure (`5832fb46-e98a-4882-8da7-7846e30beadc`) remain retained. Independent review found an explicit deadline could extend the hard cap; red proof `ff69e808-2aba-4bc5-a559-79843f7ab10d` demonstrates the ten-second child outliving that cap. The correction clamps all inputs to five seconds, and tests require elapsed bounds plus verified PID disappearance. Corrected enhanced execution passes `370f9f1e-96a3-455c-9b23-bf6a36247a23`; final stock passes `016810fc-c6b5-4b15-a76a-4523af5afa34`. Independent final review is clean. Both integration-target compilation results (`35713d10-ddd6-44db-8796-a5e201785db4` stock, `2621c270-412b-437c-93b3-8e8ff8d96de4` enhanced) precede the review correction and are historical compile evidence only. Compilation does not run the host lane or touch Keychain. This test-harness change does not claim higher production coverage or a passing host-integration/release gate.

Final diagnostics CLI validation passes stock `c8a15133-e269-49d2-b937-25412af2b787` (5.477 seconds) and enhanced `d2f6d227-698c-491f-92d4-95582af650ec` (5.823 seconds). Both require 43 discovered CLI methods and positive DiagnosticsCommand source hits in the unchanged coverage denominator. Strict changed-source checks, Markdown validation and independent complete nine-file review are clean. These timings are development observations; full checkpoint coverage and live release authorities remain separate gates.

Support-archive collection now bounds each external probe to five seconds or an earlier caller deadline. Ordinary probe timeouts remain reportable diagnostic results, but cancellation and enclosing deadline expiry propagate through collection and remove its staging directory. A narrow staging-root dependency permits isolated cleanup assertions without mutating process-wide environment. Red proof `4f8dba68-bf34-4c75-9d62-96c2fbf8c8a1` reproduced unbounded caller deadlines and swallowed cancellation; focused correction `28ac8922-0815-4eb2-a2c2-4171694343cf` passed, followed by full CLI `ef0fd2c0-4419-4add-94dc-97a9936db52f` (3.627 seconds development elapsed). Added parsed-command archive validation checks private output, missing-state non-creation, input-log preservation and refusal to overwrite an existing archive. This remains component proof, not a live runtime or release claim.

Final probe/capture-source proof passes all three affected targets in stock (`478ed6f2-4cf9-4cf8-a5de-5aa3c1b02034`, 2.746 seconds) and enhanced (`c4f9a437-ba33-48ab-b99f-358f2fbce608`, 3.117 seconds); unchanged targets are explicitly reported cached. Independent complete nine-file review is clean. The policy now requires 16 process and 18 Compose CLI methods plus executed Compose CLI source, using the unchanged shared validator. These are development/cache observations, not quiet-host benchmarks.

Compose bridge project-name and remaining-volume probes now enforce a 30-second maximum (or earlier caller deadline), limit each captured stream to 1 MiB, reject truncation, and wait for owned-child drain/reap. Volume probe failure preserves ownership; caller cancellation is propagated rather than swallowed. The shared capture calculation now clamps retained capacity to actual chunk length, eliminating negative omission counts. Red probe proof is `500f2161-9751-4bae-b48e-b3ccc2138da5`; red capture-count proof is `b13644ff-a7d0-4508-80b6-add727e00579`. Both affected stock targets pass at `119f9ede-417c-41e0-a9d3-71091eabae50` (4.079 seconds, development observation). Regressions cover pre-expired context, active deadline/reap, cancellation, oversized stdout/stderr and conservative ownership, plus exact below/at/over-limit capture accounting. Actual Compose operation limits, provider defaults and shared executable helper bytes remain unchanged; these are component results, not live parity or release qualification.

Review identified the related pre-existing gap where a caller cancelled after entering synchronous capture could not cancel its detached child. Regression `ed996762-8051-4d32-970c-41a19f5dbefa` reproduces the ignored cancellation and exceeded cleanup bound. The synchronous wait now forwards cancellation at a 50-millisecond polling boundary, still waiting for drain/reap before returning; fast completions signal immediately rather than waiting for the poll interval. No process is abandoned merely to meet the deadline. Final cancellation-source stock targets pass in `b266a245-b13a-411d-a216-d9f4ff26b49a` (4.050 seconds). Enhanced `29b42b20-748c-4029-9172-7e8ca0ddc9f1` exposed two short-deadline fixture startup races; serializing the Doctor and Plugin suites preserves all original deadlines, elapsed bounds and required PID disappearance. The corrected enhanced run `d85b7504-b7f4-43d3-8e3a-e35956a12157` passes (3.668 seconds, process target reused). A consumer-owned schema-1 policy uses the existing shared validator, preserves all ten targets and existing minima/source probes, requires the current 38 CLI and 15 process tests and additionally requires execution in ProcessRunner. This avoids changing shared executable helper bytes for a product-specific inventory update. All timings are development observations, not quiet benchmarks.

Synchronous process capture now carries the caller's request context into detached work and enforces its deadline while cancelling, draining and reaping the owned process before return. Plug-in install-root discovery sets a five-second maximum without extending an earlier caller deadline; explicit roots bypass discovery. Red regression `f48cc448-8b39-4c4b-99ec-033b12e600cf` proves both expired-before-launch and active-deadline failures with finite children. The first stock correction run `3c159be4-fe71-47a2-8b16-268fa36dfc9b` passes the affected process and CLI targets (4.974 seconds development elapsed). Added CLI checks exercise exact status arguments, failure text, timed cleanup, invalid deadlines and parsed registration/status/unregistration with no runtime executable. Final-source stock proof passes at `f4c19ca6-fbd6-4649-b77c-1be1f7cc3370` (4.452 seconds, process target reused), alongside the enhanced result above. Strict changed-source lint/format, Markdown, policy admission and independent full-delta review pass; aggregate 90% coverage, exact-head remote authority and live parity remain open. README, installation instructions, CLI help and DocC distinguish the source improvement from published 1.0.1 behavior.

Trusted runner package analysis failed for both profiles because `rules_license` 1.0.0's empty provider lacks `target_under_license`. The minimal Apache-2.0 backport from [upstream commit ed5bb251](https://github.com/bazelbuild/rules_license/commit/ed5bb2515c7e57ef48e361cc4977b54a25002d71) preserves identity on the empty and execution-configuration paths without changing dependency traversal. The real Compose bridge has an empty licence set, so the existing package smoke is the reproducer; additional tests preserve other products' deduplicated notices and require CLI/engine dependency notices in the real archive. The entirely empty-set rejection remains intact. Native stock packaging passes at `c31f3b60-aeba-4606-b34b-c080f7e5ae0d` (22.315 seconds); enhanced packaging and recovery component tests pass at `f6cbaa5e-252e-4667-8bf7-9734c31136e3` (19.509 seconds). These are development observations, not quiet benchmarks or distribution approval. Sonar's single `python:S5778` finding is corrected by preparing malformed recovery input before the exception assertion; neither serialization failure nor setup failure can satisfy that assertion.

The 18 September SwiftPM CI run `35340915853` exposed two test-harness assumptions: spelling equality for `/var` versus `/private/var`, and an integration executable selected inside the test bundle. The process test now compares directory device/inode identity; SwiftPM engine discovery requires the exact regular executable beside its `.xctest` bundle instead of recursively selecting another build. Explicit Bazel runfiles and configured executable paths remain authoritative. Pure tests reject missing files, directories, non-executable files and non-bundle roots. Focused native process/service targets pass (`c3e7f6e2-47cf-4f2c-abe0-452131de3e74`, 5.742 seconds development elapsed), strict source checks and independent review are clean. These are harness corrections, not new live integration or sanitizer acceptance; the failed CI evidence is retained.

Coverage export exposed a missing child profile in the real-terminal regression. The test now passes only `TERM` and a unique LLVM profile destination under the validated SSD coverage directory, and fails if its own child profiles are missing or empty. Focused invocation `81b1bc85-4452-48bc-a2ad-5bc858348694` passes in 4.555 seconds; the launcher covers 124/133 lines (93.23%) in this focused run, not aggregate product coverage. Independent review is clean. No production behavior, source exclusion or quality threshold changes.

Final process-ownership review added two safeguards: interactive launches use Apple's [start-suspended spawn flag](https://developer.apple.com/library/archive/documentation/System/Conceptual/ManPages_iPhoneOS/man3/posix_spawnattr_setflags.3.html) until terminal foreground transfer succeeds, and captured commands retain an unreaped leader through pipe drainage. Group signals and final reap share an ownership lock; a consumed PID cannot be waited or restarted again. A finite descendant regression fails both leader-exit orderings at `bcacf932-260d-4f75-8b2e-4bb0be20f624` (4.97 seconds to close instead of bounded cancellation). It now passes alongside a real-PTY helper exercising 12 reads/foreground restorations, injected handoff failure without changing the test host's terminal, and post-reap cancellation without a second wait. The helper is an explicit Bazel runfile and SwiftPM test dependency, not a shipped product. Final focused enhanced (`fa54cf2e-7f82-4ff5-a42e-e55a9cd6cf8c`, 8.351 seconds) and stock (`3a84aab5-1e49-49cd-9a60-5ea79f17563d`, 14.929 seconds) targets pass; the stock run reuses the profile-independent process target and runs CLI/service tests. Independent full-delta review is clean. These are development observations, not performance comparisons. Startup cleanup scope begins at server start; earlier runtime/bootstrap and real Keychain qualification remain open.

Unattended diagnostic/service failure handling now validates `doctor` output format before execution, applies a five-second probe deadline without extending a caller's earlier deadline, and reaps owned processes. Missing/unsafe sockets, launch errors, output limits and both report formats have component regressions. The combined run exposed an actual fork/exec error-path deadlock in the dependency's child `exit` cleanup (`836e7845-7ac4-47d5-8b56-72df2305a9f8`, manually terminated after a process sample). The process runner now adapts Compose's Apache-2.0 POSIX-spawn launcher with exact paths, null default streams and interruption-safe wait; attribution is recorded in NOTICE. Tests cover concurrent missing-interpreter errors, input/environment/cwd/argument fidelity, exit status and TERM-ignoring process-tree cancellation. A narrow service-lifetime owner guarantees runtime cleanup after failed start and runtime/server cleanup after wait failure or cancellation, preserving the primary error and draining child tasks. The extracted original behavior fails startup/wait/cancellation regressions at `0134827b-3e50-421e-8aeb-d96c51f3fc9c`; the original Doctor failures are retained at `2d242873-31f2-47a7-b456-04ca7b3234e4`. Combined enhanced CLI/process/service targets pass at `dc6dc060-87ea-4c54-95ae-db17f1f9c6c7` (7.614 seconds, development observation). This does not certify live startup, signing/Keychain, HTTP socket responsiveness, parity, aggregate coverage or a new release.

The configuration-command regression now preserves omitted strictness rather than resetting saved `false` to `true`. An optional flag distinguishes omission from explicit `--strict`/`--no-strict`; fresh configurations retain the strict default. Eight new test methods exercise all four backend/frontend combinations, exact stored values/private permissions, explicit and omitted strictness, malformed/invalid-input preservation, durable idempotent claims, conflicting-provider refusal and explicit reset. The pre-fix failure is retained at `b142c517-dc04-437b-8caa-40bdb4f6f4c5`. Final complete CLI targets pass in stock (`3bfa14f5-4039-4d98-808d-8e7244744a9c`, 7.767 seconds) and enhanced (`2b044b18-8357-431e-8ec6-c001fa952b7a`, 6.712 seconds), with 21 methods in three suites. Strict touched-source lint/format, Markdown checks and independent full-delta review pass. These are parser/run and private SQLite component tests, not subprocess/runtime integration or quiet benchmarks. README, manual, DocC and embedded help describe candidate-only behavior. No default frontend, installed runtime, dependency or release support claim changes.

Native create now completes mount allocation/resolution, kernel lookup and deadline validation before an explicit durable-intent callback immediately preceding the create RPC. The journal stores final resolved mount configuration, and completion uses the same operation identity. Preflight failures remain retryable; potentially submitted failures retain intent. This addresses GitHub `4045780895` and the independent review's missing-kernel extension. A managed-volume regression reproduced the original failure at `83af5b91-3c1d-483f-9a3a-22892993c8ce`; the prototype native-volume case initially selected an existing fixture volume and was corrected to a missing one. Final focused stock (`af742e72-17d6-49c0-8838-096c3ab8f66f`, 19.550 seconds) and enhanced (`ee65eff5-4a0c-4b75-84e3-e99b0103a42c`, 13.941 seconds) runs pass 23 methods, including live-client kernel/callback ordering tests that stop before any real RPC. These are development timings, not quiet benchmarks. Strict touched-source checks and full independent rereview pass; both manifests explicitly declare the already-pinned Containerization module used for kernel injection. Public docs retain candidate-only scope and the live recovery qualification gap.

The Compose wrapper now records the resolved runtime backend rather than inferring ownership from the frontend. Executable admission precedes state creation; tests exercise both frontends with both backends, missing-native no-fallback behavior, and rejection of existing cross-runtime claims. The execution context is grouped using the existing environment type, and independent review found no actionable issue. The final focused suite passes in stock (`16043d4f-d108-4f43-b538-b192c6abc81d`, 6.609 seconds Bazel elapsed) and enhanced (`0ce75960-ed2f-45b5-9fef-75cae639f5b2`, 2.058 seconds); these are development observations, not quiet benchmarks. Strict Swift lint, formatting and touched-document lint pass. README, design, compatibility ledger and both user guides distinguish this development correction from the unchanged stable matrix. Default-provider and Homebrew Docker dependency removal remain blocked on the actual Docker-free client replacement, not on this bookkeeping correction.

Verified-stopped Docker cleanup is now recoverable independently of Apple service restoration. Both normal cleanup and explicit recovery retain a root-incarnation authorization before deletion; a crash during removal or guard clearance can resume without rebuilding, restarting a VM, signalling a process or rewriting the failed case. Recovery rechecks the sealed shutdown/plan, captured incarnations, command groups and a bounded socket-connect probe. A listening socket is rejected immediately without waiting for HTTP data; only missing/refused sockets establish absence. Uncertain startup/shutdown, changed roots/markers, incomplete command receipts, surviving helpers and retention failures still preserve quarantine. The focused component checkpoint passes 362 cases in invocation `78714f69-f9ce-4082-be67-09e64d02bb40` (4.660 seconds Bazel elapsed), including the bounded-probe correction from independent review. This is not live crash-injection qualification or complete orphan recovery.

GitHub review `4045475572` identified the package suite's default 60-second timeout as shorter than its aggregate command/cleanup budget. An explicit 300-second outer timeout now covers its six 15-second command deadlines and bounded session termination without changing individual deadlines or failure outcomes. Exact signed packaging head `dff29292ad9c29ea994d4594dd28b68d207a964f` passed nine cases for stock (`d26d0fab-da3f-415c-87f5-505262e18d15`, 21.331 seconds) and enhanced (`1f7de5ba-873e-4286-99b3-a3b36dd6c126`, 93.587 seconds); both retained archives restored successfully. These measured development/cache observations are not quiet-host benchmarks.

Native packaging now tests the actual extracted archive using seven artifact/CLI checks plus two cleanup regressions, all individually timed by the existing JUnit runner. It verifies product/lock/archive hashes, exact safe layout, plugin identity and compiled provenance, then executes the main CLI, plugin CLI, service help and explicit native Compose-provider fixture with a clean private environment. No real provider, service or installer runs. The owned-session process helper is reused verbatim from Compose. Uncertain child cleanup preserves both temporary roots; both explicit archive and smoke targets are requested so durable bytes remain restorable. Stock development invocation `9798f50d-6503-4516-aacd-776dd211dcd2` passes nine cases and restores successfully without compiling. [Package evidence](bazel-workflow.md#native-candidate-archives) retains the discovery failure and review corrections. This does not certify signing, Homebrew installation, default-provider selection or live runtime compatibility. Public product behavior and DocC API claims are unchanged.

The shared coverage contract now supports an additive schema-2 consumer inventory: unchanged unit targets plus disjoint declared CLI components. Canonical combined aggregates select `unit-cli`; receipts retain that identity and gates require it explicitly alongside source/profile/policy. Legacy unit receipts export byte-identically, and devcontainer's own aggregate remains unit-only. Focused policy, receipt and real shell-routing regressions reject missing/extra targets, replaced minima, malformed counts, mismatched inventory and invalid selection. This does not turn CLI components into live runtime coverage or lower the 90% threshold.

- Pin verified Bazel tooling and native Swift/SQLite targets with declared generated version inputs.
- Enroll and validate SSD storage; isolate download/cache/output/test scratch paths.
- Adapt upstream test-source/XML writes for external-volume sandbox compatibility and export/merge native Swift coverage from the original test execution.
- Add version-generator and launcher regressions, evidence validation and opt-in Make targets.
- Import the exact stock/enhanced package pins as native dependency targets; build all three project executables and ten unit-test targets.
- Separate real Keychain and Apple-service tests into explicit uncached host targets; preserve the ordinary source suites in both profiles.
- Keep test fixtures on SSD and inject direct/fallback fake-runtime transfer storage without changing production daemon-sharing defaults. The runtime initializer also accepts an explicit transfer root for callers that own a daemon-accessible scratch directory.
- Use sibling-file atomic replacement for writes that Foundation otherwise stages outside the destination volume sandbox.
- Preserve downloaded hosts-file permissions when editing private transfer staging; this disposable copy needs no atomic replacement.
- Seal source identity, build events, XML, LCOV and outcome bytes in transactional internal storage with content deduplication. Serialize each workspace through retention; fail mixed-source or conflicting evidence.
- Bound Bazel's native disposable cache and reject dependency/environment overrides that would break the pinned/storage contract.
- Isolate inherited credentials from Bazel and its event stream, preserve only validated host-test opt-in, and distinguish query/info diagnostics from build evidence.
- Add native optimized candidate archiving with the existing deterministic archive writer, digest-bearing metadata, transactional internal retention and authenticated SSD restore without rebuilding.
- Acquire reviewed GitHub binary assets with exact release/tag/commit/asset identity and checksum verification, durable pending/sealed ingest and explicit offline reuse, without compiling references.
- Extract reviewed binary layouts on SSD without installation or compilation; retain verified executable trees and complete inventories as long-lived internal assets. Pending publication is recoverable and not executable admission; sealed assets are verified and never silently repaired. This is binary preparation, not complete runtime closure qualification.
- Prepare digest-pinned stock initialization and shared workload OCI images with anonymous Skopeo downloads, no Docker daemon/VM/build, deterministic archives and recoverable internal retention. Reuse the exact Apache-licensed Compose closure validator, bind manifest/config identities and verify offline reuse. Prepare the checksum-pinned recommended kernel as non-executable retained data without installing it; extract only its reviewed archive member with a bounded Zstandard-capable command and validate offline reuse. Enhanced-init acquisition and guest-runtime qualification remain incomplete.
- Port E05 archive-copy assertions to bounded direct Unix HTTP and deterministic in-memory PAX payloads, with negative/socket tests. This removes the probe's Docker CLI dependency; guest-resource admission and live three-lane qualification remain unfinished.
- Connect the archive probe to a journalled disposable-container component: immutable prepared image, create/start/inspect, ID-based removal, ownership verification and lost-response recovery. Live runtime/guest admission remains a separate unfinished boundary; test-server results are not runtime parity.
- Port all five E02 lifecycle observations to the same direct-HTTP ownership component, including restart-generation readiness, TERM/wait exit codes and ordinary removal before repeated-delete observation. Retain per-request timings without payloads. This is component evidence only; live three-lane admission remains unfinished.
- Register invocation ownership and add conservative leased cleanup of expired, internally retained scratch. Keep unknown/legacy directories, runtime services and GitHub resources untouched.
- Export authenticated retained LCOV as Sonar generic XML with unchanged line counts, explicit original source identity and no build/test execution. Reject corrupt, dirty, incomplete or mismatched receipts; do not silently submit historical evidence as current-head coverage.
- Retain monotonic build/test elapsed timings with toolchain, source, configuration, host load and Bazel action/cache/test metrics; compare only matching successful clean configurations, without treating ordinary observations as controlled benchmarks.
- Exercise scratch-selection precedence and rejected Bazel roots through a test helper shared by SwiftPM and Bazel, without mutating process-wide environment in parallel tests. Resolve filesystem aliases before accepting SSD scratch in both the Swift helper and shell wrapper.
- Begin the authorised [test-harness replacement](bazel-test-harness.md): immutable per-case results, explicit recovery boundaries and a Docker-independent Unix HTTP E01 probe, tested through a native Bazel target. Live artifact-only adapters and full inventory cutover remain incomplete.
- Connect an opt-in released-service E01 case for the stock/enhanced binary selections with read-only artifact admission, shared runtime locking, bounded process ownership, durable quarantine and authenticated attached evidence. This does not qualify guest workloads or the complete parity matrix; automatic hard-kill reconciliation and legacy quarantine integration remain open.
- Reject mismatched or unhealthy API services before released-case execution, bind the selected launchd identity into evidence, and retain payload-free per-request timing/error traces. Preserve the first failed live attempt and its provider-mismatch diagnosis rather than retrying it as a stock parity pass.
- Add the released metadata case's transactional service selection: private internal snapshots/journal, idle-host checks, outgoing-process closure, selected-program verification, bounded private startup logs and original-service restoration even after diagnostic retention errors. Crash/fault tests pass; live qualification and automatic hard-kill reconciliation remain incomplete.
- Add report-first operator recovery with explicit case-ID confirmation, unchanged failed-case seals, private log retention and resumable partial scratch deletion. Refuse uncertain Engine spawn, live recorded PID/group, active workers/guests and foreign state; do not guess process ownership or clear quarantine when reconciliation is incomplete.
- Address GitHub recovery findings by checking captured original-process closure before reporting readiness or mutating services, and rejecting unregistered CI listeners. Address archive-contract feedback by allowing unrelated safe tar members and an omitted directory header while still checking every expected file and rejecting duplicate expected paths.
- Tie registered-original exemptions to the current launchd PID tree rather than the unchanged service definition alone. Refuse lingering old providers/detached descendants even after a replacement registration or a crash following scratch removal; preserve quarantine without mutating services.
- Connect E02/E05 to released guest-data admission, private kernel/image provisioning and direct-HTTP operation/cleanup. Bind all guest inputs/locks into the case identity; read-only admission refuses pending or missing enhanced inputs. Journal setup process intent and private logs, share explicit provider roots with the Engine client, and verify guest cleanup before provider teardown. Legacy recovery refuses unresolved guest/process state. Live qualification and automatic interrupted-guest reconciliation remain required; Compose 0.15.1's missing guest asset cannot be backfilled because GitHub marks the release immutable.
- Recover missing provisioning diagnostics after verified guest/process closure: report a read-only retention plan and commit its bounded private snapshots before restoring services or deleting scratch. Resume partial payload/metadata retention without replacing existing evidence. Reject missing, aliased, writable or changed logs and preserve the original failed case. This does not add unsafe PID-only signalling or complete live-guest recovery.
- Address GitHub review `4037091847` by bounding Engine log snapshots to 1 MiB and retaining explicit digest/truncation metadata. A 9 MiB regression verifies that excess output no longer fails artifact retention or leaves a successfully cleaned case quarantined.
- Connect all six E03 exec-stream assertions to the released guest adapter with direct bounded full-duplex Unix HTTP, strict non-TTY framing, raw TTY output, a 4 MiB binary round trip, exact exit/environment observations, write-ahead exec identity records and sanitized timings. Real socket tests exercise backpressure, fragmentation, invalid ownership, uncertain starts and bounds without starting a runtime. Live reference/candidate qualification remains required.
- Connect all six E06 network/volume assertions with role-scoped journals for three guests, local SSD bind input, direct resource HTTP and the shared exec transport. Preserve observed provider metadata, remove only ownership-verified resources in dependency order, reject foreign/uncertain state and require an aggregate cleanup receipt before service recovery. Component resource tests fake exec outputs and do not establish actual DNS/filesystem parity. Native-driver ownership review feedback is fixed without normalizing the observed driver field.
- Prepare pinned published Colima 0.10.3, Lima 2.2.0 and Colima-core 0.10.4 Docker-image assets through the existing leased release path. Keep VM data compressed/non-executable and explicitly omit only Lima's exact documentation alias; all other link rejection remains intact. Actual acquisition, preparation, version checks and offline reuse pass without starting a VM. Add the exact existing Docker CLI oracle bottle from public GitHub Packages with immutable manifest/bottle/executable checks, retained notices and recoverable publication; do not fabricate GitHub Release metadata or change the installed Docker client. Add an opt-in private SSD Docker VM lane using the shared Engine fixtures, exact process-role shutdown authority, a durable command journal and fail-closed cleanup. Live qualification, interrupted VM recovery and the common cross-lane comparison identity remain outstanding.
- Address GitHub review `4037254342` by revalidating complete original service-definition bytes/hash after bootstrap and inspection. Regression tests preserve public job identity while changing environment/Mach-service fields and require restoration refusal without a completion record.
- Reject Bazel `--flagfile` indirection so hidden dependency/environment overrides cannot bypass the launcher's argument policy.
- Permit a sibling Bazel workspace to reuse the launcher and evidence tools with an exact consumer-owned tooling digest lock. Source snapshots bind the executing helper bytes and reject changes. Consumers now provide a declarative profile-specific test/source inventory to the shared validator, not their own validator implementation. Test discovery, production LCOV line accounting, policy-bound export and an optional unrounded coverage threshold are covered by focused regressions. This is a shared-tooling boundary, not a claim that Compose migration is complete.
- Admit clean retained native candidates for development integration through the existing released-provider harness. Reuse authenticated archives and recoverable preparation, require matching source/dependency/profile/product identities, retain a readable admission manifest and keep candidate scope in the comparison fingerprint and JUnit. Candidate results cannot mix with published-release evidence; final parity still requires downloaded releases.
- Retain failed native-create intent in schema 4 before the provider mutation, verify the final incarnation, publish successful metadata atomically and prevent inventory adoption or archive/lifecycle bypass. Preserve pending evidence after name-based deletion. Operator reconciliation and live interrupted-create proof remain open rather than clearing uncertain state to pass tests.
- Supply a disposable noninteractive keychain for the isolated SSD test HOME, without changing the operator login keychain or search list. Journal every helper's spawn/verified stop, quarantine uncertain completion and retain bounded private failure diagnostics before cleanup. Recovery reuses that same guarded path. Component and crash regressions pass; live lifecycle qualification remains required.
- Fix the guest harness's out-of-range 1.54 request: pin the same documented API 1.53 contract across lanes, validate the advertised server range before resource mutation and retain version evidence. Do not upgrade a compatibility claim or change the Docker oracle pin to hide an unsupported version.
- Preserve archive member modes under a restrictive service umask, using nested extraction inside an untouched private parent. Real-tar component tests cover file modes 0644/0750/0777 and a 0777 dot-directory entry. Corrected candidate `805940f` passes live stock E05 and E06; preceding candidate `2c43263` passes stock E02 and E03. Earlier failures remain retained; the complete published three-lane matrix is still outstanding.

## Validation and compatibility

The downloaded Docker VM passed E01 at `c8580a9` with verified shutdown (`88799304-cedb-4adb-ab41-61d3120d27fb`). Four guest cases exposed the same pre-operation image-addressing assumption and cleaned up successfully. The correction at `dde586f` authenticates Docker's pinned manifest ID without rewriting either OCI pin or assertions; E02/E03/E05/E06 then passed with verified cleanup. New published campaigns now share one complete cross-lane fingerprint, with candidate evidence still separate; 304 component tests pass (`a5949762-b735-45cc-8f96-f8354c84688a`). Full three-lane parity, startup-variance analysis, recovery and quiet benchmarks remain open; retained invocation references and measured phases are in [the harness evidence](bazel-test-harness.md#first-live-docker-qualification).

The native-create journal component passes stock runtime/state tests (`8e6bc6cc-df7b-4e3d-a64c-06cad9dbdbfc`) and the subsequent enhanced runtime/state checkpoint including schema-3 migration (`fe4bbc08-85a3-481f-b75c-7d205cac22f0`). The earlier focused stock recovery helper covered 47/47 executable lines; this is not aggregate product coverage, current-head Sonar evidence or live crash recovery. Review found archive bypass, deletion clearing a newer intent and final-observation identity gaps; the draft now guards those paths, never clears intent through ordinary removal, and validates final descriptor/platform/timestamp. The CLI compatibility-create path remains outside this native guarantee. See [the recovery contract](../DESIGN.md#native-creation-recovery) for SDK and rollback compatibility.

The final review also caught direct-library callers losing synthetic Docker identity when completion bypassed ordinary adoption. Native completion now retains a supplied label or generates one 64-hex ID before its atomic commit; regressions verify preserved identity through a restarted bridge's list/inspect. Final focused stock (`c63388b4-15c4-4258-9331-49a826cdd313`) and enhanced (`da4949a0-2777-4b5d-a3df-c1f32d5ac557`) runtime/state targets pass; the final stock recovery helper covers 81/81 executable lines. Strict touched-file SwiftLint, SwiftFormat and affected Markdown lint pass, and the independent complete-diff review found no remaining actionable source/docs issue. Live recovery, the operator interface and full release gates remain open.

The candidate-admission component checkpoint passed both Bazel targets (`fc83883c-7df9-46b1-bea4-c51075291aad`) and all 131 workflow-helper tests. Standard-library trace with missing-line accounting measured 100/101 executable lines (99%) for `prepare_candidate.py` across ten focused tests; this is helper coverage, not aggregate Swift or Sonar coverage. Review identified and fixed mixed candidate/release comparison identity and missing internal-storage enforcement, then found no further actionable source issues. Live candidate lifecycle evidence remains outstanding.

GitHub review `4040933790` exposed loss of native index/manifest aliases while reconciling containers without bridge metadata. All three inventory/inspection paths now retain resolved identities and select the original configuration ID. Parameterized regressions cover both repository-qualified digest forms through direct list and inspect. Focused stock coverage passes (`94f4ef65-46ad-45c1-82c1-7753075ed0d0`); independent source review and strict touched-file SwiftLint report no additional findings. This does not substitute for the outstanding live E02 proof.

The stock protocol case now passes after operator-approved retained-helper access, while E02 stopped before creation on config-digest image inspection. The draft source correction resolves original OCI configuration IDs through the stock API, preserves repository-qualified lookup and groups shared tags. Both full stock/enhanced unit/product coverage graphs passed at `5e640d774dec70ad50970683c55aab6a4d073ed8` (`9806600f-cc3e-4be5-938a-4fce44008d4f`, `f504daeb-b1dd-4355-b385-8ae76a9fe309`). The subsequent create implementation captures descriptor/platform, prepares a typed native configuration before allocating mounts, verifies resulting identity and only then publishes compatibility metadata. Independent review found and fixed allocation-before-validation and empty-user privilege regressions; the final source pass found no further actionable issue. Focused stock/enhanced targets pass, but live XPC coverage and E02 remain open; see the [explicit coverage shortfall](bazel-test-harness.md#approved-helpers-and-first-stock-protocol-pass). Digest tag/delete and digest creation with direct API disabled still refuse unsafe CLI operations. These are implementation gaps, not waived parity or release qualification.

The final delivery contract now explicitly requires stable builds of both projects, published-asset validation, public benchmark evidence and both DocC/VHS documentation outputs. The original tape/recorder inspection found hard-coded devcontainer version/build/Docker-CLI assumptions and a Compose recording limited to two services; those must be corrected before the final recording phase.

See the dated [qualification evidence](bazel-workflow.md#qualification-evidence). Both dependency profiles compile and pass their deterministic suites (221 stock / 232 enhanced). An unchanged enhanced request reuses all ten test results with no test execution or Swift compilation. Fifty-four helper tests exercise storage, evidence, source mutation, environment isolation, deterministic candidate layout, restore after output removal, release acquisition/recovery and leased cleanup. Both optimized archives build; the enhanced archive also restores and executes, and unchanged packaging performs no compilation or archive action. Five published binaries were verified, retained and reused offline. The complete local checkpoint passed at `db772e52f573ee9387cf9660d597c969eecd68aa`; the subsequent staging-permission regression passed focused enhanced runtime tests (invocation `e45b2d57-911c-48b8-b4fa-6079dab4a12e`). Final-head affected checks remain required. ShellCheck and focused Swift formatting checks pass. Earlier subset ASan/TSan evidence is retained but does not qualify the full graph. Existing release entry points and protected checks remain unchanged. No release is produced by this change.

## Remaining risks

API readiness now requires one bounded read-only list RPC through the exact selected released CLI, not just a running launchd identity. Empty private inventory, exit status, complete output, verified child shutdown and unchanged service identity precede the ready receipt. Intent/stop/diagnostic records support conservative interrupted-start recovery without retrying conformance requests or rewriting historical cases. The PID-only readiness regression fails at `ab610f5e-ad11-4718-9f0b-34cac59e7dbd`; all 374 native component tests pass at `c031ffce-d20a-4507-95b6-01aa2a6eeea8`. This changes the shared campaign fingerprint. Historical runtime records cannot qualify the new harness; live validation remains required.

Live stock E01 now passes the new admission boundary (`6afca16c`), while the enhanced release still stalls before serving requests. Its bounded native trace locates file-log creation, with successful cleanup; see the [startup evidence](bazel-test-harness.md#api-readiness-and-interrupted-startup). A reporting follow-up converts only the readiness subprocess deadline to sanitized `TimeoutError`, preserving cleanup and historical records. All 375 component tests pass (`4af4ff48-1ae2-44c7-b858-1662dc3c0514`) after the full case-store regression fails against generic-failure classification. This is not an enhanced parity pass or a permission workaround.

Read-only CLI contract tests now verify generated version provenance in pretty, JSON and short formats, reject invalid formats/configuration without state changes, and round-trip socket paths through a real shell including quotes, whitespace, newlines and command-substitution syntax. Internal output construction avoids redirecting process-global stdout during concurrent tests; emitted bytes and newline behavior remain unchanged. Focused stock (`a4222753-52f6-4d64-89f1-21a69dae675b`) and enhanced (`fcda4c6b-313c-4e98-a4e8-6c0d260c6fa8`) CLI coverage tests pass, and independent final review is clean. The evidence policy requires both output implementations and the expanded test inventory. These results do not establish aggregate coverage, runtime parity or release qualification.

Shared direct-HTTP framing now refuses early EOF with unsatisfied `Content-Length`, even when the truncated body contains valid JSON. A real Unix-socket regression reproduces the former silent acceptance (`4be157b5-0cb3-4159-b739-01e834f2d4c1`); all 346 native harness tests pass after the fix (`f02b1a6d-3a8c-4cf1-b6e7-fc6c12ab08f1`, 4.554 seconds development elapsed). Complete bodies at the exact size bound remain accepted, and existing HEAD/oversize cases still pass. This prevents partial HTTP output from being used as completion evidence without adding retries, changing deadlines or weakening uncertain-state cleanup. No live service, installed product, README command, public API, DocC or compatibility claim changes; the image-build fixture remains unregistered and incomplete.

The `88e39ff` scan identified nine ambiguous exception assertions (`python:S5778`) that reopened the recovery journal inside the expected-failure scope. Each setup now happens before the assertion, so only cleanup/submission can satisfy the expected error. All 344 native harness tests pass (`ba491ac4-72f5-461a-afb7-76892cc9672e`, 4.587 seconds development elapsed), and independent review is clean. Production behavior and gates are unchanged; the next exact-head scan must confirm closure.

The E04 image-ownership checkpoint passes all 344 native harness tests (`e5462fc4-1ec8-4883-a4ce-3d0d2067ce91`, 4.591 seconds development elapsed), touched-document lint and final independent review. This is image-only component evidence, not live parity or a coverage percentage. The preceding `4fd8234` checkpoint's GitHub test, stock, sanitizer, Homebrew, documentation and Sonar checks are green, with zero unresolved Sonar issues; the new source still requires its own exact-head remote checks.

E04 now has deterministic digest-pinned build contexts, safe query construction, bounded JSON-line decoding and exact image-ownership checks in `Tools/testing/build_probe.py`. Eight focused tests joined the native harness; that pure-helper checkpoint passed all 325 tests (`eddb9bdb-8c8c-486e-99a5-869655c8b190`, 4.197 seconds development elapsed), with clean independent review. The subsequent `build_images.py` component adds durable pairwise intent, prepared-base inspection, write-ahead submission/deletion, private terminal-response retention and conservative image-only recovery. Nineteen Unix HTTP/SQLite tests cover replacement, shared/base images, conflicting identities, interrupted observation, lost responses, partial cleanup, reappearance and residue. Independent review found a per-image closure gap; submission now refuses a role already marked absent even when overall cleanup remains incomplete, with a regression. Neither component is registered as a live E04 adapter. Released-builder admission, live transaction wiring, intended failure-phase proof, interrupted-builder reconciliation, cache/intermediate-container cleanup and full three-lane execution remain required. The [harness design](bazel-test-harness.md#image-build-component-not-yet-live) records that boundary; no support or release claim changes.

The follow-up at `c901b6d` passes the Sonar quality gate (90.7% new-code coverage), but the stricter zero-open-issues check still fails on 25 maintainability findings. The batch moves exception-test setup outside the asserted operation, narrows provisioning failures to their exact exception types, makes the server-error cleanup registration explicit, and groups the internal logging clients without changing their defaults or the public API. The native harness and release-input targets pass 317 and 70 tests respectively (`09a22878-9e22-4b80-9c78-d044185b864b`). The final runtime shape passes stock (`eef1824a-a535-43f7-bee2-3edaa81aea87`, 131 tests, 28.748 seconds) and enhanced (`252c1997-a828-4871-9264-21ea529bbe4c`, 143 tests, 28.010 seconds). Independent review and strict touched-file Swift lint pass. These are development observations, not quiet benchmarks or aggregate coverage. The next exact-head scan must confirm the issue closures; thresholds and exclusions are unchanged. Compose's shared-helper digest lock moves with the guest-image test change. README, DocC and install behavior remain unchanged because these are internal dependency grouping and test-quality corrections.

The 18 September analysis at `d55094a` reports 90.7% new-code coverage, but its reliability gate fails on the archive fixture's self-comparison assertion (`python:S5863`); this is not an overall-coverage pass. The regression now generates archives under two different mocked wall clocks, compares their bytes and checks zero timestamp/user/group metadata, while retaining the exact-mode assertion. Native Bazel evidence target `0b08cb89-98db-4d1c-abd9-66287f03dbdc` passes all 317 component tests in 4.527 seconds total, and independent review found no actionable issue. These are development timings, not quiet benchmarks. The next exact-head Sonar analysis remains required; no gate, suppression or coverage exclusion changed.

Approval-free mount-boundary coverage now exercises the production parser with an internal injected volume lookup; the default remains the same `ClientVolume.inspect` operation. Tests cover bind/tmpfs/volume order and read-only modes, incomplete/unknown options, malformed syntax, lookup failure and enhanced image-mount rejection without a live service. Native identity regressions also reject a mismatched descriptor and malformed platform. The full Apple runtime test target passes with coverage in enhanced (`11130fa4-4077-4445-8733-dccc6d0c265a`, 16.454 seconds) and stock (`7ff98e0b-525f-45b0-ba34-7107a686b2ad`, 28.728 seconds). These are component development timings, not aggregate coverage, live parity or quiet benchmarks. Independent final source review found no actionable issue. Public API, compatibility claims, README commands, DocC and install behavior are unchanged; the live XPC preparation/create coverage gap remains open.

The corrected exact-head E01 campaign at `2b1a920` now compares all three published lanes successfully with authenticated seals and verified cleanup. Public JSON/Markdown/JUnit evidence is linked in the README and harness document; raw Apple-lane timing ratios above 10x remain explicitly unqualified and require investigation. The new read-only report path authenticates one consistent snapshot, defaults to the full fixture manifest, rejects campaign-wide fingerprint drift and preserves failures without exporting private payloads. Component gate `93ae340d-2ae1-4733-a456-de01f386b444` and 148 workflow-helper tests pass. This is one-fixture functional evidence, not complete parity or stable-release acceptance.

Live shared E01 identity validation exposed Python import/runfiles path asymmetry: each lane passed, but Docker's workspace glob included test files absent from the Apple runfiles. Those historical records cannot compare. The explicit complete runtime closure and symlink-runfiles regression correct the fingerprint without rewriting evidence; focused Bazel invocation `5a4d7d9b-827d-4f83-b68c-22430f7639e4` passes and independent review is clean. Fresh matching live evidence remains required.

Follow-up coverage export passes 60 helper tests and exports both clean `50f74fa482843ca44203b4a3154c50d5ac644d4e` unit receipts without rebuilding. The complete checkpoint at that commit supersedes the earlier source checkpoint for the staging-permission change; see the implementation document for invocation IDs. The export-only follow-up does not alter compiled product or test inputs.

Unit-only coverage is approximately 86.5%, below the required 90% full-quality bar; no gate has been lowered. Host-runtime integration, full sanitizer/leak proof, package-manifest drift checks, complete artifact-only parity, immutable release snapshots, publication recovery, family-wide migration and broader resource/GitHub cleanup remain outstanding. The separate trusted CI worker is approved and being provisioned; arbitrary PR code must not run on the existing signing worker. Keep this opt-in until the remaining gates in the implementation document are complete. A new exact-head Sonar scan remains required after the source fixes described below.

On 17 September the operator approved the dedicated runner. Provisioning is now tracked in private `stephenlclarke/container-build` PR 1, whose registration boundary excludes public fork workflows. The subsequent public Sonar scan reported five source findings: three positional-argument reads, a hardcoded Swift test scratch root and an eight-argument internal initializer. These are corrected in maintained source without rule suppressions; the next exact-head scan remains required. Storage roots are grouped internally and the validated test wrapper declares the scratch root to Swift fixtures. The focused runtime suite also exposed descriptor fixtures that assumed a high file limit and briefly released a descriptor number into the parallel suite. The invalid fixture now uses an impossible descriptor and replacement uses atomic `dup2`; the complete focused runtime target passes (invocation `e752656b-edfd-4f0f-912c-6348d75e99dd`).
