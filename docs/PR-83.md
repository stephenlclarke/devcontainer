# Pull request 83: native Bazel build and evidence retention

## Motivation

Current foreground proof: Docker and stock E08 pass all seven observations; the inspection and resize-route defects below are fixed and live-qualified on stock. Raw timings expose an open non-quiet ~14.43x diagnostic ratio, not a performance pass. E09 now exercises the actual admitted Compose CLI's piped input, separated output, exit status and automatic removal, with identity-bound command/resource recovery. [Exact evidence and remaining requirements](bazel-test-harness.md#foreground-init-attachment-development) keep enhanced runtime, interactive CLI, quiet performance and stable release qualification explicit. Earlier pending-stock statements are historical.

Implement the native compilation/test boundary of the approved build-workflow redesign before replacing production CI and release paths. Tracked against [the workflow issue](ISSUE-build-bazel-workflow.md) and [PR 83](https://github.com/stephenlclarke/devcontainer/pull/83). This is continuing implementation of the complete system, not a claim that the initial qualification finishes the goal.

## Implementation

The shared launcher now namespaces test scratch per workspace. Concurrent identically named package tests exposed deletion of Compose's extracted executable by the other Bazel server; a failing-before launcher regression proves the collision, and all 27 launcher tests pass after isolation. Product code is unchanged by scratch naming, and subsequent evidence captures the corrected launcher. See [storage behavior and original failure](bazel-workflow.md).

C03's first real Docker run passes; stock times out on the helper build without a private builder. Native setup now admits that prerequisite without moving the helper build outside operation timing. The same investigation found missing volume-label projection: volume list/filter/inspect now project native Compose ownership consistently and reject conflicting mirrors before allocation. Three new API regression functions fail before correction and pass after it, alongside builder admission/ordering tests. [Original outcomes, exact evidence and remaining live qualification](bazel-test-harness.md#c03-compose-resources) remain explicit. Compose's corresponding logical-volume-label fix is kept on its PR 708 review branch, not directly on main.

C03 now uses the original resource fixture through downloaded Docker reference tools or admitted Docker-free candidate binaries. It reuses C02's service/network transaction, adds exact volume-incarnation checks and interrupted-deletion recovery, and refuses foreign/replaced/uncertain resource cleanup. Bazel runfiles and campaign fingerprints include every input. [The test and qualification boundary](bazel-test-harness.md#c03-compose-resources) distinguishes component proof from live parity and quiet release benchmarks. This changes the harness only: product/API/DocC, installation, package pins and published compatibility claims remain unchanged.

Read-only previous/current reporting now exports the existing D01/E02 baseline-versus-candidate evidence without rebuilding or rerunning workloads. Exact admission, source/runtime identities and case seals are validated, all samples remain visible, failed samples suppress comparisons, and neither local candidates nor uncontrolled timings are promoted to release/benchmark authority. [Usage, inspectable reports and published Compose adapter limitation](bazel-test-harness.md#previouscurrent-observation-reports) document the scope, tests and remaining quiet-release work. Product bytes and runtime behavior are unchanged.

Live stock release-baseline runs now expose two historical image identity/lookup failures while the reused current candidate passes both unchanged cases and cleanup. The original failures and exact-owned-resource recovery remain retained. The report also now suppresses ratios whenever either compared workload fails, differs semantically or has zero operation duration, preserving raw durations without advertising false speedups or 10x regressions. [Results and remaining benchmark gates](bazel-test-harness.md#published-devcontainer-baseline) remain explicitly distinct from quiet stable-release performance qualification.

The release-comparison adapter now admits the actual published devcontainer `1.0.1` for native D01-D07. It uses the old release's documented pinned external CLI/Node/Docker-client interface instead of passing unsupported `up`/`exec` commands to its administrative binary. Clients remain offline-authenticated, confined to the private Apple socket, fingerprinted and revalidated; current candidate admission and Docker-free execution remain separate. [Component proof, commands and qualification limits](bazel-test-harness.md#published-devcontainer-baseline) include rejection before runtime mutation and retained failure/cleanup semantics. No stable release or comparative performance result is claimed by this adapter change.

The full enhanced checkpoint exposed an archive-interruption fixture defect: a sparse file archived before staging could be observed, allowing the test to contact the default Engine. The replacement separately proves acknowledged real-child cancellation/stage cleanup and actual CLI SIGINT/SIGTERM against an unfinished build stream on an owned Unix socket. An internal runner seam retains the production tar path, flags and bounds; no user-facing override is added. [Failure-before, rejected intermediate fixture and positive/negative regression evidence](bazel-test-harness.md#process-io-cancellation-and-coverage-integrity) distinguish this correction from performance improvement. README now reflects the already-corrected coverage-counter defect and its remaining release-head gates.

Final review exposed a test-credential cleanup defect: the former default-Keychain deletion silently failed with OSStatus -25244, and a rejected HTTP probe could skip it entirely. The service integration lane now reuses the existing disposable-Keychain helper with an isolated SSD HOME, bounded child shutdown and verified whole-keychain removal on successful and rejected probes. Stock and enhanced checks pass. [Failure and corrected evidence](bazel-test-harness.md#bounded-service-http-probes) supersedes earlier assertions that a passing process check proved credential cleanup; legacy operator-Keychain test records have not been broadly deleted or their ACLs altered.

The service now pins shared gateway `40436017e1e93012b8dab7cfc3c79783538065c3` and declares `engine.control.recovery` version 1. Explicit GET/POST recovery controls reach the authenticated selected provider instead of failing gateway route admission. Real socket tests exercise the receiver's freeze barrier and preserved uncertainty, while both profile-specific service processes prove public discovery and capability advertisement. [Focused coverage and integration evidence](bazel-test-harness.md#bounded-service-http-probes) records the scope; live fault recovery, complete current-pin regression and release benchmarks remain open. This changes the provider fingerprint; old prepared product archives do not prove the new route.

Definite pre-create rejection now has an intent-bound recovery receipt, exposed only after complete HTTP framing. Cleanup still refuses uncertain responses, ambiguous markers, contradictory records and any named/owned resource. The unsupported fixture remains failed. [Component, focused coverage and live recovery evidence](bazel-test-harness.md#foreground-init-attachment-development) verifies automatic cleanup against the unchanged stock candidate. No dependency or product binary changed.

E15 separates explicit Engine create-time terminal dimensions from E14's post-start CLI resize. The fixture reads the first guest size before all input/resize, requires acknowledged exit 17 and automatic removal, and retains bounded raw output with truncation metadata. [Live Docker pass, stock rejection and recovered cleanup](bazel-test-harness.md#foreground-init-attachment-development) leave native implementation, enhanced qualification and uncertain-create recovery explicitly open. No product support or released-binary performance improvement is claimed.

The service-process integration target now runs its unchanged executable from a temporary standalone SSD path, with strict signature verification and exact-copy cleanup. This avoids rules_swift's incomplete `.xctest` bundle being rejected by macOS code-identity validation; production identity checks and release signing are unchanged. Both service tests pass in stock and enhanced profiles. [Failure-before, focused evidence and remaining gates](bazel-test-harness.md#bounded-service-http-probes) explicitly distinguish this Keychain-backed process check from live guest parity and released-binary benchmarks.

The coverage follow-up rejects masked optional-collector failures and LLVM profile diagnostics through both fresh and authenticated retained evidence. Shell targets already collected by the SSD LLVM exporter disable only the redundant C collector. All 208 helper tests pass after 10 fail-before assertions; version-generator coverage has clean diagnostics. Full stock source `7f4fe20` now passes 675 reported cases and the unchanged 90% gate at 91.8845% (17,153/18,668 lines). [Exact measurements and remaining gates](bazel-test-harness.md#process-io-cancellation-and-coverage-integrity) do not claim enhanced, integration, SonarQube or release completion.

Coverage-only atomic Swift/C instrumentation and sandboxed Swift compilation address reproduced parallel counter loss plus stale worker-incremental object reuse. Ordinary builds retain their worker strategy and all builds retain Bazel action caching. A separate native compiler probe checks every expected raw function/line count, including unexecuted branches, and rejects missing records, lost in-range counts and underflow; it never enters the product coverage denominator. The Make unit/checkpoint path invokes this gate before aggregate coverage. [Failure-before, clean focused results and remaining aggregate gates](bazel-test-harness.md#process-io-cancellation-and-coverage-integrity) preserve the rejected reports; no denominator or threshold is relaxed.

E14's original host-PTY probe disproves the assumption that Docker Compose supplies dimensions before the first guest command: Docker and stock Apple both produce the same unavailable-size diagnostic and fail that assertion, with zero owned residue. Docker resizes after startup. Version 2 preserves those results and raw first-query evidence while requiring bounded inherited-size convergence and host-initiated resize. Corrected diagnostic parsing passes all 975 component tests and independent source review. Live Docker (`7a82bec9`) and stock Apple (`7163b414`) now pass all seven observations with zero residue, reusing the immutable product packages. [Exact evidence and contract distinctions](bazel-test-harness.md#foreground-init-attachment-development) leave enhanced qualification, explicit Engine create-time sizing, C02 and release gates open; no failed result is normalized into success.

The immutable stock E13 candidate now passes all five observations with zero owned residue and clear recovery (`347cf245-9f7e-4b75-9eb0-9f379701280b`). Both optimized package/smoke gates pass. [Exact source, archive, timing and result identities](bazel-test-harness.md#foreground-init-attachment-development) supersede pending stock-live statements below. The Docker duplicate-signal failure remains unresolved, so this is not a parity or release pass.

Signal delivery no longer unconditionally closes host port listeners. Process-generation exits and fallback observers for restored/CLI-started containers own closure; failed inspection is not treated as exit, and shutdown joins retired observers. Targeted failure-before tests, 260 passing runtime functions, clean scoped lint and independent review are recorded in [the signal evidence](bazel-test-harness.md#foreground-init-attachment-development). The real E13 Docker run fails because a single host USR1 is delivered twice; raw output/timings and successful cleanup are retained without normalizing the difference. Native signal qualification, aggregate quality and stable delivery remain open. This current record supersedes the pending Docker statement below.

E13 adds an actual CLI signal regression: verify the admitted child, send host USR1, observe the guest trap and continued exact container identity, send host TERM, and require guest exit 23, exact streams and automatic removal. The existing owned-command, immutable journal and conservative cleanup path is reused. [Evidence and limits](bazel-test-harness.md#foreground-init-attachment-development) distinguish the passing real-child component tests from pending live Docker/native qualification and the wider C02 signal contract.

E12 adds maintained post-error-state proof for invalid terminal input: after the exact piped-TTY diagnostic and exit, the dependency must be running and the job and its image must remain absent. Docker and the corrected stock Compose package pass all five observations with zero owned residue; the 9.04x raw operation ratio is informational, not quiet benchmark evidence. These snapshots do not exclude transient image/job work or independently order dependency startup against diagnostic emission; Compose component mutation/corrected tests supply sequencing evidence. Real-child/Unix-inspection tests cover success, exact error matching, timeouts, ownership, residue and conservative recovery. [Scope and exact evidence](bazel-test-harness.md#foreground-init-attachment-development) distinguish this result from pending enhanced, full C02 and release qualification.

E11 adds the redirected-terminal regression: a service requests `tty: true`, while the real admitted CLI has redirected input/output and no terminal flags. Effective inspection and guest bytes prove the CLI selected non-TTY mode on Docker and the corrected stock Compose package; all five observations pass with zero owned residue. The existing ownership, evidence and cleanup contracts are unchanged. The raw 14.94x operation ratio remains an open performance concern; enhanced and the wider terminal contract remain open. [Fixture scope, exact live/component evidence and limitations](bazel-test-harness.md#foreground-init-attachment-development) are explicit.

The E10 quiet-mode regression reuses E09's actual immutable Compose CLI, resource ownership and five guest-I/O/exit/removal observations. It records `--quiet` before launch and selects the pinned Docker release or matching native candidate without rebuilding references. Lane-selection, candidate validation, Docker admission and real child-pipe tests cover both modes. Docker and stock now pass all five live observations with zero residue; no complete CLI-diagnostic, enhanced, performance or stable-release claim is made. [Current scope and exact evidence](bazel-test-harness.md#foreground-init-attachment-development) retain the roughly 16.50x raw operation ratio and distinguish local-candidate evidence from published-release qualification.

E09 now has passing real Docker Compose proof. Its first stock run exposed hard-coded network-mode inspection and was safely reconciled without changing the failed result. The adapter persists the accepted selector independently of endpoint attachments, normalizes Linux defaults to `bridge`, and preserves legacy decoding. Pinned Docker create/start/inspect diagnostics disproved a proposed conflicting-endpoint rejection; focused tests retain that behavior, network-ID spelling, provider preservation and SQLite reopen. The probe also retains narrow inspection diagnostics. [Exact evidence](bazel-test-harness.md#foreground-init-attachment-development) separates this unreleased correction from pending rebuilt-stock/enhanced live and release qualification. No provider dependency, CLI option or stable compatibility claim changes.

The provider now declares the existing generation-bound `ContainerResize` route, fixing the next live E08 stock failure before native dispatch. Its exhaustive route regression has fail-before/pass-after proof; attachment generation tests remain green. The host-process smoke fixture's unavailable-route example changes to pause, while provider-mode expectations now require resize. That host-Keychain test is not reported as run. DESIGN and COMPATIBILITY replace the obsolete blanket stock-resize explanation with retained-handle ownership and pending live/release qualification; no runtime API or dependency change is required. [Exact evidence](bazel-test-harness.md#foreground-init-attachment-development) remains separate from complete C02 support.

E08's live Docker oracle passes all seven observations. Stock exposed an omitted `HostConfig.AutoRemove` inspection field; the adapter now encodes `ContainerSpec.autoRemove`, with explicit true/false/default regression coverage and no provider or dependency changes. The failed stock case was safely recovered without relabelling its result. [Runtime, regression and recovery evidence](bazel-test-harness.md#foreground-init-attachment-development) keeps rebuilt native qualification separate from the passing Docker oracle; stable compatibility and DocC API claims remain unchanged.

The original Bazel-only E08 harness checkpoint (`5f0588d`) adds init-terminal output/resize, process-preserving detach and reconnect, pre-start acknowledged wait, exact exit and auto-removal observations. That checkpoint reused pinned releases, immutable candidate archives and exact-owned-resource cleanup without changing product binaries or dependencies; the subsequent adapter fix above requires a new product build. The same-container-restart negative proves a container ID is insufficient and requires a guest-held per-process UUID across reconnect. All 925 harness tests pass; the 12 focused E08 tests cover 99.35% of probe lines with clean independent re-review. Native E08 proof remains required. README, fixture/runner registration, test instructions and issue/PR handoff are updated; stable compatibility claims, CLI help and DocC API declarations are unchanged. [Exact component evidence and non-goals](bazel-test-harness.md#foreground-init-attachment-development) remain explicit.

The Bazel E07 fixture extends C02's foreground proof with pre-start init attachment, two distinct 4-MiB binary transfers, source-aware replay across restart, real exit status and an independent combined saved-prefix/live observer. The observer confirms both saved startup records before new input begins; HTTP upgrade alone is insufficient. Shared HTTP/resource ownership, exact released reference inputs, phase timing and cleanup remain unchanged. The extension passes 912 component tests, 96.77% focused probe coverage and clean independent re-review. Live eight-observation Docker (`2b0ffd34`) and stock Apple (`94b55b2b`) runs at `ec3da38` pass with zero residue, reusing immutable binaries; enhanced qualification remains pending. [Scope, exact phase timings and retained regression evidence](bazel-test-harness.md#foreground-init-attachment-development) distinguish this non-TTY fixture from the remaining live foreground, quality and release gates.

Source-aware output capture now uses schema-6 raw journals plus versioned json-file records and durable per-generation/source natural EOF. Live binary output stays exact; saved logs apply the pinned driver's logical-record encoding. Snapshot cutoffs do not flush partial records. Legacy raw history remains inspectable but cannot be silently promoted or resumed. Provider-selected logging policy is persisted and revalidated before start/restart. Runtime-neutral interfaces retain atomic log/live registration and demand-driven HTTP replay. Stock package APIs remain unchanged; no Compose library is imported. Regression proof covers exact binary/source preservation, native restart plus engine/database reopen, write/completion failure, bounded contention, removal/reuse, concurrent publication and the old-generation durable-completion barrier. [Current evidence and explicit development limits](bazel-test-harness.md#foreground-init-attachment-development) supersede older native-history-unimplemented and eager-buffer statements below. Current work remains unreleased; full foreground runtime parity, valid quality evidence and distribution gates remain open.

Native exit registration is now acknowledged before HTTP success headers and remains independent of attachment/output lifetime. The direct provider advertises the capability; unsupported providers refuse it. A retained receipt survives auto-removal, never replays a completed generation to a late subscriber and releases a pre-cancelled waiter immediately. Matching Compose PR 708 uses the acknowledged wait before startup. [Current regression evidence](bazel-test-harness.md#foreground-init-attachment-development) includes fail-before generation/cancellation tests and the complete 244-native/127-gateway stock component result. Native history, live foreground parity and release-quality gates remain open; older prepared-foreground pending statements below are historical.

The exact enhanced initialization archive can now be imported byte-for-byte from retained release authority, including read-only sources, then verified offline. The real archive and all five image pins pass preparation; [source policy, provenance and regression evidence](bazel-test-harness.md#pinned-guest-image-preparation) supersede earlier local missing-image blockers. No registry fallback, repacking, source mutation or reference rebuild occurs. Fresh-machine distribution, enhanced live qualification and the current C02 foreground contract remain open.

The runtime-owned init attachment path now has a bounded HTTP adapter with effective stream selection, non-TTY `StdinOnce` EOF, split TTY detach keys and generation-checked resize. Real-descriptor regressions cover EOF behind another client's blocked write; HTTP regressions cover quiet-client cancellation, overflow/history errors and unusable stdin-only requests. Final independent source review is clean and the affected model/API/runtime components pass both profiles. [Exact evidence and remaining work](bazel-test-harness.md#foreground-init-attachment-development) leave native source-aware history, prepared Compose foreground integration and live parity open. Instrumented runtime coverage still has invalid counters; component success is not release-quality authority. No new stable build is claimed.

The committed `73938b6` runtime follow-up passes the complete Apple component in both profiles (219 stock/226 enhanced functions) without merger warnings. A separate 24-function Compose CLI run still produces invalid raw counters; ineffective serialization/atomic diagnostic attempts are recorded and reverted, with original raw evidence retained. [The scoped handoff](bazel-test-harness.md#process-io-cancellation-and-coverage-integrity) keeps this quality gate distinct from remaining foreground implementation and does not claim a new stable release.

The output follow-up replaces the separate direct-API reader with the same nonblocking reader used by CLI and PTY sessions. Completion joins are cancellation-resistant and follow reader-owned closure/callbacks. Terminal cancellation no longer aborts its real child-exit waiter. Exact fail-before regressions and both 32-function profile results are retained in the [I/O evidence](bazel-test-harness.md#process-io-cancellation-and-coverage-integrity). Prepared foreground attachment and full coverage qualification remain open; no runtime dependency or stable distribution changes in this follow-up.

C02 supporting I/O work fixes cancellation behind a full socket/PTY write and keeps output-reader descriptor closure on its owning worker. The fail-before regression is retained; this is not prepared foreground-attach completion. Enhanced test doubles now satisfy their process protocol and test file mounts according to the selected profile. Coverage checks and retained exports reject invalid counters, merger diagnostics and missing per-result logs. Full native instrumentation still emits counter underflow and cannot qualify release coverage. [Exact evidence and review findings](bazel-test-harness.md#process-io-cancellation-and-coverage-integrity) distinguish functional tests from the unresolved quality gate.

The latest packaged C02 stock proof is `1d5b7c55` at `35ced5d`, with all four observations and cleanup passing. The exact enhanced guest archive is authenticated against the public Compose 0.15.1 authority, but restoring its missing registry tag was denied by the saved credential's package scopes. [Current source, package and timing identities](bazel-test-harness.md#c02-dependency-health-and-service-selection) supersede the earlier stock-current paragraph below. No new stable release is claimed.

C02 partial-create recovery is component-tested, with live fault qualification still open. The epoch-bound receiver freezes new work and checks all HTTP handlers, long-lived execution uncertainty, native lifecycle activity and durable pending creation before permitting the harness to capture a fully owned partial inventory. Recorded CLI failure and stopped command groups are also required; exact identity is revalidated on every removal. No failed parity record is rewritten. [Recovery design and test evidence](bazel-test-harness.md#c02-dependency-health-and-service-selection) includes 375 Swift functions, 887 harness tests and focused instrumented coverage. This supersedes historical statements below that implementation has not begun; it does not close the live recovery gate.

Current C02 stock status is passing: `314e6af8-48cb-4f9a-b6bf-77715097361c` proves all four original assertions, including startup DNS, with zero errors and verified cleanup. [Exact immutable inputs, timings and authenticated seal](bazel-test-harness.md#c02-dependency-health-and-service-selection) supersede the historical stock failures and pending-live statements below. Enhanced proof, partial-create automatic recovery, prepared foreground I/O, quiet paired benchmarks and stable publication remain open; this is not full release qualification.

C02's prepared gateway path successfully started its dependencies but rejected the app's bind `CreateMountpoint` option. The candidate now carries the policy in backward-compatible runtime metadata and creates opted-in missing host directories only after non-mutating configuration preflight. Structured omitted/false policies retain existence checks; short host-path `Binds` opt in. Existing files are preserved, unrepresentable source syntax fails before directory creation, and caller-owned directories are never container-cleanup targets. Unsupported propagation/recursive semantics remain rejected. See the [C02 evidence](bazel-test-harness.md#c02-dependency-health-and-service-selection); no stable or full parity claim is added.

The candidate adds the explicitly negotiated `ContainerImageReference` create extension for prepared Compose requests. Immutable `Image` identity remains the launch input; the validated historical declaration is optional durable metadata used only for inspect/list presentation. This fixes the review-discovered ownership-check regression without reopening a mutable tag or weakening native digest-alias validation. [Affected tests and metadata recovery evidence](bazel-test-harness.md#c02-dependency-health-and-service-selection) pass; live C02 and foreground attached-I/O integration remain open.

The candidate gateway persists finite `StopTimeout` values, reports them in inspection and uses them for stop/restart when no query override is supplied. Omitted policy uses the Linux Engine ten-second default rather than Apple's five-second CLI default. Malformed query values fail before signaling; indefinite waits and values outside the native Int32 range remain explicitly unsupported. The [354-function affected gate](bazel-test-harness.md#c02-dependency-health-and-service-selection) passes, including metadata recovery, with 24/24 timeout-helper lines covered. This does not complete live stop parity or C02 gateway cutover.

The direct Engine create API now accepts and preserves `Healthcheck.StartInterval` through strict decoding, durable model data and inspection. Zero/absent values retain the five-second default; nonzero values below one millisecond fail validation. The existing inspect-driven evaluator uses the explicit startup cadence and chooses the next interval from the previous probe's completion, so delayed inspection cannot postpone an already-due startup probe. Success or grace expiry switches subsequent scheduling to the normal interval. [The C02 evidence](bazel-test-harness.md#c02-dependency-health-and-service-selection) records the pinned Moby references and 348 passing affected tests. Legacy Compose label policy v1 still rejects this field, and first-probe/background-monitor behavior is unchanged; this is not full Docker health monitoring or live C02 qualification.

Bare Engine environment keys now remove inherited image values rather than becoming empty strings. Creation, metadata recovery and inspection preserve the distinction; CLI fallback fails explicitly for removals. The [317-test affected gate](bazel-test-harness.md#c02-dependency-health-and-service-selection) passes with 20/20 parser lines covered. Live Compose gateway integration remains open.

Native execution-settings projection now preserves memory/shm bytes, read-only rootfs, sysctls and stop signal through creation and metadata. Invalid values and forced-sysctl conflicts are rejected; CLI fallback cannot silently drop these settings. [All 310 affected component tests pass](bazel-test-harness.md#c02-dependency-health-and-service-selection), with 86/86 lines across the three new helpers covered. Independent review is clean. Live enforcement, Docker cgroup accounting parity and complete Compose routing are not established by these tests.

Gateway process creation now preserves omitted versus cleared entrypoints and resolves defaults from descriptor-bound OCI configuration before launch and durable metadata. Native and CLI creation, saved metadata and recovery are covered by [297 passing affected tests](bazel-test-harness.md#c02-dependency-health-and-service-selection), with 17/17 resolver lines covered. This implements a prerequisite for lossless Compose gateway routing; live process parity and C02 qualification remain outstanding.

The gateway candidate now preserves explicit DNS configuration through create, native/CLI submission, inspection and both adoption paths, including legacy metadata. Validation rejects invalid nameservers and resolver-token injection; explicit BuildKit nameservers take precedence over host fallback. All 291 affected component tests pass with 25/25 DNS-model lines covered; strict touched-source lint and final independent review pass. [Exact evidence and boundaries](bazel-test-harness.md#c02-dependency-health-and-service-selection) remain distinct from the still-open Compose startup routing and live three-lane qualification.

The corrected stock C02 fixture passes every original DNS/health/runServices observation and cleanup at `64db1c6` (`c63c4ae3`). The strengthened four-assertion fixture now proves the remaining first-command DNS failure on stock Apple while Docker passes; both clean up with zero owned resources. The existing optimized package (`cdfeef29`) and unchanged Compose binary were reused. [Exact identities, timings and evidence](bazel-test-harness.md#c02-dependency-health-and-service-selection) supersede historical statements below. Identity projection is fixed, but C02 remains incomplete until Compose creation uses managed pre-start hosts. Enhanced qualification, quiet benchmarks and stable release gates remain open.

Current live C02 remains failing at `e8796ba`. [Retained diagnostic evidence](bazel-test-harness.md#c02-dependency-health-and-service-selection) isolates an identity-validation defect: Docker-label mirrors exist only in projected API responses, but the runtime required them on native resources. The correction accepts a complete native version-1 identity and rejects any conflicting optional mirror. Native-only helper regressions fail before the fix (`b6cf5194`); focused coverage passes 12 functions across two suites after it (`ddb0b277`), including native-only exec reconciliation, with 17/17 helper lines covered. Temporary runtime tracing has been removed. The harness has 40 passing focused reference/native tests; corrected live qualification remains pending. Older pending-live statements below are historical.

Gateway-native C02 hosts lifecycle integration is now implemented. Creation journals the owned specification before allocating a private read-only guest mount; the real SQLite transaction verifies exact completion identity. Startup reads the exact per-network allocation before launching the application. Mounted updates reject stale start generations. Native-absence removal resumes from retained metadata and active/retired storage, and neither inventory pruning nor replacement creation may erase unresolved ownership. Preparation failures retain the booted VM without issuing a native stop; adapter stop still removes forwarding. Actual application-start failures require removal/recreation. CLI-only managed restart fails before mutation.

Full affected stock Bazel coverage invocation `281af673-1106-428f-a952-a0cd141476d5` passes 190 test functions across 19 suites (27.634 seconds invocation elapsed, 9.322 seconds tests). This includes real SQLite restart/recovery, active/retired/removed cleanup phases, generation changes, missing/mismatched allocations, previously-started bootstrap preparation failure, and an existing ephemeral forwarding listener. Focused helper LCOV is 149/167 lines (89.2%); backing storage is 236/244 (96.7%). The two thin live-XPC adapters remain unexecuted in component tests and require live qualification; these are not aggregate coverage or release claims. Strict SwiftLint and independent final source review are clean. Compose typed-create routing, pre-entrypoint oracle coverage and stock/enhanced live proof remain pending. No new live C02 result, quiet benchmark or stable release follows from this checkpoint.

The earlier prepared-mount checkpoint (`0423457a-0c23-4e10-abf6-9c2c9583868a`, 25 tests) and storage-only checkpoint (`5decb4a2-1695-464c-a786-0c30a8148d33`, 11 tests) remain historical supporting evidence. [The startup design](../DESIGN.md#startup-time-name-resolution-remaining-c02-implementation) describes the integrated boundary and remaining cross-stack work.

Final running-state checkpoint validation: `513f3d0a-5e63-45a2-9744-78db52601143` passes 167 Apple runtime plus 79 unchanged/cached Docker API test functions in 9.386 seconds; the new identity helper records 17/17 executable lines. Focused `f91f887a` also proves listener cleanup when a process exits during a suspended hosts transfer. Full independent source review and strict lint are clean. Start/restart preserve fresh inventory -> forwarding -> hosts-copy ordering inside the queue. This remains supporting C02 work rather than runtime parity or release qualification.

The current supporting C02 change validates mirrored native Compose service identities, uses observed shared-network addresses, respects runtime start generations, serializes fresh-inventory hosts transfers and scopes exec-triggered writes to the requested target. Cancellation and stale-container checks protect asynchronous transfer boundaries. Startup/restart reuse the queued inventory rather than introducing a third inventory read. Focused invocation `861bf545-57c3-43e7-9233-6f2ac26df0e2` passes 13 test functions, including the existing startup read-count regression. Startup-time name resolution is not implemented: [the design](../DESIGN.md#startup-time-name-resolution-remaining-c02-implementation) records the rejected preboot-copy path and the required managed single-file mount. No exec-only result is treated as C02 closure.

Latest C02 stock run `1dca60f5` uses signed source checkpoints devcontainer `c7fe7d2` and Compose `c8a031de`, with both optimized candidate/archive checks passing. All three services start and database health is verified, but guest service-name resolution fails. Exact cleanup leaves zero owned resources and recovery is clear. The failed result remains sealed; existing managed-host reconciliation needs an external-Compose adoption boundary before C02 can pass. [The current evidence](bazel-test-harness.md#c02-dependency-health-and-service-selection) supersedes the pending-live checkpoint below and makes no stable-release or quiet-benchmark claim.

C02 preserves the original dependency DNS/health/runServices fixture, with exact sibling/network receipts and safe partial recovery. Harness gate `a117c4cb` passes 841 tests; Docker `2946308c` passes live, while initial stock `79d52880` failed on unsupported health flags and was safely recovered. The candidate now adopts a negotiated, bounded version-1 health-policy label through both native inspection paths; actual guest probes determine status. Interval/startup/failure semantics and unique reservation tokens prevent stale probe completion from corrupting restarted or removed state. Component coverage `9ed92b84` passes all 246 functions in the three affected targets, and full two-repository source review is clean. [Corrected live stock proof remains pending](bazel-test-harness.md#c02-dependency-health-and-service-selection); the inspect-driven bridge does not claim autonomous health scheduling or stable release qualification.

Current C01 stock proof passes all original observations and cleanup at devcontainer `aa56f00` with Compose `1fe36f56`, invocation `178bf269-d0d4-48e1-ac73-8ca375c2707d`. Both exact optimized packages and archive checks pass, no owned resources remain, and recovery is clear. The pinned Docker proof is retained unchanged. Enhanced qualification remains blocked on the missing pinned guest image, so C01 is handed off incomplete rather than fully Verified. [Current evidence](bazel-test-harness.md#c01-compose-service) records timings, seals and package identities; this is not release certification or a quiet benchmark.

### Historical implementation checkpoints

The records below retain earlier checkpoint status. Their pending C01 stock statements are superseded by the current proof above.

The C01 correction now separates persisted network wire/native IDs across lookup, ownership, container attachment and deletion; projects actual endpoint membership and Compose labels; and validates digest-image spelling against the native creation descriptor before metadata adoption. Matching Compose PR 708 adds the logical network key and reserved original-image label, with no Docker dependency or extra registry lookup. API/Core and complete stock Apple-runtime component tests pass; [exact evidence and limitations](bazel-test-harness.md#c01-compose-service) distinguish implemented fixes from still-pending live/native qualification. No fixture, cleanup assertion or release gate is weakened.

The first C01 stock run (`8b876ed0`) started its service and post-create hook but failed verification; exact resources and Engine were recovered and prior services restored without altering the failed result. The confirmed project-name cause is numeric-version namespace collision: the official CLI interprets native `0.15.1` as old Docker Compose. Native short probes now return vendor-qualified output, preserve failures/diagnostics and reject malformed success without state creation; other forms and explicit Docker are unchanged. Focused coverage `0eb79010` passes 19 functions and all 30 added executable lines, with unrelated LCOV overflow warnings explicitly excluded from aggregate claims. Network/image gaps remain active. [Exact evidence](bazel-test-harness.md#c01-compose-service) supersedes earlier pre-live status.

C01 native artifact admission and execution are now implemented: explicit Compose-family preparation, matching-profile/provenance checks, repeated archive revalidation, private facade config/state and original service/network cleanup. Existing stock Compose archive `1edbd835` is authenticated and reused; devcontainer archive `a52d8072` carries `fd74630` without a second build. Preparation tests pass 104 (`83ce449e`), runtime components pass 819 (`6f3c6c08`), and focused trace evidence is recorded in the [C01 contract](bazel-test-harness.md#c01-compose-service). Live stock/enhanced results remain pending; previous admission-unimplemented statements are historical. Makefile and launcher expose the same explicit candidate inputs.

C01 native selection now defaults omitted provider choices to separately installed `container-compose`, preserves explicit Docker settings and forwards the resolved socket/runtime executable to every native child, overriding inherited Compose-specific selectors. Red `87994af7` becomes green in focused coverage `6637ba72` (33 functions); native C01 admission/live proof remains pending. README, both user guides, design, compatibility and the Docker-free repair design distinguish unreleased behavior from published 1.0.1. [Focused coverage counts and remaining qualification](bazel-test-harness.md#c01-compose-service) are explicit, including the Compose command's below-target whole-file coverage. Earlier default-selection pending checkpoints are historical.

C01's pinned Docker reference now passes all three original observations and cleanup at `e59a071`, invocation `4dcb1ff0-e06e-474e-95ec-7fe5fb671bd3`. No owned resources remain and recovery is clear. The fixture operation takes 1.809207166 seconds; separate VM setup/cleanup delays are retained and are not quiet benchmark evidence. C01 remains Active for Docker-free native provider/default selection and stock/enhanced qualification. [Exact evidence](bazel-test-harness.md#c01-compose-service) supersedes the historical pre-live checkpoint below.

C01 Compose-service configuration is Active. The reference adapter preserves fixture bytes and the three original assertions, uses the already-retained pinned Compose release, and binds service/network cleanup to exact project and resource identities. Admission, runfiles, fingerprints, diagnostic retention and completed-command recovery are connected. Component gate `292a77dd` passes 802 tests; review regression `9c0566c6` prevents deleting a reappearing closed network. Live Docker/native proof remains required, and native C01 currently rejects before mutation rather than using a Docker fallback. See the [C01 contract](bazel-test-harness.md#c01-compose-service). Earlier D07/other checkpoints below are historical.

D07 stock now passes all six original observations and cleanup at `53aafe4`, invocation `d3a775e0-6b1f-4bc5-ad82-18d86a911e32`, using one prepared release-mode archive (`1e56eae2`). Zero owned resources remain and recovery is clear. The Docker reference also passes; enhanced guest publication remains the explicit blocker, so this is handed off incomplete rather than full three-lane Verified. [Exact timings, fingerprints and limits](bazel-test-harness.md#d07-reuse-and-cleanup) are retained. C01 Compose service is next. Earlier pending-proof statements below are historical.

Current D07 correction: stock invocation `87b56dd6` now creates and reuses the named-volume container, then exposes missing `rm -f` during rebuild. The frontend implements explicit force plus one full ID through the private socket, without volume deletion or Docker fallback. Red `0847d062` becomes green in coverage `42086676` (75 functions; frontend 95/95 and command 161/161 lines). Full independent product/recovery review is clean; exact failed resources were removed and services restored without rewriting evidence. One new package and live rebuild proof remain required. [Current evidence](bazel-test-harness.md#d07-reuse-and-cleanup) supersedes the historical checkpoints below.

D07 Docker now passes all six observations at `be380dd2`; stock's first run `f3177942` exposed missing named-volume parser support before create. The candidate now projects typed named mounts through the existing native API, preserving bind defaults and rejecting unsupported syntax. Bare-source review regression `b67be500` is fixed; final frontend coverage `dd4683df` passes 71 functions and 178/178 run-parser lines. Strict touched-source checks and complete independent product review are clean. The isolated failed case was recovered with original services restored and evidence retained; corrected stock live proof requires one new package. [Full evidence and recovery limits](bazel-test-harness.md#d07-reuse-and-cleanup) supersede earlier pre-live status below.

D07 reuse/rebuild/cleanup is Active. The original six observations are implemented through separate bounded official-CLI commands, generation receipts, label/creation-bound volume ownership and explicit absence checks. Native repeated-up attachment lifetimes and Docker completed-command recovery are integrated. Component checkpoint `b19872df` passes 774 tests; final focused fault additions and live oracle/candidate proof follow. Product bytes are unchanged and package `15d10e7d` remains reusable. See the [D07 contract](bazel-test-harness.md#d07-reuse-and-cleanup). Earlier D06 status is historical.

D06 now passes its four original observations and cleanup on stock Apple at `295489b`, invocation `ecf30eb5-335e-4144-8747-65c91e32975b`, reusing package `15d10e7d` without rebuilding. Zero errors/resources remain and recovery is clear. Together with the retained Docker oracle, this proves the bounded publication contract on two lanes; enhanced qualification remains handed off incomplete on its exact guest input. [Exact timings, seals and limitations](bazel-test-harness.md#d06-published-ports) are retained. D07 reuse/rebuild/cleanup is next. Earlier pending-native paragraphs below are historical, not current status.

D06's Docker oracle passes all four observations and cleanup at `b5b60e0`, invocation `524dce17-469a-4819-9258-a210a414d7cb`, with clear recovery. The product delta now projects captured `-p`/`--publish` explicit IPv4/fixed TCP requests and native inspect host bindings through existing runtime authority. Red component proof `5a94e51a` becomes green in coverage `8b71d8e8` (69 frontend/64 API test functions, run-parser 157/157 lines). Static checks and independent product review are clean. Native live proof remains required; see [D06 evidence and limits](bazel-test-harness.md#d06-published-ports). Earlier pre-oracle statements below are historical.

D06 ports is now Active: the replacement checks actual CLI forwarding metadata, complete bounded host HTTP, guest HTTP, a specific start-time port collision and ownership-bound cleanup. The Docker-only VM forwards just the fixture loopback port using its existing owned SSH lifecycle; other fixtures and native lanes are unchanged. Real-socket tests cover incomplete/ambiguous responses and slow delivery, with admission/recovery tests for the D06 path. All 741 component tests pass at `5eb873b1`; focused trace covers 141/147 adapter lines and complete independent review is clean. Live reference/native proof remains required; the native frontend's publish option is a pending product gap. See the [D06 contract](bazel-test-harness.md#d06-published-ports). The D05 and earlier status paragraphs below preserve historical checkpoints.

D05 now passes its four unchanged Features/lockfile observations and cleanup in Docker (`9cd52ed5-55cc-4245-8058-8671ba0e948d`) and stock Apple (`f80ebfc8-b5c8-45d1-b155-b6a1e9006e2e`) at `f3841fb`. Stock reused product `533f9a7` without rebuilding. Zero errors/resources and clear recovery are recorded; separate functional campaigns do not qualify quiet timings or the full three-lane matrix. The exact enhanced guest still returns `manifest unknown` (19 September 05:39 BST), so D05 is handed off incomplete on that prerequisite. Final review is clean; 693 component tests pass and focused adapter trace covers 92/96 executable lines. [Exact timings, seals and documentation dispositions](bazel-test-harness.md#d05-locked-features) are retained. D06 ports is next. Earlier pre-live status and failures below are historical.

D05's first live negative run exposed mixed JSON/plaintext stderr in the official CLI, not a lockfile-conformance failure (`867e99e6-a7cc-417d-9d1c-71c3a33b2516`). Cleanup passed and recovery is clear. The retained failing case is not rewritten. The corrected reader matches the exact structured stdout message plus plaintext error line; deterministic regression and component gate `bf4a2f55-bb44-4039-a4bc-f8ead1fd4a96` pass. Positive reference/native Feature installation remains pending.

D05 Features is Active: reference/native adapters preserve the unchanged Ubuntu image, locked Feature versions/options and three guest observations, with an additional specific missing-frozen-lock rejection before positive creation. Both commands use frozen mode; unrelated failures cannot pass the assertion. Image ancestry/metadata, owner/workspace identity, command journals and retained negative diagnostics are checked. The admitted Ubuntu archive adds no product build. Component gate `3aa51759-293c-4a61-90ca-a1f0255a8b0b` passes 689 tests; real Docker and stock execution remain required. See the [D05 contract and evidence](bazel-test-harness.md#d05-locked-features). The following D04 material is historical checkpoint evidence, not the current active contract.

D04 now passes its unchanged lifecycle observations and cleanup in Docker (`7c41a35a-01bc-4aaa-b866-2a580cb3c560`) and stock Apple (`76ebd27c-8b98-4df1-969b-fa12c3f2e159`) at harness `57ab04c`. Docker setup/operation/cleanup are 18.216034667/0.736704542/2.895339167 seconds; stock is 6.548827792/2.733246333/1.346782375 seconds. Stock reused the `533f9a7` product archive `f3afc1a2-9e75-4464-ba17-5386abd1e8fe` without rebuilding. Both runs leave zero owned resources and runtime recovery clear; scoped reports correctly remain incomplete. Full three-lane parity is handed off incomplete on enhanced guest publication. These are functional observations, not quiet paired benchmarks. D05 Features is next. Historical pre-live and failed-gate details follow.

D04 live reference initially exposed a common harness bug, not a hook difference: the observed host marker/order exactly matched the original fixture, but expected-value loading lowercased the hook names (`9dd92996-0220-49a9-b668-d532efee6976`). Cleanup passed and recovery is clear; the failure is retained. A shared scalar serializer now preserves strings and only formats JSON booleans, used by native/Docker execution and report loading. Red report regression `07d0191d-32af-47da-a7e2-2c893160df27` becomes green in the 639-test gate `707b9f03-8a13-4e1e-9cc3-e2ce1f031331`; an incorrect lowercase observation remains a failure. No fixture/output normalization or product rebuild is introduced. Corrected live reference and stock evidence remain required.

D04 lifecycle hooks is active. Reference/native adapters reuse D01 image ownership, command lifetimes and bounded normal cleanup while preserving the original host marker and exact five-hook order. Docker's completed-command cleanup-only recovery also handles D04; interrupted native phases still require explicit reconciliation and remain quarantined. Hook configuration and probe are bound into the shared fingerprints and both runtime runfiles sets. Candidate admission requires the private bundle but does not provision a builder. Component tests inherit malformed-output, uncertain/replaced-resource and cleanup-deadline coverage; live Docker reference and stock-candidate evidence remain required. Product bytes are unchanged and the retained `533f9a7` candidate will be reused, not rebuilt.

D04's focused harness gate passes all 637 tests in 10.769 seconds (`b2fbd9c5-b5e1-4d65-bb32-5933502b0fc2`). A focused 13-test trace executes 17/17 lines of the new reference module; this is adapter coverage, not whole-product coverage. Independent source/test review is clean and confirms that the pinned CLI runs host initialization with the disposable workspace as its working directory. These component checks are not a substitute for live lifecycle execution.

Stock D03 now passes all seven unchanged user/environment observations and cleanup at `533f9a7e268c894c3e86f5dde0ca78b570a6628f`, invocation `495adb97-3b5e-435f-83ec-2e9bccd9b674`; raw setup/operation/cleanup are 11.146059708/6.883518042/2.382704250 seconds. No owned resources remain and recovery is clear. It reused the verified archive from `f3afc1a2-9e75-4464-ba17-5386abd1e8fe` without recompilation (SHA-256 `38e3080df0fb733af7e74485cddfd37f12b21e45c697c64302755ba813ef5c9a`). The build observation is 25.707116042 seconds with quiet-host verification false, not a benchmark. Docker proof remains at `caae5f4d-2d1f-4cb2-b9ae-3d54b5b13bde`; separate campaigns do not certify paired timing. The enhanced guest tag still returns `MANIFEST_UNKNOWN`, so D03 is handed off incomplete on that dependency. D04 lifecycle hooks is the next independent fixture. Complete parity, release quality/signing/Homebrew and final documentation publication remain open. Following paragraphs retain the pre-live component checkpoint.

Final D03 candidate component review is clean. Frontend coverage passes all 66 tests (`1a8b1255-f8c3-4e38-987d-f3386b9b2035`) and executes 127/127 lines in `DockerRunCommand.swift` (100% focused coverage, not whole-product coverage). Touched Swift formatting/static checks, Markdown lint and diff checks pass. The candidate package and native live run are the next proof boundary.

D03 reference component evidence: all 585 harness tests pass at `14c62e5f-9070-4290-bafd-410648ddb938`, 26/26 new adapter lines execute in a focused 15-test diagnostic trace, and independent complete-diff review is clean. These results do not replace live oracle/candidate or release proof.

D03 users/environment is active. The Docker reference now passes all seven observations and cleanup at `d0ad0aa`, invocation `caae5f4d-2d1f-4cb2-b9ae-3d54b5b13bde`; raw setup/operation/cleanup are 257.331951208/1.076954209/32.420980042 seconds, not quiet benchmark evidence. Its captured `-u vscode` now maps to native Engine create `User`, with missing/NUL/duplicate rejection before mutation and absent-user image defaults. Red parser/executable regression `b7cd9008-c8bd-4c16-b6cd-e2c16f09aec9` reproduced the missing option; green `6981a844-af79-4d0e-83bb-7f628aee4141` passes 66 frontend tests. Candidate D03 reuses D02 build ownership and cleanup, raw metadata checks and non-root workspace permissions. Independent review identified a reference-only inherited deadline test; reusing the existing candidate deadline test fixes the 60-second component timeout (`74f16700-016b-4d97-8c61-a600d557cb26`) without increasing limits. All 603 harness tests then pass in 9.659 seconds (`2be0d1e9-ce03-4ac6-a88d-466bf3a91503`). Native live qualification, quiet benchmarks and release gates remain open; see the [contract](bazel-test-harness.md#d03-users-and-environment).

Stock D02 now passes all four unchanged observations with verified cleanup and zero remaining owned resources at signed source `106144abaf5c68e21acb839211bf2c6a5f5b2280`, invocation `3530b684-a77e-42fb-b1a0-11e4c507e401`, campaign `candidate-d02-native-20260919a`. Setup/operation/cleanup took 11.082251250/5.716879250/2.264501042 seconds. Runtime recovery is clear. One optimized package build (`395f1178-2659-455c-9fb0-0bcb6e2671b4`) passed the real archive smoke tests in 25.987 seconds Bazel elapsed, with 1,235 action-cache hits and 28 executed actions. The archive SHA-256 is `aaeb1290b4a8803d6301d445e696422cbf752ec0ca6a72de4da52bbe51c0bcb9`; preparation and live execution reused it without recompilation. The Docker proof below remains reusable functional evidence, not a paired quiet benchmark. The three-lane D02 contract is handed off incomplete on enhanced guest publication, not Verified. Full parity, release quality, signing, Homebrew, documentation publication and VHS gates remain open. The following implementation paragraphs describe the pre-live checkpoint.

D02 Docker reference passes its four unchanged observations and cleanup at `05cae9e`, invocation `0686509c-bf9c-4ba3-a405-02fb0ebca041`; setup/operation/cleanup are 17.683057833/1.214592000/2.948016291 seconds, not quiet benchmark evidence. The Docker-free frontend now implements the captured build command through the shared streaming HTTP client and existing owned-process runner. Generated external Dockerfiles receive archive-only exclusions; local files retain their context paths. Bounds, explicit unsupported-option/ignore-file rejection, streamed failures and signal-driven joined cleanup preserve failure semantics. Candidate D02 reuses the reference observations and native attachment lifetime with released-builder admission, pre-build identity/DNS verification and guest-before-builder cleanup. Native image inspection now projects descriptor-bound OCI rootfs diff IDs through a backward-compatible optional snapshot field. Live candidate qualification and release gates remain outstanding; full interrupted-build recovery is not claimed.

Independent review found and resolved external Dockerfile inclusion in `COPY .`, temporary-path alias containment, missing signal-to-task cancellation and absent native image ancestry. The interruption regression also exposed Foundation temporary-directory caching; staging now explicitly honors the workflow's SSD `TMPDIR`. A real SIGTERM initially crashed on an inherited-main-actor dispatch callback (`84cb0a0c-3ab6-4097-86ba-a31d2740a9b6`); explicitly sendable cancellation now passes with clean stage removal (`85e88be4-a92f-4787-9949-5dd158c51708`). All five affected component targets pass (`5d2219b7-fb30-4a10-b66b-f7204c120f3e`), including 564 harness tests and native image/API/model regressions. Final frontend coverage (`b482306a-a3c9-4813-b4f6-f1e77792bc0f`) passes 65 tests and measures archive 109/113 lines (96.5%), parser 70/70 (100%) and streaming build 57/58 (98.3%). Native image identity measures 144/158 (91.1%) in the component run. These focused measurements do not establish product-wide coverage or live parity. Final immutable packaging/live proof follows. D01's enhanced dependency is unchanged at the 19 September 02:06 BST registry/release recheck. The paragraphs below retain historical implementation checkpoints, not current completion claims.

After extracting streaming-command dispatch to keep the parser within the complexity limit, final frontend coverage rerun `ed24e1ad-6dc2-49b5-bb41-7a29d75ee9f1` passes the same 65 tests with unchanged new-module percentages. Strict lint passes affected production and test sources. `Package.swift` retains two unrelated pre-existing findings (the six-parameter runtime dependency helper and long process-test dependency list); the final repository quality phase owns their resolution before release. Touched Markdown lint and independent final-diff review pass.

Corrected live stock D01 passes all four unchanged observations and cleanup at harness `e346eed`, invocation `46a33e35-686a-45f4-9168-80ba6ecafa31`, campaign `candidate-d01-native-20260919b`. It reuses retained product `f8dc210` without rebuilding. Setup/operation/cleanup: 6.772554542/2.210318375/1.615386041 seconds; no owned resources remain and recovery is clear. These are functional observations, not quiet benchmarks or complete three-lane/release qualification. The following paragraphs retain the implementation and failed-run history.

Live stock D01 exposed an attachment-lifetime harness bug, not an `up` failure: the CLI returned success and post-create ran, but its foreground attachment remained active as designed. Cleanup now retries known-completed command diagnostics before resource reconciliation, removes only the authenticated guest, then verifies helper-group closure and seals diagnostics. One outer cleanup deadline avoids nested alarms. Real subprocess, log-retention-failure and actual cleanup-wrapper regressions pass in the 525-test component gate `18429076-b9d2-49e8-b7e4-f5ea344f049c`; independent source review is clean. Failed live run `85a573e8` remains recorded as failed and was recovered without replaying commands or rebuilding the retained product.

Native D01 now admits the already prepared schema-2 candidate bundle and runs its public CLI against the selected private stock/enhanced Engine. It reuses the reference fixture's exact configuration, probe, image digest and ownership checks; native UUID identities remain unmodified. Image preparation is outside the measured `up`/`exec` operation. Commands have durable intent, separate output streams, exit/verified-group-stop receipts, bounded retained diagnostics and fail-closed recovery. The prior adapter checkpoint passed 522 component tests (`e34f5f9c-6e7f-43dc-8e73-56fc4f7fbd26`); its historical focused trace covered 86/86 candidate-adapter lines in 71 tests, before the attachment cleanup changes above. The Docker D01 path remains distinct and unchanged. Successful live candidate and release qualification remain outstanding; no source product rebuild is needed to test the retained `f8dc210` package.

The native unsigned archive now includes all four project executables and private prebuilt Node 24.21.0 / official CLI 0.88.0, with their licences/notices, exact download checksums and per-file receipt digests. Schema-2 admission binds the runtime lock to captured source and preserves schema-1 preparation identities. Real private-runtime package smoke passes 13 cases on stock (`5644b329-ce50-4d24-a396-885ba650bdca`) and enhanced (`d22e3623-b98c-4348-946f-ed329c171f7e`); configuration reading uses the real Node/CLI/frontend and isolates only the Engine inventory response. The enhanced checkpoint also passes 97 preparation tests, including changed inner-runtime payloads, and native Starlark lock validation. Review exposed an empty-integrity bypass; guard removal fails at `35ec502a-b5bf-4dd1-ab74-c2f6c33e0808`, restored exact-SRI validation passes. Earlier `b7b70a6c`/`980cf6d0` package failures omitted the inventory socket needed by the reference CLI and remain recorded as failed. Package/source tests do not qualify candidate D01, signing/notarization, Homebrew or a release. Earlier paragraphs below preserve historical checkpoints, not current completion claims.

Enhanced facade proof `7b286cf1-9e2b-4a50-9b1e-9f29b4e8be4a` passes the same 55-function CLI suite. Both profiles now require that inventory and execution in `LifecycleCommand.swift`; source and documentation reviews are clean. No private upstream bundle or stable release has been published by this checkpoint.

The public lifecycle forwarding boundary now registers `up`, `build`, `exec`, `read-configuration` and `run-user-commands`, preserves pass-through options/terminators/help, and replaces the process with installation-private Node using exact project frontend paths. It requires contained regular assets, supports relocated and plug-in entry points, removes Node/DYLD startup hooks and rejects both dashed and upstream camelCase backend-option overrides before `--`. Independent review's alias finding is covered by red `f7c238af-536e-4bfd-a028-24fd544343ef`; corrected stock coverage passes at `6e6aee2b-48ea-4dea-ae56-33bd2d61d0ea` (55 CLI test functions, 0.953 seconds; new facade source 75/82 lines, 91.46%). Exact restored source reuses that result at `845f37bb-8ced-4007-bf01-412081ef22f9`, without redundant execution. The actual executable test uses a shell stand-in for Node and proves forwarding/stdin/stdout/stderr/exit preservation only. Initial `aa6ace55`/`152e1b03` failures came from copying a Bazel runfile symlink rather than a relocated binary; the test now copies its resolved bytes. Real private reference packaging, candidate D01 and release qualification remain incomplete.

Enhanced startup/events result `41683683-4f7b-430e-87f1-3737ffad5794` also passes the same 55-function frontend suite. Both profile policies now require all 55 functions and positive execution in all eleven client source files, without lowering the aggregate quality gate or substituting this component evidence for live parity.

Foreground startup and JSON events now have real-executable/private-socket proof with no external clients on PATH. Startup preserves supported mounts/environment/labels/entrypoint, attaches before start and observes terminal exit after output EOF. Independent review caught a deadline gap after early EOF: regression `6f0c939a-a51b-49be-9fe9-80b2e0ce6624` fails on the earlier implementation; corrected run proof `8c6af83d-a252-4bec-bde4-aff4ea0d858b` passes with one deadline across the whole operation and a separate long-poll wait transport. Failures close attachments without deleting the caller-owned container.

Event streaming preserves original numeric JSON bytes, keeps at most one bounded partial record, applies pipe backpressure and joins the synchronous callback on cancellation. Shared source `f32e1829d0f0293bd68a69a7a6f93f67953c31e9` prevents HTTP error documents entering successful streams. Final stock component result `18b0064d-e568-4388-a376-4d6b0e66b751` passes 55 functions (5.031 seconds test execution); the eleven client files cover 824/837 lines (98.45%). The collector's unsigned-counter warnings were traced through the retained raw LLVM profile to external NIO `CNIOLLHTTP/c_nio_llhttp.c`, lines 332-335, not these maintained client sources; the original log/profile remain available. This is focused development evidence, not whole-product coverage, a quiet benchmark or live parity. Complete source/test independent review is clean. Facade/private CLI production packaging, candidate D01 execution and release gates remain outstanding.

Enhanced frontend coverage invocation `fb5b7ebf-472f-467d-9b99-2b4f0d97e7a9` also passes all 37 test functions (5.025 seconds test execution); the invocation and input validation both exit zero. This is focused development evidence, not a quiet benchmark or enhanced live-runtime qualification.

The Docker-free frontend now has non-TTY exec over shared source `2bff309d7ec93e5063ccfa246851903172a0da29`: recorded upstream argument forms, incremental multiplex decoding, stdin half-close, distinct stdout/stderr and inspected exit status. Independent review found and corrected native UUID rejection, premature failure while the stopped state was still being published, and an uncancellable full output pipe. Real executable/private-server tests use a native UUID and an empty external-client PATH. Delayed and never-published terminal state, invalid identity/status, blocked output, unchanged inherited flags and pending-stdin cancellation have focused regressions. Stock coverage invocation `194c16ea-d7d5-4c45-afeb-729e575cb488` passes 37 test functions in 5.6 seconds (including the deliberate five-second terminal-state timeout); eight client files cover 552/559 lines (98.75%). This excludes the separately linked executable entry point and is not whole-product coverage, a quiet benchmark or live D01 parity. Earlier coverage paragraphs below remain historical component evidence.

Failed output experiments `53a205d3` and `fced1ff1` remain retained: per-descriptor no-SIGPIPE settings changed the inherited open-file description, while thread-only signal masking was insufficient on this host. The final executable owns its SIGPIPE disposition; the reusable writer changes neither signal policy nor inherited flags. `4e8248a2` exposed XNU's first-write bookkeeping flag in the test baseline; the regression now establishes that state with an ordinary write/read before comparing all flags, without masking caller-owned values. Current source/fixture review is clean. Container startup/events, TTY, facade/private CLI packaging, both candidate D01 lanes and release gates remain incomplete.

The development-only `devcontainer-docker` executable now handles the exact metadata commands observed in D01 through the shared user-owned Unix socket, without discovering or executing Docker. It preserves inspect payload bytes and rejects unsupported commands, flags, remote hosts and implicit context fallback. Native stock coverage invocation `4324e152-a8b4-41b5-ba93-26f76a631c5c` passes the focused frontend suite, including actual executable tests with no external clients on PATH; the three new client source files cover 251/251 executable lines. This is focused component coverage, not whole-product coverage or live parity. The dependency pins source `58772d295fc044f080a966dc710b46b88392594d` from [shared client PR 45](https://github.com/stephenlclarke/container-engine-api/pull/45), which corrects absolute deadlines, cancellation, framing bounds and cooperative-executor starvation. Interactive execution, container startup/events, private CLI packaging and lifecycle-facade integration remain on the same active D01 critical path; both candidate lanes still refuse admission and no stable compatibility claim changes.

Docker D01 now passes all four unchanged observations and verified cleanup at source `c6daa35fde14da9de59f080136865728c92841ac`, invocation `83e7687b-5db8-4788-8dc2-5cf3b33ca656`, campaign `docker-d01-native-20260918c`. Retained setup/operation/cleanup durations are 20.340733000/0.744793291/6.458950417 seconds; host recovery is clear. Final 68-test trace reports 95% rounded reference-adapter coverage, while the preceding full harness passed 500 tests. Independent final code/doc review is clean. This closes the Docker reference portion only: both candidate D01 lanes, quiet paired benchmarks, remaining fixtures and stable publication are still outstanding. Earlier failed attempts below are historical, immutable results, not current qualification status.

The second D01 attempt at `60490e4` completed both commands and cleanup, but the CLI's JSON logging mode routed probe output into stderr (`9217930a-b5c9-4d18-aa86-8b525d369ab0`). The adapter now restricts JSON mode to `up` and uses ordinary `exec` stdout, matching the original fixture runner; a focused command-contract regression locks this distinction. No payload values are filtered or normalized and the failed record is preserved. The shared stream correction passed the full harness at `006b5789-509c-4502-bb19-db78b8223aed` before this argument-only adjustment.

The first D01 live reference run at `97a8b46` failed output parsing after successful CLI creation, with cleanup verified (`94cc76fa-ba5b-48a0-92df-830aa28c1d7c`). Its retained log identifies a Node deprecation warning interleaved with stdout because the VM runner merged streams. Optional separate capture now retains both stdout/stderr plus individual bounded metadata; D01 result/probe parsing consumes only stdout and rejects unexpected records. Warnings are preserved, not silenced or normalized. Real process regression verifies the distinction. The original failed case is immutable, and corrected live proof remains pending.

The native D01 reference adapter now runs the unchanged fixture through pinned official CLI `up`/`exec` in the owned Docker VM, preserving all four observations. Exact case label, image and workspace ownership precede ID-only cleanup; original preparation/fixture bytes join the comparison fingerprint. Whole-phase deadlines, per-command timings, bounded retained logs and cleanup-only interrupted-command recovery reuse the existing VM/journal path. Candidate D01 fails before runtime mutation until the Docker-free frontend exists. Component gate `6160f0b0-1ba9-4b96-91a9-a6d2523dc302` passes after fixing one test assertion that incorrectly compared two equivalent guard objects by object identity. Independent review identified and resolved missing CLI log retention and missing normal-phase deadlines, with actual subprocess and slow Unix-socket regressions. README, harness and DocC Testing document execution/recovery; public product help, DESIGN and COMPATIBILITY claims remain unchanged. Live D01 reference proof and complete three-lane qualification are still pending.

D01 image configuration is now the active native harness contract; immutable reference-tool preparation is its first supporting boundary, not a completed parity slice. `make bazel-prepare-devcontainers-cli` prepares exact official CLI 0.88.0 and private prebuilt Node 24.21.0 with shared leases, bounded integrity-checked downloads, selected Node binary/licence extraction and recoverable immutable retention. Offline verification never fetches or repairs. Official npm/Node distributions are an explicit GitHub-asset sourcing exception, and neither tool becomes a candidate product dependency. Seventeen focused tests and all 92 tests in Bazel preparation gate `d7c1634e-2b12-415e-815b-1385fe8496d3` pass; trace coverage is 142/154 new-module and 26/27 selected-extractor executable lines. Real acquisition, identical offline receipts and isolated version probes pass without starting a VM. README, launcher help, Makefile, harness instructions and DocC Testing describe this boundary; public CLI, DESIGN and COMPATIBILITY claims are unchanged. D01 execution/cleanup, Docker-free candidate frontend, enhanced inputs and final release gates remain outstanding.

The native F01 Docker reference passes all five assertions and cleanup (`17d5908b-93f2-49db-9a1c-6db8f2778157`). Stock F01 failed after wait/inspection with cleanup verified (`3dfb2d73-bcfc-484d-a2bc-5cbf852994f8`). The protocol adapter's separate `stopped` versus Docker `exited` defect is corrected with all five model states covered (`ba72830e` / `16b81bc5`); the DTO label assertion now requires the supplied label. That spelling fix did not resolve the live failure. Private observations from `a63ac21f-4b33-4cfd-8402-16d6b2eea5d9` show inspection still returning `running`/0 after wait: Apple's process completion precedes its exit-monitor teardown. The wait path now reinspects native state before returning a recorded exit, respecting the request deadline instead of fabricating a stopped snapshot. The lifecycle regression fails at `404f3135-f2fe-43e3-b25e-482b32085398` and passes at `e6486f0c-f3c2-4a61-a00a-0b54a5c77c4f`. Enhanced live qualification remains required; the subsequent stock result is recorded below.

Stock E04 now passes the full positive/negative build contract and cleanup at source `378a5b0`, invocation `0acf86ea-3bc4-47e5-8468-fba047e51352`; host recovery is clear. Docker E04 already passes. Enhanced qualification remains concretely blocked by the recorded host access and matching guest-release closure, not waived. The next independent native harness contract is F01: concurrent start, separate never-started removal race, exact TERM result, bounded events and missing-backend behavior. It reuses journalled guest ownership and adds absolute socket deadlines for worker threads; final review identified both worker liveness and lost created-state removal coverage, now corrected with real socket regressions. Full parity and release completion remain outstanding.

Review of the F01 wait barrier also identified exit-code loss when automatic removal wins reinspection. The corrected waiter retains the observed exit across removal and rejects observed replacement identities; regression `582ea484` fails before this correction and `efa5188b` passes after it. Full stock/enhanced runtime components pass at `daa193d5` / `b0f37825`; the changed wait and finish helper measure 56/57 covered lines (the earlier 63/64 region included adjacent unchanged helpers). Stock live F01 now passes all five assertions and cleanup at `d996ac7c-d3d4-494e-a26a-793dfa800c89`, source `a16379b`, using package `676e2cd7`. Recorded setup/operation/cleanup are 5.335279500/2.733305750/1.296612333 seconds. This is not quiet paired timing, live enhanced parity, or stable-release qualification.

Captured-client recovery completes the new spawn-identity boundary: a separate `--stop-client` phase verifies the original binaries and all build/helper closure before a single bounded `TERM`, rejects PID reuse and recursive descendants even across process groups, and retains final private Engine diagnostics before root disposal. Report mode sends no signal; uncertain prior signals are never retried. Independent review caught and corrected the cross-group descendant and missing-log-retention cases. This automation is intentionally unavailable for legacy PID-only records. Stock case `76fd3dcb` was separately authorized by the operator, its builder/Engine were stopped, original services restored and owned root removed; host recovery is clear. This is recovery proof, not a retroactive passing test or completed stable release.

Stock E04 reached all three intended build observations at `442cfce7-4b44-43b0-b04c-097afb2ea102`, but failed cleanup because the product ignored image label filters. Named output deletion itself succeeded. The router now honors label predicates and returns real labels (red `594d448b`, green `ec16f7d2`). Resource-only Apple recovery authenticates already-absent outputs, the original captured builder and prior helper closure before scoped builder cleanup; it never signals the legacy Engine, restores services or clears quarantine. Independent review found and corrected missing prior-helper closure and missing builder-creation preconditions, with regressions. New cases capture the live child incarnation before readiness so future recovery has durable process identity; this does not backfill old evidence. All 441 component tests pass at `6df5a45a-1282-45b8-acff-e4722c1071b9`. Full stock cleanup/live parity and stable publication remain open.

Docker E04 now passes (`8281d69f-8045-4b52-b816-a4de3cf2745a`), including actual failed-command execution and zero-residue cleanup. Stock candidate `0775064a-b1ae-4530-aac9-3b7bcc1a80e5` provisioned its released builder but stopped before building because inspection omitted repository-qualified manifest metadata; its cleanup passed. The correction keeps manifest/config IDs distinct and uses native named image removal without garbage collection. Explicit private-store harness cleanup binds mode, tag, config and manifest in its journal, refuses uncertain completion/replacement/shared references, and never falls back from Docker full-ID removal. Public digest mutations remain unsupported. Stock/enhanced image-boundary tests pass (`d65ddfe8-500b-4792-a13c-a9194696394f` / `11762c34-afe1-4455-914a-6cb0f972a968`); 431 harness component tests pass at `2c958e33-e199-4db1-ac1d-e9e84c666ee5`. Focused missing-line trace reports 96% image transaction and 100% build adapter coverage. Live stock/enhanced proof, quiet paired timing and release qualification remain open.

The next live E04 Docker attempt (`4b375881-126d-4d38-af1e-1ded0ec11d69`) built its positive image but failed cleanup because containerd assigns a repository digest to local build output. Exact owned-repository/content-ID/descriptor matching now admits that metadata without accepting foreign/shared images. A cleanup-only recovery path verifies the original downloaded closure, PID incarnations, response bytes and exact image ownership before image deletion and owned VM shutdown, then uses existing verified root cleanup. Classic untagged/unused intermediate images additionally require matching terminal-response IDs, descriptor and case label; child-first full-ID deletion has per-image receipts and no broad pruning. Report mode does not mutate, apply requires the exact case ID, sealed failures remain failures and no build is retried. Final component evidence `9e74606b-a0db-49d9-b96b-d7d97ddb5f83` passes 424 tests, with focused missing-line trace coverage rounded 95% image transaction, 100% completed-build recovery and 94% common recovery. README, harness instructions and DocC describe this narrower recovery boundary; it does not claim complete interrupted-build or release qualification.

The first E04 Docker run at `bf2ef08` failed a read-only prerequisite, not a build: its authenticated official Hub image appears as `alpine@sha256:...` in `RepoDigests`. Base admission now accepts that exact Docker spelling alongside the qualified `docker.io/library/alpine@sha256:...`, with foreign registry/namespace/digest regressions. Red component evidence `689e3d40-d899-4bb3-8d82-b9b1308c823b` becomes green `74eaae0e-2b56-4fee-ba85-7530fac145fa` (411 tests). The first live record `b54b87db-8128-4cb3-92be-c90f35080029` retains its failure and verified zero-residue shutdown. No functional observation, gate or historical result is normalized or waived.

The E04 harness now includes released stock/enhanced builder-image admission, direct binary build requests, real failed-command phase checks, private worker ownership/cleanup, per-request timings, and conservative interrupted-state recovery checks. Both published OCI archives were downloaded and verified without rebuilding. All 409 component tests pass (`0d4d83d5-afc2-424f-a2a1-d9e23058c84b`); focused trace coverage is 100% build adapter and builder ownership, rounded 99% pure build probes and 97% guest integration. Independent review resolved four issues: the outer timeout now reserves teardown time without changing phase deadlines; local lookup uses the loaded reference (red regression `26968347-f787-4811-b4b6-1076a873c986`); initial DNS matches the product and is revalidated before each submission; native index metadata is authenticated through the pinned manifest/config rather than equating index and leaf digests. Final source review is clean. Live three-lane E04 and full interrupted-build recovery are not yet qualified. See the [harness contract](bazel-test-harness.md#image-build-contract).

The E04 candidate now preserves stdout/stderr from a completed nonzero Apple image build and terminates its provider stream with the original error. The Docker API lazily emits one `error`/`errorDetail` JSON record only after the coordinated stream has marked the operation failed. Preflight rejection remains an HTTP error; cancellation still throws and abandonment cannot commit. Red regressions are retained at `649c49b6-0497-4e4e-8ac7-eb4b4f9d88d5`; focused stock and enhanced results pass at `81428389-fdd9-456b-bc55-f21257c059f0` and `fa645bcd-d4c2-434b-97e6-7045c0d6de92`. Full affected stock components pass with coverage at `b0e96bee-a751-406e-b28c-4e74bcba40e3`; the new stream adapter executes 34/34 measured lines. These are injected-provider/CLI component checks, not live failed-`RUN` proof or complete E04 parity. Independent review and strict touched-Swift lint pass.

Follow-up findings `4048937714` and `4048940843` replace Git-status-based cleanliness with raw blob/mode/inventory comparison against the captured commit tree. Ignored inputs beneath `Sources` are included because Swift/DocC globs consume them, and only direct, tracked symlink targets can attest a clean checkpoint. Real-repository regressions reproduce hidden index flags, all three ignore mechanisms, disabled file-mode checks, ignored targets and indirect symlink substitution (including parent-path components) before the fix; all 35 launcher/source-identity tests pass afterward. Both Git SHA-1 and SHA-256 object formats are tested. Ordinary dirty development builds remain available; DocC requires the clean checkpoint.

Review finding `4048788515` exposed dirty-source DocC mislabelling. The launcher now propagates captured cleanliness, rejects override spellings and malformed metadata, and DocC requires a clean checkpoint during analysis. The focused regressions first fail five assertions, then all 28 launcher/input-identity tests pass. Real dirty-source rejection is retained as `89cbc2fd-a5fb-4b0c-add2-99c35a3e8733`, with zero compilation/conversion actions. Compose adopts the same guard and exact updated helper digests; ordinary dirty development builds remain available.

Native DocC now reuses optimized Bazel modules through `//:documentation_tests` (`make bazel-docs`). Nine separately cached module archives resolve the existing Core catalog and merge into a self-contained static site. Declared catalog/merge input links are materialized without mutating inputs; foreign/directory links fail. Tests exercise arguments, identity and inventory rejection, timeouts/failures, link safety and the real site. There is no SwiftPM rebuild or publication. Both product repositories use the same rule/action/test snapshot; final DocC publication follows release and parity gates. Optimized symbols omit file locations, so the archive records the source revision without claiming per-symbol source links.

Provider identity projection is now an internal side-effect-free function used by the real service bootstrap. The runtime/owner/build provenance, route inventory and conditional enhanced logging advertisement are unchanged. Four focused contracts test the two ownership selections independently of compiled profile, native/emulated/unavailable mappings, complete route inventory, fingerprint stability/sensitivity and rejection of empty runtime provenance. Stock service coverage passes `ad284651-869a-40bf-92bf-48f8ceb9c37f`, executing all 38 measured lines in the new projection; enhanced service coverage passes `f290faa5-7910-4c9f-8c8c-db353a6f8738`. The policy now requires 23 service methods and positive projection-source execution, without removing any existing source or lowering the 90% aggregate gate. This is component evidence, not live provider enrollment or full coverage qualification.

Host-service HTTP probes now reuse the existing bounded process runner: concurrent pipe draining, five-second or tighter enclosing deadline, 64 KiB per stream, strict response parsing and curl configuration/proxy isolation. The previous unbounded Foundation wait/capture implementation is removed. All 19 discovered service methods are now required by both profile policies. Initial fixture failures (`572dcffa-fa8e-4411-92c7-c43895280c05`) and a Swift Testing macro compilation failure (`5832fb46-e98a-4882-8da7-7846e30beadc`) remain retained. Independent review found an explicit deadline could extend the hard cap; red proof `ff69e808-2aba-4bc5-a559-79843f7ab10d` demonstrates the ten-second child outliving that cap. The correction clamps all inputs to five seconds, and tests require elapsed bounds plus verified PID disappearance. Corrected enhanced execution passes `370f9f1e-96a3-455c-9b23-bf6a36247a23`; final stock passes `016810fc-c6b5-4b15-a76a-4523af5afa34`. Independent final review is clean. Both integration-target compilation results (`35713d10-ddd6-44db-8796-a5e201785db4` stock, `2621c270-412b-437c-93b3-8e8ff8d96de4` enhanced) precede the review correction and are historical compile evidence only. Compilation does not run the host lane or touch Keychain. This test-harness change does not claim higher production coverage or a passing host-integration/release gate.

Final diagnostics CLI validation passes stock `c8a15133-e269-49d2-b937-25412af2b787` (5.477 seconds) and enhanced `d2f6d227-698c-491f-92d4-95582af650ec` (5.823 seconds). Both require 43 discovered CLI methods and positive DiagnosticsCommand source hits in the unchanged coverage denominator. Strict changed-source checks, Markdown validation and independent complete nine-file review are clean. These timings are development observations; full checkpoint coverage and live release authorities remain separate gates.

Support-archive collection now bounds each external probe to five seconds or an earlier caller deadline. Ordinary probe timeouts remain reportable diagnostic results, but cancellation and enclosing deadline expiry propagate through collection and remove its staging directory. A narrow staging-root dependency permits isolated cleanup assertions without mutating process-wide environment. Red proof `4f8dba68-bf34-4c75-9d62-96c2fbf8c8a1` reproduced unbounded caller deadlines and swallowed cancellation; focused correction `28ac8922-0815-4eb2-a2c2-4171694343cf` passed, followed by full CLI `ef0fd2c0-4419-4add-94dc-97a9936db52f` (3.627 seconds development elapsed). Added parsed-command archive validation checks private output, missing-state non-creation, input-log preservation and refusal to overwrite an existing archive. This remains component proof, not a live runtime or release claim.

Final probe/capture-source proof passes all three affected targets in stock (`478ed6f2-4cf9-4cf8-a5de-5aa3c1b02034`, 2.746 seconds) and enhanced (`c4f9a437-ba33-48ab-b99f-358f2fbce608`, 3.117 seconds); unchanged targets are explicitly reported cached. Independent complete nine-file review is clean. The policy now requires 16 process and 18 Compose CLI methods plus executed Compose CLI source, using the unchanged shared validator. These are development/cache observations, not quiet-host benchmarks.

Compose bridge project-name and remaining-volume probes now enforce a 30-second maximum (or earlier caller deadline), limit each captured stream to 1 MiB, reject truncation, and wait for owned-child drain/reap. Volume probe failure preserves ownership; caller cancellation is propagated rather than swallowed. The shared capture calculation now clamps retained capacity to actual chunk length, eliminating negative omission counts. Red probe proof is `500f2161-9751-4bae-b48e-b3ccc2138da5`; red capture-count proof is `b13644ff-a7d0-4508-80b6-add727e00579`. Both affected stock targets pass at `119f9ede-417c-41e0-a9d3-71091eabae50` (4.079 seconds, development observation). Regressions cover pre-expired context, active deadline/reap, cancellation, oversized stdout/stderr and conservative ownership, plus exact below/at/over-limit capture accounting. Actual Compose operation limits, provider defaults and shared executable helper bytes remain unchanged; these are component results, not live parity or release qualification.

Review identified the related pre-existing gap where a caller cancelled after entering synchronous capture could not cancel its detached child. Regression `ed996762-8051-4d32-970c-41a19f5dbefa` reproduces the ignored cancellation and exceeded cleanup bound. The synchronous wait now forwards cancellation at a 50-millisecond polling boundary, still waiting for drain/reap before returning; fast completions signal immediately rather than waiting for the poll interval. No process is abandoned merely to meet the deadline. Final cancellation-source stock targets pass in `b266a245-b13a-411d-a216-d9f4ff26b49a` (4.050 seconds). Enhanced `29b42b20-748c-4029-9172-7e8ca0ddc9f1` exposed two short-deadline fixture startup races; serializing the Doctor and Plugin suites preserves all original deadlines, elapsed bounds and required PID disappearance. The corrected enhanced run `d85b7504-b7f4-43d3-8e3a-e35956a12157` passes (3.668 seconds, process target reused). A consumer-owned schema-1 policy uses the existing shared validator, preserves all ten targets and existing minima/source probes, requires the current 38 CLI and 15 process tests and additionally requires execution in ProcessRunner. This avoids changing shared executable helper bytes for a product-specific inventory update. All timings are development observations, not quiet benchmarks.

Synchronous process capture now carries the caller's request context into detached work and enforces its deadline while cancelling, draining and reaping the owned process before return. Plug-in install-root discovery sets a five-second maximum without extending an earlier caller deadline; explicit roots bypass discovery. Red regression `f48cc448-8b39-4c4b-99ec-033b12e600cf` proves both expired-before-launch and active-deadline failures with finite children. The first stock correction run `3c159be4-fe71-47a2-8b16-268fa36dfc9b` passes the affected process and CLI targets (4.974 seconds development elapsed). Added CLI checks exercise exact status arguments, failure text, timed cleanup, invalid deadlines and parsed registration/status/unregistration with no runtime executable. Final-source stock proof passes at `f4c19ca6-fbd6-4649-b77c-1be1f7cc3370` (4.452 seconds, process target reused), alongside the enhanced result above. Strict changed-source lint/format, Markdown, policy admission and independent full-delta review pass; aggregate 90% coverage, exact-head remote authority and live parity remain open. README, installation instructions, CLI help and DocC distinguish the source improvement from published 1.0.1 behavior.

Trusted runner package analysis failed for both profiles because `rules_license` 1.0.0's empty provider lacks `target_under_license`. The minimal Apache-2.0 backport from [upstream commit ed5bb251](https://github.com/bazelbuild/rules_license/commit/ed5bb2515c7e57ef48e361cc4977b54a25002d71) preserves identity on the empty and execution-configuration paths without changing dependency traversal. The real Compose bridge has an empty licence set, so the existing package smoke is the reproducer; additional tests preserve other products' deduplicated notices and require CLI/engine dependency notices in the real archive. The entirely empty-set rejection remains intact. Native stock packaging passes at `c31f3b60-aeba-4606-b34b-c080f7e5ae0d` (22.315 seconds); enhanced packaging and recovery component tests pass at `f6cbaa5e-252e-4667-8bf7-9734c31136e3` (19.509 seconds). These are development observations, not quiet benchmarks or distribution approval. Sonar's single `python:S5778` finding is corrected by preparing malformed recovery input before the exception assertion; neither serialization failure nor setup failure can satisfy that assertion.

The 18 September SwiftPM CI run `35340915853` exposed two test-harness assumptions: spelling equality for `/var` versus `/private/var`, and an integration executable selected inside the test bundle. The process test now compares directory device/inode identity; SwiftPM engine discovery requires the exact regular executable beside its `.xctest` bundle instead of recursively selecting another build. Explicit Bazel runfiles and configured executable paths remain authoritative. Pure tests reject missing files, directories, non-executable files and non-bundle roots. Focused native process/service targets pass (`c3e7f6e2-47cf-4f2c-abe0-452131de3e74`, 5.742 seconds development elapsed), strict source checks and independent review are clean. These are harness corrections, not new live integration or sanitizer acceptance; the failed CI evidence is retained.

Coverage export exposed a missing child profile in the real-terminal regression. The test now passes only `TERM` and a unique LLVM profile destination under the validated SSD coverage directory, and fails if its own child profiles are missing or empty. Focused invocation `81b1bc85-4452-48bc-a2ad-5bc858348694` passes in 4.555 seconds; the launcher covers 124/133 lines (93.23%) in this focused run, not aggregate product coverage. Independent review is clean. No production behavior, source exclusion or quality threshold changes.

Final process-ownership review added two safeguards: interactive launches use Apple's [start-suspended spawn flag](https://developer.apple.com/library/archive/documentation/System/Conceptual/ManPages_iPhoneOS/man3/posix_spawnattr_setflags.3.html) until terminal foreground transfer succeeds, and captured commands retain an unreaped leader through pipe drainage. Group signals and final reap share an ownership lock; a consumed PID cannot be waited or restarted again. A finite descendant regression fails both leader-exit orderings at `bcacf932-260d-4f75-8b2e-4bb0be20f624` (4.97 seconds to close instead of bounded cancellation). It now passes alongside a real-PTY helper exercising 12 reads/foreground restorations, injected handoff failure without changing the test host's terminal, and post-reap cancellation without a second wait. The helper is an explicit Bazel runfile and SwiftPM test dependency, not a shipped product. Final focused enhanced (`fa54cf2e-7f82-4ff5-a42e-e55a9cd6cf8c`, 8.351 seconds) and stock (`3a84aab5-1e49-49cd-9a60-5ea79f17563d`, 14.929 seconds) targets pass; the stock run reuses the profile-independent process target and runs CLI/service tests. Independent full-delta review is clean. These are development observations, not performance comparisons. Startup cleanup scope begins at server start; earlier runtime/bootstrap and real Keychain qualification remain open.

Unattended diagnostic/service failure handling now validates `doctor` output format before execution, applies a five-second probe deadline without extending a caller's earlier deadline, and reaps owned processes. Missing/unsafe sockets, launch errors, output limits and both report formats have component regressions. The combined run exposed an actual fork/exec error-path deadlock in the dependency's child `exit` cleanup (`836e7845-7ac4-47d5-8b56-72df2305a9f8`, manually terminated after a process sample). The process runner now adapts Compose's Apache-2.0 POSIX-spawn launcher with exact paths, null default streams and interruption-safe wait; attribution is recorded in NOTICE. Tests cover concurrent missing-interpreter errors, input/environment/cwd/argument fidelity, exit status and TERM-ignoring process-tree cancellation. A narrow service-lifetime owner guarantees runtime cleanup after failed start and runtime/server cleanup after wait failure or cancellation, preserving the primary error and draining child tasks. The extracted original behavior fails startup/wait/cancellation regressions at `0134827b-3e50-421e-8aeb-d96c51f3fc9c`; the original Doctor failures are retained at `2d242873-31f2-47a7-b456-04ca7b3234e4`. Combined enhanced CLI/process/service targets pass at `dc6dc060-87ea-4c54-95ae-db17f1f9c6c7` (7.614 seconds, development observation). This does not certify live startup, signing/Keychain, HTTP socket responsiveness, parity, aggregate coverage or a new release.

The configuration-command regression now preserves omitted strictness rather than resetting saved `false` to `true`. An optional flag distinguishes omission from explicit `--strict`/`--no-strict`; fresh configurations retain the strict default. Eight new test methods exercise all four backend/frontend combinations, exact stored values/private permissions, explicit and omitted strictness, malformed/invalid-input preservation, durable idempotent claims, conflicting-provider refusal and explicit reset. The pre-fix failure is retained at `b142c517-dc04-437b-8caa-40bdb4f6f4c5`. Final complete CLI targets pass in stock (`3bfa14f5-4039-4d98-808d-8e7244744a9c`, 7.767 seconds) and enhanced (`2b044b18-8357-431e-8ec6-c001fa952b7a`, 6.712 seconds), with 21 methods in three suites. Strict touched-source lint/format, Markdown checks and independent full-delta review pass. These are parser/run and private SQLite component tests, not subprocess/runtime integration or quiet benchmarks. README, manual, DocC and embedded help describe candidate-only behavior. No default frontend, installed runtime, dependency or release support claim changes.

Native create now completes mount allocation/resolution, kernel lookup and deadline validation before an explicit durable-intent callback immediately preceding the create RPC. The journal stores final resolved mount configuration, and completion uses the same operation identity. Preflight failures remain retryable; potentially submitted failures retain intent. This addresses GitHub `4045780895` and the independent review's missing-kernel extension. A managed-volume regression reproduced the original failure at `83af5b91-3c1d-483f-9a3a-22892993c8ce`; the prototype native-volume case initially selected an existing fixture volume and was corrected to a missing one. Final focused stock (`af742e72-17d6-49c0-8838-096c3ab8f66f`, 19.550 seconds) and enhanced (`ee65eff5-4a0c-4b75-84e3-e99b0103a42c`, 13.941 seconds) runs pass 23 methods, including live-client kernel/callback ordering tests that stop before any real RPC. These are development timings, not quiet benchmarks. Strict touched-source checks and full independent rereview pass; both manifests explicitly declare the already-pinned Containerization module used for kernel injection. Public docs retain candidate-only scope and the live recovery qualification gap.

The Compose wrapper now records the resolved runtime backend rather than inferring ownership from the frontend. Executable admission precedes state creation; tests exercise both frontends with both backends, missing-native no-fallback behavior, and rejection of existing cross-runtime claims. The execution context is grouped using the existing environment type, and independent review found no actionable issue. The final focused suite passes in stock (`16043d4f-d108-4f43-b538-b192c6abc81d`, 6.609 seconds Bazel elapsed) and enhanced (`0ce75960-ed2f-45b5-9fef-75cae639f5b2`, 2.058 seconds); these are development observations, not quiet benchmarks. Strict Swift lint, formatting and touched-document lint pass. README, design, compatibility ledger and both user guides distinguish this development correction from the unchanged stable matrix. Default-provider and Homebrew Docker dependency removal remain blocked on the actual Docker-free client replacement, not on this bookkeeping correction.

Verified-stopped Docker cleanup is now recoverable independently of Apple service restoration. Both normal cleanup and explicit recovery retain a root-incarnation authorization before deletion; a crash during removal or guard clearance can resume without rebuilding, restarting a VM, signalling a process or rewriting the failed case. Recovery rechecks the sealed shutdown/plan, captured incarnations, command groups and a bounded socket-connect probe. A listening socket is rejected immediately without waiting for HTTP data; only missing/refused sockets establish absence. Uncertain startup/shutdown, changed roots/markers, incomplete command receipts, surviving helpers and retention failures still preserve quarantine. The focused component checkpoint passes 362 cases in invocation `78714f69-f9ce-4082-be67-09e64d02bb40` (4.660 seconds Bazel elapsed), including the bounded-probe correction from independent review. This is not live crash-injection qualification or complete orphan recovery.

GitHub review `4045475572` identified the package suite's default 60-second timeout as shorter than its aggregate command/cleanup budget. An explicit 300-second outer timeout now covers its six 15-second command deadlines and bounded session termination without changing individual deadlines or failure outcomes. Exact signed packaging head `dff29292ad9c29ea994d4594dd28b68d207a964f` passed nine cases for stock (`d26d0fab-da3f-415c-87f5-505262e18d15`, 21.331 seconds) and enhanced (`1f7de5ba-873e-4286-99b3-a3b36dd6c126`, 93.587 seconds); both retained archives restored successfully. These measured development/cache observations are not quiet-host benchmarks.

Native packaging now tests the actual extracted archive using seven artifact/CLI checks plus two cleanup regressions, all individually timed by the existing JUnit runner. It verifies product/lock/archive hashes, exact safe layout, plugin identity and compiled provenance, then executes the main CLI, plugin CLI, service help and explicit native Compose-provider fixture with a clean private environment. No real provider, service or installer runs. The owned-session process helper is reused verbatim from Compose. Uncertain child cleanup preserves both temporary roots; both explicit archive and smoke targets are requested so durable bytes remain restorable. Stock development invocation `9798f50d-6503-4516-aacd-776dd211dcd2` passes nine cases and restores successfully without compiling. [Package evidence](bazel-workflow.md#native-candidate-archives) retains the discovery failure and review corrections. This does not certify signing, Homebrew installation, default-provider selection or live runtime compatibility. Public product behavior and DocC API claims are unchanged.

The shared coverage contract now supports an additive schema-2 consumer inventory: unchanged unit targets plus disjoint declared CLI components. Canonical combined aggregates select `unit-cli`; receipts retain that identity and gates require it explicitly alongside source/profile/policy. Legacy unit receipts export byte-identically, and devcontainer's own aggregate remains unit-only. Focused policy, receipt and real shell-routing regressions reject missing/extra targets, replaced minima, malformed counts, mismatched inventory and invalid selection. This does not turn CLI components into live runtime coverage or lower the 90% threshold.

- Pin verified Bazel tooling and native Swift/SQLite targets with declared generated version inputs.
- Enroll and validate SSD storage; isolate download/cache/output/test scratch paths.
- Adapt upstream test-source/XML writes for external-volume sandbox compatibility and export/merge native Swift coverage from the original test execution.
- Add version-generator and launcher regressions, evidence validation and opt-in Make targets.
- Import the exact stock/enhanced package pins as native dependency targets; build all three project executables and ten unit-test targets.
- Separate real Keychain and Apple-service tests into explicit uncached host targets; preserve the ordinary source suites in both profiles.
- Keep test fixtures on SSD and inject direct/fallback fake-runtime transfer storage without changing production daemon-sharing defaults. The runtime initializer also accepts an explicit transfer root for callers that own a daemon-accessible scratch directory.
- Use sibling-file atomic replacement for writes that Foundation otherwise stages outside the destination volume sandbox.
- Preserve downloaded hosts-file permissions when editing private transfer staging; this disposable copy needs no atomic replacement.
- Seal source identity, build events, XML, LCOV and outcome bytes in transactional internal storage with content deduplication. Serialize each workspace through retention; fail mixed-source or conflicting evidence.
- Bound Bazel's native disposable cache and reject dependency/environment overrides that would break the pinned/storage contract.
- Isolate inherited credentials from Bazel and its event stream, preserve only validated host-test opt-in, and distinguish query/info diagnostics from build evidence.
- Add native optimized candidate archiving with the existing deterministic archive writer, digest-bearing metadata, transactional internal retention and authenticated SSD restore without rebuilding.
- Acquire reviewed GitHub binary assets with exact release/tag/commit/asset identity and checksum verification, durable pending/sealed ingest and explicit offline reuse, without compiling references.
- Extract reviewed binary layouts on SSD without installation or compilation; retain verified executable trees and complete inventories as long-lived internal assets. Pending publication is recoverable and not executable admission; sealed assets are verified and never silently repaired. This is binary preparation, not complete runtime closure qualification.
- Prepare digest-pinned stock initialization and shared workload OCI images with anonymous Skopeo downloads, no Docker daemon/VM/build, deterministic archives and recoverable internal retention. Reuse the exact Apache-licensed Compose closure validator, bind manifest/config identities and verify offline reuse. Prepare the checksum-pinned recommended kernel as non-executable retained data without installing it; extract only its reviewed archive member with a bounded Zstandard-capable command and validate offline reuse. Enhanced-init acquisition and guest-runtime qualification remain incomplete.
- Port E05 archive-copy assertions to bounded direct Unix HTTP and deterministic in-memory PAX payloads, with negative/socket tests. This removes the probe's Docker CLI dependency; guest-resource admission and live three-lane qualification remain unfinished.
- Connect the archive probe to a journalled disposable-container component: immutable prepared image, create/start/inspect, ID-based removal, ownership verification and lost-response recovery. Live runtime/guest admission remains a separate unfinished boundary; test-server results are not runtime parity.
- Port all five E02 lifecycle observations to the same direct-HTTP ownership component, including restart-generation readiness, TERM/wait exit codes and ordinary removal before repeated-delete observation. Retain per-request timings without payloads. This is component evidence only; live three-lane admission remains unfinished.
- Register invocation ownership and add conservative leased cleanup of expired, internally retained scratch. Keep unknown/legacy directories, runtime services and GitHub resources untouched.
- Export authenticated retained LCOV as Sonar generic XML with unchanged line counts, explicit original source identity and no build/test execution. Reject corrupt, dirty, incomplete or mismatched receipts; do not silently submit historical evidence as current-head coverage.
- Retain monotonic build/test elapsed timings with toolchain, source, configuration, host load and Bazel action/cache/test metrics; compare only matching successful clean configurations, without treating ordinary observations as controlled benchmarks.
- Exercise scratch-selection precedence and rejected Bazel roots through a test helper shared by SwiftPM and Bazel, without mutating process-wide environment in parallel tests. Resolve filesystem aliases before accepting SSD scratch in both the Swift helper and shell wrapper.
- Begin the authorised [test-harness replacement](bazel-test-harness.md): immutable per-case results, explicit recovery boundaries and a Docker-independent Unix HTTP E01 probe, tested through a native Bazel target. Live artifact-only adapters and full inventory cutover remain incomplete.
- Connect an opt-in released-service E01 case for the stock/enhanced binary selections with read-only artifact admission, shared runtime locking, bounded process ownership, durable quarantine and authenticated attached evidence. This does not qualify guest workloads or the complete parity matrix; automatic hard-kill reconciliation and legacy quarantine integration remain open.
- Reject mismatched or unhealthy API services before released-case execution, bind the selected launchd identity into evidence, and retain payload-free per-request timing/error traces. Preserve the first failed live attempt and its provider-mismatch diagnosis rather than retrying it as a stock parity pass.
- Add the released metadata case's transactional service selection: private internal snapshots/journal, idle-host checks, outgoing-process closure, selected-program verification, bounded private startup logs and original-service restoration even after diagnostic retention errors. Crash/fault tests pass; live qualification and automatic hard-kill reconciliation remain incomplete.
- Add report-first operator recovery with explicit case-ID confirmation, unchanged failed-case seals, private log retention and resumable partial scratch deletion. Refuse uncertain Engine spawn, live recorded PID/group, active workers/guests and foreign state; do not guess process ownership or clear quarantine when reconciliation is incomplete.
- Address GitHub recovery findings by checking captured original-process closure before reporting readiness or mutating services, and rejecting unregistered CI listeners. Address archive-contract feedback by allowing unrelated safe tar members and an omitted directory header while still checking every expected file and rejecting duplicate expected paths.
- Tie registered-original exemptions to the current launchd PID tree rather than the unchanged service definition alone. Refuse lingering old providers/detached descendants even after a replacement registration or a crash following scratch removal; preserve quarantine without mutating services.
- Connect E02/E05 to released guest-data admission, private kernel/image provisioning and direct-HTTP operation/cleanup. Bind all guest inputs/locks into the case identity; read-only admission refuses pending or missing enhanced inputs. Journal setup process intent and private logs, share explicit provider roots with the Engine client, and verify guest cleanup before provider teardown. Legacy recovery refuses unresolved guest/process state. Live qualification and automatic interrupted-guest reconciliation remain required; Compose 0.15.1's missing guest asset cannot be backfilled because GitHub marks the release immutable.
- Recover missing provisioning diagnostics after verified guest/process closure: report a read-only retention plan and commit its bounded private snapshots before restoring services or deleting scratch. Resume partial payload/metadata retention without replacing existing evidence. Reject missing, aliased, writable or changed logs and preserve the original failed case. This does not add unsafe PID-only signalling or complete live-guest recovery.
- Address GitHub review `4037091847` by bounding Engine log snapshots to 1 MiB and retaining explicit digest/truncation metadata. A 9 MiB regression verifies that excess output no longer fails artifact retention or leaves a successfully cleaned case quarantined.
- Connect all six E03 exec-stream assertions to the released guest adapter with direct bounded full-duplex Unix HTTP, strict non-TTY framing, raw TTY output, a 4 MiB binary round trip, exact exit/environment observations, write-ahead exec identity records and sanitized timings. Real socket tests exercise backpressure, fragmentation, invalid ownership, uncertain starts and bounds without starting a runtime. Live reference/candidate qualification remains required.
- Connect all six E06 network/volume assertions with role-scoped journals for three guests, local SSD bind input, direct resource HTTP and the shared exec transport. Preserve observed provider metadata, remove only ownership-verified resources in dependency order, reject foreign/uncertain state and require an aggregate cleanup receipt before service recovery. Component resource tests fake exec outputs and do not establish actual DNS/filesystem parity. Native-driver ownership review feedback is fixed without normalizing the observed driver field.
- Prepare pinned published Colima 0.10.3, Lima 2.2.0 and Colima-core 0.10.4 Docker-image assets through the existing leased release path. Keep VM data compressed/non-executable and explicitly omit only Lima's exact documentation alias; all other link rejection remains intact. Actual acquisition, preparation, version checks and offline reuse pass without starting a VM. Add the exact existing Docker CLI oracle bottle from public GitHub Packages with immutable manifest/bottle/executable checks, retained notices and recoverable publication; do not fabricate GitHub Release metadata or change the installed Docker client. Add an opt-in private SSD Docker VM lane using the shared Engine fixtures, exact process-role shutdown authority, a durable command journal and fail-closed cleanup. Live qualification, interrupted VM recovery and the common cross-lane comparison identity remain outstanding.
- Address GitHub review `4037254342` by revalidating complete original service-definition bytes/hash after bootstrap and inspection. Regression tests preserve public job identity while changing environment/Mach-service fields and require restoration refusal without a completion record.
- Reject Bazel `--flagfile` indirection so hidden dependency/environment overrides cannot bypass the launcher's argument policy.
- Permit a sibling Bazel workspace to reuse the launcher and evidence tools with an exact consumer-owned tooling digest lock. Source snapshots bind the executing helper bytes and reject changes. Consumers now provide a declarative profile-specific test/source inventory to the shared validator, not their own validator implementation. Test discovery, production LCOV line accounting, policy-bound export and an optional unrounded coverage threshold are covered by focused regressions. This is a shared-tooling boundary, not a claim that Compose migration is complete.
- Admit clean retained native candidates for development integration through the existing released-provider harness. Reuse authenticated archives and recoverable preparation, require matching source/dependency/profile/product identities, retain a readable admission manifest and keep candidate scope in the comparison fingerprint and JUnit. Candidate results cannot mix with published-release evidence; final parity still requires downloaded releases.
- Retain failed native-create intent in schema 4 before the provider mutation, verify the final incarnation, publish successful metadata atomically and prevent inventory adoption or archive/lifecycle bypass. Preserve pending evidence after name-based deletion. Operator reconciliation and live interrupted-create proof remain open rather than clearing uncertain state to pass tests.
- Supply a disposable noninteractive keychain for the isolated SSD test HOME, without changing the operator login keychain or search list. Journal every helper's spawn/verified stop, quarantine uncertain completion and retain bounded private failure diagnostics before cleanup. Recovery reuses that same guarded path. Component and crash regressions pass; live lifecycle qualification remains required.
- Fix the guest harness's out-of-range 1.54 request: pin the same documented API 1.53 contract across lanes, validate the advertised server range before resource mutation and retain version evidence. Do not upgrade a compatibility claim or change the Docker oracle pin to hide an unsupported version.
- Preserve archive member modes under a restrictive service umask, using nested extraction inside an untouched private parent. Real-tar component tests cover file modes 0644/0750/0777 and a 0777 dot-directory entry. Corrected candidate `805940f` passes live stock E05 and E06; preceding candidate `2c43263` passes stock E02 and E03. Earlier failures remain retained; the complete published three-lane matrix is still outstanding.

## Validation and compatibility

Full enhanced ASan invocation `53c7676c-51b1-4264-8eb0-8519b6cc86df` passed
nine targets but timed out in Apple runtime tests after 300 seconds. A process
sample traced the stall through `AppleProcessSession`, the dependency's
fork-based `Command`, and ASan allocator locks during `fork`. Non-TTY Apple CLI
sessions now reuse the existing Compose-derived POSIX-spawn primitive from
`DevContainerProcess`; stream monitors, process groups and completion ordering
remain intact. Concurrent missing-interpreter, invalid-working-directory and
signal-exit regressions exercise the real session boundary. Focused enhanced
ASan invocation `7086c870-0612-461d-9f8d-fddc34212bde` passes both process/runtime
targets; stock invocation `75765b2b-f82b-41a6-9e25-39d9202c9f25` also passes
both targets. The previously stalled target completes in nine seconds. This is
development diagnostic timing, not a benchmark. PTY sessions retain their
separate controlling-terminal implementation; no blanket fork-free claim is made.

The local native-coverage Sonar scan of `91dddb3cab5e3b4f53e256b7a01ac5b30375ce5c`
passed its quality gate (task `AaC09U6_WbyLjcxNZWXT`) but retained seven open
maintainability findings. The correction keeps exception-test setup outside the
single throwing invocation and extracts the synchronous process runner's
task-local/deadline scope without changing cancellation or cleanup ordering.
Focused stock invocation `ebc34cd6-206f-42f9-922b-5373d0e9070d` and enhanced
invocation `a0b08f08-9412-4759-8c18-f866c4d4107d` pass the process and harness
component suites; the enhanced result legitimately reuses profile-independent
actions. Independent review is clean. A subsequent scan, rather than the prior
green quality gate, must establish that the open findings are cleared.

The downloaded Docker VM passed E01 at `c8580a9` with verified shutdown (`88799304-cedb-4adb-ab41-61d3120d27fb`). Four guest cases exposed the same pre-operation image-addressing assumption and cleaned up successfully. The correction at `dde586f` authenticates Docker's pinned manifest ID without rewriting either OCI pin or assertions; E02/E03/E05/E06 then passed with verified cleanup. New published campaigns now share one complete cross-lane fingerprint, with candidate evidence still separate; 304 component tests pass (`a5949762-b735-45cc-8f96-f8354c84688a`). Full three-lane parity, startup-variance analysis, recovery and quiet benchmarks remain open; retained invocation references and measured phases are in [the harness evidence](bazel-test-harness.md#first-live-docker-qualification).

The native-create journal component passes stock runtime/state tests (`8e6bc6cc-df7b-4e3d-a64c-06cad9dbdbfc`) and the subsequent enhanced runtime/state checkpoint including schema-3 migration (`fe4bbc08-85a3-481f-b75c-7d205cac22f0`). The earlier focused stock recovery helper covered 47/47 executable lines; this is not aggregate product coverage, current-head Sonar evidence or live crash recovery. Review found archive bypass, deletion clearing a newer intent and final-observation identity gaps; the draft now guards those paths, never clears intent through ordinary removal, and validates final descriptor/platform/timestamp. The CLI compatibility-create path remains outside this native guarantee. See [the recovery contract](../DESIGN.md#native-creation-recovery) for SDK and rollback compatibility.

The final review also caught direct-library callers losing synthetic Docker identity when completion bypassed ordinary adoption. Native completion now retains a supplied label or generates one 64-hex ID before its atomic commit; regressions verify preserved identity through a restarted bridge's list/inspect. Final focused stock (`c63388b4-15c4-4258-9331-49a826cdd313`) and enhanced (`da4949a0-2777-4b5d-a3df-c1f32d5ac557`) runtime/state targets pass; the final stock recovery helper covers 81/81 executable lines. Strict touched-file SwiftLint, SwiftFormat and affected Markdown lint pass, and the independent complete-diff review found no remaining actionable source/docs issue. Live recovery, the operator interface and full release gates remain open.

The candidate-admission component checkpoint passed both Bazel targets (`fc83883c-7df9-46b1-bea4-c51075291aad`) and all 131 workflow-helper tests. Standard-library trace with missing-line accounting measured 100/101 executable lines (99%) for `prepare_candidate.py` across ten focused tests; this is helper coverage, not aggregate Swift or Sonar coverage. Review identified and fixed mixed candidate/release comparison identity and missing internal-storage enforcement, then found no further actionable source issues. Live candidate lifecycle evidence remains outstanding.

GitHub review `4040933790` exposed loss of native index/manifest aliases while reconciling containers without bridge metadata. All three inventory/inspection paths now retain resolved identities and select the original configuration ID. Parameterized regressions cover both repository-qualified digest forms through direct list and inspect. Focused stock coverage passes (`94f4ef65-46ad-45c1-82c1-7753075ed0d0`); independent source review and strict touched-file SwiftLint report no additional findings. This does not substitute for the outstanding live E02 proof.

The stock protocol case now passes after operator-approved retained-helper access, while E02 stopped before creation on config-digest image inspection. The draft source correction resolves original OCI configuration IDs through the stock API, preserves repository-qualified lookup and groups shared tags. Both full stock/enhanced unit/product coverage graphs passed at `5e640d774dec70ad50970683c55aab6a4d073ed8` (`9806600f-cc3e-4be5-938a-4fce44008d4f`, `f504daeb-b1dd-4355-b385-8ae76a9fe309`). The subsequent create implementation captures descriptor/platform, prepares a typed native configuration before allocating mounts, verifies resulting identity and only then publishes compatibility metadata. Independent review found and fixed allocation-before-validation and empty-user privilege regressions; the final source pass found no further actionable issue. Focused stock/enhanced targets pass, but live XPC coverage and E02 remain open; see the [explicit coverage shortfall](bazel-test-harness.md#approved-helpers-and-first-stock-protocol-pass). Digest tag/delete and digest creation with direct API disabled still refuse unsafe CLI operations. These are implementation gaps, not waived parity or release qualification.

The final delivery contract now explicitly requires stable builds of both projects, published-asset validation, public benchmark evidence and both DocC/VHS documentation outputs. The original tape/recorder inspection found hard-coded devcontainer version/build/Docker-CLI assumptions and a Compose recording limited to two services; those must be corrected before the final recording phase.

See the dated [qualification evidence](bazel-workflow.md#qualification-evidence). Both dependency profiles compile and pass their deterministic suites (221 stock / 232 enhanced). An unchanged enhanced request reuses all ten test results with no test execution or Swift compilation. Fifty-four helper tests exercise storage, evidence, source mutation, environment isolation, deterministic candidate layout, restore after output removal, release acquisition/recovery and leased cleanup. Both optimized archives build; the enhanced archive also restores and executes, and unchanged packaging performs no compilation or archive action. Five published binaries were verified, retained and reused offline. The complete local checkpoint passed at `db772e52f573ee9387cf9660d597c969eecd68aa`; the subsequent staging-permission regression passed focused enhanced runtime tests (invocation `e45b2d57-911c-48b8-b4fa-6079dab4a12e`). Final-head affected checks remain required. ShellCheck and focused Swift formatting checks pass. Earlier subset ASan/TSan evidence is retained but does not qualify the full graph. Existing release entry points and protected checks remain unchanged. No release is produced by this change.

## Remaining risks

API readiness now requires one bounded read-only list RPC through the exact selected released CLI, not just a running launchd identity. Empty private inventory, exit status, complete output, verified child shutdown and unchanged service identity precede the ready receipt. Intent/stop/diagnostic records support conservative interrupted-start recovery without retrying conformance requests or rewriting historical cases. The PID-only readiness regression fails at `ab610f5e-ad11-4718-9f0b-34cac59e7dbd`; all 374 native component tests pass at `c031ffce-d20a-4507-95b6-01aa2a6eeea8`. This changes the shared campaign fingerprint. Historical runtime records cannot qualify the new harness; live validation remains required.

The 20 September E07 stock package now passes all seven binary attachment/history observations and zero-residue cleanup at `f744a85` (`6108421b-44df-404b-ae80-4732aa5d5f90`). Enhanced setup sampling (`b64b70e5-c444-46ca-8a63-3bab7055b992`) identifies provider-key creation waiting on missing-login-keychain UI before the former Engine-only keychain setup. The harness now prepares its existing isolated keychain before selected API launch, verifies helper completion before restoration, and deletes it only after both consumers stop. Final journal receipts include deletion; failure preserves quarantine. The ordering regressions fail at `43f765df` and all 906 corrected component tests pass at `0f70782b-5a5f-4882-ba3f-4e2525d7be2e`. No personal-keychain settings, provider binary, timeout or compatibility claim is relaxed. Detailed inputs, private diagnostic digest and raw non-quiet timings remain in the [test-keychain record](bazel-test-harness.md#isolated-test-keychain). Enhanced live validation and the rest of C02 remain open.

Live stock E01 now passes the new admission boundary (`6afca16c`), while the enhanced release still stalls before serving requests. Its bounded native trace locates file-log creation, with successful cleanup; see the [startup evidence](bazel-test-harness.md#api-readiness-and-interrupted-startup). A reporting follow-up converts only the readiness subprocess deadline to sanitized `TimeoutError`, preserving cleanup and historical records. All 375 component tests pass (`4af4ff48-1ae2-44c7-b858-1662dc3c0514`) after the full case-store regression fails against generic-failure classification. This is not an enhanced parity pass or a permission workaround.

Read-only CLI contract tests now verify generated version provenance in pretty, JSON and short formats, reject invalid formats/configuration without state changes, and round-trip socket paths through a real shell including quotes, whitespace, newlines and command-substitution syntax. Internal output construction avoids redirecting process-global stdout during concurrent tests; emitted bytes and newline behavior remain unchanged. Focused stock (`a4222753-52f6-4d64-89f1-21a69dae675b`) and enhanced (`fcda4c6b-313c-4e98-a4e8-6c0d260c6fa8`) CLI coverage tests pass, and independent final review is clean. The evidence policy requires both output implementations and the expanded test inventory. These results do not establish aggregate coverage, runtime parity or release qualification.

Shared direct-HTTP framing now refuses early EOF with unsatisfied `Content-Length`, even when the truncated body contains valid JSON. A real Unix-socket regression reproduces the former silent acceptance (`4be157b5-0cb3-4159-b739-01e834f2d4c1`); all 346 native harness tests pass after the fix (`f02b1a6d-3a8c-4cf1-b6e7-fc6c12ab08f1`, 4.554 seconds development elapsed). Complete bodies at the exact size bound remain accepted, and existing HEAD/oversize cases still pass. This prevents partial HTTP output from being used as completion evidence without adding retries, changing deadlines or weakening uncertain-state cleanup. No live service, installed product, README command, public API, DocC or compatibility claim changes; the image-build fixture remains unregistered and incomplete.

The `88e39ff` scan identified nine ambiguous exception assertions (`python:S5778`) that reopened the recovery journal inside the expected-failure scope. Each setup now happens before the assertion, so only cleanup/submission can satisfy the expected error. All 344 native harness tests pass (`ba491ac4-72f5-461a-afb7-76892cc9672e`, 4.587 seconds development elapsed), and independent review is clean. Production behavior and gates are unchanged; the next exact-head scan must confirm closure.

The E04 image-ownership checkpoint passes all 344 native harness tests (`e5462fc4-1ec8-4883-a4ce-3d0d2067ce91`, 4.591 seconds development elapsed), touched-document lint and final independent review. This is image-only component evidence, not live parity or a coverage percentage. The preceding `4fd8234` checkpoint's GitHub test, stock, sanitizer, Homebrew, documentation and Sonar checks are green, with zero unresolved Sonar issues; the new source still requires its own exact-head remote checks.

E04 now has deterministic digest-pinned build contexts, safe query construction, bounded JSON-line decoding and exact image-ownership checks in `Tools/testing/build_probe.py`. Eight focused tests joined the native harness; that pure-helper checkpoint passed all 325 tests (`eddb9bdb-8c8c-486e-99a5-869655c8b190`, 4.197 seconds development elapsed), with clean independent review. The subsequent `build_images.py` component adds durable pairwise intent, prepared-base inspection, write-ahead submission/deletion, private terminal-response retention and conservative image-only recovery. Nineteen Unix HTTP/SQLite tests cover replacement, shared/base images, conflicting identities, interrupted observation, lost responses, partial cleanup, reappearance and residue. Independent review found a per-image closure gap; submission now refuses a role already marked absent even when overall cleanup remains incomplete, with a regression. Neither component is registered as a live E04 adapter. Released-builder admission, live transaction wiring, intended failure-phase proof, interrupted-builder reconciliation, cache/intermediate-container cleanup and full three-lane execution remain required. The [harness design](bazel-test-harness.md#image-build-contract) records that boundary; no support or release claim changes.

The follow-up at `c901b6d` passes the Sonar quality gate (90.7% new-code coverage), but the stricter zero-open-issues check still fails on 25 maintainability findings. The batch moves exception-test setup outside the asserted operation, narrows provisioning failures to their exact exception types, makes the server-error cleanup registration explicit, and groups the internal logging clients without changing their defaults or the public API. The native harness and release-input targets pass 317 and 70 tests respectively (`09a22878-9e22-4b80-9c78-d044185b864b`). The final runtime shape passes stock (`eef1824a-a535-43f7-bee2-3edaa81aea87`, 131 tests, 28.748 seconds) and enhanced (`252c1997-a828-4871-9264-21ea529bbe4c`, 143 tests, 28.010 seconds). Independent review and strict touched-file Swift lint pass. These are development observations, not quiet benchmarks or aggregate coverage. The next exact-head scan must confirm the issue closures; thresholds and exclusions are unchanged. Compose's shared-helper digest lock moves with the guest-image test change. README, DocC and install behavior remain unchanged because these are internal dependency grouping and test-quality corrections.

The 18 September analysis at `d55094a` reports 90.7% new-code coverage, but its reliability gate fails on the archive fixture's self-comparison assertion (`python:S5863`); this is not an overall-coverage pass. The regression now generates archives under two different mocked wall clocks, compares their bytes and checks zero timestamp/user/group metadata, while retaining the exact-mode assertion. Native Bazel evidence target `0b08cb89-98db-4d1c-abd9-66287f03dbdc` passes all 317 component tests in 4.527 seconds total, and independent review found no actionable issue. These are development timings, not quiet benchmarks. The next exact-head Sonar analysis remains required; no gate, suppression or coverage exclusion changed.

Approval-free mount-boundary coverage now exercises the production parser with an internal injected volume lookup; the default remains the same `ClientVolume.inspect` operation. Tests cover bind/tmpfs/volume order and read-only modes, incomplete/unknown options, malformed syntax, lookup failure and enhanced image-mount rejection without a live service. Native identity regressions also reject a mismatched descriptor and malformed platform. The full Apple runtime test target passes with coverage in enhanced (`11130fa4-4077-4445-8733-dccc6d0c265a`, 16.454 seconds) and stock (`7ff98e0b-525f-45b0-ba34-7107a686b2ad`, 28.728 seconds). These are component development timings, not aggregate coverage, live parity or quiet benchmarks. Independent final source review found no actionable issue. Public API, compatibility claims, README commands, DocC and install behavior are unchanged; the live XPC preparation/create coverage gap remains open.

The corrected exact-head E01 campaign at `2b1a920` now compares all three published lanes successfully with authenticated seals and verified cleanup. Public JSON/Markdown/JUnit evidence is linked in the README and harness document; raw Apple-lane timing ratios above 10x remain explicitly unqualified and require investigation. The new read-only report path authenticates one consistent snapshot, defaults to the full fixture manifest, rejects campaign-wide fingerprint drift and preserves failures without exporting private payloads. Component gate `93ae340d-2ae1-4733-a456-de01f386b444` and 148 workflow-helper tests pass. This is one-fixture functional evidence, not complete parity or stable-release acceptance.

Live shared E01 identity validation exposed Python import/runfiles path asymmetry: each lane passed, but Docker's workspace glob included test files absent from the Apple runfiles. Those historical records cannot compare. The explicit complete runtime closure and symlink-runfiles regression correct the fingerprint without rewriting evidence; focused Bazel invocation `5a4d7d9b-827d-4f83-b68c-22430f7639e4` passes and independent review is clean. Fresh matching live evidence remains required.

Follow-up coverage export passes 60 helper tests and exports both clean `50f74fa482843ca44203b4a3154c50d5ac644d4e` unit receipts without rebuilding. The complete checkpoint at that commit supersedes the earlier source checkpoint for the staging-permission change; see the implementation document for invocation IDs. The export-only follow-up does not alter compiled product or test inputs.

Unit-only coverage is approximately 86.5%, below the required 90% full-quality bar; no gate has been lowered. Host-runtime integration, full sanitizer/leak proof, package-manifest drift checks, complete artifact-only parity, immutable release snapshots, publication recovery, family-wide migration and broader resource/GitHub cleanup remain outstanding. The separate trusted CI worker is approved and being provisioned; arbitrary PR code must not run on the existing signing worker. Keep this opt-in until the remaining gates in the implementation document are complete. A new exact-head Sonar scan remains required after the source fixes described below.

On 17 September the operator approved the dedicated runner. Provisioning is now tracked in private `stephenlclarke/container-build` PR 1, whose registration boundary excludes public fork workflows. The subsequent public Sonar scan reported five source findings: three positional-argument reads, a hardcoded Swift test scratch root and an eight-argument internal initializer. These are corrected in maintained source without rule suppressions; the next exact-head scan remains required. Storage roots are grouped internally and the validated test wrapper declares the scratch root to Swift fixtures. The focused runtime suite also exposed descriptor fixtures that assumed a high file limit and briefly released a descriptor number into the parallel suite. The invalid fixture now uses an impossible descriptor and replacement uses atomic `dup2`; the complete focused runtime target passes (invocation `e752656b-edfd-4f0f-912c-6348d75e99dd`).
